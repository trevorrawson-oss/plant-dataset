# Zucchini / Courgette Steps 1-3 kickoff -- the first of the last 2 rail-riders

Assembled 2026-06-23 (Claude Code). Start SHA `0b767fc2` (`blueberry_step11_cert`). Zucchini is a RAIL-RIDER:
a warm-season annual on the proven `warm_season_fruiting` rails (no new archetype, no new tooling) + a
succession crop. Same arc as green-beans (which ran in a day).

## Contents
- **00_KICKOFF_steps1-3.md** -- the task spec (DRAFT; Trevor voices + sends).
- **zucchini-courgette_current_slice.json** -- the blank author-fresh shell (your base).
- **SLICE_INTEGRITY.md** -- the slice's crop-SHA preflight (`8e3faa1f…`).
- **zucchini_sources_and_catalog.json** -- the 122-parent source catalog (reuse parents; mint specific-page
  sub-ids under a trusted parent) + the crop's current `sources_summary`.
- **CURRENT_STATE.md** / **STATE_HISTORY.md** / **LATEST.txt** -- the refreshed orientation trio.

## The scope (Steps 1-3)
Source set (T1) + scalars/structured biology (incl. the full `succession_policy` and `self_fertile:true` +
pollination note) + the variety set + companions. NO design spec needed (standard warm-season annual). NO
compounds yet (those are Steps 6-8). The defining feature is the succession block (carrot/green-beans shape;
`pause_in_heat` likely false -- source it).

## Flow
1. Trevor finalizes 00_KICKOFF_steps1-3.md -> uploads to claude.ai.
2. claude.ai authors Steps 1-3 -> hands back a patch/slice + post-author crop SHA + source-mint flags.
3. Claude Code RELEASES: preflight `0b767fc2` -> apply -> whole_crop_gate + register_completeness + register_fill
   -> promote. Then Step 3.5 (region shells) -> Step 4 (region fill + A8 succession derivation) -> Steps 6-8
   (pests: squash vine borer / squash bug / cucumber beetle; disease: powdery mildew + bacterial wilt) -> Step 9 -> cert.

## After zucchini: broccoli
The last anchor -- a COOL-season annual (`cool_season_annual` rails, not warm-season). Its own kickoff after zucchini.
Then ~18.

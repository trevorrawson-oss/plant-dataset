## Session `zucchini-courgette_steps1-3_author` (2026-06-23) — claude.ai author lane

**Start-SHA (crop, preflight, re-asserted before mutation):** `8e3faa1f2968515f7ec0d490c7613af79a1f46633de01cd81e4f7a3e68a5512f`
**End-SHA (crop, post-author):** `11c3b048c507bf898b718ac11e800004e2de8d4e673a260113bb404f6b14fa02`
**Live full-file base at session start:** `0b767fc27b33d229fb5e41fc0ef0576ea592dd1e76647e53d781134563b7fb81` (post-`blueberry_step11_cert`)
**Gate:** not run this session (claude.ai authored the slice + this entry + findings only; Claude Code releases — preflight, apply, collateral/key-delta audit, `whole_crop_gate` STRUCTURAL-only at Steps 1-3, `register_completeness`, `register_fill`, promote, re-pin SHA, regenerate CURRENT_STATE). This is the next `warm_season_fruiting` rail-rider anchor after green-beans-bush; broccoli (cool-season) is the last anchor.

### What happened
Authored **zucchini-courgette Steps 1-3** on the proven `warm_season_fruiting` rails — no new archetype, tooling, design spec, compounds, or region work. Delivered: Step 1 source set (T1-only), Step 2 scalars + structured biology, Step 3 varieties + companions. The author-fresh shell's prose-heavy consumer surfaces stay null/empty for Steps 6-8.

- **Scalars/enums:** `days_to_maturity [50,65]` (mid 55), `spacing_inches [24,36]`, `germination_temp_f [70,95]`, `sunlight full_sun`, `sunlight_hours [6,10]`, `water moderate`, `harvest_urgency high`, `weeks_indoors 3`. Each justified by its OWN source (A1): DTM = umd_ext/osu_ext 50-65; spacing = umd_ext single-plant 2-3 ft; germ = osu_ext optimum 70-95; sun = umd_ext "at least 6, prefers 8-10".
- **Soil:** core `[loam]` / tolerated `[sandy_loam, clay_loam]` / problematic `[clay]`; seasoned `[loam, silt loam]` / `[sandy loam, clay loam]` / `[clay]`; `well_draining`; `organic_matter high`; both `preferred_description` registers authored (compost + raised beds/hills; sandy loams warm early; clay holds water and invites crown rot). sources umn_ext/umd_ext/osu_ext + anchoring.
- **pH:** preferred `[6.0,6.5]` (umn_ext), tolerated `[5.8,7.5]` (osu_ext); both `note` registers; sources umn_ext/osu_ext + anchoring.
- **Succession (carrot shape, full):** `interval_weeks 3` (umn_ext "finish... three weeks later"), `successions 3`, `notify_days_before 3`, `max_successions_per_season 3`, `window_type continuous`, **`pause_in_heat false`** (heat-loving — fall succession is borer/mildew-driven, NOT a heat pause; the deliberate contrast to carrot and the green-beans precedent), `trigger last_frost`; both `tip` registers (don't sow all at once; the midsummer replacement beats borer + powdery mildew on the tired first planting).
- **Start method:** `start both` (umn_ext/sdsu_ext direct-preferred; umd_ext transplant-for-early), `weeks_before 3`; both `hardening_off` + both `notes` registers (resents root disturbance, direct-sow once soil past 60°F ideally 70°F is simplest, indoors only to gain 2-3 wk short-season with larger cells). `start_method` has no sources/anchoring slot in the shell — none added.
- **self_fertile + pollination:** `self_fertile true` + both `pollinator_notes` registers (monoecious, single plant fruits, insect-pollinated, poor pollination → small blossom-end-rot / misshapen fruit, early male-flower drop normal, plant flowers + don't spray blooms + hand-pollinate if set stalls). NO `bloom_group`/`pollinizer` machinery — a note, not apple-style cross-pollination structure.
- **Container (core; deep prose → 6-8):** `container_ok true`, `min_pot_gallons 5`, `recommended_pot_gallons 10` (soft), `depth_inches_min 12`, `container_recommended true`, drainage `{drainage_holes_required:true, gravel_layer:false}` (T1: layering does not aid drainage), `container_suitable_varieties [Raven, Astia, Bush Baby, Patio Star, Eight Ball]`; sources uwi_hort/umd_ext + anchoring.
- **Watering (structured enums; all prose → 6-8):** `watering_method base` (foliage-dry / powdery-mildew nexus), `drought_tolerance low`; sources umn_ext/sdsu_ext + anchoring.
- **Rotation:** `family Cucurbitaceae` only (rotation_years + prose → 6-8).
- **Varieties (schema 2.9 `{name, recommended_note}`):** Black Beauty, Costata Romanesco, Raven, Goldrush (yellow), Cocozelle, Dunja — categorical notes (habit/color/type, no disease-resistance claims). Both `note` registers (choose by space/color; bush for small/containers, heirlooms reward room, mix a green + a yellow). sources umn_ext/umd_ext + anchoring.
- **Companions (three-array ×2):** `good_seasoned[]` rich roster of 7 (corn, pole beans, nasturtium — research_backed; marigold, sweet alyssum — likely; borage, sage — traditional); `good_beginner_seasoned[]` light tight subset (corn, pole beans, nasturtium); `good_beginner[]` empty. `bad_seasoned[]` rich (potatoes, fennel — both traditional); `bad_beginner_seasoned[]` light (potatoes, fennel); `bad_beginner[]` empty. Both `note` registers. companions sources umn_ext (companion page) + anchoring. **Bad companions surfaced to BOTH registers per Trevor's adjudication, labeled traditional + calibrated down.**

### Trevor's adjudications resolved this session
1. **Fertilizer = MODERATE feeder** (NOT heavy — corrects the kickoff guess; umd_ext "Medium requirement"). The fertilizer BLOCK is deferred to 6-8 (entangled prose+notification); the sourced profile is recorded in the findings doc.
2. **Companions NOT gated on T1** — three-tier vocab (`research_backed`/`likely`/`traditional`) carries the honesty; folklore allowed as `traditional` when labeled. Provenance object per onion calibration (research/likely → verified_against_sources:true + date; traditional → false/null; confidence calibrated down for soft folklore).
3. **Two-gardener split via the array architecture** — beginners get the tight set (`good_beginner_seasoned`), seasoned get the fuller rich roster (`good_seasoned`). Bad companions mentioned even when traditional.

### Key shape judgment recorded (precedent)
**Companion three-array VISIBILITY model authored as COMPLETE-ROSTER, not disjoint.** `good_seasoned[]` holds every companion (tight + fuller) as rich `{…why_seasoned, provenance, sources…}` objects; `good_beginner_seasoned[]` is the LIGHT `{name, why_beginner}` projection of the both-modes tight subset; `good_beginner[]` is born empty. The both-modes items (corn/beans/nasturtium) therefore appear in both arrays — rich in `good_seasoned` (why_seasoned), light in `good_beginner_seasoned` (why_beginner). **Forcing rationale:** the research_backed items ARE the both-modes tight items, and they carry the only T1 provenance/sources in the set; the disjoint reading (good_seasoned = seasoned-EXCLUSIVE) would strand their provenance in the light beginner array. Information-preservation selects the complete-roster model. **Flagged for CC to confirm against the carrot/onion certified template; if disjoint, conform but preserve corn/beans/nasturtium provenance.** (3 new top-level keys also recorded: `self_fertile`, `pollinator_notes_seasoned`, `pollinator_notes_beginner` — expect exactly these in the key-delta audit.)

### Verification done (author lane, SHA-gated)
- **Preflight gate PASS** — crop SHA re-asserted `== 8e3faa1f…` in-script before any mutation (`sys.exit`-equivalent assert).
- **Full validation gauntlet — ALL PASS:** (1) key-delta = exactly +3 expected keys, 0 removals, every touched block's nested key set unchanged; (2) collateral — every untouched top-level field byte-identical (zones, regions, fertilizer, pests, diseases, growth_stages, tips_by_stage, storage, yield_expectations, description_*, harvest_ready_*, moon_phase); (3) register-pair — 21 prose `_seasoned`/`_beginner` pairs balanced, 0 half-pairs; (4) numeric fidelity — every figure matches its source, pH tolerated brackets preferred, soil coherence; (5) copy-rules — 0 em-dash, 0 `--`, 0 "degrees F", 0 smart quotes, 0 Title-Case crop names, °F present; (6) 8-gram source-verbatim scan — 0 overlaps (all prose paraphrased); (7) companion shape — born-empty beginner arrays, rich/light shapes, provenance/source rules per label; (8) T1-only — every in-use source ID ∈ T1.
- **Validator self-correction (recorded):** initial run false-positived the list-valued companion visibility arrays (`good_seasoned`/`good_beginner`) as prose register pairs; matcher corrected to skip list-valued `_seasoned` keys. The genuine companion prose pair (`note_seasoned`/`note_beginner`) is balanced. The `_beginner` visibility arrays are born empty BY DESIGN.

### Residual after this session (zucchini-courgette)
Steps 1-3 author lane DONE. Release lane owes: preflight `0b767fc2…` → apply → collateral + key-delta audit → conform shape flags (findings §3, esp. companion visibility model + timing/notify/category enums + variety object shape + weeks_before int-vs-range + recommended_pot_gallons softness) → `whole_crop_gate` (structural-only) + `register_completeness` + `register_fill` → promote → re-pin LATEST.txt → mint source sub-IDs if desired (parents all catalogued). Then Step 3.5 (region shells) → Step 4 (region fill) → **Steps 6-8** (fertilizer block on the moderate profile; pests = squash vine borer / squash bug / cucumber beetle; diseases = powdery mildew / bacterial wilt; growth stages, tips, yield, storage, watering prose, container deep prose, rotation_years) → Step 9 (dash-resolution pass) → cert. **The arc is NOT done — Steps 1-3 are.**

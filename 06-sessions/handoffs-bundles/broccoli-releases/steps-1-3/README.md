# Broccoli Steps 1-3 kickoff -- the last of the 2 rail-riders (~18)

Assembled 2026-06-23 (Claude Code). Start SHA `0b767fc2` (`blueberry_step11_cert`). Broccoli is a RAIL-RIDER:
a COOL-season annual on the proven `cool_season_annual` rails (lettuce-leaf / carrot; no new archetype, no new
tooling) + a succession crop (spring + fall).

## Contents
- **00_KICKOFF_steps1-3.md** -- the task spec (DRAFT; Trevor voices + sends).
- **broccoli_current_slice.json** -- the blank author-fresh shell (your base).
- **SLICE_INTEGRITY.md** -- the slice's crop-SHA preflight (`2f81cd13…`).
- **broccoli_sources_and_catalog.json** -- the 122-parent source catalog + the crop's `sources_summary`.
- **CURRENT_STATE.md** / **STATE_HISTORY.md** / **LATEST.txt** -- the refreshed orientation trio.

## The scope (Steps 1-3)
Source set (T1) + scalars/structured biology (incl. the full `succession_policy` with `pause_in_heat: true`) +
the variety set + companions. NO design spec (standard cool-season annual). NO compounds yet (Steps 6-8). The
defining feature is the cool-season succession block + the spring/fall double-window (materialized at Step 4).

## The contrast with zucchini (the parallel session)
Zucchini = WARM-season (`pause_in_heat` false, single summer season, direct-sown). Broccoli = COOL-season
(`pause_in_heat` TRUE, spring + fall windows with a summer heat gap, transplant-led). Both ride proven rails.

## Flow
1. Trevor finalizes 00_KICKOFF_steps1-3.md -> uploads to claude.ai (its OWN session, parallel to zucchini).
2. claude.ai authors Steps 1-3 -> hands back a patch/slice + post-author crop SHA + source-mint flags.
3. Claude Code RELEASES (single session, one crop at a time, re-preflighting LATEST before each apply):
   preflight -> apply -> whole_crop_gate + register_completeness + register_fill -> promote. Then Step 3.5 ->
   Step 4 (region fill + spring/fall double-window + A8 succession + heat_pause) -> Steps 6-8 (pests: cabbage-worm
   complex / aphids / flea beetles / root maggot; disease: clubroot / black rot / downy mildew) -> Step 9 -> cert.

## After broccoli: ~18 anchors. The roster's done.

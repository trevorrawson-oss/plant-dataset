# STATE_HISTORY snippet -- strawberry Step 4 fl_peninsula (z10-11) [anchor 13]
# APPEND to STATE_HISTORY.md as the most-recent entry (above prior entries, below the header).
# Authoring lane = claude.ai. Promotion (deriver/gates/SHA re-pin/commit) = Claude Code.

## 2026-06-18 -- strawberry Step 4: fl_peninsula z10-11 (frost-free fall-plant plasticulture proof cell) AUTHORED (patch handed back, not yet promoted)

- **Third (and last) Step-4 proof cell authored:** `fl_peninsula` z10 + z11, the frost-free fall-plant winter-annual (plasticulture) sub-shape -- the PULLED annual ca_interior's carried bed does not cover, and the first frost-free strawberry cell. Two prior proof cells already RELEASED this day: `northern_tier` (perennial) + `ca_interior` (interior summer-plant annual). With this cell authored, the three-shape map (perennial north / interior summer-plant annual / frost-free fall-plant annual) has a worked instance of each.
- **Patch:** `strawberry_step4_fl_peninsula_patch_v1.json`, 14 ops, RFC-6901 with `from`-guards on every op. Start SHA `adf6f86f` (`strawberry_step4_ca_interior`). Scope = `fl_peninsula` z10 + z11 ONLY; the other 8 warm regions untouched. `calendar[]` left `[]` for the deriver.
- **grown_as = annual, SOURCED not analogized.** Anchored to UF/IFAS EDIS HS1154/HS403 (Growing Strawberries in the Florida Home Garden), fetched + verified live 2026-06-18: "strawberries are grown as an annual crop rather than as perennials"; single-crown transplants set late Sept-early Nov; flowering Nov, fruiting in 2-3 cycles into Apr/May; "plants are usually removed at the end of the fruiting season, and new plants are planted the following fall" (the PULL). Corroborated by the South Florida Gardening Calendar (ENH1191/EP452, rev Oct 2024) for z10b-11 timing.
- **Window shape (fall-plant pulled annual):** plant_out `Oct - early Nov` (month-resolved absolute; central/west-central FL = mid-late Oct per breeder Whitaker); bloom `Nov - Dec`; harvest z10 `Nov - Apr`, z11 `Nov - May`. `resolution_method = absolute_month_uf_planting_guidance`. `resolved_from: null` on both cells -- forced by ABSENT FROST (z11 frost-free; z10 nearly so per zone_frost_data: z11 none/none, z10 a nominal Jan 15 / Dec 31 with 365 frost-free days), NOT by heat-avoidance as in ca_interior. The clean reason for the same field value.
- **z10 vs z11 differentiated on the freeze axis (per-cell, not templated):** z10 carries a rare-radiational-frost caveat (cover blossoms below 32F per HS403); z11 frost-free, back-end bounded by warming spring + day length, harvest runs one month longer (through May). Same plant/harvest core, both grounded in UF guidance; not analogized from one row.
- **First strawberry cell expected to emit `season_over`.** Pulled annual -> genuine summer off-season (Jun-Sep). Deriver validation that `season_over` emerges (and that NO `dormant`/`renovation` appears -- annual cell) is part of Claude Code's release. (Illustrative local derive confirmed the shape: harvest Nov-May, plant Oct, season_over Jun-Sep, zero dormant/renovation.)
- **Per-rule-entry `anchoring_urls` authored from the start** (one-per-source-id on each plantings arm + each resolved cell), the ca_interior lesson; region-root carries `sources` but no anchoring (per-arm/per-cell convention). `track` flipped `perennial` -> `annual`. Tree keys absent. Dual-register pairs present + distinct.
- **A5 in BOTH directions, restated in plantings_provenance:** do NOT template this FL fall-plant window onto the interior deserts (ca_desert / low_desert_az / warm_arid = ca_interior summer-plant); do NOT template the interior summer-plant window onto FL.
- **Source admission (Claude Code release lane):** `uf_ifas` is NOT yet in strawberry `sources_summary`. Mint `uf_ifas_hs403` under parent `uf_ifas` at release (EDIS HS1154/HS403, T1 inherited, `_admission_provenance`), per the `uf_ifas_vh021` / `clemson_hgic_1149` sub-id precedent; `uf_ifas_ep452` corroboration page optional to admit.
- **Local verification (claude.ai, advisory; gates are the defense):** 14 ops applied with all drift guards passing; collateral clean (only `fl_peninsula` changed, top-level byte-identical); D9-style structural checks PASS; user-facing copy gate (em/en-dash, `--`, "degrees F") PASS; post-apply crop-object SHA `0f3c3898bd9116cfd24467774ed1fed569f3dd77eb25eae44821837be5e43f24` reproduced on an independent re-apply.
- **Open Step-5 boundary flag surfaced (Trevor's call, NOT resolved here):** crop-level `hardiness_zone_max = 10` and `reliable_fruit_zone_max = 9` vs a now-authored z11 cell that fruits well as an annual. See the re-scope note -- options (extend the scalar to 11 with a "fruits-as-annual" qualifier vs leave the perennial-survival scalar and let the region cell carry the annual reality) are laid out there.

### NEXT
- Claude Code: preflight SHA, apply v1, run the deriver (validate `season_over` emerges + no dormant/renovation), `whole_crop_gate` (A2/A10/A11) + `register_completeness_gate`, collateral audit, re-pin LATEST.txt + commit. Post-apply expected canonical content SHA shifts from `adf6f86f`; the crop-object SHA above is the fl_peninsula-filled strawberry object.
- Then the remaining 7 warm regions scale on the now-THREE proven templates: interior deserts (ca_desert / low_desert_az / warm_arid) on the ca_interior summer-plant template (re-source per A5); coastal-CA (ca_north_coast / ca_south_coast) = genuine perennial-vs-annual source call; se_gulf + hawaii_tropical = open grown_as calls (hawaii is an elevation/niche crop -- do not assume year_round). Then Steps 5 / 6-8 / 9 / 11 cert.

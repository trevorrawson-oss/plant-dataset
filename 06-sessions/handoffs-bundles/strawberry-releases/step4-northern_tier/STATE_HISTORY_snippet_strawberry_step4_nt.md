## 2026-06-18 -- strawberry Step 4 northern_tier PROOF CELL (z3-7) -- the perennial path's first region fill [claude.ai author lane]

**Start-SHA:** `b4a76fd52094e4866d369dcebd7dead307252bf280d3e108af1a577c9cd54659` (`strawberry_step3_5`; matches LATEST.txt). **Deliverable:** `strawberry_step4_northern_tier_patch.json` -- 77 replace ops, all `from`-guarded, base `b4a76fd5`. **Pre-deriver crop SHA (sorted-min, calendar[] still []):** `fd63aadf188bac6eaf9226e28a8e863fe9e211ff1a926470b02cda9c68906ee4` -- this is the author-lane SHA BEFORE the calendar deriver runs; Claude Code's post-deriver SHA supersedes it.

**Scope = PROOF CELL ONLY (NA-3b rule, Trevor-ratified this session).** `northern_tier` (z3-7) authored end-to-end; the other 9 regions UNTOUCHED. northern_tier proves the `perennial` path + the deriver's harder frost-bracketed branch. Re-scope after release: ONE annual proof cell next (fl_peninsula or ca_interior) before the warm regions scale, so the annual deriver branch is never scaled unproven.

**What was authored (the perennial matted-row June-bearing spine):**
- **`grown_as: perennial` on all 5 cells (z3-7).** A SOURCE finding, not inferred -- UMN, OSU EC1307, and USU home-garden guides all describe the north as a perennial matted-row bed (June-bearing, multi-year, renovated after harvest, replaced every 3-4 years). All 3 fetched LIVE this session.
- **Window dates LEAD FROST-RELATIVE (per the kickoff directive).** Region-constant `plantings[0]` carries the establishment rules as offsets from `last_frost`: plant_out -14d/win21 (dormant crowns set ~2 wk before last frost, as soon as soil can be worked -- USU: dormant stock needs no frost-free wait); bloom +14d/win21 (established bed opens ~mid-spring; UMN: mid-May in southern MN = z4); harvest_start +42d, harvest_end +70d (~4 wk after flowering per UMN, ~4 wk pick window). Per-zone resolved date strings computed from `zone_frost_data.json` last_spring frost:
  - z3 plant May 1-22 / bloom May 29-Jun 19 / harvest Jun 26-Jul 24
  - z4 plant Apr 17-May 8 / bloom May 15-Jun 5 / harvest Jun 12-Jul 10
  - z5 plant Apr 1-22 / bloom Apr 29-May 20 / harvest May 27-Jun 24
  - z6 plant Mar 18-Apr 8 / bloom Apr 15-May 6 / harvest May 13-Jun 10
  - z7 plant Mar 1-22 / bloom Mar 29-Apr 19 / harvest Apr 26-May 24
  Cross-checked vs UMN anchor: z4 plant "April, May" / bloom "mid-May" / harvest "mid-June to early July" -- all match.
- **The soil-readiness cue ("as soon as the soil can be worked") is carried as PROSE, never as the window key** (a pure soil-temp anchor is non-resolvable per zone -- the onion `bulb_threshold` trap). NO material soil-temp divergence to flag: every source frames spring planting as dormant-stock / soil-workable, which the frost-relative window already expresses.
- **`resolved_from` from `zone_frost_data.json` (the onion NT lesson):** z3 May15/Sep15, z4 May1/Oct1, z5 Apr15/Oct15, z6 Apr1/Oct31, z7 Mar15/Nov15. `resolution_method: perennial_herbaceous_precompute` on all 5.
- **`calendar[]` left `[]` on all 5 cells** for the `berry_herbaceous_calendar` deriver. The deriver emits the dormant+growing+bloom+harvest+renovation cycle bracketed by the frost dates; A11 then checks it against these dates. (The renovation token = the month after the June harvest, per D4.)
- **Dual-register prose:** region_notes_seasoned/_beginner + per-cell grown_as_note_seasoned/_beginner (cold-edge mulch at z3, reliable floor at z4, milder/earlier through z7) + per-cell frost_risk_note_seasoned (late frost on open blossoms). 6 dual pairs, 0 byte-identical, 0 half-null. frost_risk_note_seasoned is seasoned-only by schema.

**Sourcing (T1 only, all catalog-admitted, ZERO to mint):** `umn_ext` (Growing strawberries in the home garden -- the calendar/spacing/mulch/renovation anchor, fetched in full), `osu_ext` (EC1307 Growing Strawberries in Your Home Garden -- renovation/crown-damage/matted-row), `usu_ext` (Strawberries in the Garden -- dormant bare-root early-spring planting, spacing). `cornell_ext` co-anchors the harvest-timing rule (the canonical Steps 1-3 harvest co-anchor). Region-root carries `sources` only (NO anchoring_urls -- lettuce/basil/carrot pattern); per-arm anchoring in `plantings[0]`; per-cell anchoring in each cell. All anchored leaves: sources == anchoring_urls keys.

**Local gate analogues (claude.ai pre-handoff):** from-guard 0 errors; collateral 0 (only northern_tier changed, 112 leaves, all in-region); dash/em-dash user-facing 0; "degrees F" 0 (one temp 10-20°F uses the °F symbol -- fixed from a bare-F draft); British spelling 0; 6 dual pairs 0 byte-identical / 0 half-null; anchoring sources!=keys 0; region-root anchoring_urls absent (correct); source-tier 4 IDs / 0 uncatalogued / 0 non-T1; verbatim 8-word vs fetched UMN/OSU/USU bodies 0 HARD lifts; numeric fidelity all 7 claims (spacing 18-24, rows 3-4 ft, mow 2 in, mulch 2-3 in, crown 10-20°F, ~4 wk flower-to-fruit, 3-4 yr bed) present and matching the fetched T1.

**Step 5 (verification) carried in this proof cell:** windows cross-checked against the cited live T1 (numbers match, see numeric fidelity). `reliable_fruit_zone` warm-edge refinement (z9-10, heat-limited) is NOT in scope here -- it belongs to the warm-region pass; flagged for the re-scope. chill stays informational (crop-level field null; no clean T1 numeric surfaced for the north).

**For Claude Code (release):** preflight full-file SHA == `b4a76fd5`; apply the 77 ops; re-reconcile all 5 `resolved_from` against `zone_frost_data.json`; run the `berry_herbaceous_calendar` deriver to fill the 5 calendars; **validate the deriver's frost-bracket boundary against these FIRST real perennial cells** (the tree-calendar precedent -- if a generated calendar looks off, refine the deriver, not the dates); run A10/A11 + whole_crop_gate + register_completeness_gate; collateral audit (only strawberry changed, other 122 crops byte-identical); promote. whole_crop_gate strawberry should drop from 10 violations to ~5 (the 9-region admission gaps minus the 5 northern_tier cells now filled, i.e. the 9 region_notes-null -> ... actually NT had 1 region_notes-pair-null counted; expect the region-fill count to drop by northern_tier's share). A10/A11 should stay 0.

**NEXT (after release + re-scope):** ONE annual proof cell (fl_peninsula z10/z11 OR ca_interior z8/z9) to prove the annual deriver branch (fall plant -> winter-spring harvest -> season_over, NO renovation/dormant), THEN the remaining warm regions, THEN Steps 6-8 -> Step 9 -> Step 11 cert + flip.

**RE-SCOPE NOTES for the remaining 9 regions (what this proof cell surfaced):**
1. **The frost-relative perennial window pattern WORKS and is the template for the other perennial regions** (mild-coastal CA if sourced perennial). The plant_out -14/bloom +14/harvest +42-70 offsets reproduce the UMN anchor; reuse for perennial cells, re-source per region (A5).
2. **`grown_as` per region is still a per-region SOURCE call for all 9.** The prior schema-2.8 design memo (project knowledge) already anchored ca_interior=annual (UC ANR Central Valley) and FL z11=winter annual (UF/IFAS) to T1, and noted coastal CA z9 = perennial -- these are strong CANDIDATES but must be re-verified live at their region pass (inheritance = candidate only). se_gulf, warm_arid, hawaii_tropical remain genuinely open.
3. **The annual cells need their own window logic** (fall plug-setting plant_out; late-winter bloom; winter-spring harvest_start/end; deriver fills season_over). The annual proof cell must establish this before the 4-5 annual regions scale.
4. **Frost-free cells** (fl_peninsula z10/z11, hawaii z11) carry `resolved_from: null` + absolute month-resolution + a `resolution_method` naming the source table (the certified lettuce/onion frost-free pattern) -- NOT frost-relative.
5. **`reliable_fruit_zone` 4-9 warm-edge refinement** (heat-limited z9-10) is a Step-5 source call that lands during the warm-region pass, not the north.

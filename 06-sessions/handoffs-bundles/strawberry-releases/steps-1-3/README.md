# Strawberry Steps 1-3 kickoff bundle -- manifest

Assembled 2026-06-18 (Claude Code). Hand this to claude.ai to OPEN the strawberry gold-standard arc (anchor 13, the first `berries_herbaceous` crop). Steps 1-3 only; the Claude Code structural lane (gates + shell builder + deriver) is already built + pushed.

## Contents
- **00_KICKOFF_DRAFT.md** -- the structural kickoff frame (DRAFT; Trevor voices + sends the final prompt).
- **DESIGN_SPEC.md** -- the approved design of record (D1-D9 + the Phase-5 variety/delta resolution). The law for this arc.
- **strawberry_slice.json** -- the current `strawberry` crop object (the empty author-fresh shell to author into). Pretty-printed for reading; the canonical file stays compact.
- **SOURCE_POINTERS.md** -- a T1 lead list for Step 1 (verify before admitting).
- **CURRENT_STATE.md** / **STATE_HISTORY.md** / **LATEST.txt** -- the orientation trio. Read CURRENT_STATE + STATE_HISTORY at session start; preflight the SHA against LATEST.txt.

## How to use
1. Trevor finalizes 00_KICKOFF_DRAFT.md into the kickoff prompt and uploads this bundle to the claude.ai project.
2. claude.ai authors Steps 1-3 (source set, Step-2 scalars incl. the perennial lifecycle scalars + `start_method=bare_root_dormant`, companions) to DESIGN_SPEC.md.
3. claude.ai hands back the authored crop object + a STATE_HISTORY snippet in the canonical patch format.
4. Claude Code verifies + applies, runs Step 3.5 (`build_region_shells` flips the basis + builds the 10 shells), then the arc continues to Steps 4-8.

## Standing-rule note (archive the prior consumed handoff)
The handoffs-bundles archive (`plant-dataset/06-sessions/handoffs-bundles/`) is currently empty, and there is no prior STRAWBERRY handoff (this is the first). Per the standing rule, the prior CONSUMED handoff (the onion arc's) should be archived to `06-sessions/handoffs-bundles/onion-releases/<step>/` -- flagged for Trevor since I do not have a definitive pointer to the exact onion handoff file to move.

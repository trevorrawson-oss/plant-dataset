# Field-Addition Register — the live queue for cross-crop field additions

**What this is:** the standing, tracked queue of new fields to add across the crop roster, plus the
status and **trigger condition** for each. Consult this **before** adding any field that spans crops.
It is the "track" that makes the cross-crop field-addition process visible instead of buried.

**Method:** follow `docs/gs_cross_crop_field_addition_v0.md` (the "column GS arc" template — contract
first → diverse pilot incl. a legitimately-N/A case → bot rollout with a schema-validation gate +
coverage report → fold into the per-crop checklist OR run as a post-roster column pass; amend
already-certified crops with per-field provenance, never a full re-cert).

**Standing principle (load-bearing):** **run a column pass against a STABLE / complete roster, not
mid-certification.** Adding a datapoint while crops are still being certified re-opens already-done
crops *and* bolts the field onto in-flight ones — a moving target. Prefer: finish the roster, then
one clean column pass.

---

## Register

| # | Field | Status | Trigger | Approach | Consumer / notes |
|---|---|---|---|---|---|
| 1 | `growth_stages[].day_range_from_sow` (germination "expect sprouts in ~N days" window) | **folded into #4** (2026-07-07: the timing-spine bundle; the ladder is authored WITH `dtm_anchor`+DTM, not alone) | full crop roster (~123) certified | single **post-roster column pass** (NOT folded into the per-crop checklist this round) | plant-app **seed trays** reads it for the sprout-window line; it **graceful-omits** where absent, so the feature is **not blocked** — the line lights up when this lands. The number is already authored in each germination stage's prose ("in about 5 to 10 days…"), so this is a *structuring* job, not new research. Present today on ~12 of 31 app-certified crops; missing on tray-started annuals (brussels, cabbage, cauliflower, kale, broccoli, pumpkin, butternut, zucchini, zinnia). Legitimately-N/A: crops not grown from seed (perennials/trees — bare-root/nursery). |
| 2 | Establishment-only `calendar[]` for chill-limited `survives_no_fruit` cells (perennials) | **queued** | full crop roster certified | post-roster column pass + **A3 gate amendment**: today the no-fruit split (perennial_gate A3, incognito-audit B2) requires chill-limited `survives_no_fruit` cells to carry an EMPTY calendar (no over-promised bloom/harvest dates). Amend to require an establishment-only calendar instead — plant/growing/dormant/prune/care tokens derived from the region's `plantings[]` + dormancy window via a new `derive_establishment_calendar` beside `derive_tree_calendar`; still ZERO bloom/harvest tokens, so the honesty invariant holds. Cold-edge no-fruit cells (chill met) are unaffected. ~40 cells across 13 tree crops today. | plant-astro guide zone pages render the care timeline under the suitability note ("survives but rarely crops"); currently the calendar section graceful-omits. Product call Trevor 2026-07-02 (origin: sour cherry `ca_interior` z9 — "even if it survives people might still plant it"). |
| 3 | `pet_safe` (pet-friendly vs. not — cats/dogs/horses toxicity) | **queued** | full crop roster certified (post-114) | post-roster column pass. A structured, consumer-facing flag (bool or small enum, e.g. `safe` / `toxic` / `caution`) with per-crop provenance, so plant-astro can render a **quick pet-friendly / not-pet-friendly icon** on each crop. Today the safety fact lives only as PROSE in `failure_diagnostics` (rosemary = safe; chives = toxic, added Wave 4) — inconsistent and un-iconizable. Contract-first pilot must include a legitimately-safe crop, a clearly-toxic one (allium/chives, nightshade foliage, sweet-pea seed, elderberry raw), and a caution case. **Source-tier decision to settle here:** ASPCA's Toxic/Non-Toxic Plants list is the canonical pet-toxicity authority but is non-`.edu` (same class as `rhs`) — decide whether to admit it as the anchor source for this field, alongside the extension `.edu` pages (NCSU plant toolbox tags "problem for cats/dogs/horses"). | Trevor 2026-07-06: wants pet-friendly / not-pet-friendly as a reliable roster dimension, rendered as a **quick icon**; "a big issue... something I want to have and not get wrong." Chives (allium, toxic) + rosemary (safe) already give both poles for the pilot. |

| 4 | **Timing-spine bundle** -- `propagule`, `dtm_anchor`, `sow_depth_inches`, `thin_to_inches`, `harvest_window_days`, `divide_every_years` (+ the pre-existing `growth_stages[].day_range_from_sow` ladder, subsumes register #1) | **in progress** (Phase 1 done 2026-07-07) | full 114 roster certified (MET) | **coupled-bundle** column GS arc: one-time contract + gate layer (`docs/timing_spine_contract.md`, `tools/timing_spine_gate.py`), then author per-crop in archetype batches (garlic pilot -> fall/winter -> rest). NOT a field-by-field sweep -- the ladder only means anything relative to `dtm_anchor`+DTM, and depth/spacing/propagule are archetype-determined. Amend-not-recert with per-field provenance. Fold into the per-crop checklist for new (§E) crops. | plant-app `crop-timing.ts` (already built, graceful): `daysToHarvestFromStage` reads the ladder, `effectiveDtm`/`dtmAnchor` read DTM+anchor, accessors expose depth/window/propagule/thin. Consumer contract: `plant-app/src/lib/guides.ts`. Phase 1 baseline (`4abf43a5`): 1 hard defect surfaced (`shallot` ladder order) + 8 anchor-dependent warnings -- see `docs/2026-07-07-timing-spine-phase1-findings.md`. Absorbs register #1 (`day_range_from_sow`). |

| 5 | `watering.schedule_by_stage[]` -- PER-STAGE watering (rate/amount, frequency, method, level + dual-register notes per growth stage; the "how much to water after sprouts grow" data) | **COMPLETE 2026-07-07 (114/114 certified)** -- authored across 12 batches (W1 tomatoes -> W7 herbs, B8-B9 flowers, B10 microgreens, B11 citrus, B12 woody herbs; canonical `6f584c3b` -> `b15a5a0f`). Each crop authored INDIVIDUALLY from its own certified watering prose (blanket-authoring guarded, per-crop distinctions surfaced every batch); annuals/microgreens got FULL-stage coverage, the 10 perennials (citrus + woody herbs, Trevor's approved judgment call) got the SPARSE tree pattern (3 key-stage entries, subset of real growth-stage ids, blueberry precedent). Gate-clean throughout (whole_crop_gate + register_completeness + dash/temp + release_verify + pre-commit backstop). The 10 crops without it = the uncertified §E shells, correctly excluded. | done | backfilled for annuals using the EXISTING `schedule_by_stage` shape (already on 23 winter + 21 trees/perennials, e.g. the blueberry anchor); per-stage, from certified crop-level watering prose; its OWN focused pass. Paired with the day_range_from_sow ladder (same stage ids). | plant-app per-stage watering guidance (seedling -> established -> mature). Captures germination "keep the surface damp" + post-sprout "water deeply, less often". |
| 6 | GERMINATION + SEEDLING **LIGHT** -- `germination_light` (enum: light_required/dark_preferring/neutral, or null=no-home-seed-path), `seedling_light` (enum: bright_default/na/blackout_then_bright/photoperiod_capped), `seedling_light_cap_hours` (int, iff photoperiod_capped) | **COMPLETE 2026-07-07** (all 114 certified: germ SET 85/N-A 29, seedling bright 49/na 57/blackout 8; TODO = 10 §E shells) | prior column passes (#4/#5/#7) complete on stable 114 roster | Column GS arc. Contract `docs/seedling_light_contract.md`. germination_light = the genuinely-varying per-crop fact (full sourced classification pass, grouped sourcing); seedling_light = default (`bright_default`) + typed exceptions. N/A rule: germination_light SET iff realistically home-seed-startable (propagule==seed MUST be SET; seed-startable herbs SET despite transplant propagule; trees/citrus/vegetative = null). Microgreens IN-scope (opposite of #7): neutral + blackout_then_bright. photoperiod_capped RESERVED (0 live members; gate RED-test proven). Gate `tools/seedling_light_gate.py` (TDD, `--coverage`). C11: germination_light/seedling_light ruled into EXCLUDED_KEYS. | plant-app seedling-light guidance (leggy-seedling avoidance) + germination guidance (surface-sow vs cover-to-exclude-light). |

| 7 | STRUCTURED climate **thresholds** -- `heat_threshold_f` + `heat_effect`, `frost_tolerance_f` + `frost_effect`, `chilling_sensitivity_f` (`germination_temp_f` already structured) | **COMPLETE 2026-07-07 (`b15a5a0f` -> Phase 1 `d3a6912f` -> ROLLOUT `6659042d`); all 106 outdoor certified crops carry the 3 fields** | before the notifications + WeatherKit build (~week of 2026-07-14) | Column GS arc. Contract `docs/climate_thresholds_contract.md` (single-value thresholds not bands; heat=daytime-HIGH; coherence frost<chilling<heat; heat null=N/A heat-lover). Gate `tools/climate_threshold_gate.py` (TDD, `--coverage`, `INDOOR_SLUGS`). Trevor confirmed: keep the *_effect enums (make the deterministic alert message accurate) + add `chilling_sensitivity_f` (numeric-only, N/A for cold-adapted crops). Semantic calls: fruit-tree frost = spring BLOSSOM frost ~28F (not winter wood hardiness; fig/pomegranate exception); "chill hours" = dormancy requirement NOT chilling injury; microgreens 8 = N/A-indoor; very-hardy crops use USDA-zone hardiness. C11: heat_effect/frost_effect ruled into register EXCLUDED_KEYS. Coverage: heat SET 46/N-A 60, frost SET 106, chilling SET 29/N-A 77; only TODO = the 10 uncertified §E shells (pick up at certification). **Follow-ons:** fold into the per-crop GS-arc checklist; optional `*_night_f` for the deferred night-temp effects. | plant-app notifications engine + AI context-assembly. Dataset already had partial scaffolding (`hard_freeze`/`FROST_PROTECT` tip tokens) -- these fields give them firing numbers. See memory `notifications-ai-architecture`. |

| 8 | **Register-coverage HARD gate** (infrastructure, not a field) -- enforce that every CERTIFIED crop carries each SHIPPED register field or its explicit null/N-A | **COMPLETE 2026-07-07** (tooling only, canonical untouched) -- `tools/register_coverage_gate.py` (+ `tools/test_register_coverage_gate.py`, TDD RED->GREEN), wired into `whole_crop_gate` as **A39**. GREEN on all 114 certified (A39=0 each; standalone PASS). RETROACTIVE #4-#7 + #6. | a register field's rollout is COMPLETE (turn on per field only then) -- all four met | Universal present-or-explicit-null cert gate modeled on `whole_crop_gate` A17 (npk_ratio) / A20 (display-readiness). A certified crop (`verified_gs_arc`) must carry, or its defined null/N-A: **#4** `propagule` (universal), `dtm_anchor` (unless empty-DTM), `sow_depth_inches` (seed-like non-microgreen); **#5** `watering.schedule_by_stage` (non-empty); **#6** `germination_light` + `seedling_light` (key present; null is a present value); **#7** `heat_threshold_f`/`frost_tolerance_f`/`chilling_sensitivity_f` (key present). Exempt: uncertified §E shells (no-op off `verified_gs_arc`) + legit-N/A predicates **imported** from the standalone gates (`timing_spine_gate.dtm_empty`/`is_microgreen`/`SEED_LIKE`, `climate_threshold_gate.INDOOR_SLUGS`). N-A tallies: 29 empty-DTM (no anchor), 43 sow_depth-N/A, 8 INDOOR_SLUGS, 29 germ-null. The ladder / thin_to / harvest_window / divide_every stay archetype-OPTIONAL (not hard-required). | plant-dataset cert integrity -- stops silent coverage drift as new crops certify. |

| 9 | **Seed-tray cell protocol** -- the tray-start step between germination and harden-off: sow-density + cell-thinning (+ pot-up). `tray_sowing` enum { `multi_sow_thin_to_one` (default) / `single_sow` / `multisow_clump` (RESERVED, 0 live) / `na` } + `pot_up` enum { `recommended` / `optional` / `not_needed` } (present iff a real tray value). | **COMPLETE 2026-07-07** (all 114 certified; `6990f1da` -> `17ff6b67`). Contract `docs/seed_tray_protocol_contract.md`; gate `tools/seed_tray_gate.py` (TDD); C11 ruled both enums into `register_completeness` EXCLUDED_KEYS; A39 folded (`register_coverage_gate` requires `tray_sowing` present-or-na). Coverage: multi 33 / single 16 / clump 0 (reserved) / na 65; pot_up recommended 13 / optional 15 / not_needed 21. **`pot_up` shipped as a 3-value ENUM not a bool** (a bool could not say "optional" -- broccoli surfaced it); **`multisow_clump` RESERVED** (no crop's authored prose supports seed-tray multisowing; leek + spring-onion are individual-transplant, so leek=multi_sow_thin_to_one). Sourced from each crop's own certified prose (prose scan caught 3 refinements: cauliflower recommended, zinnia/parsley not_needed). | stable 114 roster (MET); design-first | Column GS arc, mostly-standard-with-exceptions (mirrors seedling_light). THE GAP (verified): tray-started crops carry `start_method.hardening_off_*` but NO structured "sow a few seeds per cell -> thin to the strongest -> pot up" guidance. The existing `thinning{}` object is a DIFFERENT concept -- DIRECT-SOWN seedbed/row thinning to a final in-ground `to_spacing` (present on carrot/lettuce; absent on tomato/pepper/broccoli). Exceptions: multisow-clump (leeks/scallions/beets, transplant the clump, do NOT thin); single-sow large seed (cucurbits if tray-started); `na` = direct-sown (its thinning is the in-ground `thinning{}`). Ties to `seedling_light`/`weeks_indoors` (applies to the wi>0 tray-started set). Harden-off already structured (`hardening_off_*`), stays put. | plant-app seed-tray flow: "put 2-3 seeds in each cell, thin to the strongest, pot up before hardening off." The missing middle of the seed->transplant journey. |

| 10 | **Multisow-clump showcase** -- give register #9's RESERVED `multisow_clump` value a real live member by AUTHORING the seed-tray multisow method into a bunching onion. A CONTENT arc, not an enum change. | **COMPLETE 2026-07-07** (`17ff6b67` -> `00e0b6b1`; spring-onion). A THIRD start_method route authored into both registers (sow a pinch of ~6-10 seeds/cell, transplant the whole clump undivided, no thinning, space clumps 4-6 in, harvest the biggest first), then `tray_sowing` flipped `multi_sow_thin_to_one` -> `multisow_clump` (`pot_up` stays `not_needed`). Additive amend, coherent with spring-onion's existing direct-sow + individual-transplant routes. Sourced RHS + Johnny's bunching-onion guide (method popularized by Charles Dowding), cited "(RHS)" inline. **LEGAL:** facts/methods are not copyrightable (17 USC 102(b)/Feist); original prose, no reliance on the Old Farmer's Almanac. seed_tray_gate PASS (multisow_clump 1 live / 0 reserved), whole_crop_gate + register_completeness + register_coverage + gate_all + release_verify all clean. | after #9 (MET); Trevor's go (given 2026-07-07) | Authored into spring-onion (NOT a new crop -- roster stays 124). | plant-app seed-tray flow: the "sow a cluster, transplant the whole clump, no thinning" line -- surfaces plant's forward-thinking method guidance. |

| 11 | **Seed-start indoor OFFER** for the 12 seed-startable non-seed crops -- make the indoor-from-seed path fully offered (method prose + `seedling_light`/`tray_sowing`/`pot_up`/`weeks_indoors`), not just `germination_light`. A CONTENT + enum-re-adjudication arc over EXISTING fields, not a new field. | **COMPLETE 2026-07-08** (`00e0b6b1` -> `86ccf8c3`; 12 crops in 5 batches). Contract `docs/seed_start_method_contract.md`; design/plan under `docs/superpowers/`. THE GAP: #6 set `germination_light` from the from-seed fact but keyed `seedling_light` (then #9 `tray_sowing`) off the RECOMMENDED path, so nursery-stock crops cascaded to `na` -- the data asserted an indoor-from-seed path while withholding the tools to walk it. FIX (Trevor's source-of-truth call, extending the #6 principle): 11 crops flipped `seedling_light` na->bright_default + a real `tray_sowing` + `pot_up`; **chives->multisow_clump** (2nd live member); 5 gained `weeks_indoors` (lavender/rosemary/thyme 10, oregano/sage 8); **onion/shallot `germination_light` null->neutral** (their prose already teaches from-seed); method prose authored per gap (mint/lavender/rosemary/oregano/thyme + pawpaw's full stratification method). **pawpaw = honest exception:** keeps `seedling_light`/`tray_sowing` na (shade seedling, deep-pot single seed) + gets the KSU stratification method in prose + `germination_temp_f` []->[75,85]. **lemongrass DEFERRED** (genuine no-seed grass, null correct). No new gate, no gate change (scratch-spike + full-battery verified; `gate_all` 114/114). Coverage: germ SET 85->87/N-A 29->27; seedling bright 49->60/na 57->46; tray multi 42/single 16/clump 2/na 54. | after the #4-#10 spine complete on stable 114 (MET); Trevor's go 2026-07-08 | Sourced per-crop T1 (RHS chives, Johnny's mint/rosemary, KSU pawpaw, extension lavender/oregano/thyme, own prose sage/onion/shallot); original prose (17 USC 102(b)/Feist); provenance in the contract + STATE_HISTORY. amend-not-recert; SHA-guarded per batch via `tools/apply_patch.py` + `tools/batches/seed_start_b1..b5_*.json`. | plant-app: a full "How to start [crop] indoors from seed" offer (directions + a `weeks_indoors`-computable start date) alongside the recommended transplant path. Follow-ons (deferred kickoffs): growth-stage arc; per-region indoor-start calendar. |

| 12 | **Variety-detail bundle** (per-variety, FLAT) -- `id` (stable slug), `days_to_maturity` (LOAD-BEARING), `maturity_class` (early/mid/late; unifies the old dtm_class/season split), `seed_type` (open_pollinated/hybrid/heirloom), `seed_color`, `seed_size`, `plant_habit`, `primary_use`, `is_reference` (exactly one/crop), `confidence_tier`, `note_beginner`/`note_seasoned`, `sources`/`anchoring_urls` (+ optional `disease_notes`/`regional_fit`) | **PILOT in progress 2026-07-11** (dry-bean; spec + plan `docs/superpowers/{specs,plans}/2026-07-11-dry-bean-variety-pilot*`). Soft `tools/variety_detail_gate.py` (TDD, timing_spine pattern) + `register_completeness` ruling of the new keys, both landed; canonical splice pending. | **HARD-FLIP (INV-1):** flip `variety_detail_gate` from soft/standalone into the A39 register-coverage hard floor + `gate_all` when the Spec-2 rollout column pass reaches full-roster coverage. Soft is a stage, not a resting state (timing_spine -> A39 precedent). | Single-crop GS-arc **PILOT first** (dry-bean, 5 varieties, soft gate), THEN a roster-wide **column pass** reconciling the 5 existing variety shapes (37 simple `{name,dtm,note}` / 30 name-strings / 10 exploratory `delta` / 39 assorted) + folding in `varieties_detail[]` (26 trees/berries) + **retiring the exploratory `delta` overlay** (Spec 2). **FLAT, override-by-ABSENCE** (a variety carries a field only where it differs; the crop default is inherited by absence -- NOT a `delta{value,parent,changed}` diff, which stores drift-prone cached parents). DTM is source-authoritative (a gate that rejects a sourced value is broken; crop-band coherence is advisory, never blocks). | plant-astro variety-driven timing recompute + **region x variety** ("your short season fits Navy@85, not Kidney@100"), Spec 2. **INV-2:** the app must NOT consume a crop's variety DTM as load-bearing until that crop is gate-clean / the gate is hard. Memory `variety-dtm-load-bearing-deferred`. |

| 13 | **Tree-variety block** (per-variety, tree-fruit archetype) -- `bloom_group` (very_early..very_late, T1-sourced), `bloom_window_relative` ([start,end] in [0,1], DERIVED from bloom_group by a fixed 5-band map, not independently sourced), `bloom_duration_days`, `chill_hours_required`, `use`, `triploid` (bool sterile-pollen flag), optional `self_fruitful` (no/partial/yes override of crop `self_fertile`); `maturity_class` reused as RIPENING season, kept DISTINCT from bloom_group; NO `days_to_maturity` (grafted/season-only) | **PILOT COMPLETE 2026-07-11** (apple, 16 varieties; spec+plan `docs/superpowers/{specs,plans}/2026-07-11-apple-variety-pilot*`; canonical `340c2983` -> `42743be1`). `variety_detail_gate` refactored to common-core + `variety_archetype` dispatch (absence=annual_dtm so dry-bean byte-untouched; apple declares tree_fruit); tree-block validators + bloom coherence (adversarial RED proven on real apple shape); register rulings for `bloom_group`+`self_fruitful` (varieties.recommended path) + `variety_archetype` (EXCLUDED_KEYS). Soft/standalone. | **HARD-FLIP (INV-1):** flip the `variety_detail_gate` tree-block checks from soft/standalone into the A39 register-coverage hard floor + `gate_all` when the Spec-2 rollout column pass reaches full-roster coverage. Soft is a stage, not a resting state. | Tree-archetype PILOT (apple) proving the schema before the roster-wide column pass (reconcile the 5 variety shapes + fold in `varieties_detail[]` on 26 trees/berries, Spec 2). Common-core + archetype-block dispatch: the row-12 bean traits are ANNUAL-archetype-required, this tree block is TREE_FRUIT-required, selected by crop `variety_archetype`. Sourcing: bloom/ripening/pollination T1 (Utah State, Missouri G6001, Texas A&M, UF/IFAS, NC State, Maine, WSU); chill T1 only for warm-climate types, else T2/T3/T4 recorded in `confidence_tier` + a crop-level transparency note (per-variety `sources` stay T1-ONLY so cert E.source-tier holds). Low-chill trio kept at University of Arizona field-cropping values (`uariz_ext`), not the higher stated lab reqs. | plant-astro **honest bloom calendar**: anchor x relative bloom = per-region dates; chill-viability filter ("won't fruit here"); bloom-overlap cross-pollination; triploid exclusion from pollinizer picks. **INV-2:** do NOT consume variety bloom/chill as load-bearing until the crop is gate-clean. Memory `apple-variety-pilot-tree-archetype`. |

| 14 | **Photoperiod-variety block** (per-variety, photoperiod-annual archetype) -- `day_length_type` (enum long_day/intermediate_day/short_day, the LOAD-BEARING regional-viability class -- "will it bulb at your latitude?"), sharing `days_to_maturity` (LOAD-BEARING, from_planting) with the DTM archetypes, + `use`; common core (`id`/`maturity_class`/`is_reference`/`confidence_tier`/`note_beginner`/`note_seasoned`/`sources`/`anchoring_urls`); NO bean traits, NO tree block | **PILOT COMPLETE 2026-07-12** (onion, 6 varieties; spec+plan `docs/superpowers/{specs,plans}/2026-07-12-onion-variety-pilot*`; canonical `a6ead469` -> `e45bcf3c`). `variety_detail_gate` extended to a 3-WAY archetype dispatch (`photoperiod_annual` added; SHARES the `_dtm_checks` DTM machinery across {annual_dtm, photoperiod_annual}; `day_length_type` enum-validated only); adversarial RED proven on real onion shape. **NO new register ruling needed** (`day_length_type`/`use`/common-core keys already ruled; A25 = 0 unruled on the release battery). The A9 `photoperiod_gate` honesty engine (coverage + window-fit + variety typing) already existed and is UNTOUCHED -- day-length-vs-region checks are NOT duplicated in `variety_detail_gate`. Soft/standalone. | **HARD-FLIP (INV-1):** flip the `variety_detail_gate` photoperiod-block checks from soft/standalone into the A39 register-coverage hard floor + `gate_all` when the Spec-2 rollout column pass reaches full-roster coverage. Soft is a stage, not a resting state. | Photoperiod-archetype PILOT (onion) -- the THIRD variety archetype after dry-bean (annual_dtm) + apple (tree_fruit), proving the schema on a DTM annual whose varieties ALSO carry a day-length class (two load-bearing per-variety dimensions). Common-core + archetype-block dispatch: onion declares `variety_archetype: photoperiod_annual`. Sourcing (Trevor-signed 2026-07-12): 4 T1 / 2 T2; 5 of 6 `day_length_type` cited to a catalogued T1 (Cimarron's intermediate-day is stated by NC State, real T1-tier but uncatalogued, so carried at T2 with day-length uncited); Cimarron + Super Star are the two T2 varieties (T2 on DTM: derived / seed-trade), honesty in `confidence_tier` + prose + a crop-level DTM-transparency note (per-variety `sources` T1-ONLY so cert E.source-tier holds); source_set +msu_ext, NO source_catalog additions. Onion = photoperiod + **ALLIUM-family** exemplar (shallot/leek/garlic follow-on). | plant-astro **per-latitude variety detail**: the region already resolves "grow long-day here"; enriched varieties render full per-variety detail matched to latitude. **INV-2:** do NOT consume variety `days_to_maturity` as load-bearing until the crop is gate-clean (`day_length_type` already trustworthy via A9). Memory `onion-variety-pilot-photoperiod`. |

*(Add a row when a consumer needs a new cross-crop field. Keep status/trigger current.)*

---

## To adopt (cert session)

1. **Surface it where it'll be seen** — add a one-line pointer to plant-dataset `CLAUDE.md` (the file
   every cert session auto-loads), e.g.:

   ```markdown
   ## Adding a cross-crop field
   Before adding any field across crops, follow `docs/gs_cross_crop_field_addition_v0.md` and check
   `docs/field_addition_register.md` (live queue + trigger conditions). Run column passes against a
   stable roster, never mid-certification.
   ```

2. **Graduate the method** from `gs_cross_crop_field_addition_v0.md` (proposal) to `_v1_0` once adopted.
3. **Commit** this register + the note on your terms (left untracked by the plant-app session that
   drafted them — origin: the seed-trays feature design, 2026-06-30).

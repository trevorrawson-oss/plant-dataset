# Field-Addition Register — the live queue for cross-crop field additions

**What this is:** the standing, tracked queue of new fields to add across the crop roster, plus the
status and **trigger condition** for each. Consult this **before** adding any field that spans crops.
It is the "track" that makes the cross-crop field-addition process visible instead of buried.

**Method:** follow `docs/gs_cross_crop_field_addition_v0.md` (the "column GS arc" template — contract
first → diverse pilot incl. a legitimately-N/A case → bot rollout with a schema-validation gate +
coverage report → fold into the per-crop checklist OR run as a post-roster column pass; amend
already-certified crops with per-field provenance, never a full re-cert).

**Standing principle (load-bearing):** **run a column pass against a STABLE / complete roster, not
mid-certification.** Adding a datapoint while crops are still being certified re-opens already-done
crops *and* bolts the field onto in-flight ones — a moving target. Prefer: finish the roster, then
one clean column pass.

---

## Register

| # | Field | Status | Trigger | Approach | Consumer / notes |
|---|---|---|---|---|---|
| 1 | `growth_stages[].day_range_from_sow` (germination "expect sprouts in ~N days" window) | **folded into #4** (2026-07-07: the timing-spine bundle; the ladder is authored WITH `dtm_anchor`+DTM, not alone) | full crop roster (~123) certified | single **post-roster column pass** (NOT folded into the per-crop checklist this round) | plant-app **seed trays** reads it for the sprout-window line; it **graceful-omits** where absent, so the feature is **not blocked** — the line lights up when this lands. The number is already authored in each germination stage's prose ("in about 5 to 10 days…"), so this is a *structuring* job, not new research. Present today on ~12 of 31 app-certified crops; missing on tray-started annuals (brussels, cabbage, cauliflower, kale, broccoli, pumpkin, butternut, zucchini, zinnia). Legitimately-N/A: crops not grown from seed (perennials/trees — bare-root/nursery). |
| 2 | Establishment-only `calendar[]` for chill-limited `survives_no_fruit` cells (perennials) | **queued** | full crop roster certified | post-roster column pass + **A3 gate amendment**: today the no-fruit split (perennial_gate A3, incognito-audit B2) requires chill-limited `survives_no_fruit` cells to carry an EMPTY calendar (no over-promised bloom/harvest dates). Amend to require an establishment-only calendar instead — plant/growing/dormant/prune/care tokens derived from the region's `plantings[]` + dormancy window via a new `derive_establishment_calendar` beside `derive_tree_calendar`; still ZERO bloom/harvest tokens, so the honesty invariant holds. Cold-edge no-fruit cells (chill met) are unaffected. ~40 cells across 13 tree crops today. | plant-astro guide zone pages render the care timeline under the suitability note ("survives but rarely crops"); currently the calendar section graceful-omits. Product call Trevor 2026-07-02 (origin: sour cherry `ca_interior` z9 — "even if it survives people might still plant it"). |
| 3 | `pet_safe` (pet-friendly vs. not — cats/dogs/horses toxicity) | **queued** | full crop roster certified (post-114) | post-roster column pass. A structured, consumer-facing flag (bool or small enum, e.g. `safe` / `toxic` / `caution`) with per-crop provenance, so plant-astro can render a **quick pet-friendly / not-pet-friendly icon** on each crop. Today the safety fact lives only as PROSE in `failure_diagnostics` (rosemary = safe; chives = toxic, added Wave 4) — inconsistent and un-iconizable. Contract-first pilot must include a legitimately-safe crop, a clearly-toxic one (allium/chives, nightshade foliage, sweet-pea seed, elderberry raw), and a caution case. **Source-tier decision to settle here:** ASPCA's Toxic/Non-Toxic Plants list is the canonical pet-toxicity authority but is non-`.edu` (same class as `rhs`) — decide whether to admit it as the anchor source for this field, alongside the extension `.edu` pages (NCSU plant toolbox tags "problem for cats/dogs/horses"). | Trevor 2026-07-06: wants pet-friendly / not-pet-friendly as a reliable roster dimension, rendered as a **quick icon**; "a big issue... something I want to have and not get wrong." Chives (allium, toxic) + rosemary (safe) already give both poles for the pilot. |

| 4 | **Timing-spine bundle** -- `propagule`, `dtm_anchor`, `sow_depth_inches`, `thin_to_inches`, `harvest_window_days`, `divide_every_years` (+ the pre-existing `growth_stages[].day_range_from_sow` ladder, subsumes register #1) | **in progress** (Phase 1 done 2026-07-07) | full 114 roster certified (MET) | **coupled-bundle** column GS arc: one-time contract + gate layer (`docs/timing_spine_contract.md`, `tools/timing_spine_gate.py`), then author per-crop in archetype batches (garlic pilot -> fall/winter -> rest). NOT a field-by-field sweep -- the ladder only means anything relative to `dtm_anchor`+DTM, and depth/spacing/propagule are archetype-determined. Amend-not-recert with per-field provenance. Fold into the per-crop checklist for new (§E) crops. | plant-app `crop-timing.ts` (already built, graceful): `daysToHarvestFromStage` reads the ladder, `effectiveDtm`/`dtmAnchor` read DTM+anchor, accessors expose depth/window/propagule/thin. Consumer contract: `plant-app/src/lib/guides.ts`. Phase 1 baseline (`4abf43a5`): 1 hard defect surfaced (`shallot` ladder order) + 8 anchor-dependent warnings -- see `docs/2026-07-07-timing-spine-phase1-findings.md`. Absorbs register #1 (`day_range_from_sow`). |

| 5 | `watering.schedule_by_stage[]` -- PER-STAGE watering (rate/amount, frequency, method, level + dual-register notes per growth stage; the "how much to water after sprouts grow" data) | **COMPLETE 2026-07-07 (114/114 certified)** -- authored across 12 batches (W1 tomatoes -> W7 herbs, B8-B9 flowers, B10 microgreens, B11 citrus, B12 woody herbs; canonical `6f584c3b` -> `b15a5a0f`). Each crop authored INDIVIDUALLY from its own certified watering prose (blanket-authoring guarded, per-crop distinctions surfaced every batch); annuals/microgreens got FULL-stage coverage, the 10 perennials (citrus + woody herbs, Trevor's approved judgment call) got the SPARSE tree pattern (3 key-stage entries, subset of real growth-stage ids, blueberry precedent). Gate-clean throughout (whole_crop_gate + register_completeness + dash/temp + release_verify + pre-commit backstop). The 10 crops without it = the uncertified §E shells, correctly excluded. | done | backfilled for annuals using the EXISTING `schedule_by_stage` shape (already on 23 winter + 21 trees/perennials, e.g. the blueberry anchor); per-stage, from certified crop-level watering prose; its OWN focused pass. Paired with the day_range_from_sow ladder (same stage ids). | plant-app per-stage watering guidance (seedling -> established -> mature). Captures germination "keep the surface damp" + post-sprout "water deeply, less often". |
| 6 | GERMINATION + SEEDLING **LIGHT** -- `germination_light` (enum: light_required/dark_preferring/neutral, or null=no-home-seed-path), `seedling_light` (enum: bright_default/na/blackout_then_bright/photoperiod_capped), `seedling_light_cap_hours` (int, iff photoperiod_capped) | **COMPLETE 2026-07-07** (all 114 certified: germ SET 85/N-A 29, seedling bright 49/na 57/blackout 8; TODO = 10 §E shells) | prior column passes (#4/#5/#7) complete on stable 114 roster | Column GS arc. Contract `docs/seedling_light_contract.md`. germination_light = the genuinely-varying per-crop fact (full sourced classification pass, grouped sourcing); seedling_light = default (`bright_default`) + typed exceptions. N/A rule: germination_light SET iff realistically home-seed-startable (propagule==seed MUST be SET; seed-startable herbs SET despite transplant propagule; trees/citrus/vegetative = null). Microgreens IN-scope (opposite of #7): neutral + blackout_then_bright. photoperiod_capped RESERVED (0 live members; gate RED-test proven). Gate `tools/seedling_light_gate.py` (TDD, `--coverage`). C11: germination_light/seedling_light ruled into EXCLUDED_KEYS. | plant-app seedling-light guidance (leggy-seedling avoidance) + germination guidance (surface-sow vs cover-to-exclude-light). |

| 7 | STRUCTURED climate **thresholds** -- `heat_threshold_f` + `heat_effect`, `frost_tolerance_f` + `frost_effect`, `chilling_sensitivity_f` (`germination_temp_f` already structured) | **COMPLETE 2026-07-07 (`b15a5a0f` -> Phase 1 `d3a6912f` -> ROLLOUT `6659042d`); all 106 outdoor certified crops carry the 3 fields** | before the notifications + WeatherKit build (~week of 2026-07-14) | Column GS arc. Contract `docs/climate_thresholds_contract.md` (single-value thresholds not bands; heat=daytime-HIGH; coherence frost<chilling<heat; heat null=N/A heat-lover). Gate `tools/climate_threshold_gate.py` (TDD, `--coverage`, `INDOOR_SLUGS`). Trevor confirmed: keep the *_effect enums (make the deterministic alert message accurate) + add `chilling_sensitivity_f` (numeric-only, N/A for cold-adapted crops). Semantic calls: fruit-tree frost = spring BLOSSOM frost ~28F (not winter wood hardiness; fig/pomegranate exception); "chill hours" = dormancy requirement NOT chilling injury; microgreens 8 = N/A-indoor; very-hardy crops use USDA-zone hardiness. C11: heat_effect/frost_effect ruled into register EXCLUDED_KEYS. Coverage: heat SET 46/N-A 60, frost SET 106, chilling SET 29/N-A 77; only TODO = the 10 uncertified §E shells (pick up at certification). **Follow-ons:** fold into the per-crop GS-arc checklist; optional `*_night_f` for the deferred night-temp effects. | plant-app notifications engine + AI context-assembly. Dataset already had partial scaffolding (`hard_freeze`/`FROST_PROTECT` tip tokens) -- these fields give them firing numbers. See memory `notifications-ai-architecture`. |

| 8 | **Register-coverage HARD gate** (infrastructure, not a field) -- enforce that every CERTIFIED crop carries each SHIPPED register field or its explicit null/N-A | **COMPLETE 2026-07-07** (tooling only, canonical untouched) -- `tools/register_coverage_gate.py` (+ `tools/test_register_coverage_gate.py`, TDD RED->GREEN), wired into `whole_crop_gate` as **A39**. GREEN on all 114 certified (A39=0 each; standalone PASS). RETROACTIVE #4-#7 + #6. | a register field's rollout is COMPLETE (turn on per field only then) -- all four met | Universal present-or-explicit-null cert gate modeled on `whole_crop_gate` A17 (npk_ratio) / A20 (display-readiness). A certified crop (`verified_gs_arc`) must carry, or its defined null/N-A: **#4** `propagule` (universal), `dtm_anchor` (unless empty-DTM), `sow_depth_inches` (seed-like non-microgreen); **#5** `watering.schedule_by_stage` (non-empty); **#6** `germination_light` + `seedling_light` (key present; null is a present value); **#7** `heat_threshold_f`/`frost_tolerance_f`/`chilling_sensitivity_f` (key present). Exempt: uncertified §E shells (no-op off `verified_gs_arc`) + legit-N/A predicates **imported** from the standalone gates (`timing_spine_gate.dtm_empty`/`is_microgreen`/`SEED_LIKE`, `climate_threshold_gate.INDOOR_SLUGS`). N-A tallies: 29 empty-DTM (no anchor), 43 sow_depth-N/A, 8 INDOOR_SLUGS, 29 germ-null. The ladder / thin_to / harvest_window / divide_every stay archetype-OPTIONAL (not hard-required). | plant-dataset cert integrity -- stops silent coverage drift as new crops certify. |

| 9 | **Seed-tray cell protocol** -- the tray-start step between germination and harden-off: sow-density + cell-thinning (+ pot-up). `tray_sowing` enum { `multi_sow_thin_to_one` (default) / `single_sow` / `multisow_clump` (RESERVED, 0 live) / `na` } + `pot_up` enum { `recommended` / `optional` / `not_needed` } (present iff a real tray value). | **COMPLETE 2026-07-07** (all 114 certified; `6990f1da` -> `17ff6b67`). Contract `docs/seed_tray_protocol_contract.md`; gate `tools/seed_tray_gate.py` (TDD); C11 ruled both enums into `register_completeness` EXCLUDED_KEYS; A39 folded (`register_coverage_gate` requires `tray_sowing` present-or-na). Coverage: multi 33 / single 16 / clump 0 (reserved) / na 65; pot_up recommended 13 / optional 15 / not_needed 21. **`pot_up` shipped as a 3-value ENUM not a bool** (a bool could not say "optional" -- broccoli surfaced it); **`multisow_clump` RESERVED** (no crop's authored prose supports seed-tray multisowing; leek + spring-onion are individual-transplant, so leek=multi_sow_thin_to_one). Sourced from each crop's own certified prose (prose scan caught 3 refinements: cauliflower recommended, zinnia/parsley not_needed). | stable 114 roster (MET); design-first | Column GS arc, mostly-standard-with-exceptions (mirrors seedling_light). THE GAP (verified): tray-started crops carry `start_method.hardening_off_*` but NO structured "sow a few seeds per cell -> thin to the strongest -> pot up" guidance. The existing `thinning{}` object is a DIFFERENT concept -- DIRECT-SOWN seedbed/row thinning to a final in-ground `to_spacing` (present on carrot/lettuce; absent on tomato/pepper/broccoli). Exceptions: multisow-clump (leeks/scallions/beets, transplant the clump, do NOT thin); single-sow large seed (cucurbits if tray-started); `na` = direct-sown (its thinning is the in-ground `thinning{}`). Ties to `seedling_light`/`weeks_indoors` (applies to the wi>0 tray-started set). Harden-off already structured (`hardening_off_*`), stays put. | plant-app seed-tray flow: "put 2-3 seeds in each cell, thin to the strongest, pot up before hardening off." The missing middle of the seed->transplant journey. |

| 10 | **Multisow-clump showcase** -- give register #9's RESERVED `multisow_clump` value a real live member by AUTHORING the seed-tray multisow method into a bunching onion. A CONTENT arc, not an enum change. | **COMPLETE 2026-07-07** (`17ff6b67` -> `00e0b6b1`; spring-onion). A THIRD start_method route authored into both registers (sow a pinch of ~6-10 seeds/cell, transplant the whole clump undivided, no thinning, space clumps 4-6 in, harvest the biggest first), then `tray_sowing` flipped `multi_sow_thin_to_one` -> `multisow_clump` (`pot_up` stays `not_needed`). Additive amend, coherent with spring-onion's existing direct-sow + individual-transplant routes. Sourced RHS + Johnny's bunching-onion guide (method popularized by Charles Dowding), cited "(RHS)" inline. **LEGAL:** facts/methods are not copyrightable (17 USC 102(b)/Feist); original prose, no reliance on the Old Farmer's Almanac. seed_tray_gate PASS (multisow_clump 1 live / 0 reserved), whole_crop_gate + register_completeness + register_coverage + gate_all + release_verify all clean. | after #9 (MET); Trevor's go (given 2026-07-07) | Authored into spring-onion (NOT a new crop -- roster stays 124). | plant-app seed-tray flow: the "sow a cluster, transplant the whole clump, no thinning" line -- surfaces plant's forward-thinking method guidance. |

| 11 | **Seed-start indoor OFFER** for the 12 seed-startable non-seed crops -- make the indoor-from-seed path fully offered (method prose + `seedling_light`/`tray_sowing`/`pot_up`/`weeks_indoors`), not just `germination_light`. A CONTENT + enum-re-adjudication arc over EXISTING fields, not a new field. | **COMPLETE 2026-07-08** (`00e0b6b1` -> `86ccf8c3`; 12 crops in 5 batches). Contract `docs/seed_start_method_contract.md`; design/plan under `docs/superpowers/`. THE GAP: #6 set `germination_light` from the from-seed fact but keyed `seedling_light` (then #9 `tray_sowing`) off the RECOMMENDED path, so nursery-stock crops cascaded to `na` -- the data asserted an indoor-from-seed path while withholding the tools to walk it. FIX (Trevor's source-of-truth call, extending the #6 principle): 11 crops flipped `seedling_light` na->bright_default + a real `tray_sowing` + `pot_up`; **chives->multisow_clump** (2nd live member); 5 gained `weeks_indoors` (lavender/rosemary/thyme 10, oregano/sage 8); **onion/shallot `germination_light` null->neutral** (their prose already teaches from-seed); method prose authored per gap (mint/lavender/rosemary/oregano/thyme + pawpaw's full stratification method). **pawpaw = honest exception:** keeps `seedling_light`/`tray_sowing` na (shade seedling, deep-pot single seed) + gets the KSU stratification method in prose + `germination_temp_f` []->[75,85]. **lemongrass DEFERRED** (genuine no-seed grass, null correct). No new gate, no gate change (scratch-spike + full-battery verified; `gate_all` 114/114). Coverage: germ SET 85->87/N-A 29->27; seedling bright 49->60/na 57->46; tray multi 42/single 16/clump 2/na 54. | after the #4-#10 spine complete on stable 114 (MET); Trevor's go 2026-07-08 | Sourced per-crop T1 (RHS chives, Johnny's mint/rosemary, KSU pawpaw, extension lavender/oregano/thyme, own prose sage/onion/shallot); original prose (17 USC 102(b)/Feist); provenance in the contract + STATE_HISTORY. amend-not-recert; SHA-guarded per batch via `tools/apply_patch.py` + `tools/batches/seed_start_b1..b5_*.json`. | plant-app: a full "How to start [crop] indoors from seed" offer (directions + a `weeks_indoors`-computable start date) alongside the recommended transplant path. Follow-ons (deferred kickoffs): growth-stage arc; per-region indoor-start calendar. |

| 12 | **Variety-detail bundle** (per-variety, FLAT) -- `id` (stable slug), `days_to_maturity` (LOAD-BEARING), `maturity_class` (early/mid/late; unifies the old dtm_class/season split), `seed_type` (open_pollinated/hybrid/heirloom), `seed_color`, `seed_size`, `plant_habit`, `primary_use`, `is_reference` (exactly one/crop), `confidence_tier`, `note_beginner`/`note_seasoned`, `sources`/`anchoring_urls` (+ optional `disease_notes`/`regional_fit`) | **PILOT in progress 2026-07-11** (dry-bean; spec + plan `docs/superpowers/{specs,plans}/2026-07-11-dry-bean-variety-pilot*`). Soft `tools/variety_detail_gate.py` (TDD, timing_spine pattern) + `register_completeness` ruling of the new keys, both landed; canonical splice pending. | **HARD-FLIP (INV-1):** flip `variety_detail_gate` from soft/standalone into the A39 register-coverage hard floor + `gate_all` when the Spec-2 rollout column pass reaches full-roster coverage. Soft is a stage, not a resting state (timing_spine -> A39 precedent). | Single-crop GS-arc **PILOT first** (dry-bean, 5 varieties, soft gate), THEN a roster-wide **column pass** reconciling the 5 existing variety shapes (37 simple `{name,dtm,note}` / 30 name-strings / 10 exploratory `delta` / 39 assorted) + folding in `varieties_detail[]` (26 trees/berries) + **retiring the exploratory `delta` overlay** (Spec 2). **FLAT, override-by-ABSENCE** (a variety carries a field only where it differs; the crop default is inherited by absence -- NOT a `delta{value,parent,changed}` diff, which stores drift-prone cached parents). DTM is source-authoritative (a gate that rejects a sourced value is broken; crop-band coherence is advisory, never blocks). | plant-astro variety-driven timing recompute + **region x variety** ("your short season fits Navy@85, not Kidney@100"), Spec 2. **INV-2:** the app must NOT consume a crop's variety DTM as load-bearing until that crop is gate-clean / the gate is hard. Memory `variety-dtm-load-bearing-deferred`. |

| 13 | **Tree-variety block** (per-variety, tree-fruit archetype) -- `bloom_group` (very_early..very_late, T1-sourced), `bloom_window_relative` ([start,end] in [0,1], DERIVED from bloom_group by a fixed 5-band map, not independently sourced), `bloom_duration_days`, `chill_hours_required`, `use`, `triploid` (bool sterile-pollen flag), optional `self_fruitful` (no/partial/yes override of crop `self_fertile`); `maturity_class` reused as RIPENING season, kept DISTINCT from bloom_group; NO `days_to_maturity` (grafted/season-only) | **PILOT COMPLETE 2026-07-11** (apple, 16 varieties; spec+plan `docs/superpowers/{specs,plans}/2026-07-11-apple-variety-pilot*`; canonical `340c2983` -> `42743be1`). `variety_detail_gate` refactored to common-core + `variety_archetype` dispatch (absence=annual_dtm so dry-bean byte-untouched; apple declares tree_fruit); tree-block validators + bloom coherence (adversarial RED proven on real apple shape); register rulings for `bloom_group`+`self_fruitful` (varieties.recommended path) + `variety_archetype` (EXCLUDED_KEYS). Soft/standalone. | **HARD-FLIP (INV-1):** flip the `variety_detail_gate` tree-block checks from soft/standalone into the A39 register-coverage hard floor + `gate_all` when the Spec-2 rollout column pass reaches full-roster coverage. Soft is a stage, not a resting state. | Tree-archetype PILOT (apple) proving the schema before the roster-wide column pass (reconcile the 5 variety shapes + fold in `varieties_detail[]` on 26 trees/berries, Spec 2). Common-core + archetype-block dispatch: the row-12 bean traits are ANNUAL-archetype-required, this tree block is TREE_FRUIT-required, selected by crop `variety_archetype`. Sourcing: bloom/ripening/pollination T1 (Utah State, Missouri G6001, Texas A&M, UF/IFAS, NC State, Maine, WSU); chill T1 only for warm-climate types, else T2/T3/T4 recorded in `confidence_tier` + a crop-level transparency note (per-variety `sources` stay T1-ONLY so cert E.source-tier holds). Low-chill trio kept at University of Arizona field-cropping values (`uariz_ext`), not the higher stated lab reqs. | plant-astro **honest bloom calendar**: anchor x relative bloom = per-region dates; chill-viability filter ("won't fruit here"); bloom-overlap cross-pollination; triploid exclusion from pollinizer picks. **INV-2:** do NOT consume variety bloom/chill as load-bearing until the crop is gate-clean. Memory `apple-variety-pilot-tree-archetype`. |

| 14 | **Photoperiod-variety block** (per-variety, photoperiod-annual archetype) -- `day_length_type` (enum long_day/intermediate_day/short_day, the LOAD-BEARING regional-viability class -- "will it bulb at your latitude?"), sharing `days_to_maturity` (LOAD-BEARING, from_planting) with the DTM archetypes, + `use`; common core (`id`/`maturity_class`/`is_reference`/`confidence_tier`/`note_beginner`/`note_seasoned`/`sources`/`anchoring_urls`); NO bean traits, NO tree block | **PILOT COMPLETE 2026-07-12** (onion, 6 varieties; spec+plan `docs/superpowers/{specs,plans}/2026-07-12-onion-variety-pilot*`; canonical `a6ead469` -> `e45bcf3c`). `variety_detail_gate` extended to a 3-WAY archetype dispatch (`photoperiod_annual` added; SHARES the `_dtm_checks` DTM machinery across {annual_dtm, photoperiod_annual}; `day_length_type` enum-validated only); adversarial RED proven on real onion shape. **NO new register ruling needed** (`day_length_type`/`use`/common-core keys already ruled; A25 = 0 unruled on the release battery). The A9 `photoperiod_gate` honesty engine (coverage + window-fit + variety typing) already existed and is UNTOUCHED -- day-length-vs-region checks are NOT duplicated in `variety_detail_gate`. Soft/standalone. | **HARD-FLIP (INV-1):** flip the `variety_detail_gate` photoperiod-block checks from soft/standalone into the A39 register-coverage hard floor + `gate_all` when the Spec-2 rollout column pass reaches full-roster coverage. Soft is a stage, not a resting state. | Photoperiod-archetype PILOT (onion) -- the THIRD variety archetype after dry-bean (annual_dtm) + apple (tree_fruit), proving the schema on a DTM annual whose varieties ALSO carry a day-length class (two load-bearing per-variety dimensions). Common-core + archetype-block dispatch: onion declares `variety_archetype: photoperiod_annual`. Sourcing (Trevor-signed 2026-07-12): 4 T1 / 2 T2; 5 of 6 `day_length_type` cited to a catalogued T1 (Cimarron's intermediate-day is stated by NC State, real T1-tier but uncatalogued, so carried at T2 with day-length uncited); Cimarron + Super Star are the two T2 varieties (T2 on DTM: derived / seed-trade), honesty in `confidence_tier` + prose + a crop-level DTM-transparency note (per-variety `sources` T1-ONLY so cert E.source-tier holds); source_set +msu_ext, NO source_catalog additions. Onion = photoperiod + **ALLIUM-family** exemplar (shallot/leek/garlic follow-on). | plant-astro **per-latitude variety detail**: the region already resolves "grow long-day here"; enriched varieties render full per-variety detail matched to latitude. **INV-2:** do NOT consume variety `days_to_maturity` as load-bearing until the crop is gate-clean (`day_length_type` already trustworthy via A9). Memory `onion-variety-pilot-photoperiod`. |

| 15 | **`rgv` region** (roster-wide region column) -- Rio Grande Valley / subtropical South Texas, `zone_span` `["9","10"]`, `regions.rgv` cell per crop + `region_chill_delivered.rgv` | **SHIPPED 2026-07-13** (canonical `d0832254`; commit `4e2e9e7`; count 125 unchanged, 116 certified unchanged). Landed across all 108 certified region-carrying crops in one atomic, SHA-guarded release (`tools/build_rgv_promote.py`): `EXPECTED_SPANS.rgv` + 108 `regions.rgv` cells + `region_chill_delivered.rgv = {"9":[0,300],"10":[0,300]}` + a provenance replace, together. Retires the se_gulf z10 interim (the 2026-07-12 reconciliation's borrowed Gulf-coast dates for 95 TX z10 ZIPs). | full 108-crop certified roster (region-carrying subset) | Column GS arc, same method as any new region: 79 frost_anchored annuals, 5 flagship citrus (lime marginal), 14 chill-gated trees (A3 no-fruit split, pawpaw unsuitable), 5 woody herbs, 4 berries, strawberry. No new gate -- reuses A45 `zone_span_gate`, A3 `perennial_gate`, A31/A32 `coverage_floor_gate` (all already region-generic). New one-off tooling: `tools/rgv_cell_audit.py`, `tools/rgv_harness.py`, `tools/build_rgv_promote.py`. | T1 (TAMU AgriLife Lower Rio Grande Valley / South Texas extension guides). plant-app consumer follow-up: 785xx ZIP3 fence to `rgv` (kickoff `docs/kickoffs/26-rgv-plant-app-zip3-fence.md`, roadmap item 2); plant-astro consumes the new region automatically once the submodule bumps. Spec+plan `docs/superpowers/{specs,plans}/2026-07-13-rgv-subtropical-tx-region*`. |
| 16 | **Hardiness-variety block + overwinter engine** (per-variety, hardiness-annual archetype) -- `cold_hardiness_class` (enum tender/hardy/very_hardy, the LOAD-BEARING overwintering-viability class), optional `min_temp_f` (int, T1-or-OMITTED, allows negatives -- NOT the tree positive-int validator), sharing `days_to_maturity` (LOAD-BEARING, from_transplant) with the DTM archetypes, + `use`; common core; PLUS a crop-level `winter_hardiness` model (dual-register `explainer_beginner`/`explainer_seasoned`, mirrors onion's `photoperiod` object) + a `gating_factors` `winter_hardiness` token | **PILOT COMPLETE 2026-07-14** (leek, 6 varieties; canonical `d0832254` -> `77f03cc8`; gate commits `f0516bb`/`b8d4c36`, builder `a05942b`). `variety_detail_gate` extended to a **4-WAY** archetype dispatch (`hardiness_annual` added; SHARES `_dtm_checks`; `cold_hardiness_class` enum-validated + `min_temp_f` band-checked incl. negatives). NEW standalone **`overwinter_hardiness_gate`** engine (the real build, the A9-analog): fires on the `winter_hardiness` gating token, enforces coverage (>=2 classes) + window-fit (very_hardy!=early / tender!=late); no-op off scope; adversarial RED proven (9 defect classes bounce). `register_completeness` ruled `cold_hardiness_class` into EXCLUDED_KEYS (TDD, like `day_length_type`). Both pieces SOFT/standalone. Sourcing **4 T1 / 2 T2** (Trevor-signed): class T1 all 6; DTM T1 for 4 via the Cornell CCE 2020 leek trial (doc_944), T2 for 2 OP heirlooms. | **HARD-FLIP (INV-1):** flip the `variety_detail_gate` hardiness-block checks **and** the `overwinter_hardiness_gate` from soft/standalone into the A39 register-coverage hard floor + `gate_all` when the Spec-2 rollout column pass reaches full-roster coverage. Soft is a stage, not a resting state. | Hardiness/overwintering-archetype PILOT (leek) -- the **4th** variety archetype after annual_dtm (dry-bean) / tree_fruit (apple) / photoperiod_annual (onion). An **ENGINE BUILD**, not just a schema add: leek's honesty engine (`overwinter_hardiness_gate`) did not exist, unlike onion which reused A9. The zone-coupling machinery is built to be REUSED -- **garlic + artichoke inherit it** -- but leek's survives-cold viability RULE is leek-specific (vernalization / needs-cold designed separately). The crop-level `winter_hardiness` model is the sourced footing the honesty engine + the app both rest on. Sourcing: class T1 all 6 (RHS/USU/UMN/Cornell); DTM T1 for King Richard 85/Lancelot 100/Tadorna 110/Bandit 120 via Cornell CCE 2020 trial (doc_944, "DTM from Transplant"), T2 for Large American Flag/Giant Musselburgh (OP heirlooms, not in the extension trials); `min_temp_f` OMITTED (no per-variety T1 degF); crop descriptions folded in the variety spectrum (Trevor 2026-07-14, the backfill template for apple/dry-bean/onion). | plant-astro **per-zone overwintering viability** (**INV-2**): the engine validates the data can SUPPORT an overwintering story; the app produces the per-zone "which class survives in zone N" claim from the region's USDA zone. Memory `leek-variety-hardiness-archetype-ready` / `onion-variety-pilot-photoperiod`. |
| 17 | **`pnw` region** (roster-wide region column) -- maritime Pacific Northwest, WA/OR west of the Cascades, `zone_span` `["8","9"]`, `regions.pnw` cell per crop + `region_chill_delivered.pnw` | **SHIPPED 2026-07-14** (canonical `8dd4ac4c`; `060d8711` -> `8dd4ac4c`; count 125 unchanged, 116 certified unchanged). Landed across all 108 certified region-carrying crops in one atomic SHA-guarded promote (`tools/build_region_promote.py`, 110 patches): `EXPECTED_SPANS.pnw` + 108 `regions.pnw` cells + `region_chill_delivered.pnw = {"8":[968,1950],"9":[700,1500]}` + a provenance replace-append, together. | full 108-crop certified roster (region-carrying subset) | Column GS arc, same method as RGV (row 15) BUT **frost-ANCHORED, the inversion**: `resolution_method="frost_anchored_resolved"` + real `resolved_from` frost dates (z8 Sea-Tac NOAA / z9 Astoria) + the standard `annual_calendar` deriver + `cold_pause` winters -- NO Hawaii-shape hand-authoring, far lighter than RGV. 79 frost_anchored annuals (summer is the growing window, no `heat_pause`; warm crops transplant-led + honest-marginal per OSU EM9027, okra/sweet-potato/melons "not suitable"), 14 chill-gated trees (**A3 FRUIT flip** -- `fruits_reliably`/`marginal`, never `survives_no_fruit`-empty since chill `[968,1950]` clears the floor; the opposite of RGV), 5 citrus cold-limited, 5 woody herbs (lavender thrives), 4 berries (WA #1 raspberry, premier blueberry) + strawberry. No new gate -- reuses A45 `zone_span_gate`, A3 `perennial_gate`, A31/A32 `coverage_floor` (all region-generic). New region-generic tooling `tools/region_harness.py` + `tools/region_cell_audit.py` + `tools/build_region_promote.py` (parametrized from the RGV one-offs; `cold_pause` allowed for the frost-anchored region). | T1 (WSU/OSU extension: OSU EM9027 vegetable calendar, WSU EB0937 western-WA fruit handbook, WSU Skagit chill, NOAA frost normals). plant-app consumer follow-up: west-side ZIP3 fence to `pnw` (kickoff `docs/kickoffs/28-pnw-plant-app-zip3-fence.md`) so the hot-dry east-of-the-Cascades z8 pockets do not resolve to a maritime calendar; plant-astro consumes the new region automatically once the submodule bumps. Spec+plan `docs/superpowers/{specs,plans}/2026-07-14-maritime-pnw-region*`. |

| 18 | **`berry` archetype (bearing-habit) + the `hero_description` common-core standard** -- per-variety `bearing_habit` (enum sub-dispatched by a crop-level `berry_group` {strawberry|cane|bush}; strawberry {june_bearing/everbearing/day_neutral}, cane/bush + `chill_hours_required` RESERVED), season-only (no DTM, no chill for strawberry), `maturity_class` = ripening onset; PLUS a NEW universal-common-core `hero_description` (single-register marquee line, additive above the dual-register notes) | **PILOT COMPLETE 2026-07-15** (strawberry, 9 varieties; canonical `8dd4ac4c` -> `db35a673`; count 125 unchanged, strawberry stays certified, 116 certified). `variety_detail_gate` -> 5-WAY archetype dispatch (`berry` added; `ARCHETYPE_ENUMS["berry"]=()` so `bearing_habit` validates dynamically vs `berry_group`; berry NOT in DTM_ARCHETYPES); a standalone honesty engine is explicitly OUT of scope (SHAPE-only; chill-vs-region is a cane/bush-era concern). Cane/bush branches designed + adversarially RED-tested (8/8 defect classes) + RESERVED (0 live). `register_completeness` rules `bearing_habit`/`berry_group` (EXCLUDED_KEYS) + `hero_description` (path-scoped `varieties.recommended`, single-register analog of `pet_safe.note`). Both SOFT/standalone. `hero_description` backfilled onto the 4 prior pilots (33 varieties). | **HARD-FLIP (INV-1):** flip the `variety_detail_gate` berry-block checks into the A39 register-coverage hard floor + `gate_all` when the Spec-2 rollout column pass reaches full-roster coverage. Soft is a stage, not a resting state. | Bearing-habit-archetype PILOT (strawberry) -- the 5th variety archetype after annual_dtm/tree_fruit/photoperiod_annual/hardiness_annual. Single-crop pilot (strawberry isolated for its region-dependent perennial-vs-annual lifecycle); the `berry` schema generalizes to cane/bush (raspberry/blackberry/blueberry/elderberry), which drop in on the reserved branches. Sourcing all T1 (cornell/osu EC1618/umd/umn, already catalogued -> `source_catalog` +none); flagship Albion; content reviews caught + fixed 2 factual + several restatement hero lines. `hero_description` is the going-forward variety common-core standard (Trevor 2026-07-15). | plant-astro/plant-app **per-variety berry detail** (bearing pattern -> harvest planning; for cane/bush, chill -> climate fit). **INV-2:** do NOT consume variety `bearing_habit` as load-bearing until the crop is gate-clean. Memory `variety-dtm-load-bearing-deferred`. Spec+plan `docs/superpowers/{specs,plans}/2026-07-15-berry-variety-pilot*`. |
| 19 | **`mid_atlantic` region** (roster-wide region column) -- Mid-Atlantic Piedmont + Coastal Plain (NC/VA/MD/DC/DE/NJ/PA), `zone_span` `["7","8"]`, `regions.mid_atlantic` cell per crop + `region_chill_delivered.mid_atlantic` | **SHIPPED 2026-07-20** (canonical `e1e01c47` -> `af5dcee9`; 113-patch atomic promote = 111 cells + chill band + provenance; count 128 unchanged, 119 certified). Roadmap item 8, the 3rd authored region after RGV/PNW, frost-anchored. **NO new gate, NO new field** -- reuses A45/A31/A32/A3/A43; sources `ncsu_ext`/`vce_426_331` already catalogued (`source_catalog` +none). The headline is the FALL CYCLE: ~30 warm+cool annuals carry a `second_planting` fall window via the NEW `tools/second_cycle.py` combine-then-split helper (`derive_annual_calendar` does not render `second_planting`). Mid-arc heat_pause correction (cool-crop summer gap = backed heat_pause matching se_gulf, not season_over/growing). Chill band z7[1100,1500]/z8[1000,1350], 4-state T1 basket. Apricot + cherry-sweet `marginal` (Trevor final-review call). | (region column, terminal -- no hard-flip pending) | Roster-wide column via the region GS-arc method (RGV/PNW toolchain: `region_harness`/`region_cell_audit`/`build_region_promote`, all already region-generic). Independent opus content review SHIP-WITH-FIXES (honesty clean, no fabricated sources). Spec+plan `docs/superpowers/{specs,plans}/2026-07-20-mid-atlantic-region*`; kickoff #31; dry-run `docs/reviews/notes/2026-07-20/mid_atlantic_promote_dryrun.md`. | plant-app kickoff #33 (`REGION_STATES.mid_atlantic`; no ZIP3 fence; the z7 half depends on the isWarm resolution fix #32). Items 9-11 (mid-South/Nevada/Utah) follow; item 9 has the identical fall-cycle gap. |
| 20 | **`mid_south` region** (roster-wide region column) -- Mid-South Ozark Uplands + Delta Lowlands (AR/OK/TN/MO), `zone_span` `["7","8"]`, `regions.mid_south` cell per crop + `region_chill_delivered.mid_south` + **6 new `source_catalog` entries** (`nws_lzk` + `uada_ext_{fsa6001,spring_veg,fall_veg,chill,fsa6105}`) | **SHIPPED 2026-07-20** (canonical `af5dcee9` -> `931c1653`; 119-patch atomic promote = 111 cells + chill band + provenance + 6 source adds; count 128 unchanged, 119 certified). Roadmap item 9, the 4th authored region, frost-anchored, templated off mid-Atlantic. **NO new gate, NO new field** -- reuses A45/A31/A32/A3/A43. The 3 deltas from mid-Atlantic: (1) sources NOT pre-catalogued (registered in the batch; `region_harness` injects `staging/<region>_sources.json` into the scratch canonical for per-crop gating, `build_region_promote` emits the source add-patches); (2) real intra-state chill gradient (band z7[1000,1300]/z8[900,1100]; Hope SW warm edge 901 clears McIntosh 900 by ~1hr, flagged honest); (3) blackberry the signature crop (UAEX FSA6105/UA breeding program). Fall cycle from UAEX's tighter table (tomato Jul 1-15 vs VCE Jul 1-Aug 10; +3 UAEX adds sweet-corn/green-beans-bush/potato). apricot/cherry-sweet/pomegranate marginal. | (region column, terminal -- no hard-flip pending) | Roster-wide column via a deterministic controller transform of the certified mid_atlantic cells (`tools/build_mid_south_cells.py`: spring dates shifted by the anchor delta, fall cycles authored fresh from UAEX via `second_cycle.py`, calendars re-derived + heat_pause ported from ground truth) + a 7-shard subagent prose-honesty fan-out + independent content review (SHIP-WITH-FIXES, caught onion/leek/garlic fabricating a nonexistent UAEX table window) + NEW `tools/prose_window_sweep.py` (prose-vs-resolved date-window honesty check; caught a stale-VCE-window residue the sampling review missed). gate_all 119/119; footprint EXACT. Spec `docs/superpowers/specs/2026-07-20-mid-south-region-design.md`; kickoff #34; dry-run + sources note `docs/reviews/notes/2026-07-20/mid_south_*`. | plant-app kickoff #35 (`REGION_STATES.mid_south=AR,OK,TN,MO`; no ZIP3 fence; the z7 half depends on the isWarm resolution fix #32). Open flag: onion/shallot kept `long_day` (belt reaches long-day z7 north; AR z8 core arguably intermediate-day). |
| 21 | **`nevada` region** (roster-wide region column) -- Nevada: Mojave High Desert (Las Vegas Valley), `zone_span` `["8","9","10"]`, `regions.nevada` cell per crop + `region_chill_delivered.nevada` + **5 new `source_catalog` entries** (`nws_vef`, `unr_sp2007`, `unr_sp9911`, `unr_fs0261`, `unlv_mg_svn`) | **SHIPPED 2026-07-21** (canonical `a071f0c1` -> `9b4c478a`; 118-patch atomic promote = 111 cells + chill band + provenance + 5 source adds; count 128 unchanged, 119 certified). Roadmap item 10, the 5th authored region, high-desert archetype. **NO new gate, NO new field** -- reuses A45/A31/A32/A3/A43/A9/chill. | (region column, terminal -- no hard-flip pending) | The high-desert delta: warm crops SINGLE spring + summer `heat_pause` + NO fall replant (UNR SP-99-11; inverse of the humid belts' added fall cycle; load-bearing fix = early-Feb indoor start so Jan stays `cold_pause` and the deriver renders the honest Nov-Dec cold_pause). 6 authoring shapes (A fruiting / B quick-two-window via `second_cycle` / C heat-lovers-no-pause / D warm herbs / E cool two-window / F fall alliums). 3 deltas: warm no-fall; apple `fruits_reliably` + `chill_basis` variety steering (Trevor Option A, prose-only) off UNR SP-20-07 North Las Vegas field trial; garlic narrow Sep 1-Oct 15. onion/shallot intermediate_day fall-planted (A9 0). pawpaw unsuitable (arid); citrus cold-limited. Built SDD: controller reference cells (cherry-tomato/apple/alliums) + 15 family shards (disjoint staging, controller-merged, no per-subagent commits) + 2 opus content reviews (SHIP-WITH-FIXES: citrus over-attributed "colder than Phoenix" to SP-20-07 -> reattributed to elevation; celery z8 spring-only; parsley z8 heat_pause fix). NEW `tools/staging/nevada_merge.py`. gate_all 119/119; footprint EXACT; dry-run A45/A43/A9 RED-proven. Spec/plan `docs/superpowers/{specs,plans}/2026-07-21-nevada-high-desert-region*`; notes `docs/reviews/notes/2026-07-21/nevada_*`. | plant-app kickoff #37 (`REGION_STATES.nevada=NV` + southern-Clark-County ZIP3 fence 889/890/891 so northern-NV z6-7 stays `northern_tier`; **NO isWarm #32 dependency** -- whole belt z8-10). |
| 22 | **`utah_dixie` region** (roster-wide region column) -- Utah: St. George Dixie (Mojave-edge high desert), `zone_span` `["8"]` (SINGLE zone), `regions.utah_dixie` cell per crop + `region_chill_delivered.utah_dixie` + **8 new USU `source_catalog` sub-ids** (`usu_ext_{veg_dates,tomato,wash_fruits,raspberry,garlic,fall_veg,wash_frost,peaches}` under the `usu_ext` family) | **SHIPPED 2026-07-22** (canonical `b1045e04` -> `f7e3afe3`; 121-patch atomic promote = 111 cells + chill band + provenance + 8 source adds; count 128 unchanged, 119 certified). Roadmap item 11, the 6th authored region, Nevada's single-zone near-twin. **NO new gate, NO new field** -- reuses A45/A31/A32/A3/A43/A9/chill. | (region column, terminal -- no hard-flip pending) | Single-zone simplification vs Nevada: A45 single-zone parity (`warm_arid ["8"]` precedent), NO gradient derivation, NO early-Feb-indoor-start trick (Mar 30 frost leaves Jan naturally inactive). Sourcing lighter (`usu_ext` catalogued) but 8 per-pub sub-ids REQUIRED because `region_cell_audit` forbids one source id mapping to two URLs in a cell (surfaced by gating the reference cell). USU documents NO warm-crop fall planting -> all 42 warm crops single-spring (Utah drops Nevada's quick-crop replant). The 3 deltas: (1) apple + pear-asian + pear-european MARGINAL (marquee flip from Nevada's fruits_reliably; Washington County "Fruits" page recommends apple/pear only for higher-elevation towns; region chill_basis names the lowest-chill third Dorsett Golden/Anna/Ein Shemer); (2) raspberry/blackberry marginal fall-bearing steer (USU names "Utah's Dixie"; primocane Caroline/Josephine/Polana/Joan J/Polka) + strawberry a low-elevation thriver; (3) honest chill-band gap ([250,450] elevation-bracketed inference, no USU St.-George number, flagged in provenance). onion/shallot intermediate_day fall-plant (A9 0); garlic USU late-Sep-Nov; cherry-sour dropped to marginal on review (high-chill, no low-chill cultivar); pawpaw unsuitable; citrus cold-limited. Built SDD: cherry-tomato reference cell + 14 family shards (disjoint staging, controller-merged, no per-subagent commits) + 2 opus content reviews (SHIP-WITH-FIXES: hot chiles habanero/cayenne/jalapeno re-authored Shape-A -> heat-lover, cherry-sour -> marginal, Nevada/Heflebower leak scrub). NEW `tools/staging/utah_dixie_merge.py`. gate_all 119/119; footprint EXACT; dry-run A45/A43/A9 RED-proven; real apply byte-identical to the dry-run scratch. Spec/plan `docs/superpowers/{specs,plans}/2026-07-22-utah-dixie-region*`; notes `docs/reviews/notes/2026-07-22/utah_dixie_*`. | plant-app kickoff #39 (`REGION_STATES.utah_dixie=UT` + a 847xx-ONLY ZIP3 fence so the Wasatch Front z6-7 stays `northern_tier`; **NO isWarm #32 dependency** -- the whole belt is z8). |

| 23 | **Pest/IPM control-ladder** -- shared top-level `control_methods` catalog (authored-once methods, mirrors `source_catalog`) + per-problem `control_ladder` (flat ORDERED softest-first array of `{method, note_*}`) + stable per-problem `id` (kebab; the variety-resistance vocabulary) + `type` + new top-level `pesticide_safety_education` | **PILOT COMPLETE 2026-07-22** (broccoli/celery/microgreens; canonical `2c98dd2b` -> `4f7789aa`; commits `b4bb71b` catalog / `a1755b0` safety / `857a856` ladders; **UNPUSHED**). NEW standalone **`control_ladder_gate`** (TDD, `--coverage`): referential (method exists) + monotonic-tier (`cultural`<`physical`<`biological`<`soft_chemical`<`conventional`) + `applies_to` coherence (method targets fit problem `type`) + unique kebab `id`. SOFT/scoped-to-ladder-bearing-problems now. Task-4 ruled the new keys into `register_completeness` EXCLUDED_KEYS/PATH. gate_all 119/119, control_ladder_gate 0, register PASS; adversarial RED battery (6 defect classes) bounces on real shapes. | **HARD-FLIP (INV-1):** flip `control_ladder_gate` into the A39 register-coverage hard floor + `gate_all` (add a coverage check requiring every certified problem to carry `id`+`type`+`control_ladder`) when the roster-wide rollout reaches full coverage. Soft is a stage, not a resting state. | Restructure + normalization column GS arc (NOT greenfield -- pests/diseases already existed as per-crop blobs). Pilot = 24-method catalog (T1: ucanr_ext/npic_orst/umn/umd/clemson/psu/msu) + 16 ladders across 3 crops; old `organic_treatment_*`/`management_*` blobs retired. Honesty: Option-2 synthetics (name active class + example, full caution set); organic-isn't-safe cons stated; honest short/cultural-only ladders (clubroot/blackheart/pink-rot/microgreens/root-maggot/rust-fly/leafminer); ONE justified conventional rescue rung (flea-beetle->carbaryl). Independent T1-fidelity review (caught + fixed an unsupported neem bee claim) + horticulture review (3 ladder fixes). ROLLOUT = ~897 problems across 119 certified, in family batches (catalog grows). Consumer: plant-app honest IPM pest guidance + the **variety-resistance arc** (references the pest/disease `id`s). Spec/plan `docs/superpowers/{specs,plans}/2026-07-22-pest-ipm-ladder-*`. NEXT after rollout: Approach C (crop-level pest hub) as a future row. |

| 24 | **Per-variety disease-`resistance`** -- optional per-variety `resistance` map keyed by the crop's own pest/disease `id`s (the row-23 vocabulary), valued by a grade enum `immune`/`resistant`/`tolerant`/`susceptible`. Bare-string grades; documented grades only (a missing disease key = "not studied", NEVER susceptible-by-default; `susceptible` recorded only when a source states it; a variety with no data omits `resistance` = the legit N/A branch). | **PILOT COMPLETE 2026-07-23** (apple + strawberry; canonical `7b1aa59d` -> `89d37c1a`; branch `worktree-variety-resistance`, **UNMERGED/UNPUSHED**). Each crop FULLY pest-migrated first (apple 8 + strawberry 12 problems -> `id`/`type`/`control_ladder`, `organic_treatment_*` retired; 13 new T1 `control_methods`; `control_ladder_gate` extended for the `vertebrate` type). Apple 51 grades / strawberry 11 grades (+3 varieties honest-N/A; Liberty scab-immune, Gala the scab-susceptible showcase). NEW standalone **`variety_resistance_gate`** (TDD): referential (key is a real crop pest/disease `id`) + grade-enum + shape; the N/A branch (no `resistance`, or `{}`) is silent. `resistance` ruled into `register_completeness`. gate_all 119/119, variety_resistance_gate 0, control_ladder_gate 0, register PASS; adversarial RED battery (5 defect classes: dangling id / bad grade / insecticide-on-fungal / conventional-before-cultural / bird+insecticide) bounces on real shapes. | **HARD-FLIP (INV-1):** flip `variety_resistance_gate` into A39 + `gate_all` at the roster-wide rollout (when the other enriched-variety crops gain `resistance`). Soft is a stage, not a resting state. | NEW per-variety field extending the variety-detail bundle (rows 12-14/16/18). T1-only, sourced from extension variety-resistance tables (Cornell Apple Variety DB + Khan Lab + Purdue BP-132-W + UMN for apple; UMaine 2184 + Cornell red-stele + NC State anthracnose + MU G6135 for strawberry; 10 new T1 `source_catalog` sub-ids). Independent T1-fidelity review on EVERY grade caught + fixed 7 apple defects (3 absence-inferred fire-blight grades removed; a Cornell-DB markdown-parse column-shift trap defeated via raw HTML + PDF). Existing `disease_notes`/`note_seasoned` prose kept for display; the map is the machine-readable layer (content review checks they agree). Consumer: plant-app shrinks a variety's pest watch-list + marks IPM rung-1 "pick a resistant variety" as satisfied. ROLLOUT = the other enriched-variety crops (onion/leek/dry-bean, then the rest as the pest rollout gives them disease `id`s). Spec/plan `docs/superpowers/{specs,plans}/2026-07-23-variety-resistance-pilot*`. |
| 25 | **`category` = "Perennial Vegetables"** -- a NEW `category` value (not a new field: a new value for the existing consumer taxonomy grouping), for the `herbaceous_perennial`-archetype crops that UC Master Gardener classifies as perennial cool-season STEM/flower vegetables, NOT fruiting vegetables. | **ASPARAGUS SET 2026-07-24** (canonical `ccf5e890` -> `419c9bd1`, part of the asparagus cert #120; no category-vocabulary gate exists, so the new value is dataset-safe). Asparagus moved Fruiting Veg -> Perennial Vegetables. | (taxonomy value, no gated hard-flip) -- **trigger: re-home ARTICHOKE** (currently Fruiting Veg, same `herbaceous_perennial` archetype) into Perennial Vegetables at its cert; consider rhubarb/cardoon if they ever join. | Single-crop set at cert (asparagus); the grouping grows as `herbaceous_perennial` crops certify. Ruling: UC Master Gardener "Vegetable Classifications" (perennial + cool-season + edible-part "stems"); Trevor-approved 2026-07-24. | **plant-astro frontend handoff (BEFORE the submodule bump):** the new `category` value needs graceful category grouping/rendering (the dataset-shape-change-breaks-frontends lesson) -- grep plant-astro for the category list + confirm it renders an unknown category without breaking the build. |
| 26 | **`harvest_ramp_weeks`** -- crop-level ORDERED array of `{bed_year: int, weeks: [min, max]}`, pure numbers and no prose, encoding how many weeks of cutting a bed sustains at each age. Lets the app compute "you are in bed year 3, cut two weeks not eight" from the user's own `plantedAt` instead of publishing one flat window that is wrong for every establishing bed. The multi-year perennial guidance Trevor identifies as the product differentiator. | **PILOT COMPLETE 2026-07-27, asparagus ONLY** (canonical `79862bc3` -> `0da1d234`; commits `6f2b379` pilot + `0c6c229` year-2 fix; PUSHED `origin/main` `0c6c229`). Values: yr1 `[0,0]`, yr2 `[0,2]`, yr3 `[2,4]`, yr4 `[6,8]`, yr5+ `[8,10]`. **NO GATE EXISTS** -- the field is unvalidated and un-floored; it is invisible to A39, and being optional it would go vacuous anyway (a gate validating a field's SHAPE cannot notice the field's ABSENCE -- the shape that produced the asparagus defect and drove A47/A48). **AMENDED 2026-07-28** (`tools/harvest_duration_reconciliation` arc, canonical `a995333f` -> `f37b228b`): bed year 5 `[8,10]` -> `[6,10]`, the same false-precision collapse-to-the-upper-end this row already flags for year 2, this time caught by the shipped RAMP-PROSE check reading the crop's own `harvest_ready_*` prose against the ramp's mature entry. **THE ROW ALSO MISSED A SECOND AXIS: REGION.** Duration varies by bed age (this field) AND by region (the literature makes region-specific claims that bed-age alone cannot carry). That axis is now handled by a SIBLING field, not by this one: the sparse per-cell `harvest_duration_weeks` override (shipped alongside `harvest_stop_rule`, row 27) -- present ONLY where a cited in-region source states a regional duration (asparagus: 2 of 12 candidate cells), absent everywhere else, where the crop's `harvest_ramp_weeks` default is inherited. Ten of twelve candidates correctly got no override; that is the honest outcome of a real source check, not a coverage gap. | **Trigger: a STABLE roster** -- artichoke (#121) is mid-cert and currently PAUSED, so the column pass is blocked per the standing principle above. **HARD-FLIP at rollout, CORRECTED 2026-07-28:** the row originally stated the coherence check as "the first bed_year whose `weeks` max is non-zero must fall inside `years_to_first_harvest`". **THAT FORMULATION WOULD NOT HAVE CAUGHT THE DEFECT IT WAS WRITTEN FOR.** With year 2 shipped as `[0,0]`, the first non-zero bed year is 3, and 3 falls inside `years_to_first_harvest: [2,3]` -- so a range-containment check PASSES on the exact bug it exists to catch. The shipped check (`RAMP-FIRST`, `tools/harvest_duration_gate.py::ramp_violations`) instead requires the first non-zero bed year to **EQUAL THE MINIMUM** of `years_to_first_harvest`, which forces the ramp to keep year 2 open -- what `[0,2]` honestly encodes and `[0,0]` did not. Verified RED at commit `6f2b379` (fails against the pre-fix `[0,0]` ramp), GREEN on the live canonical. Soft/standalone today (not yet in `whole_crop_gate`, queued behind artichoke's A48); this correction travels with it into the hard-flip. | **SCOPE IS NARROWER THAN THE PILOT ASSUMED -- settle it before the column pass.** The asparagus session recorded the rollout as "the 25 establishment crops"; measured on `0da1d234` it is **30** crops carrying `years_to_first_harvest`/`establishment_years`, and the field's semantics do not generalize to most of them. `harvest_ramp_weeks` means *weeks of repeated cutting a persistent crown can fund*. On the 19 fruit trees + 4 woody berries, establishment shows up as **yield**, not duration -- a pear has no "cut for 2 weeks not 8" rule -- and on the 5 cut-as-needed woody herbs (thyme/rosemary/oregano/sage/lavender, the same five A48's scope note flags) there is no discrete harvest window to ramp at all. Honest candidate set today: **asparagus (live), artichoke, plus rhubarb/cardoon if they ever join** -- i.e. the `herbaceous_perennial` archetype, not the establishment set. A trees-and-berries analogue would be a *different* field about yield fraction. | **Consumer: plant-app's three-state establishment model reads `years_to_first_harvest`**, so a ramp that disagrees with it renders a light-harvest ribbon beside a zero-week duration -- the app session caught exactly that before it shipped and correctly refused to build on it. **TWO HAZARDS THE ROLLOUT MUST CARRY.** (1) *Year-counting:* sources use three incompatible conventions -- "year after planting crowns", "year the plants are in the garden", "harvest year" -- that all describe the same season; ours counts **seasons in the ground from a one-year-old crown**, so every sourced number must be restated into that convention before it is recorded. (2) *Ranges:* where the establishment literature disagrees, the ramp must **carry the range, not pick a side**. Year 2 shipped as `[0,0]`, collapsing a genuine UMN/Missouri (light second-spring cut) vs MSU/UNH (wait for year three) disagreement to its conservative end and presenting it as certainty in a brand-new field, while `years_to_first_harvest: [2,3]` *was* that disagreement honestly expressed; corrected to `[0,2]`. The identical error recurred at year 5 (see AMENDED note) and was caught the second time by a gate, not by hand. Origin: `docs/2026-07-27-state-of-play-and-next-steps.md` §2b. |
| 27 | **`harvest_stop_rule`** -- crop-level object `{signal: enum, threshold_inches: [min, max], note_beginner, note_seasoned, sources, anchoring_urls}` naming the OBSERVABLE, crop-own signal that ends the harvest season (not a calendar date), plus a NEW sibling per-cell `harvest_duration_weeks: [min, max]` override on `regions.<slug>.resolved_by_zone.<zone>` for the sparse case where a cited in-region source states a regional duration that differs from the crop default. Together with `harvest_ramp_weeks` (row 26) these are the three layers a mature-bed duration claim can be made in: crop-level structured ramp, crop-level structured stop rule, and per-cell structured override -- plus the crop-level `harvest_ready_beginner`/`_seasoned` prose, which must defer to all three rather than restate a bare week count. | **PILOT COMPLETE 2026-07-28, asparagus ONLY** (canonical `a995333f` -> `f37b228b`, commit `9212543`, 9 paths). `signal: "spear_diameter"`, `threshold_inches: [0.25, 0.5]` -- three T1 institutions publish three different numbers for the SAME signal (NMSU 1/4 in., MU G6405 3/8 in., UC Marin MG 1/2 in.), so the field carries the disagreement as a range rather than picking one, while the signal itself (pencil-diameter spears) is unanimous and carried as a single enum. `harvest_duration_weeks` landed on exactly 2 of 12 checked cells (`mid_south` z7 `[4,8]`, `utah_dixie` z8 `[6,8]`), each on its own in-region T1 quote; the other 10 correctly inherit the crop default, the honest outcome of a real check, not a gap. `harvest_ready_beginner`/`_seasoned` rewritten bed-year-aware, deferring to the stop rule instead of stating a bare flat week count. `tools/harvest_duration_gate.py` gained STOP-SHAPE (+ RAMP-FIRST, RAMP-PROSE, OVERRIDE-REACH, OVERRIDE-PROSE; 31 tests, RED git-pinned to commits `6f2b379` and `7870051`); `tools/register_completeness_gate.py` gained a `signal` ruling. `STOP_SIGNALS` (`tools/harvest_duration_gate.py`) is **currently a vocabulary of one** (`spear_diameter`). Gauntlet: `whole_crop_gate` asparagus PASS, `gate_all` 120/120, `harvest_duration_gate` 0, `zone_order_gate` 0, `prose_window_sweep` 0, `register_completeness_gate` PASS, `release_verify` CLEAN. | **Trigger: artichoke certifies** -- the `herbaceous_perennial` archetype's ONLY OTHER MEMBER (#121, currently PAUSED mid-arc). Authored **NATIVELY AT ITS CERT**, per the method doc's fold-into-the-per-crop-checklist branch (`docs/gs_cross_crop_field_addition_v0.md` §2 step 5) -- NOT by a backfill column pass, because a 2-member archetype is not the "stable/complete roster" the standing principle above gates a column pass on. Artichoke is a bud/head crop, not a spear crop, so its stop signal will not be `spear_diameter`: expect `STOP_SIGNALS` to need a real second member, and `threshold_inches` (built for a linear caliper measurement) to need checking against whatever artichoke's own signal actually measures, not just extended by name. **HARD-FLIP:** fold STOP-SHAPE into the A39 register-coverage floor once the archetype has two members (i.e. once artichoke ships its own `harvest_stop_rule`). Until then STOP-SHAPE stays soft/standalone, the `zone_order_gate` pattern -- a 1-member archetype cannot demonstrate the coverage floor is meetable. | Reconciles what had been unstructured, disagreeing prose (`harvest_ready_beginner`/`_seasoned` bare week counts, vs. the structured `harvest_ramp_weeks`, vs. per-cell regional prose) into one queryable, sourced stop signal plus a sparse regional override. plant-app's per-bed "when to stop cutting this season" guidance: the ramp says how much a bed can give across the season, the stop rule says when to call it. **OWED, not yet clean (surfaced by this pilot, unresolved):** `harvest_ready_sources` still lists `msu_ext` (an 85-char JS shell with zero asparagus content at all three URL spellings) and omits the five sources the rewritten registers actually rest on; no gate covers that field. Six anchoring URLs point at extension portal roots with zero asparagus content (`ndsu_ext`, `sdsu_ext`, `umaine_ext`, `iastate_ext`, `uconn_ext`, and `mu_ext`-as-root on `northern_tier` z7, the same id correctly anchored at the real G6405 publication on `mid_south` z7) -- concrete instances for the queued citation cleanup arc. Three unsourced week counts still stand in cell notes (`mid_atlantic` z8, `northern_tier` z3, `pnw` z8). `se_gulf` z8: UGA C1026 states the six-to-eight-week figure verbatim but `uga_c1026` is absent from `source_catalog` and uncited on the cell -- deferred rather than fabricated; one catalog row plus one cell citation clears it. Spec/plan `docs/superpowers/sdd/2026-07-27-harvest-duration-reconciliation/`. |

*(Add a row when a consumer needs a new cross-crop field. Keep status/trigger current.)*

---

## To adopt (cert session)

1. **Surface it where it'll be seen** — add a one-line pointer to plant-dataset `CLAUDE.md` (the file
   every cert session auto-loads), e.g.:

   ```markdown
   ## Adding a cross-crop field
   Before adding any field across crops, follow `docs/gs_cross_crop_field_addition_v0.md` and check
   `docs/field_addition_register.md` (live queue + trigger conditions). Run column passes against a
   stable roster, never mid-certification.
   ```

2. **Graduate the method** from `gs_cross_crop_field_addition_v0.md` (proposal) to `_v1_0` once adopted.
3. **Commit** this register + the note on your terms (left untracked by the plant-app session that
   drafted them — origin: the seed-trays feature design, 2026-06-30).

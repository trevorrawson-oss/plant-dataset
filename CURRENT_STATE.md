# plant -- CURRENT STATE (live surface)

> ## ⚠️ SESSION PROTOCOL -- read before doing anything
> 1. **Confirm the SHA.** `shasum -a 256 crops_data_final.json` must equal `LATEST.txt`. If it does not, STOP and reconcile. **The uploaded `LATEST.txt` / `CURRENT_STATE` / `STATE_HISTORY` are the SOLE authority for the canonical SHA, the fill count, and the next cell. If your MEMORY conflicts with them, the files win and your memory is STALE -- the dataset advances through sessions (including Claude-Code-only catalog admits) faster than any memory refreshes, so memory is always a lagging snapshot. Re-derive arc position from the files, never from memory. Memory may hold stable facts (lanes, methodology, file locations), never the moving SHA/count/next-cell.**
> 2. **Locate your step on the arc checklist** (gold-standard arc checklist **v1.5**) BEFORE acting. Do not infer the next action from "a cell is done" -- check the checklist. The checklist's auto-derived denominator is authority over any kickoff's named field list. **Kickoffs SUMMARIZE; they are not authority on arc position -- re-derive the next unowned step from the live crop + the checklist.**
> 3. **This file is LIVE STATE ONLY.** Never delta-edit it (the past near-miss came from header-only patches; a corrected header on a stale body is worse than a uniformly-stale file). At session close, **fully regenerate this whole file** from the true state.
> 4. **History is append-only** in `STATE_HISTORY.md` -- APPEND a dated entry there at close, never rewrite it. That file is the recovery net.
> 5. **CLOSE RITUAL (every session ends here).** If the dataset changed: PROMOTE -- write canonical, re-pin `LATEST.txt` (new SHA + date + session). Then ALWAYS: regenerate this file (#3), append to `STATE_HISTORY.md` (#4), sync `00-current/`, and commit (+ push).
> 6. **RELEASE VERIFICATION (Claude Code, BEFORE promoting any claude.ai-authored cell -- a green gate is NOT a clean release).** Run, in order, and BLOCK on any concern: (a) `python3 tools/whole_crop_gate.py <slug>` (the shape FLOOR); (b) `python3 tools/release_verify.py <candidate.json> --base crops_data_final.json --slug <slug>` (the gate's blind spots: collateral, violation-diff, calendar coherence, user-facing dash/temp scan, exemplar key-diff, region_notes presence -- exits 1 on hard concerns; pause-legibility/biology surface as non-blocking Step-5.5 notes); (c) **cross-check the cell against claude.ai's own STATE_HISTORY entry claims** (exact months/dates/keys changed -- did the bytes match what it said? did the COUNTS add up?). Only then PROMOTE (#5). Biology correctness (is a date TRUE per the source) is NOT this step -- that is the Step-5 4-round side-by-side. The lesson behind #6: leaning on a passing gate let two cells ship with a vestigial key the gate cannot see; release_verify + the claim cross-check close that. (5.C the cross-check also caught an 8-vs-7 verified-count drift in the entry header.)

---

## 🥬 LETTUCE FLIPPED (M15) · 🍅 CHERRY -- Step 5/5.5 verification IN PROGRESS (5.A+5.B+5.C done); NEXT = 5.D (M16, 2026-06-07)
`lettuce-leaf` is the FIRST flipped anchor (1 of 9): `launch_ready_core=True` + `launch_ready_seasoned=True` (`status` stays `"unverified"` pending the M16 vocab decision). **M16 is in flight on `cherry-tomato`, CHERRY FIRST then beefsteak.** Cherry has completed **Step 3.5** (region shells), **Step 9** (source-name dash), **Step 10** (`harvest_to_table` ruling), and **Step 4 + all 10 region cells AUTHORED.** Now in the whole-crop **Step 5/5.5 verification pass** (4-round side-by-side + calendar coherence), sub-sessions **5.A-5.D**: **5.A (northern_tier) DONE**, **5.B (CA single-window) DONE**, **5.C (two-window/desert) DONE** -- se_gulf + ca_desert + low_desert_az verified (one quote-integrity fix + one source-grounded date correction); **`warm_arid` Path A: chart READ + two-window CONFIRMED (Trevor opened the Dona Ana MG chart, Claude Code confirmed); date correction (1st planting May->March) + honesty region_note AUTHORING owed (claude.ai).** ca_desert fall-band grounding bar RESOLVED (accept mechanism+corroborator -- CA/AZ low desert = same Sonoran climate). **NEXT = 5.D (tropical/peninsula + the consolidated winter-`wait` ruling)**, then Steps 6/7/8, then Claude Code Step 11. **7 of 10 region cells now 4-round-verified.** **Lettuce was NOT touched -- byte-identical, still certified.** **(Operating model: claude.ai authors, Claude Code releases -- now 7 releases, 6 clean + 1 merge.)**

## Canonical pointer
- **Current SHA:** `9f61c52f8cc6f7ea762e4c9df0358cb02bd164079399fec3a1146f1134455891` (M16 cherry Step 5.C -- two-window/desert 4-round verify; low_desert_az spring Feb->mid-March + heat_pause [6,7,8]->[7,8] correction; ca_desert fall-band quote-integrity fix; se_gulf verified no-change; warm_arid HELD). `LATEST.txt` session line: `m16_cherry_5c_two_window_desert`.
- **claude.ai: preflight sub-session 5.D (tropical/peninsula cells + winter-`wait` ruling) against `9f61c52f`.**
- **Predecessor chain:** `9f61c52f` (5.C two-window/desert) <- `adf9dcb4` (5.B CA single-window) <- `dadd18d1` (5.A northern_tier) <- `eeaeae37` (northern_tier cells) <- `7dd2837e` (hawaii_tropical) <- `7d4bf50c` (fl_peninsula) <- `842ee139` (warm_arid) <- `bf96d1d1` (catalog +nmsu_donaana_mg) <- `9d8784f7` (4 coastal+desert cells) <- `813bade9` (catalog +ucanr_san_diego_mg) <- `349fb7af` (ca_interior) <- `339933f2` (se_gulf) <- `f916e8fe` (Steps 9+10) <- `1b4fea68` (north lifted_from_zone strip) <- `a65c7175` (Step 3.5 region shells) <- `29b3aaa9` (M15 lettuce flip) <- ... (full chain in STATE_HISTORY).
- Every PROMOTE re-pins this SHA and `LATEST.txt`. Confirm at session start (protocol #1).

## What just happened (2026-06-07, session `m16_cherry_5c_two_window_desert`) -- STEP 5 VERIFICATION 5.C
- **Sub-session 5.C released (PATCH-format, 18 edits).** The four two-window/desert cells 4-round-verified against live T1. **Bytes changed: ca_desert + low_desert_az ONLY** (se_gulf + warm_arid byte-identical).
- **`se_gulf` VERIFIED, no byte change.** The z9 4c open item (Step 3.5/4) CLOSED by verification (UF/IFAS VH021 Table 1 "Late Mar-Apr, Aug" = genuine two-window; two independent T1 = UGA C943 + UF/IFAS).
- **`low_desert_az` VERIFIED with a source-grounded biology CORRECTION** (U of Arizona az2078, peer-reviewed, "two short growing seasons"): spring band Feb->mid-March (`plant_out` "Feb 1-Feb 28"->"Mar 1-Mar 21", `start_indoors`->"Jan 18-Feb 1", `harvest_start` "Apr 2"->"May 5", `harvest`/`first_plant_date` follow); **heat_pause narrowed [6,7,8]->[7,8]** (az2078 names Jul-Aug; June is main-season); calendar re-derived; 2 spring source_quotes re-anchored. Claude Code recompute CONFIRMED the leaves + calendar coherent (followup #1; one Apr-30-vs-May-5 boundary straddle logged, no override).
- **`ca_desert` VERIFIED main window + heat_pause; fall-band QUOTE-INTEGRITY DEFECT corrected** (5.B-class, gate-invisible, NO date change). Table 13.2 gives the desert tomato row only "Dec-March"; six fall/main `source_quote`s had reused that main-winter quote for the September fall band -> nulled; 2 fall `synthesis_note`s re-attribute to the UC IPM heat-break mechanism. **The fall CROP is sound** (mechanism + az2078 corroboration), only the cited quote was wrong.
- **Verified per protocol #6:** gate 21 (unchanged); release_verify clean exit 0 (only cherry -> only ca_desert+low_desert_az; catalog 82; lettuce byte-identical; no new violations/novel keys/user-facing dash-temp; low_desert_az absent from the wait-list); claim cross-check byte-confirmed against the 18-path manifest; the cross-check also CAUGHT an 8-vs-7 verified-count drift (corrected to 7/10).

## warm_arid PATH A -- CHART READ, two-window CONFIRMED; date correction + region_note AUTHORING OWED (2026-06-07)
1. **`warm_arid` -- Path A DONE on the read side: Trevor opened the Shillingburg / Dona Ana MG chart PDF (`donaanamastergardeners.nmsu.edu/documents/foodgardenplantingchart-1.pdf`) and Claude Code independently confirmed the tomato row from a screenshot (archived: `06-sessions/.../m16-cherry-releases/warm_arid_path_a_evidence/`).** The chart STATES a genuine **two-window** structure (Path A satisfied; it IS two-window, sourced to the catalogued T1 `nmsu_donaana_mg`, now actually read not inferred). **Tomato row, verified:** 1st planting = start indoors ~mid-Jan -> **transplant early March**; 2nd planting = start ~mid-June -> **transplant ~mid-July**. (The chart shows planting/transplant timing only, not harvest.)
   - **THE CORRECTION the read forces:** the current cell was authored on the DESERT ANALOGY and is WRONG on the first planting -- it has spring transplant **May 1-21** (start indoors Mar 20), but the chart says **early March** (start indoors mid-Jan), ~2 months earlier. The SECOND planting is already ~right (encoded `plant_out` Jul 15-Aug 4 / `start_indoors` Jun 3-17 ≈ the chart's mid-July transplant / mid-June start). The first-planting shift cascades into harvest dates, the July heat treatment (heat_pause-vs-set-dip), and the calendar.
   - **Trevor's product call:** KEEP the second planting, and add an HONEST-PROVENANCE seasoned `region_note` disclosing it comes from the Dona Ana County MG (Las Cruces) planting chart -- a single regional MG source, not independently corroborated.
   - **AUTHORING OWED (claude.ai lane):** re-author warm_arid z8 to the chart (1st transplant -> early March with the frost/beginner-safety reconciliation, same as low_desert_az's mid-March beginner-safe call; re-derive harvests; re-judge the July heat_pause; dual-register region_notes incl. the honesty note via the copywriting skill). Then Claude Code releases. warm_arid stays HELD/UNVERIFIED until that shape is authored; a green gate must NOT flip it. **Open: confirm whether claude.ai is doing this inside the running 5.D or as a focused follow-up (avoid double-authoring).**
2. **`ca_desert` fall-band grounding bar -- RESOLVED: ACCEPT mechanism + corroborator.** Trevor's call (deserts are not materially different): the CA low desert (Imperial/Coachella) and AZ low desert (Yuma/Phoenix) are the SAME Sonoran low desert, climatically identical, so az2078's "two short growing seasons" + the UC IPM heat-break mechanism are sufficient grounding for the fall band. No CA-specific source required. A CA desert fall source (UC ANR Pub 8159 / Riverside-Imperial County MG chart) is an OPPORTUNISTIC citation upgrade only -- non-blocking, add if claude.ai happens across one. **ca_desert is fully verified (5.C).**

## Active work + exact next step
- **5.D = NEXT: tropical/peninsula cells** (`fl_peninsula`, `hawaii_tropical`) 4-round + whole-crop non-regional claims + the consolidated **Step-5.5 WINTER-`wait` ruling** across the 8 warm-cell winter `wait` months (release_verify-surfaced: se_gulf z8 Jan/Dec; ca_interior z8 Jan, z9 Jan/Dec; ca_north_coast z9/z10 Jan/Feb/Nov/Dec; ca_south_coast z9 Jan/Feb/Dec, z10 Jan/Feb; warm_arid z8 Jan/Feb/Apr/Dec -- classify cold_pause vs legit gap per source; the frost-free z10 rows + warm_arid-Apr are counter-candidates). Preflight `9f61c52f`. (SUMMER-pause legibility already cleared in 5.C: zero silent summer `wait`s across all two-window cells.)
- **Then in sequence:** Steps 6/7/8 (seasoned depth-lift; beginner siblings incl. `cause_beginner`; dual-voice coverage gate to 0 -- **author ~27, gate displays 21**, see blind spot). Step 2 rider (`gs_exemplar_finding_003`). Then Claude Code **Step 11** + flip + the `launch_ready` reset. **warm_arid's structure decision blocks its Step-5 close but not 5.D or the other cells.**
- **`status`-vocab decision still owed (Appendix B item 1) -- THREE-state unification.** Cherry `"verified_retro_complete"`; lettuce `"unverified"`. Successor (leaning `verified_arc`) decided at cherry's flip, back-applied. Do NOT set before decided + flip fires.
- **Blossom-drop `fruit_set_temp_f` -- T1 anchors in hand, ruling owed.** Now includes az2078 (nights >55°F to set; >90°F reduces viable pollen) + UC IPM (>95°F CA desert) + San Diego MG 65-90°F + VH021 + s1b_finding_006. Schema-touching -> Trevor rules shape. Deferred to family close.

## Gate record (2026-06-07, post-5.C, GATE-CONFIRMED on canonical)
- **`GATE: 21 VIOLATION(S)`** -- all Steps 6/7/8. Composition: **21** dual-voice siblings (gate-counts 21, author ~27 -- see blind spot). **0** region_notes-null; **0** anchoring gaps; **0** uncatalogued. UNCHANGED across 5.C (backend source_quote/synthesis + date/calendar changes are all gate-invisible).
- **§A2 shape classes 0** (`stub:0 | null-track:0 | stale nested:0`); `second_planting` validates clean on every two-window cell (incl. low_desert_az post-correction); §E 0 uncatalogued / 0 non-T1; §C/D 0; §F 0 gaps. ALL 10 region cells carry `region_notes` + coherent calendars.

## Region fill state (10 of 10 authored; 7/10 4-round-verified, 1 HELD, 2 pending)
| region | zones | status | window | heat_pause | second_planting |
|---|---|---|---|---|---|
| `northern_tier` | 3-7 | VERIFIED (5.A) | cold (frost-bracketed) | none (frost-limited) | yes, z6-7 (structured) |
| `ca_interior` | 8-9 | VERIFIED (5.B) | single | none | none |
| `ca_north_coast` | 9-10 | VERIFIED (5.B) | single (May) | none (COOL-limited) | none |
| `ca_south_coast` | 9-10 | VERIFIED (5.B) | single (long Apr-Jul15) | none (mild marine) | none |
| `se_gulf` | 8-9 | VERIFIED (5.C) | two-window | month 7 (cherry-narrowed) | yes |
| `ca_desert` | 9-10 | VERIFIED (5.C) | two-window | Jun-Aug (absolute) | yes (Sep) -- fall band accepted on mechanism+az2078 |
| `low_desert_az` | 9 | VERIFIED (5.C) | two-window | **Jul-Aug** (absolute, corrected) | yes (Sep) |
| `warm_arid` | 8 | **HELD** -- two-window CONFIRMED by chart; dates+notes authoring owed | two-window (chart-confirmed) | July? (re-judge: planting in Jul per chart) | yes (chart: start Jun -> transplant ~mid-Jul) |
| `fl_peninsula` | 10-11 | authored (5.D pending) | near-continuous (z10+z11 distinct) | Jul-Aug (cherry-narrowed) | none |
| `hawaii_tropical` | 11 | authored (5.D pending) | year_round (all-active) | none (oceanic-tropical) | none |

## Flip gates (the four distinct "flips" -- never conflate them)
1. **Per-crop `launch_ready` flip** -- ✅ lettuce DONE (1 of 9). Cherry mid-arc (Steps 3.5+9+10 done; all 10 cells authored; 5.A+5.B+5.C verified, warm_arid HELD; owes 5.D + Steps 6-8 + Step 11; the stale M10 `launch_ready=true` resets at the flip). Beefsteak after cherry.
2. **Region read-layer flip** -- renderer reads `regions{}` first. **Gate:** shape proven on 3 provers (lettuce ✅; cherry all 10 cells real-filled ✅; beefsteak owes both). Ships with `zones{}` fallback. **2.9+.** (The plant-astro renderer rewrite -- read `regions{}` + consume `second_planting` -- is gated here.)
3. **Authoring-model flip** -- carrots onward authored region-first. **Gate:** 3 provers done.
4. **Schema perennial bump** (`lifecycle_override`) -- FUTURE, **2.9+.** After carrots.

**Schema version lineage:** 2.7.5 -> **2.8 (current)** -> region read-layer flip + perennial extension are **2.9+.**

## Live locked decisions / guardrails
- **CHERRY HEAT PAUSE is a PER-REGION judgment, NOT universal.** se_gulf + warm_arid(?): cherry's heat resistance narrowed the summer pause to month 7. `ca_desert` + `low_desert_az`: NOT narrowed -- peak desert summer is climatic-absolute and stops fruit set even for heat-tolerant cherry (low_desert_az corrected to Jul-Aug per az2078; June is main-season). Apply per region from the source. beefsteak re-derives.
- **`harvest_to_table` T2-as-evidence -- RULED (Trevor, 2026-06-05): T1-only as evidence, NO grandfathering.** Where a T2 is the SOLE backing, replace with a T1; where T1-anchored and the T2 is redundant, DROP it. De-citation is whole-crop.
- **TEMPERATURE in user-facing copy -- canonical `°F`, NEVER spelled "degrees F" (M16-CA-INT-003).** region_notes + all user-facing strings use `°F`; §C/D enforces user-facing only. Spelled "N degrees F" is correct + dominant in BACKEND prose (`synthesis_note_seasoned`, `heat_pause.basis_seasoned`, `source_quote`) and is NOT policed. (Owed: add to `tip_region_authoring_standard`.)
- **WINDOW STRUCTURE is a SOURCE FINDING, not a default.** Single-window (5.B-confirmed): ca_interior, ca_north_coast, ca_south_coast. Two-window (5.C-confirmed): se_gulf (VH021 "Late Mar-Apr, Aug"), ca_desert (Table 13.2 + IPM mechanism), low_desert_az (az2078 "two short growing seasons"). **`warm_arid` two-window is UNCONFIRMED and HELD (5.C)** -- its sources reduce to one unreadable chart; readable NMSU points to a single long season. Do NOT trust its shape until the Path A/B decision.
- **CATALOG ADMISSION (county MG = UC ANR = T1).** UC MG county programs are UC ANR extension, T1, admissible. **Note A clarification (5.B):** statewide `uc_mg` IS the authoritative publisher of Table 13.2; county MG IDs corroborate where they carry region-specific prose, NOT mandatory per-cell substitutes. LANE: discovery + verified-live-URL handoff = claude.ai; catalog write = Claude Code. Future discovery->admit candidates: an NMSU tomato-date T1 (if Path A for warm_arid); a CA desert FALL source (if Trevor requires one for ca_desert: UC ANR Pub 8159 / Riverside-Imperial County MG chart).
- **`second_planting` structure (Claude Code lane) -- PROVEN on se_gulf + ca_desert + low_desert_az + northern_tier z6/z7.** Shape: `resolved_by_zone[z].second_planting = {start_indoors, plant_out, harvest_start, harvest_end, sources, anchoring_urls}` on applicable zones + region-constant `plantings[]` entry `track:"second_planting"`. Cell-level dates = envelope across both bands. Discrete (NOT succession). Seasoned-only. Spec: `docs/superpowers/specs/2026-06-05-second-planting-region-shell-model-design.md`. **DEFERRED UI question** (structured single-season flag placement) -- Phase C read-layer rewrite.
- **Resolved-layer shape standards RATIFIED (2026-06-05):** main = flat cell fields; succession = `succession_spring`/`succession_fall` (lettuce); `second_planting` = discrete-window object (cherry). Each crop carries ONLY its structures. **Lettuce is NOT reshaped.**
- **REGION SHELL-BUILD RULE:** every crop builds all 10 cells to the lettuce bar. North builds FROM legacy cold `zones{}` (promote + verify + hoist succession; strip nested `plantings`; re-stamp `zone_promoted_verified`; strip tautological `lifted_from_zone`). Warm/CA re-derive from T1. `whole_crop_gate.py` §A2 enforces the structure. **Owed: claude.ai folds the lifted_from_zone-strip into checklist v1.5 Step 3.5 text.**
- **Governing checklist: v1.5** (Step 3.5 Region shell build; reference-GS-crop = `lettuce-leaf`; two-callsite admission/certification). Dataset is authoritative; flag doc lag.
- **Lane split.** STRUCTURAL/MECHANICAL = Claude Code (region shells, transforms, gates, audits, SHA re-pins, flips, `second_planting`, dual-voice-walker fix, catalog admission, envelope recompute). Biology + consumer copy + voice/IP + URL discovery + which-zones/dates for second_planting + the T2 ruling = claude.ai.
- **Anchoring gate is LAYER-SCOPED (1A):** claim-bearing leaves only; legacy `zones{}` + the 10 `regions{}` root rollup `sources` arrays excluded; sibling `*_sources`/`*_anchoring_urls` pairs included; `bolting.*` inherit-class excluded.
- **Keep `zones{}` coherent until Phase C.** The current renderer still reads `zones{}`.

## Owed checklist amendments (claude.ai, OPEN)
- `lifted_from_zone`-strip into v1.5 Step 3.5 north sub-procedure text (tool already enforces it).
- dual-voice-walker blind-spot note (the 6 uncounted `why_beginner` companions siblings) for Claude Code.
- `°F`-in-user-facing-copy rule (M16-CA-INT-003) into `tip_region_authoring_standard`.
- Retire the "every region cell needs a county MG" framing (Note A, 5.B) -- the real precedent is "county MG corroborates where it has region-specific prose."
- **Window-structure-is-a-source-finding precedent (5.C warm_arid):** the gold-standard arc must NOT carry a multi-window shape forward unless a readable source STATES or shows the structure -- analogy to a neighboring climate is insufficient (the warm_arid hold). Fold into Step 5 guidance.

## Owed (Claude Code lane, OPEN)
- **`warm_arid` re-authoring (Path A read DONE 2026-06-07; AUTHORING owed to claude.ai).** The chart read is verified + archived (two-window confirmed; 1st transplant early-March, 2nd transplant mid-July). claude.ai re-authors z8 to the chart -- the substantive fix is the first planting (currently May, must move to early March with frost/beginner-safety reconciliation) + re-derived harvests + re-judged July heat treatment + the honest-provenance seasoned region_note. Claude Code then releases. warm_arid stays HELD until authored.
- **`uc_mg` catalog-url nit (5.B):** entry `url` is legacy `https://mg.ucanr.edu`; anchors point at `ucanr.edu/program/...`. Catalog-hygiene call, batched (not blocking).
- **Pre-commit/promote-wrapper release-verify hook** -- enforce protocol #6 mechanically at the beefsteak/pipeline transition (3 commit types: cell release = full verify; catalog admit = source_catalog-only; doc-only = skip). Deferred from manual cherry phase.

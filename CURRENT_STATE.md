# plant -- CURRENT STATE (live surface)

> ## ⚠️ SESSION PROTOCOL -- read before doing anything
> 1. **Confirm the SHA.** `shasum -a 256 crops_data_final.json` must equal `LATEST.txt`. If it does not, STOP and reconcile.
> 2. **Locate your step on the arc checklist** (gold-standard arc checklist **v1.5**) BEFORE acting. Do not infer the next action from "a cell is done" -- check the checklist. The checklist's auto-derived denominator is authority over any kickoff's named field list. **Kickoffs SUMMARIZE; they are not authority on arc position -- re-derive the next unowned step from the live crop + the checklist.**
> 3. **This file is LIVE STATE ONLY.** Never delta-edit it (the past near-miss came from header-only patches; a corrected header on a stale body is worse than a uniformly-stale file). At session close, **fully regenerate this whole file** from the true state.
> 4. **History is append-only** in `STATE_HISTORY.md` -- APPEND a dated entry there at close, never rewrite it. That file is the recovery net.
> 5. **CLOSE RITUAL (every session ends here).** If the dataset changed: PROMOTE -- write canonical, re-pin `LATEST.txt` (new SHA + date + session). Then ALWAYS: regenerate this file (#3), append to `STATE_HISTORY.md` (#4), sync `00-current/`, and commit (+ push).

---

## 🥬 LETTUCE FLIPPED (M15) · 🍅 CHERRY Step 4 IN PROGRESS -- `se_gulf` + `ca_interior` SOURCED (M16, 2026-06-05)
`lettuce-leaf` is the FIRST flipped anchor (1 of 9): `launch_ready_core=True` + `launch_ready_seasoned=True` (`status` stays `"unverified"` pending the M16 vocab decision). **M16 is in flight on `cherry-tomato`, CHERRY FIRST then beefsteak.** Cherry has completed **Step 3.5** (region shells, Claude Code), **Step 9** (residual source-name dash), **Step 10** (`harvest_to_table` T2-as-evidence ruling), and **Step 4 is in progress: 2 of 10 region cells sourced -- `se_gulf` (zones 8-9) + `ca_interior` (zones 8-9), both authored to the lettuce reference bar.** **NEXT = the remaining 7 warm/CA regions of Step 4** (catalog now unblocks `ca_south_coast`: `ucanr_san_diego_mg` admitted this session; **preflight against `813bade9`**), then Steps 5-8, then Claude Code Step 11. **Lettuce was NOT touched -- byte-identical, still certified.**

## Canonical pointer
- **Current SHA:** `813bade981f488e54479f83db2ba72d06f3a12f0cb791149e638d1a14da6a040` (M16 catalog admission -- `ucanr_san_diego_mg`, Claude Code lane; no crop changed, only `source_catalog` 80 -> 81). `LATEST.txt` session line: `m16_cherry_catalog_admit_sandiego_mg`.
- **claude.ai: preflight `ca_south_coast` against `813bade9`, NOT `349fb7af`** -- this catalog promote sits on top of ca_interior.
- **Predecessor chain:** `813bade9` (catalog +ucanr_san_diego_mg) <- `349fb7af` (ca_interior + temp fix) <- `928210dd` (ca_interior cell, intermediate) <- `339933f2` (se_gulf sourcing) <- `f916e8fe` (Steps 9+10) <- `1b4fea68` (north `lifted_from_zone` strip) <- `a65c7175` (cherry Step 3.5 region shells + `second_planting` structure) <- `29b3aaa9` (M15 lettuce flip) <- `6880ed37` (lettuce write-back) <- `37bfc12d` <- ... (full lettuce chain in STATE_HISTORY).
- Every PROMOTE re-pins this SHA and `LATEST.txt`. Confirm at session start (protocol #1).

## What just happened (2026-06-06, session `m16_cherry_catalog_admit_sandiego_mg`)
- **Catalog admission (Claude Code lane): `ucanr_san_diego_mg` admitted** so claude.ai can cite San Diego County MG in `ca_south_coast`. Triggered by a lane-boundary block (claude.ai correctly would not introduce an uncatalogued ID inline) + a Trevor lane question, resolved from the record: admission = Claude Code, source discovery/verification = claude.ai. UC ANR MG San Diego County program, T1, verified-live URL handed over by claude.ai. `source_catalog` 80 -> 81; NO crop changed; cherry residual stays 30; lettuce `GATE: PASS`.
- **`ucd_plantsci` HELD** (UC Davis Plant Sciences) -- no confirmed stable citable URL yet; claude.ai confirms a specific doc next, then reports whether to admit. Don't admit against a bare department root.
- **Git hygiene:** committed claude.ai's uncommitted `ca_interior` (`349fb7af`) promote first to keep git current (recurring: promote scripts land on disk but the commit step isn't firing -- worth folding `git commit+push` into them). Full lane-clarification + admission detail in STATE_HISTORY (2026-06-06 entry).
- *Prior session `m16_cherry_step4_ca_interior` (ca_interior) detail (also in STATE_HISTORY):*
- **Second Step-4 biology cell on cherry: `ca_interior` (California Interior Valleys, zones 8-9) sourced + authored**, closing the ca_interior slice of `gs_exemplar_finding_001`. The only cell changed this session.
- **GATE RUN + CONFIRMED (resolves the se_gulf gate-not-run caveat).** Baseline confirmed at **31** (matching the prior session's derived estimate). After authoring + the §C/D fix, **gate = 30**: 8 region_notes-null + 21 dual-voice (gate-counted) + 1 `cornell_ext` zone-6 anchoring. The "~31 DERIVED" figure is now gate-confirmed history.
- **Windows re-derived from T1, not lifted** (climate-contamination guard): **z8 + z9 = UC ANR Master Gardener Handbook Table 13.2** via the online *Time of Planting* page (page self-identifies as Table 13.2; updated 2026-05-08) -- Interior Valleys tomatoes = April-May (spring transplant). Corroborated by **Sacramento County MG monthly tips** (single late-April, soil ~65°F). Both fetched + verified live this session.
- **SINGLE-WINDOW verified PER SOURCE -- the `fl_peninsula`-style trap landing at ca_interior.** Two independent T1 sources give NO fall tomato window (contrast se_gulf, where UGA C943 gives an explicit ~Jul 20 fall deadline → two-window). UC Davis Plant Sciences (read for confidence, NOT cited) independently frames Central Valley tomato as one spring planting + a single long harvest tail (70-120 d). → **`ca_interior` carries one main planting; NO `second_planting`, NO `heat_pause`.** Cherry is heat resistant, the valley grows one continuous season, `calendar[12]` reads `growing`/`harvest` through summer.
- **Second-planting decision SETTLED for ca_interior:** z8 + z9 single-season, no second planting. Resolved by confirming absence with a second T1 source (Trevor's direction), not by inventing a window or dropping it silently.
- **region_notes pair authored** (seasoned + beginner), deliberately carrying the "why no fall planting here" explanation per Trevor's calendar-UI call -- the explanation lives in region_notes (the same field se_gulf uses to explain its two-season split, opposite content), NOT a dedicated absence-field.
- **Frost variant = mediterranean (interior-valley NOAA variant)**, NOT humid_subtropical. The gap between mediterranean last-frost (Jan/Feb) and actual plant-out (Apr-May) is itself the single-season signal (soil-temp/heat-gated, not frost-release).
- **Sourcing correction mid-session:** initial draft cited UC Davis as a NEW id `ucd_plantsci`; caught (not in catalog; admission is Claude Code lane) and corrected to already-admitted **`uc_mg`** (publisher of Table 13.2 + Sac County tips) + **`ucanr_ext`** (umbrella). §E stayed at 0 BECAUSE of this correction. UC Davis logged in provenance as `sources_read_not_cited`.
- **§C/D temp-form fix (NEW guardrail, M16-CA-INT-003):** first authoring used spelled "65 degrees F" in `region_notes_seasoned` → gate flagged it. region_notes are user-facing; §C/D requires degree-sign `°F` there. One-field fix cleared it. The spelled form remains correct in BACKEND prose (`synthesis_note_seasoned` etc.); §C/D scopes to user-facing strings only.
- **Shape matched to live se_gulf/lettuce exactly:** `plantings_provenance` (+ `window_structure: single_window`); `resolution_method: ca_interior_month_resolution`; `planting_note: single_season`; NO `sources_pending_admission`; NO `lifted_from_zone` (re-derived).
- **Write discipline:** two SHA-gated scratch applies, minified `separators=(',',':')`, `ensure_ascii=False`, atomic swap, pre-swap backups. Collateral audit: ONLY `cherry-tomato.regions.ca_interior` changed; all 122 other crops + all top-level keys byte-identical; **lettuce AND se_gulf byte-identical**. Crop count 123, schema 2.8 -- both unchanged.

## Active work + exact next step
- **M16 cherry is at Steps 3.5 + 9 + 10 DONE; Step 4 IN PROGRESS (se_gulf + ca_interior done). NEXT = the remaining 7 warm/CA regions of Step 4 (claude.ai).** In order per their `region_source_map` anchors: `ca_north_coast`, `ca_south_coast`, `ca_desert`, `warm_arid`, `low_desert_az`, `fl_peninsula`, `hawaii_tropical`. **Verify window structure PER SOURCE** (one-vs-two windows is a source finding, not a default -- ca_interior turned out single-window, lettuce's fl_peninsula did too). Heat-primary anchoring for heat-inverted/warm-arid regions; **`ca_north_coast` is NEXT and is the cool-LIMITED exception** (marine layer; warm season constrained by LACK of summer heat, NOT a summer pause -- heat-primary anchoring does NOT apply; zone_span 9-10, zone 8 dropped per `region_source_map` marker resolution). Each warm cell cites a source the `region_source_map` lists for that region (the sourcing guard). Re-derive from T1, do NOT carry warm `zones{}` data as verified. **Author region_notes with `°F` from first draft (M16-CA-INT-003).**
- **Then in sequence:** Step 5/5.5 (4-round side-by-side on every claim incl. the se_gulf + ca_interior windows just authored + the remaining region windows; derive every region `calendar[12]`; classify north winter gaps as `cold_pause`; discover the `cornell_ext` zone-6 URL). Steps 6/7/8 (seasoned depth-lift; beginner siblings incl. `cause_beginner`; author the remaining 8 `region_notes_*` pairs; dual-voice coverage gate to 0 -- **author 27, gate displays 21**, see blind spot below). Step 2 rider (`gs_exemplar_finding_003` extreme-zone computation record). Then Claude Code **Step 11** certification + the flip.
- **`status`-vocab decision still owed (Appendix B item 1) -- THREE-state unification.** Cherry's `verification_status.status` is `"verified_retro_complete"`; lettuce holds `"unverified"`. The successor (leaning `verified_arc`) decided at cherry's flip, back-applied to lettuce + both tomatoes, reconciles THREE states. Do NOT set the successor before it is decided + the flip fires.
- **Blossom-drop fruit-set temperature threshold -- T1 anchors in hand, ruling owed (claude.ai surfaces shape, Claude Code adds field).** VH021 + s1b_finding_006 (Clemson HGIC / PSU: >90°F day, <55°F night). Proposed shape: crop-level `fruit_set_temp_f` paralleling `germination_temp_f` -- CP explanatory prose gets `_seasoned`/`_beginner`, numeric thresholds CATEGORICAL/single-register. Schema-touching -> Trevor rules shape; field add is Claude Code's lane. Rule together with s1b_finding_006 (deferred to family close).

## Gate record (2026-06-05, post-ca_interior, GATE-CONFIRMED on canonical)
- **`GATE: 30 VIOLATION(S)`** -- gate-confirmed this session (no longer derived). Composition: **8** region_notes-null (Steps 6/7 -- se_gulf + ca_interior now authored; `northern_tier` + 7 warm regions remain), **dual-voice siblings** (Steps 7/8; gate-counted 21, author 27 -- see blind spot), **1** `cornell_ext` zone-6 URL (Step 5).
- **Baseline this session was 31** (pre-apply, confirming the prior derived estimate). The ca_interior cell apply cleared 1 region_notes pair (9→8) but a NEW §C/D temp-form violation masked the drop (held at 31) until the one-field fix; then 30. **31 → 30 is the real movement.**
- **§A2 shape classes remain 0** (`stub:0 | null-track:0 | stale nested:0`) -- Step 3.5's success condition, unchanged.
- **7 warm regions still PENDING** (`ca_north_coast`, `ca_south_coast`, `ca_desert`, `warm_arid`, `low_desert_az`, `fl_peninsula`, `hawaii_tropical`) -- Step 4 work-list. `northern_tier` promoted-verified (cold), owes only its `calendar[12]` cold_pause derivation (Step 5.5) + region_notes (Steps 6/7).
- **§E source-tier: 0 uncatalogued / 0 non-T1** (40 distinct IDs). **§C/D: 0** dash, **0** temp forms (after the fix). **§F: 1** gap (the known `cornell_ext` zone-6 malformed entry).
- **Lettuce `GATE: PASS`, byte-identical.** Collateral audit clean (only `cherry-tomato.regions.ca_interior` changed; se_gulf untouched).

## Flip gates (the four distinct "flips" -- never conflate them)
1. **Per-crop `launch_ready` flip** -- ✅ lettuce DONE (1 of 9). Cherry is mid-arc (Steps 3.5 + 9 + 10 done; Step 4 in progress, se_gulf + ca_interior done; owes 7 warm regions + Steps 5-8 + 11). Beefsteak after cherry.
2. **Region read-layer flip** -- renderer reads `regions{}` first. **Gate:** shape proven on the 3 provers (lettuce ✅; cherry shell ✅ + se_gulf + ca_interior real-filled, owes 7 more; beefsteak owes both). Ships with `zones{}` fallback. **2.9+.** (The plant-astro renderer rewrite -- read `regions{}` + consume `second_planting` instead of synthesizing from `zones{}` multi-window strings -- is gated here.)
3. **Authoring-model flip** -- carrots onward authored region-first. **Gate:** 3 provers done.
4. **Schema perennial bump** (`lifecycle_override`) -- FUTURE, **2.9+.** After carrots.

**Schema version lineage:** 2.7.5 -> **2.8 (current)** -> region read-layer flip + perennial extension are **2.9+.**

## Live locked decisions / guardrails
- **`harvest_to_table` T2-as-evidence -- RULED (Trevor, 2026-06-05): T1-only as evidence, NO grandfathering.** Precedent for every pre-arc-built crop. Where a T2 is the SOLE backing, replace with a T1; where the claim is already T1-anchored and the T2 is redundant (cherry's case), DROP it. De-citation is whole-crop: evidence fields AND registry (`sources_summary.primary` + `verification_status.source_set`).
- **TEMPERATURE in user-facing copy -- canonical `°F`, NEVER spelled "degrees F" (M16-CA-INT-003, 2026-06-05).** region_notes + all user-facing strings use degree-sign `°F`; the gate's §C/D enforces this on user-facing fields only. The spelled "N degrees F" form is correct + dominant in BACKEND prose (`synthesis_note_seasoned`, etc.) and is NOT policed. Same user-facing-vs-backend split as the em-dash rule. **Author region_notes with `°F` from the first draft on every remaining warm region.** (Owed: add to `tip_region_authoring_standard` if not already explicit.)
- **WINDOW STRUCTURE is a SOURCE FINDING, not a default (reaffirmed at ca_interior).** One-vs-two windows per region is decided by reading the source. se_gulf = two-window (UGA explicit fall deadline); ca_interior = single-window (UC ANR gives only April-May, confirmed by a second T1). Do NOT mirror se_gulf onto warm cells by analogy. Confirm absence of a second window with a second source before authoring single-window.
- **`second_planting` structure (Claude Code lane, Trevor 2026-06-05) -- PROVEN on cherry se_gulf; ABSENT on ca_interior (single-season).** Resolved shape: `resolved_by_zone[z].second_planting = {start_indoors, plant_out, harvest_start, harvest_end, sources, anchoring_urls}` on applicable zones only, + a region-constant `plantings[]` entry `succession_id:2, label:"second", track:"second_planting"` mirroring the beginner rule shape. Main band stays flat on the cell; `second_planting` holds band 2; cell-level `first_plant_date`/`last_plant_date`/`harvest`/`calendar` are the envelope across both bands. Discrete (NOT succession -- tomato `succession_policy.suitable=false`; do NOT use `succession_spring`/`succession_fall`). Seasoned-only visibility. Spec: `docs/superpowers/specs/2026-06-05-second-planting-region-shell-model-design.md`. **DEFERRED UI question (ca_interior):** whether the renderer wants a structured single-season flag (e.g. `second_planting:"none_single_season"`) to place the region-note explanation -- decide at the Phase C read-layer rewrite, not speculatively.
- **Resolved-layer shape standards RATIFIED (2026-06-05):** main planting = flat cell fields (universal); succession = `succession_spring`/`succession_fall` cadence (lettuce's shape); `second_planting` = the discrete-window object above (cherry's shape). Each crop carries ONLY the structures for what it is; each concept has ONE fixed shape across crops. **Lettuce is NOT reshaped.**
- **REGION SHELL-BUILD RULE (Trevor 2026-06-05):** every crop's arc builds ALL 10 region cells to the lettuce bar. North (`northern_tier`, zones 3-7) builds FROM legacy cold `zones{}` (promote + verify + hoist succession; strip nested cell `plantings`; re-stamp `zone_promoted_verified`; **strip the tautological `lifted_from_zone` key**). Warm/CA re-derive from T1 (zone data may be climate-contaminated) -- **se_gulf + ca_interior done this way, no `lifted_from_zone`.** `whole_crop_gate.py` §A2 enforces the structural side. **Owed: claude.ai folds the `lifted_from_zone`-strip into checklist v1.5 Step 3.5 north sub-procedure text** (tool already enforces it).
- **Governing checklist: v1.5** (Step 3.5 Region shell build; reference-GS-crop = `lettuce-leaf`; two-callsite admission/certification model -- null `region_notes` / empty `anchoring_urls` accepted at Step 3.5, violations at Step 11). Dataset is authoritative; flag doc lag.
- **Lane split.** STRUCTURAL/MECHANICAL = Claude Code (region shells, transforms, gates, audits, SHA re-pins, flips, the `second_planting` representation, the dual-voice-walker fix below, catalog admission). Biology + consumer copy + voice/IP + URL discovery + which-zones/dates for second_planting + the T2 ruling + Appendix-A = claude.ai.
- **Anchoring gate is LAYER-SCOPED (1A):** claim-bearing leaves only; legacy `zones{}` + the 10 `regions{}` root rollup `sources` arrays excluded; sibling-named `*_sources`/`*_anchoring_urls` pairs included; `bolting.*` inherit-class excluded.
- **Keep `zones{}` coherent until Phase C** (deletion gate: region carries everything + consumers read region-first + round-trip + frost-input independence). The current renderer still reads `zones{}`.
- **Inheritance is candidate, not verified (v1.4.1 §4).** Pest/disease = highest-scrutiny cluster. Verify cherry AND beefsteak independently even where biology overlaps. **Cherry heat-pause width is cherry-specific (heat resistant); beefsteak re-derives its own, wider. ca_interior's NO-heat-pause call is likewise cherry-specific; beefsteak re-derives.**
- **DASH -- per-sense, per-crop** (cherry carries zero user-facing `--` after Steps 3.5 + 9; confirm at Step 11; se_gulf + ca_interior prose use commas, no em-dashes, per the user-facing rule).
- **Two-field readiness predicate, never a bare count:** `blocks_launch AND status != "resolved"`.
- **Dataset push autonomous (announce-then-execute); plant-astro stays gated.**

## Open items owed
- **M16 cherry Step 4 remainder + Steps 5-8 (claude.ai, IN SEQUENCE):** the **7** remaining warm-window cells (sourcing per `region_source_map`, structure per source; `ca_north_coast` NEXT = cool-limited exception); their `second_planting` data where applicable (cold zones 6-7 + warm 9+); region `calendar[12]` derivation with proper pause states (north `cold_pause`; warm heat-pauses per region; ca_interior has none); the remaining 8 `region_notes_*` pairs; **27 dual-voice siblings** (gate shows 21 -- see blind spot); `gs_exemplar_finding_003` extreme-zone computation record (Step 2); `cornell_ext` zone-6 URL (Step 5). Then Claude Code Step 11 + flip.
- **GATE BLIND SPOT (owed to Claude Code):** the dual-voice completeness walker does NOT descend into `*_beginner_seasoned` companion arrays -> 6 null `why_beginner` siblings (3 `good_beginner_seasoned` + 3 `bad_beginner_seasoned`) are real dual-voice gaps the gate does not count. Authoring count for this arc = **27**; gate displays **21** until fixed. Same blind-spot class as the lettuce readiness gate; will recur per-crop on the pipeline if unfixed.
- **`ucd_plantsci` catalog admission (owed to Claude Code, OPTIONAL):** UC Davis Plant Sciences was read-not-cited at ca_interior (legitimately T1, will recur for CA crops). If wanted as a citable source, queue a catalog-admission entry.
- **Blossom-drop `fruit_set_temp_f` field -- ruling owed (Trevor) + add (Claude Code).** T1 anchors in hand (VH021 + s1b_finding_006). Schema-touching; rule shape, then add. Rule together with s1b_finding_006.
- **`container_notes.overwintering.approach_beginner` null/null -- LEAVE AS-IS.** `applicable: false`; cherry is `lifecycle: annual`, frost-killed. Documented absence (reason field present), not a hole. Do NOT author it.
- **`status`-vocab decision (Appendix B item 1) -- THREE-state unification:** successor to `"unverified"` / `"verified_retro_complete"`; decided at cherry's flip, back-applied to lettuce + tomatoes.
- **Lettuce riders (claude.ai):** s11_finding_004.2 Option-A thermoinhibition source; Appendix-A inventory merge.
- **Beefsteak:** full arc after cherry flips (Step 3.5 reuses `tools/build_region_shells.py`, slug-parameterized, re-run-safe).
- **The Appendix-C findings on cherry:** `_shell` CLOSED (3.5); **`_002` CLOSED** (Steps 9/10 session); **`_001` se_gulf + ca_interior slices CLOSED** (treat `_001` as fully closed when all warm cells sourced + the `cornell_ext` URL is found); `_003` (extreme-zone record), `_004` (`cause_beginner`) remain for Steps 4-8.
- **Owed checklist amendments (claude.ai):** fold the `lifted_from_zone`-strip into v1.5 Step 3.5 north sub-procedure; note the dual-voice-walker blind spot for Claude Code; encode the Step-10-detects / Step-11-closes anchoring-backfill split; archetype-driven biology checklist; dual-voice gate v2 ship-into-tools; **add the `°F`-in-user-facing-copy rule (M16-CA-INT-003) to `tip_region_authoring_standard`.**
- **Deferred (unchanged):** pipeline/operating-model doc; `safe_sowing_note` migration (Phase C); `track` `beginner` -> `main` rename; companions reconciliation (§5).

## Inherited findings (unchanged)
- `finding_001` -- warm `zones{}` 8-11 SE-mis-sourced (legacy layer, own pass); excluded from the anchoring gate by 1A definition. (The cherry `se_gulf` + `ca_interior` re-source at Step 4 is the region-layer fix; the legacy zone re-sourcing is its own pass.)
- `phase_3_tomatoes_s3c_finding_001` -- Minnesota framing on `start_indoors` zones 3-11 (deferred to family close); z9 slice superseded by the se_gulf re-source, zones 3-8/10/11 slices remain.
- na3d-na3g sourcing-sibling findings; `na3d_finding_003` cosmetic.
- Two-field predicate returns 0 unresolved blockers (s11_finding_004 open but non-blocking).

*Update this file at each session close.*

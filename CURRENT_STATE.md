# plant -- CURRENT STATE (live surface)

> ## ⚠️ SESSION PROTOCOL -- read before doing anything
> 1. **Confirm the SHA.** `shasum -a 256 crops_data_final.json` must equal `LATEST.txt`. If it does not, STOP and reconcile.
> 2. **Locate your step on the arc checklist** (gold-standard arc checklist **v1.5**) BEFORE acting. Do not infer the next action from "a cell is done" -- check the checklist. The checklist's auto-derived denominator is authority over any kickoff's named field list. **Kickoffs SUMMARIZE; they are not authority on arc position -- re-derive the next unowned step from the live crop + the checklist.**
> 3. **This file is LIVE STATE ONLY.** Never delta-edit it (the past near-miss came from header-only patches; a corrected header on a stale body is worse than a uniformly-stale file). At session close, **fully regenerate this whole file** from the true state.
> 4. **History is append-only** in `STATE_HISTORY.md` -- APPEND a dated entry there at close, never rewrite it. That file is the recovery net.
> 5. **CLOSE RITUAL (every session ends here).** If the dataset changed: PROMOTE -- write canonical, re-pin `LATEST.txt` (new SHA + date + session). Then ALWAYS: regenerate this file (#3), append to `STATE_HISTORY.md` (#4), sync `00-current/`, and commit (+ push).

---

## 🥬 LETTUCE FLIPPED (M15) · 🍅 CHERRY Steps 3.5 + 9 + 10 DONE (M16, 2026-06-05) -- NEXT = Step 4
`lettuce-leaf` is the FIRST flipped anchor (1 of 9): `launch_ready_core=True` + `launch_ready_seasoned=True` (`status` stays `"unverified"` pending the M16 vocab decision). **M16 is in flight on `cherry-tomato`, CHERRY FIRST then beefsteak.** Cherry has now completed: **Step 3.5** (region shells, Claude Code) and, this session, the two zero-biology downstream items **Step 9** (the residual source-name dash, closing `gs_exemplar_finding_002`) and **Step 10** (the `harvest_to_table` T2-as-evidence arc decision: drop, no grandfather). Both were pulled forward out of checklist order (surfaced + accepted) because they were already-decided and provably independent of the Step 4-8 surface. **NEXT = Step 4 (warm-region window sourcing), starting `se_gulf`.** **Lettuce was NOT touched -- byte-identical, still certified.**

## Canonical pointer
- **Current SHA:** `f916e8fec820bb21a3f52e667e5efe6760764a12dfc2824a3ee20234872b8fd1` (M16 cherry Steps 9+10 -- `harvest_to_table` 4-field de-citation + UC Davis source-name dash). `LATEST.txt` session line: `m16_cherry_steps9_10_mechanical`.
- **Predecessor chain:** `f916e8fe` (Steps 9+10) <- `1b4fea68` (north `lifted_from_zone` strip) <- `a65c7175` (cherry Step 3.5 region shells + `second_planting` structure) <- `29b3aaa9` (M15 lettuce flip) <- `6880ed37` (lettuce write-back) <- `37bfc12d` <- `cdcbf175` <- `df4d24c7` <- `da4b8bc5` <- `7e9eeceb` <- ... (full lettuce chain in STATE_HISTORY).
- Every PROMOTE re-pins this SHA and `LATEST.txt`. Confirm at session start (protocol #1).

## What just happened (2026-06-05, session `m16_cherry_steps9_10_mechanical`)
- **First claude.ai-lane session on cherry's downstream (Steps 4-11) work.** Pulled two zero-biology items forward out of order; the reordering was surfaced to Trevor and accepted (both items provably independent of the Step 4-8 authoring surface: the dash lives in a source NAME 4-8 never regenerate; the T2 is on a pre-arc germination tip warm-region sourcing never touches).
- **Scope grounded in data, not assumed.** "Bump them all to T1" was checked: a full non-T1-source-as-evidence scan of the ENTIRE crop found exactly ONE T2-as-evidence source (`harvest_to_table`) on one leaf -- zero elsewhere (not legacy `zones{}`, not region rollups). "All" == that one citation.
- **Step 10 -- `harvest_to_table` T2 ruling (arc decision, precedent):** T1-only as evidence, NO grandfathering. Executed as a **DROP, not a swap** (the tip is already 4-T1-anchored). A **4-field** de-citation, because the SHA-gated collateral guard caught two REGISTRY references still holding it after the evidence was cleared (`sources_summary.primary` with `citation_count:2`; `verification_status.source_set` 23->22). Cherry's `harvest_to_table` count is now 0.
- **Step 9 -- residual source-name dash (CLOSES `gs_exemplar_finding_002`):** `"UC Davis Postharvest Technology Center -- Produce Facts Sheets"` -> `", Produce Facts Sheets"` (comma; publisher-publication apposition; per-sense, distinct from the region_label colon). **Flagged as a one-character copy call** for Trevor's review.
- **Write discipline:** scratch apply, SHA-gated (`sys.exit(1)` on mismatch), minified `separators=(',',':')`, `ensure_ascii=False`, no trailing newline (verified). Collateral: only `cherry-tomato`; lettuce byte-identical. Independent post-write re-verify (output-only): 9/9 pass.
- **Gate:** residual 34 -> **32** (cleared the dash + the T2). Remaining 32 are ALL Steps 4-8.

## Active work + exact next step
- **M16 cherry is at Steps 3.5 + 9 + 10 DONE. NEXT = Step 4 (warm-region sourcing), claude.ai, starting `se_gulf`.** Source verified region-appropriate windows from T1 anchors into the 9 warm/CA shells Step 3.5 built (the sourcing guard: each warm cell cites a source the `region_source_map` lists for that region). For `se_gulf` (zones 8-9): anchor = **UGA** (C943/C963/B577) for z8, **UGA + UF/IFAS VH021 North Florida** for z9 (editorial pick of one T1 anchor per crop). Drop the orphaned UMN/Minnesota `synthesis_note` text nested in legacy `zones["9"]` and re-source `regions.se_gulf` from the real SE anchor (`gs_exemplar_finding_001`). Author region-tip overrides where T1 sources fork the grower action (Step 4 rider).
- **Then in sequence:** Step 5/5.5 (4-round side-by-side incl. region windows; derive every region `calendar[12]` from windows + declared pauses, precedence pause > plant > harvest > growing > wait; classify the north winter gaps as `cold_pause`; encode the `se_gulf` summer heat-pause the legacy prose describes but the legacy calendar omits; resolve the cherry z9 `calendar[]`<->prose heat-pause drift; discover the `cornell_ext` zone-6 URL). Steps 6/7/8 (seasoned depth-lift; beginner siblings incl. `cause_beginner`; author all 10 `region_notes_*`; dual-voice coverage gate to 0 -- **author 27, gate displays 21**, see blind spot below). Step 2 rider (`gs_exemplar_finding_003` extreme-zone computation record). Then back to Claude Code for **Step 11** certification + the flip.
- **`second_planting` DATA is Step 4/5 biology (structure already defined + gate-validated):** author which zones get a discrete second planting + the dates. Cold zones 6-7 ("two crops possible") + warm 9+ (a late-summer batch). Resolved layer: `regions[r].resolved_by_zone[z].second_planting = {plant_out, start_indoors, harvest_start, harvest_end, sources, anchoring_urls}` on applicable zones only; rule layer: a region-constant `plantings[]` entry `track:"second_planting"`. It is DISCRETE, NOT succession (tomato `succession_policy.suitable=false`) -- do NOT encode it as `succession_spring`/`succession_fall`. Seasoned-only visibility. The legacy `zones["9"]` already carries it as a comma-string (`Feb 7 - Feb 28, Jun 24 - Jul 8`, "two full seasons") -- de-multiplex into the structured object.
- **`status`-vocab decision still owed (Appendix B item 1) -- now a THREE-state unification.** Cherry's `verification_status.status` is currently `"verified_retro_complete"` (from `phase_3_m10_cleanup`), NOT `"unverified"`; lettuce holds `"unverified"`. The successor (leaning `verified_arc`) decided at cherry's flip, back-applied to lettuce + both tomatoes, reconciles THREE states. Do NOT set `gold_standard`/the successor before it is decided + the flip fires.

## Gate record (2026-06-05, post-Steps-9/10, on canonical)
- **Residual 32** -- all downstream Steps 4-8: **10** region_notes-null (Steps 6/7), **21** dual-voice siblings gate-counted (Steps 7/8; see blind spot -> author 27), **1** `cornell_ext` zone-6 URL (Step 5). The 1 source-name dash + the 1 `harvest_to_table` T2 from the prior 34 are CLEARED this session.
- **§A2 shape classes remain 0** (`stub:0 | null-track:0 | stale nested:0`) -- Step 3.5's success condition, unchanged. `both region_notes null: 10` remains (admission-acceptable per the v1.5 two-callsite model; becomes a Step-11 cert violation; claude.ai authors at Steps 6/7).
- **Lettuce `GATE: PASS`, byte-identical.** Collateral audit clean (only `cherry-tomato` changed; the 5 changed leaves are the 4-field de-citation + the 1 source-name).

## Flip gates (the four distinct "flips" -- never conflate them)
1. **Per-crop `launch_ready` flip** -- ✅ lettuce DONE (1 of 9). Cherry is mid-arc (Steps 3.5 + 9 + 10 done; owes Steps 4-8 + 11). Beefsteak after cherry.
2. **Region read-layer flip** -- renderer reads `regions{}` first. **Gate:** shape proven on the 3 provers (lettuce ✅; cherry shell ✅, owes fill; beefsteak owes both). Ships with `zones{}` fallback. **2.9+.** (The plant-astro renderer rewrite -- read `regions{}` + consume `second_planting` instead of synthesizing from `zones{}` multi-window strings -- is gated here.)
3. **Authoring-model flip** -- carrots onward authored region-first. **Gate:** 3 provers done.
4. **Schema perennial bump** (`lifecycle_override`) -- FUTURE, **2.9+.** After carrots.

**Schema version lineage:** 2.7.5 -> **2.8 (current)** -> region read-layer flip + perennial extension are **2.9+.**

## Live locked decisions / guardrails
- **`harvest_to_table` T2-as-evidence -- RULED (Trevor, 2026-06-05): T1-only as evidence, NO grandfathering.** Precedent for every pre-arc-built crop. Where a T2 is the SOLE backing of a claim, replace with a T1 that supports the claim; where the claim is already T1-anchored and the T2 is redundant (cherry's case), DROP it. De-citation is whole-crop: evidence fields AND registry (`sources_summary.primary` entry + `verification_status.source_set`) -- a de-cited source must not remain declared.
- **`second_planting` structure (Claude Code lane, Trevor 2026-06-05).** Cherry is the first crop to need a discrete second planting. Representation OWNED by Claude Code. Resolved shape: `resolved_by_zone[z].second_planting = {plant_out, start_indoors, harvest_start, harvest_end, sources, anchoring_urls}` on applicable zones only, + a region-constant `plantings[]` entry `track:"second_planting"`. Discrete plantings (`beginner` + `second_planting`) share one window shape; SUCCESSION is a SEPARATE cadence. Seasoned-only visibility. The gate validates the structure when present. Spec: `docs/superpowers/specs/2026-06-05-second-planting-region-shell-model-design.md`.
- **Resolved-layer shape standards RATIFIED (2026-06-05):** main planting = flat cell fields (universal); succession = `succession_spring`/`succession_fall` cadence (lettuce's shape, ratified -- carrot matches it); `second_planting` = the discrete-window object above. Each crop carries ONLY the structures for what it is; each concept has ONE fixed shape across crops. **Lettuce is NOT reshaped.**
- **REGION SHELL-BUILD RULE (Trevor 2026-06-05):** every crop's arc builds ALL 10 region cells to the lettuce bar. North (`northern_tier`, zones 3-7) builds FROM legacy cold `zones{}` (promote + verify + hoist succession into region-constant `plantings[]` where applicable; strip nested cell `plantings`; re-stamp `static_precompute -> zone_promoted_verified`; **strip the tautological `lifted_from_zone` key** -- in `build_region_shells.py`, applies to all crops' north). Warm/CA re-derive from T1 (zone data may be climate-contaminated). `whole_crop_gate.py` §A2 enforces the structural side. **Owed: claude.ai folds the `lifted_from_zone`-strip into checklist v1.5 Step 3.5 north sub-procedure text** (tool already enforces it).
- **Governing checklist: v1.5** (Step 3.5 Region shell build; reference-GS-crop = `lettuce-leaf`; two-callsite admission/certification model -- null `region_notes` / empty `anchoring_urls` accepted at Step 3.5, violations at Step 11). Dataset is authoritative; flag doc lag.
- **Lane split.** STRUCTURAL/MECHANICAL = Claude Code (region shells, transforms, gates, audits, SHA re-pins, flips, the `second_planting` representation, the dual-voice-walker fix below). Biology + consumer copy + voice/IP + URL discovery + which-zones/dates for second_planting + the T2 ruling + Appendix-A = claude.ai. **Note: per-sense dash resolution + the T2-as-evidence ruling are claude.ai** (editorial/judgment), even though the work is small -- both executed this session.
- **Anchoring gate is LAYER-SCOPED (1A):** claim-bearing leaves only; legacy `zones{}` + the 10 `regions{}` root rollup `sources` arrays excluded; sibling-named `*_sources`/`*_anchoring_urls` pairs included; `bolting.*` inherit-class excluded.
- **Keep `zones{}` coherent until Phase C** (deletion gate: region carries everything + consumers read region-first + round-trip + frost-input independence). The current renderer still reads `zones{}`. (The orphaned UMN text drop in `zones["9"]` at Step 4 is a finding-scoped correction, not a structural change -- it removes wrong text, preserving coherence.)
- **Inheritance is candidate, not verified (v1.4.1 §4).** Pest/disease = highest-scrutiny cluster. Verify cherry AND beefsteak independently even where biology overlaps.
- **TEMPERATURE -- canonical `°F`** user-facing. **DASH -- per-sense, per-crop**, executed by each crop's arc (cherry's 4 region_label dashes done at 3.5; the 1 source-name dash done this session -> cherry now carries zero user-facing `--`, confirm at Step 11).
- **Two-field readiness predicate, never a bare count:** `blocks_launch AND status != "resolved"`.
- **Dataset push autonomous (announce-then-execute); plant-astro stays gated.**

## Open items owed
- **M16 cherry Steps 4-8 (claude.ai, IN SEQUENCE):** warm-window sourcing (start `se_gulf`); the `second_planting` data (which zones + dates) against the defined structure; region `calendar[12]` derivation with proper pause states (north `cold_pause`, se_gulf summer heat-pause, z9 drift); 10 `region_notes_*` pairs; **27 dual-voice siblings** (gate shows 21 -- see blind spot); `gs_exemplar_finding_003` extreme-zone computation record (Step 2); `cornell_ext` zone-6 URL (Step 5). Then Claude Code Step 11 + flip.
- **GATE BLIND SPOT (owed to Claude Code):** the dual-voice completeness walker does NOT descend into `*_beginner_seasoned` companion arrays -> 6 null `why_beginner` siblings (3 `good_beginner_seasoned` + 3 `bad_beginner_seasoned`) are real dual-voice gaps the gate does not count. Authoring count for this arc = **27**; gate displays **21** until fixed. Same blind-spot class as the lettuce readiness gate; will recur per-crop on the pipeline if unfixed.
- **`container_notes.overwintering.approach_beginner` null/null -- LEAVE AS-IS.** `applicable: false`; cherry is `lifecycle: annual`, frost-killed. Documented absence (reason field present), not a hole. Do NOT author it.
- **`status`-vocab decision (Appendix B item 1) -- THREE-state unification:** successor to `"unverified"` / `"verified_retro_complete"`; decided at cherry's flip, back-applied to lettuce + tomatoes.
- **Lettuce riders (claude.ai):** s11_finding_004.2 Option-A thermoinhibition source; Appendix-A inventory merge.
- **Beefsteak:** full arc after cherry flips (Step 3.5 reuses `tools/build_region_shells.py`, slug-parameterized, re-run-safe).
- **The Appendix-C findings on cherry:** `_shell` CLOSED (3.5); **`_002` CLOSED (this session)**; `_001` (warm sourcing + `cornell_ext` URL + orphan UMN text), `_003` (extreme-zone record), `_004` (`cause_beginner`) remain for Steps 4-8.
- **Owed checklist amendments (claude.ai):** fold the `lifted_from_zone`-strip into v1.5 Step 3.5 north sub-procedure; encode the Step-10-detects / Step-11-closes anchoring-backfill split; archetype-driven biology checklist; dual-voice gate v2 ship-into-tools.
- **Deferred (unchanged):** pipeline/operating-model doc; `safe_sowing_note` migration (Phase C); `track` `beginner` -> `main` rename; companions reconciliation (§5).

## Inherited findings (unchanged)
- `finding_001` -- warm `zones{}` 8-11 SE-mis-sourced (legacy layer, own pass); excluded from the anchoring gate by 1A definition. (The cherry `se_gulf` re-source at Step 4 is the region-layer fix; the legacy zone re-sourcing is its own pass.)
- na3d-na3g sourcing-sibling findings; `na3d_finding_003` cosmetic.
- Two-field predicate returns 0 unresolved blockers (s11_finding_004 open but non-blocking).

*Update this file at each session close.*

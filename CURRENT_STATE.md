# plant -- CURRENT STATE (live surface)

> **⚠️ SESSION PROTOCOL -- read before acting.**
> 1. **Confirm canonical FIRST:** `shasum -a 256 crops_data_final.json` == `LATEST.txt`; also `git -C ~/plant-dataset log --oneline -1` + `git status -sb` (the working tree can sit AHEAD of HEAD -- an uncommitted hand-promote). Locate the active step on the gold-standard arc checklist (v2.0) before acting -- never infer "next" from "a cell is done".
> 2. **CURRENT_STATE.md = LIVE STATE ONLY.** Never delta-edit; FULLY REGENERATE at close via `tools/gen_current_state.py`, then carry the prose slots forward + amend. The mechanical sections are generated from true state.
> 3. **STATE_HISTORY.md = APPEND-ONLY** -- a dated entry most-recent-first below its header; never rewrite. It is the recovery log that caught the premature-flip near-miss.
> 4. **RELEASE VERIFICATION before any promote** (protocol #6): `whole_crop_gate` + `tools/release_verify.py` + cross-check vs claude.ai's STATE_HISTORY claims. A green gate is NOT a clean release.
> 5. **Four flips are distinct, never conflated:** per-crop `launch_ready` (that crop's Step 11 == 0), region read-layer, authoring-model, schema bump.

---


**18 GS-arc anchors CERTIFIED (roster COMPLETE 2026-06-23). On top of that, the post-roster AUDIT's Phase A cross-cutting fixes are now SHIPPED in the dataset (2026-06-24, `9739e373`):** the render-ready `npk_ratio` pill field (audit F3) and the shared, crop-invariant `region_chill_delivered` table (audit F2). All 18 anchors stay gate-clean (whole_crop_gate 18/18, +2 new universal gates A17/A18). The chill-delivered numbers are a realistic PLACEHOLDER (the union of the old per-crop bands) pending a claude.ai T1-reconcile pass. The plant-astro card UI (npk pill + chill both-numbers) is BUILT + build-verified but its submodule bump is GATED on Trevor (batching all audit fixes before anything ships live).

## Canonical pointer
- **Current SHA:** `9739e37365f32561caf8994c46034fc08e32ca4b3c9a0be983c7f2a7172283cd`. `LATEST.txt` session: `phase_a_npk_chill` (2026-06-24).
- **Predecessor chain** (most-recent commits touching `crops_data_final.json`; content SHAs):
  - `9739e373` -- feat(schema): Phase A -- npk_ratio field + chill shared-delivered table (audit F2/F3)
  - `6e9538e1` -- feat(broccoli): CERTIFIED -- anchor 18 (Step 11 flip); the LAST GS anchor, the roster is DONE (~18)
  - `956fc987` -- feat(broccoli): Steps 6-8 -- consumer prose + the 7 compounds (whole_crop_gate -> 0, structurally cert-ready)
  - `b0ccdd2d` -- feat(zucchini-courgette): CERTIFIED -- anchor 17 (Step 11 flip); the warm-season rail-rider
  - `678312b2` -- feat(zucchini-courgette): Steps 6-8 -- consumer prose + the 7 compounds (whole_crop_gate 17 -> 0, structurally cert-ready)
  - `78ef87cd` -- feat(broccoli): Steps 4-5.5 -- region fill (10 regions: windows + calendars + succession geometry + A8 successions_realized) + 11 source-catalog sub-id mints
  - `642f4890` -- feat(zucchini-courgette): Steps 4-5.5 -- region fill (10 regions: windows + calendars + succession geometry + A8 successions_realized)

## What just happened (session `phase_a_npk_chill`, 2026-06-24)

**PHASE A of the post-roster AUDIT -- the two cross-cutting STRUCTURAL fixes, each "fix the 18 + arm the bots" in one motion (write the gate, then fix to green).** base `6e9538e1` -> `9739e373` (data commit `3d85300`). 17 crops changed (npk), 3 of them also chill; microgreens untouched.

- **F3 NPK pill.** Added `fertilizer.npk_ratio`, a render-ready "N-P-K" string lifted DETERMINISTICALLY from the verified `npk_hint_seasoned` (`tools/migrate_npk_ratio.py`): the 13 ratio anchors (e.g. cherry `5-10-10`, broccoli `21-0-0`, strawberry `16-16-16`). The 4 ratio-less by design (lemon/onion/lavender/blueberry) carry explicit `npk_ratio: null` + a short `npk_tag` (e.g. "Nitrogen-forward", "Acid-forming, ammonium N"). **GATE A17 `npk_gate.npk_ratio_violations`** (present-or-explicit-null; no-op off the npk_hint surface); `register_completeness_gate` rules `npk_ratio`/`npk_tag` USER-FACING-CATEGORICAL. The pill bug: FeedingCard `.fert-npk` + the app `ap-npk` printed the whole prose paragraph.
- **F2 chill refactor.** `chill_hours_delivered` was a CLIMATE datum authored PER-CROP, so peach/apple/blueberry disagreed at the same region+zone (blueberry string-typed). Consolidated into ONE shared, crop-invariant top-level **`region_chill_delivered`** table (region -> {zone -> [lo,hi]}, 10 regions / 20 zone cells; `tools/migrate_chill_shared_table.py`); STRIPPED the per-crop field (80 occurrences). chill-REQUIRED stays per-variety. **`perennial_gate` (A3) now reads the shared table** for the no-fruit split (signature `perennial_cert_violations(crop, chill_table)`); **GATE A18 `chill_gate.chill_delivered_absent_violations`** forbids any per-crop copy; **`release_verify` section H** validates the table [lo,hi] shape; **`build_region_shells`** no longer scaffolds the dead per-cell field; `berries_woody_gate` comments de-reference it. The seeded numbers are the UNION of the old per-crop bands -- realistic PLACEHOLDER, NOT independently sourced (see `region_chill_delivered_provenance`).
- **Gates/tests:** whole_crop_gate 18/18 PASS; register + npk + chill gates clean; precommit_release_verify "no regression" (apple/peach cleared 35, blueberry 21 under the new chill gate); 31 tooling tests pass.
- **plant-astro (branch `fix/calendar-harvest-two-row`, NOT pushed):** FeedingCard renders the ratio/tag pill + KEEPS the npk_hint prose as a dual-register paragraph; `ap-npk` + `demo.ts` + `feeding.ts` updated; ChillHoursCard + TreeCalendarCard read the shared `region_chill_delivered` via TreeGuide and show BOTH ("your area banks ~X; these varieties need ~Y"), guarded so citrus shows no gauge. `astro build` 312 pages clean; 240 vitest pass.

## Active work + next step -- Phase A done; chill numbers + the rest of the audit remain

- **claude.ai chill-sourcing handoff (THE open dependency):** reconcile every `region_chill_delivered[region][zone]` from the placeholder union to ONE T1-sourced value (UC IPM chill maps + regional extension chill data; do NOT derive from the USDA zone). Kickoff staged at `~/plant-dataset/docs/handoffs/2026-06-24-chill-sourcing-kickoff.md`. Also reconcile the per-crop `chill_basis_*` prose against the final numbers.
- **plant-astro submodule bump = GATED on Trevor.** The dataset is pushed; the plant-astro bump (-> dataset `9739e373`) + the card UI are prepared on the branch but NOT merged/pushed (push auto-deploys to Netlify; we are batching ALL audit fixes before anything ships live).
- **Rest of the audit (`plant-astro/docs/gs-arc-audit-2026-06-24.md`):** F1 green-beans calendar two-row fix is banked on the same plant-astro branch (uncommitted, plant-astro-only -- no dataset change). Phase B = gate-then-fix the systematic classes (companions bare-string F4/F6, display-readiness F5, tips coverage F7). Phase C = the un-gateable source-truth sample per batch.

## Gate record (generated 2026-06-24, on canonical `9739e373`)
- **cherry-tomato: `PASS` (0)**
- **beefsteak-tomato: `PASS` (0)**
- **carrot: `PASS` (0)**
- **basil: `PASS` (0)**
- **zucchini-courgette: `PASS` (0)**
- **green-beans-bush: `PASS` (0)**
- **broccoli: `PASS` (0)**
- **peach: `PASS` (0)**
- **apple: `PASS` (0)**
- **lemon: `PASS` (0)**
- **blueberry: `PASS` (0)**
- **lettuce-leaf: `PASS` (0)**
- **onion: `PASS` (0)**
- **strawberry: `PASS` (0)**
- **orange-navel: `PASS` (0)**
- **microgreens-mix: `PASS` (0)**
- **lavender: `PASS` (0)**
- **zinnia: `PASS` (0)**
- **register_completeness_gate: `PASS`**

## Region fill state (generated)
- **cherry-tomato: 10/10 region cells filled**; 8 heat_pause, 8 second_planting
- **beefsteak-tomato: 10/10 region cells filled**; 8 heat_pause, 6 second_planting
- **carrot: 10/10 region cells filled**; 13 heat_pause
- **basil: 10/10 region cells filled**
- **zucchini-courgette: 10/10 region cells filled**
- **green-beans-bush: 10/10 region cells filled**
- **broccoli: 10/10 region cells filled**; 7 heat_pause, 14 second_planting
- **peach: 10/10 region cells filled**
- **apple: 10/10 region cells filled**
- **lemon: 10/10 region cells filled**
- **blueberry: 10/10 region cells filled**
- **lettuce-leaf: 10/10 region cells filled**; 15 heat_pause
- **onion: 10/10 region cells filled**
- **strawberry: 10/10 region cells filled**
- **orange-navel: 10/10 region cells filled**
- **microgreens-mix: 0/0 region cells filled**
- **lavender: 10/10 region cells filled**
- **zinnia: 10/10 region cells filled**

## Flip gates (generated)
- **cherry-tomato:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **beefsteak-tomato:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **carrot:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **basil:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **zucchini-courgette:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **green-beans-bush:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **broccoli:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **peach:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **apple:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **lemon:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **blueberry:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **lettuce-leaf:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **onion:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **strawberry:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **orange-navel:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **microgreens-mix:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **lavender:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **zinnia:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **18 anchors certified** (launch_ready true + status `verified_gs_arc`). (Target denominator is a roadmap call -- see the headline slot -- not derivable here.)

## Live locked decisions / guardrails
- **NPK PILL = a dedicated `fertilizer.npk_ratio` (Phase A, audit F3, 2026-06-24):** a render-ready bare "N-P-K" string (e.g. `5-10-10`) is the SINGLE SOURCE OF TRUTH for the feeding pill -- derived DETERMINISTICALLY (first `\d+-\d+-\d+`) from the verified `npk_hint_seasoned`, never re-derived at render. Ratio-less crops (citrus/allium/lavender/blueberry) carry explicit `npk_ratio: null` + a short `npk_tag`. `npk_ratio`/`npk_tag` are USER-FACING-CATEGORICAL (single-form, ruled in `register_completeness_gate`). GATE **A17** = present-or-explicit-null (no-op off the npk_hint surface). The pill renders the ratio OR the tag; the npk_hint PROSE still renders as a dual-register paragraph (the "excess-N is the mistake" guidance survives).
- **CHILL-DELIVERED = ONE shared crop-invariant `region_chill_delivered` table (Phase A, audit F2, 2026-06-24):** chill-DELIVERED is a CLIMATE datum (region+zone -> [lo,hi]), authored ONCE at the dataset top level, NEVER per-crop. chill-REQUIRED stays per-variety (`chill_hours_required` / `chill_hours_range`). NO crop may carry `chill_hours_delivered` (GATE **A18**); `perennial_gate` A3's no-fruit split + the plant-astro chill cards both READ the shared table; `release_verify` H validates its [lo,hi] shape; `build_region_shells` does not scaffold a per-cell chill field. Numbers are a placeholder union pending claude.ai T1 sourcing (`region_chill_delivered_provenance`); the gate enforces SHAPE + crop-invariance, never the values.
- **BERRIES_WOODY ARCHETYPE -- CERTIFIED (anchor 18, the FIRST + only `berries_woody`; the 16th GS anchor; design spec `2026-06-22-blueberry-berries-woody-model-design.md`, D1-D8; cert 2026-06-23 `0b767fc2`):** a woody fruiting SHRUB whose growable TYPE is CHILL-GATED by region and whose calendar SHAPE splits by per-cell `leaf_habit` (deciduous/evergreen); `calendar_basis:"berries_woody"`, frost resolution ON; planted ONCE 20-50 yrs; acid soil pH 4.5-5.5; `type` top-level=`"berry"` (the family precedent + fixes the plant-astro TreeGuide mis-route). **TOOLSET (test-first, 29/29):** `berry_woody_calendar` deriver (deciduous = the tree dormant/prune/bloom/growing/harvest/care cycle; evergreen = growing year-round + bloom/harvest/care, REUSE tree tokens) + A16; `berries_woody_gate` A15 (the chill signature -- chill_hours_required IS the gate basis, the INVERSE of woody_ornamental which rejects it; the type COVERAGE invariant; leaf_habit<->token placement); `_build_berry_woody_shells` (dispatched BEFORE `_is_tree` -- blueberry is lifecycle=permanent); `derive_berry_woody_calendars`. **leaf_habit = OPTION B (region-biology binary, NOT type-locked):** SHB reads deciduous in cool CA, evergreen in the warm South. **CERT source-fidelity fetch (8 cited live T1): ZERO contradictions** (NHB yield 5-10 EXACT, rabbiteye consistent, pH 4.5-5.5 corroborated 4x, pollination + chlorosis, SHB/rabbiteye chill bands, storage). 3 open findings all blocks_launch:false (chill_hours_delivered climate estimates -- NOW the shared table; warm_arid harvest [uarizona AZ1585 PDF unparseable]; legacy zones{} null shell). **GATE RULING from this arc:** `schedule_by_stage[].rate` = USER-FACING-CATEGORICAL (machinery, sibling to `frequency`). Template for raspberry/blackberry/currant/gooseberry/grape (all `berries_woody` shells).
- **WARM-SEASON-ANNUAL OFF-SEASON TOKEN = `cold_pause`, NEVER `wait` (green-beans Steps 4-5.5, 2026-06-22; conformed in the Claude Code lane):** a frost-tender annual's frost-killed WINTER off-season months render `cold_pause` (the frost-bracketed dormant period) -- every certified warm-season annual (cherry-tomato/beefsteak/basil/zinnia, and the succession exemplar lettuce-leaf) carries `cold_pause` with ZERO `wait`. `wait` is the ILLEGIBLE token Step 5.5 pause-legibility says to resolve; it is NOT a valid resting state for a populated calendar. claude.ai's region fill correctly resolved the MID-SEASON desert gaps to `heat_pause` but used `wait` for the WINTER off-season; CC swapped all 73 to `cold_pause` (all in frost-bracketed z3-10; the frost-free z11 cells correctly used `season_over` for their heat/bounded off-season). The A5 gate flags wait-tokens as ADVISORY NOTES (not blocking), so this needs the CC eye at release -- FEED FORWARD to the remaining rail-riders (zucchini/broccoli). **`region_label` = the certified COLON convention (`California: Interior Valleys`), never ` -- ` (a gate-C user-facing dash); conform to the reference crop lettuce-leaf's labels.** **SUCCESSION MIXED-GEOMETRY (proven inverse-of-carrot on green-beans): geometry is keyed PER-REGION by continuous-vs-split (second_planting spec v1.1 §4), NOT warm-vs-cold -- continuous -> `succession_continuous` (deriver rule-3 from first/last_plant_date) + split -> `succession_spring`/`succession_fall` comma-lists (rule-2 counts them; rule-2 wins before the rule-3 heat_pause split, so the legibility token + the count never collide). `successions_realized` is CC-DERIVED at A8, never authored; `succession_policy.successions` reconciles to max-over-zones.**
- **RAIL-RIDER RELEASE = conform claude.ai's field SHAPES to the certified contract in the Claude Code lane (green-beans-bush, anchor 15, Steps 1-3, 2026-06-22):** an annual on the proven `warm_season_fruiting` rails needs NO new archetype/tooling, but a fresh-authored slice can still DRIFT from the certified field shapes, and `register_completeness_gate` HALTs on the novel/unruled prose patterns (the designed human-in-the-loop checkpoint). Three drifts CONFORMED at release -- the contract is stable across all 14 certified anchors, so carrot/onion are the TEMPLATES (NOT new rulings): (1) companion `provenance` = the rich `{label, confidence, reason, verified_against_sources, verified_date}` + item-level `evidence_label`/`confidence`/`verified_against_sources` (onion = the all-`traditional` template: well-documented-traditional -> confidence `medium`, weak folk-caution -> `low`, `verified_against_sources:false` / `verified_date:null`); NEVER a flat `{basis, note}`. (2) `soil.organic_matter_preference` = a USER-FACING-CATEGORICAL token (`moderate`/`high`), NOT prose -- prep guidance belongs in the soil/fertilizer prose pairs. (3) `container_notes.shape_requirements` is ruled CP, so a populated `_seasoned` REQUIRES a populated `_beginner` (the B dual-voice gate rejects a populated-seasoned/null-beginner half-pair; the null-`_beginner` scaffold is ONLY for an entirely-unused field) -- authoring the beginner register is claude.ai's COPY lane, so DROP the optional field at Steps 1-3 + defer to Steps 6-8 as a real pair rather than freehand it. **`successions_realized` stays OUT-OF-SCOPE at Steps 1-3 (A8 derives it at region fill); the crop-level `succession_policy` is the Step-2 deliverable, with `pause_in_heat:false` for heat-loving beans (the carrot contrast).**
- **WOODY-ORNAMENTAL ARCHETYPE -- CERTIFIED (anchor 14, the FIRST perennial_woody_ornamental) 2026-06-22 -- cert source-fidelity fetch CLEAN; 5 blocks_launch:false open findings (lavender, anchor 14; the FIRST `perennial_woody_ornamental`, the 7th `calendar_basis`; design spec `2026-06-19-lavender-woody-ornamental-model-design.md`, D1-D12; tooling built 2026-06-22, commits `b6e5115..9395854`):** a woody perennial SUBSHRUB grown for BLOOMS (not fruit) whose LIFECYCLE is region-dependent -- per-cell `grown_as` in {perennial, annual} (the COLD-driven inverse of strawberry's heat flip: cold-hardy zones = in-ground perennial shrub, coldest zones / tender species = container/replant annual). **Calendar tokens:** `dormant`/`growing`/`bloom` + the NEW `prune` (the hard cut-back, the month after bloom end; perennial cells only; user label "Cut back"); annual cells use `plant`/`growing`/`bloom`/`season_over`. **NO `harvest`/`renovation` token** -- for an ornamental the BLOOM window IS the cut-for-use window. Frost resolution stays ON (frost-bracketed dormancy; frost-free -> growing year-round, the evergreen analog). **DERIVER** `tools/woody_ornamental_calendar.py`; **FILL** `tools/derive_woody_ornamental_calendars.py`; **GATES** (test-first, no-op off basis) `woody_ornamental_violations` (whole_crop_gate **A13** structural) + `woody_ornamental_calendar_violations` (**A14** coherence). **Cold-hardiness handled LIGHTER than citrus: `gating_factors` EMPTY, NO A9 coverage gate** (D7); no tree machinery (rootstock/chill-gate/pollinizer). **FIRST-RUN TRIGGER GOTCHA:** `build_region_shells._is_woody_ornamental` keys on `archetype == "woody_ornamental"` -- the shell default `companion_and_ornamental_flower` is SHARED with zinnia + the herbaceous perennials, so **claude.ai must SET `archetype = "woody_ornamental"` at Step 1-2**. Template for the woody subshrub-herb family (rosemary/sage/thyme/butterfly-bush/Russian-sage).
- **TIPS RENDERING-CONFORMANCE (`tips_by_stage`) is GATED at A12 (`compound_population_gate.py`, 2026-06-21):** the renderer (`GrowingJourneyCard`) does `tipsByStage[stage.id].text_seasoned`, so a tip ONLY renders if (a) its list is non-empty, (b) it uses the `text_seasoned`/`text_beginner` field (NOT `tip_*`), and (c) its KEY is a real `growth_stages` id. `tips_violations` enforces all three; the generic `empty_compound_violations` (also A12) covers the other consumer compounds. **INDOOR crops are EXEMPT from tips_by_stage** -- their tip surface is `indoor_cycle.tip_*` (gated A6). **When `growth_stages` ids change, re-key `tips_by_stage` in the same pass.** (Audit F7: apple `scaffold_formation` + onion `bulb_initiation` still have a real stage with no tip key -- a Phase B fix, extend A12 from no-orphan-keys to COVERAGE.)
- **PER-ZONE `successions_realized` = a DERIVED integer, NOT sourced (succession pass, 2026-06-15):** `resolved_by_zone.<z>.successions_realized` = the realized sowing count a zone's season supports. Deriver (`tools/derive_realized_successions.py`, gap-aware); **Cap 12 GLOBAL**. Scope = `succession_policy.suitable==True` + region-filled + non-indoor. `whole_crop_gate` **A8** re-derives-and-asserts-equality + presence + reconciliation + out-of-scope absence. Spec v1.1 in `05-methodology/current/`.
- **BERRIES_HERBACEOUS ARCHETYPE -- CERT-PROVEN on strawberry (anchor 13 CERTIFIED 2026-06-18; the FIRST and ONLY berries_herbaceous crop):** a herbaceous perennial whose LIFECYCLE is region-dependent. **`calendar_basis: perennial_herbaceous`** (frost resolution stays ON). **Per-cell `grown_as` in {perennial, annual}**. **Calendar vocab:** `dormant` + ONE new token `renovation` (perennial June-bearing cells); annual cells use `season_over`. **GATES:** `berry_herbaceous_violations` (**A10** structural) + `berry_calendar_violations` (**A11** coherence). Photoperiod is the deliberate INVERSE of onion (type is a VARIETY attribute, NO `gating_factors:["photoperiod"]`, NO A9). chill INFORMATIONAL (`chill_hours_required` null).
- **PHOTOPERIOD / DAY-LENGTH ARCHETYPE -- cert-proven on onion (anchor 12, the FIRST photoperiod crop):** day length gates which CULTIVAR bulbs by latitude (long/intermediate/short_day), NOT the calendar -- onion STAYS `calendar_basis:frost_anchored`. Crop-level `gating_factors:["photoperiod"]` + a dual-register `photoperiod` block; variety-level `day_length_type`; per-cell `recommended_day_length_type`. **A9 gate (`photoperiod_violations`):** every variety + resolved cell has a valid type + the COVERAGE invariant. **BOLTING is VERNALIZATION (cold-then-warm), NOT photoperiod.** hawaii_tropical = short_day, NOT year_round.
- **EVERGREEN + HEAT model -- COMPLETE + cert-proven (lemon cold-only, orange cold+heat).** heat `marginal` -> suitability `marginal`. TREE per-variety schema = lemon's 11-key set incl. `delta`. The perennial no-fruit split (A3) is gating_factor-keyed (chill Goldilocks for deciduous; cold-only monotone for evergreen; the heat FLOOR for heat_accumulation). Chill-delivered for the deciduous branch now comes from the shared `region_chill_delivered` table.
- **INDOOR / `non_seasonal_indoor` ARCHETYPE -- proven on microgreens-mix (anchor 11):** grown INDOORS year-round; NO frost/season/hardiness zone. **`regions{}` AND `zones{}` COLLAPSED to `{}`.** Self-describes via `zone_independent:true` + `indoor_cycle{}` + `calendar_basis:non_seasonal_indoor`. RENDER = a relative day/week timeline. EXEMPT from the npk pill (no npk_hint), tips A12, weather_triggers.
- **COMPANION ITEM SHAPE = the certified carrot rich-object shape (NOT a flat `why`).** `good_seasoned[]` = {name, category, timing, why_seasoned, notify_weeks_before, provenance:{...}, sources, anchoring_urls}; `good_beginner_seasoned[]` = {name, why_beginner}. **VOCAB = `research_backed` / `likely` / `traditional`.** (Audit F4/F6: lemon/orange/basil/green-beans carry bare-STRING companion entries that the card silently drops, + apple's goods are in the beginner-only bucket -- a Phase B gate-then-fix.)
- **SOURCE-TIER: certified crops carry ZERO non-T1.** `johnny_seeds` / seed-company / almanac = T2 (evidence-log corroboration only); `whole_crop_gate` E flags any non-T1.
- **DISPLAY-READINESS FIELDS ARE A SEPARATE BAR FROM CERT (garden-calculator-surfaced):** the calculator gates PLACEABILITY on `spacing_inches` (+ `companions`) and CONTAINER on `container_ok==true` AND `min_pot_gallons != null`; cert validates BIOLOGY/sources and does NOT require them. **Audit display-readiness PER consuming surface.** (Audit F5: lemon sun/water/feeding blanks + orange pH/container/feeding blanks -- a Phase B fill.)
- **CERT mechanics / THE FLIP:** source-verbatim (vs cited URLs) is the flip gate; `verification_status` block (status verified_gs_arc + phase + launch_ready x2 + last_audited + source_set + verification_log_ref + open_findings all blocks_launch:false) + top-level last_reviewed/_session. CERT RE-VERIFICATION = an INDEPENDENT SOURCE FETCH FOR FIDELITY (WebFetch a sample of cited URLs, check the NUMBERS), not just the lift scan.
- **GOTCHA: canonical JSON is COMPACT** (`json.dumps(separators=(",",":"), ensure_ascii=False)`, no trailing newline, never indent=2). **Dataset commits go in `~/plant-dataset` on `main`** (the submodule is detached-HEAD); push is AUTONOMOUS (announce-then-execute). The plant-astro merge/push stays GATED on Trevor.

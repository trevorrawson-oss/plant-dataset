# plant -- CURRENT STATE (live surface)

> **⚠️ SESSION PROTOCOL -- read before acting.**
> 1. **Confirm canonical FIRST:** `shasum -a 256 crops_data_final.json` == `LATEST.txt`; also `git -C ~/plant-dataset log --oneline -1` + `git status -sb` (the working tree can sit AHEAD of HEAD -- an uncommitted hand-promote). Locate the active step on the gold-standard arc checklist (v2.0) before acting -- never infer "next" from "a cell is done".
> 2. **CURRENT_STATE.md = LIVE STATE ONLY.** Never delta-edit; FULLY REGENERATE at close via `tools/gen_current_state.py`, then carry the prose slots forward + amend. The mechanical sections are generated from true state.
> 3. **STATE_HISTORY.md = APPEND-ONLY** -- a dated entry most-recent-first below its header; never rewrite. It is the recovery log that caught the premature-flip near-miss.
> 4. **RELEASE VERIFICATION before any promote** (protocol #6): `whole_crop_gate` + `tools/release_verify.py` + cross-check vs claude.ai's STATE_HISTORY claims. A green gate is NOT a clean release.
> 5. **Four flips are distinct, never conflated:** per-crop `launch_ready` (that crop's Step 11 == 0), region read-layer, authoring-model, schema bump.

---


**14 GS-arc anchors CERTIFIED** (cherry-tomato, beefsteak-tomato, carrot, lettuce-leaf, peach, apple, lemon, orange-navel, basil, zinnia, microgreens-mix, onion, strawberry, lavender) of a ~18 target. **IN PROGRESS: green-beans-bush (anchor 15) -- Steps 1-3 RELEASED 2026-06-22 (`af3b1627`, commit `7c9c65e`).** A **rail-rider**: a warm-season annual on the proven `warm_season_fruiting` rails (NO new archetype/tooling) + a **succession crop** (the A8 `successions_realized` machinery already built). **ANCHOR REORDER (Trevor, 2026-06-22):** green-beans promoted to anchor 15 AHEAD of blueberry -- knock out the quick rail-riders (green-beans, then zucchini/broccoli) before the blueberry `berries_woody` long-pole (a NEW archetype + tooling), which moves to a later slot. **NEXT = green-beans Step 3.5 (region shells, Claude Code lane) -> Steps 4-5.5 (region fill + A8 succession derivation).** green-beans + 3 (zucchini/broccoli/blueberry) to ~18.


## Canonical pointer
- **Current SHA:** `af3b1627ffcf298757dac0d63971ad9255bf0f93e01b824f9d6c390c5bc0278e`. `LATEST.txt` session: `green_beans_steps_1_3` (2026-06-22).
- **Predecessor chain** (most-recent commits touching `crops_data_final.json`; content SHAs):
  - `af3b1627` -- feat(green-beans-bush): Steps 1-3 released -- sources + scalars + varieties + companions (anchor 15)
  - `1499251f` -- feat(lavender): CERTIFIED -- anchor 14, the FIRST perennial_woody_ornamental
  - `b76e0aab` -- feat(lavender): Steps 6-8 -- consumer prose; whole_crop_gate PASS (0)
  - `993d762e` -- feat(lavender): Step 4 SCALE-UP -- 9 regions filled; REGION FILL 10/10
  - `c840bf5a` -- feat(lavender): Step 4 PROOF region northern_tier (z3-7) -- region fill + derived calendars
  - `7c0f149a` -- feat(lavender): Step 3.5 -- woody-ornamental region shells
  - `1753a301` -- feat(lavender): Steps 1-3 released -- sources + scalars + varieties + companions

## What just happened (session `green_beans_steps_1_3`, 2026-06-22)

**GREEN-BEANS-BUSH (anchor 15) Steps 1-3 RELEASED.** claude.ai authored the source set + scalars/biology + varieties/companions on the blank shell; Claude Code released (base `1499251f` -> `af3b1627`, commit `7c9c65e`). NOT a flip (status stays `in_progress`; both launch_ready false).
- **Authored:** 6 T1 extension parents (Clemson HGIC 1302, USU, UMN, UIUC, MSU, UMD), each anchored to its specific page; difficulty easy, days_to_maturity [50,60]/mid 55, full_sun [6,8], spacing_inches [2,4] in-row, germination_temp_f [60,85], soil/ph/fertilizer/watering blocks, `start_method.start = "direct"` (drives the 3.5 direct_sow shape); 5 bush cultivars (Provider / Bush Blue Lake 274 / Contender / Royal Burgundy / Roma II); companions (Three Sisters corn/squash + cucumber good, allium onion/garlic bad); rotation Fabaceae 3-yr legume benefit.
- **SUCCESSION (the rail-rider's defining feature):** `succession_policy` fully authored -- interval_weeks 2, window_type continuous, trigger last_frost, **`pause_in_heat: false`** (the carrot contrast: beans are heat-lovers; the binding limit is the fall-frost runway, not a midsummer no-plant gap), max_successions_per_season 12 (the global cap). Per-zone `successions_realized` NOT authored -- DERIVED at Step 4 (A8).
- **Claude Code release-lane CONFORMANCE (3 shapes vs the certified contract; consumer copy left verbatim):** companion `provenance` `{basis, note}` -> the certified `{label, confidence, reason, verified_against_sources, verified_date}` + item-level siblings (the onion all-`traditional` model: Three Sisters `medium`, allium-caution `low`, `verified_against_sources:false`); `soil.organic_matter_preference` prose -> the categorical token `"moderate"` (compost-prep guidance already lives in the soil/fertilizer prose pairs); dropped the optional bare `container_notes.shape_requirements` (ruled CP -> needs a `_beginner` register = claude.ai's copy lane -> deferred to Steps 6-8 as a real pair; the wide>deep-narrow/shallow-roots nuance flagged for that pass).
- **DEMOTED / standard-practice claims (Trevor-reviewed, non-blocking, handled honestly):** container pot dims (5 gal/8 in -> `container_notes.sources:[]`), `rotation_years` 3 (necessity sourced UIUC/USU; the 3-yr interval is convention), ph floor corrected 5.5 -> 5.8 (the 5.5 traced only to a non-T1 aggregator). Want a sourced confirm at a later display-readiness pass.
- **Gates:** whole_crop_gate `17` (all pre-region A2 + Steps-6-8 A12 empties; A8 succession out-of-scope; source-tier 5 IDs all T1, 0 uncatalogued); release_verify clean (only green-beans changed, lettuce-leaf byte-identical, no new violations); register_completeness PASS; precommit no-regression (17 total).


## Active work + next step

- **NEXT = green-beans Step 3.5 (region shells -- Claude Code structural lane: the standard direct-sow annual path via `apply_region_shells.py`, SHA-gated on LATEST `af3b1627`), then Steps 4-5.5** (claude.ai authors per-cell windows + grown-as + region_notes; then `tools/derive_realized_successions.py` fills per-zone `successions_realized` and whole_crop_gate **A8** enforces it). green-beans rides the proven annual rails -- no new design/tooling. After it: zucchini + broccoli (the other quick rail-riders), then blueberry (`berries_woody`, the deferred new-archetype long-pole). GS arc target ~18.
- **green-beans Steps 6-8 follow-up (flagged this session):** author `container_notes.shape_requirements` as a proper CP `_seasoned`/`_beginner` pair (the wide>deep-narrow/shallow-roots nuance dropped at Steps 1-3); confirm the demoted container dims + rotation interval against a bean-specific T1 at the display-readiness pass.
- **Lavender post-cert follow-ups (non-blocking, carried):** tighten the se_gulf/hawaii anchors from the nola.com / westhawaiitoday republications to primary LSU AgCenter / CTAHR URLs; the community-correction open findings (humid-heat regions + cold-zone z3/4 first-year-bloom). plant-astro: build the woody-ornamental GUIDE PAGE template when ready.
- **Tooling follow-ups (carried):** teach `apply_patch.py` the handback variants seen recently (bare-list ops + `_meta`/`ops`/`from_guard`, and the slice / `crop_ops`+`source_catalog_ops` envelope); sync `register_bearing_field_inventory_v1_0.md` with the lavender gate rulings.


## Gate record (generated 2026-06-22, on canonical `af3b1627`)
- **cherry-tomato: `PASS` (0)**
- **beefsteak-tomato: `PASS` (0)**
- **carrot: `PASS` (0)**
- **basil: `PASS` (0)**
- **peach: `PASS` (0)**
- **apple: `PASS` (0)**
- **lemon: `PASS` (0)**
- **lettuce-leaf: `PASS` (0)**
- **onion: `PASS` (0)**
- **strawberry: `PASS` (0)**
- **orange-navel: `PASS` (0)**
- **microgreens-mix: `PASS` (0)**
- **lavender: `PASS` (0)**
- **zinnia: `PASS` (0)**
- **register_completeness_gate: `PASS`**

## Region fill state (generated)
- **cherry-tomato: 10/10 region cells filled**; 8 heat_pause, 8 second_planting
- **beefsteak-tomato: 10/10 region cells filled**; 8 heat_pause, 6 second_planting
- **carrot: 10/10 region cells filled**; 13 heat_pause
- **basil: 10/10 region cells filled**
- **peach: 10/10 region cells filled**
- **apple: 10/10 region cells filled**
- **lemon: 10/10 region cells filled**
- **lettuce-leaf: 10/10 region cells filled**; 15 heat_pause
- **onion: 10/10 region cells filled**
- **strawberry: 10/10 region cells filled**
- **orange-navel: 10/10 region cells filled**
- **microgreens-mix: 0/0 region cells filled**
- **lavender: 10/10 region cells filled**
- **zinnia: 10/10 region cells filled**

## Flip gates (generated)
- **cherry-tomato:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **beefsteak-tomato:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **carrot:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **basil:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **peach:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **apple:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **lemon:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **lettuce-leaf:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **onion:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **strawberry:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **orange-navel:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **microgreens-mix:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **lavender:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **zinnia:** launch_ready_core=True launch_ready_seasoned=True status=`verified_gs_arc`
- **14 anchors certified** (launch_ready true + status `verified_gs_arc`). (Target denominator is a roadmap call -- see the headline slot -- not derivable here.)

## Live locked decisions / guardrails
- **RAIL-RIDER RELEASE = conform claude.ai's field SHAPES to the certified contract in the Claude Code lane (green-beans-bush, anchor 15, Steps 1-3, 2026-06-22):** an annual on the proven `warm_season_fruiting` rails needs NO new archetype/tooling, but a fresh-authored slice can still DRIFT from the certified field shapes, and `register_completeness_gate` HALTs on the novel/unruled prose patterns (the designed human-in-the-loop checkpoint). Three drifts CONFORMED at release -- the contract is stable across all 14 certified anchors, so carrot/onion are the TEMPLATES (NOT new rulings): (1) companion `provenance` = the rich `{label, confidence, reason, verified_against_sources, verified_date}` + item-level `evidence_label`/`confidence`/`verified_against_sources` (onion = the all-`traditional` template: well-documented-traditional -> confidence `medium`, weak folk-caution -> `low`, `verified_against_sources:false` / `verified_date:null`); NEVER a flat `{basis, note}`. (2) `soil.organic_matter_preference` = a USER-FACING-CATEGORICAL token (`moderate`/`high`), NOT prose -- prep guidance belongs in the soil/fertilizer prose pairs. (3) `container_notes.shape_requirements` is ruled CP, so a populated `_seasoned` REQUIRES a populated `_beginner` (the B dual-voice gate rejects a populated-seasoned/null-beginner half-pair; the null-`_beginner` scaffold is ONLY for an entirely-unused field) -- authoring the beginner register is claude.ai's COPY lane, so DROP the optional field at Steps 1-3 + defer to Steps 6-8 as a real pair rather than freehand it. **`successions_realized` stays OUT-OF-SCOPE at Steps 1-3 (A8 derives it at region fill); the crop-level `succession_policy` is the Step-2 deliverable, with `pause_in_heat:false` for heat-loving beans (the carrot contrast).**
- **WOODY-ORNAMENTAL ARCHETYPE -- CERTIFIED (anchor 14, the FIRST perennial_woody_ornamental) 2026-06-22 -- cert source-fidelity fetch CLEAN; 5 blocks_launch:false open findings (lavender, anchor 14; the FIRST `perennial_woody_ornamental`, the 7th `calendar_basis`; design spec `2026-06-19-lavender-woody-ornamental-model-design.md`, D1-D12; tooling built 2026-06-22, commits `b6e5115..9395854`):** a woody perennial SUBSHRUB grown for BLOOMS (not fruit) whose LIFECYCLE is region-dependent -- per-cell `grown_as` in {perennial, annual} (the COLD-driven inverse of strawberry's heat flip: cold-hardy zones = in-ground perennial shrub, coldest zones / tender species = container/replant annual). **Calendar tokens:** `dormant`/`growing`/`bloom` + the NEW `prune` (the hard cut-back, the month after bloom end; perennial cells only; user label "Cut back"); annual cells use `plant`/`growing`/`bloom`/`season_over`. **NO `harvest`/`renovation` token** -- for an ornamental the BLOOM window IS the cut-for-use window. Frost resolution stays ON (frost-bracketed dormancy; frost-free -> growing year-round, the evergreen analog). **DERIVER** `tools/woody_ornamental_calendar.py` (pure fn of grown_as + windows, like berry/tree); **FILL** `tools/derive_woody_ornamental_calendars.py`; **GATES** (test-first, no-op off basis) `woody_ornamental_violations` (whole_crop_gate **A13** structural) + `woody_ornamental_calendar_violations` (**A14** coherence). **Cold-hardiness handled LIGHTER than citrus: `gating_factors` EMPTY, NO A9 coverage gate** (D7 -- which-species-survives is a variety nuance, not a per-region cultivar gate); no tree machinery (rootstock/chill-gate/pollinizer). **FIRST-RUN TRIGGER GOTCHA:** `build_region_shells._is_woody_ornamental` keys on `archetype == "woody_ornamental"` (or the post-flip basis) -- the shell default `companion_and_ornamental_flower` is SHARED with zinnia + the herbaceous perennials bee-balm/echinacea, so **claude.ai must SET `archetype = "woody_ornamental"` at Step 1-2** (mirrors strawberry's shell becoming `berries_herbaceous`), else 3.5 routes it down the annual path. Template for the woody subshrub-herb family (rosemary/sage/thyme/butterfly-bush/Russian-sage). **RESOLVED-CELL `bloom` = display STRING (the deriver parses it), NOT a `[start,end]` list (that is the RULE-arm format) -- proof-cell learning 2026-06-22.** **WARM/low-frost zones: author bloom/plant_out from REAL regional T1, NOT frost+offset -- the perennial deriver places bloom at the string's months + brackets by resolved_from, and IGNORES plant_out (display-only); z11 frost-free -> resolved_from null -> growing year-round (climate-derived, no fabricated source). Scale-up learning 2026-06-22.** Variety schema `{name, species, use, hardiness_note, delta}`; tender Spanish/French + the lavandins ride as VARIETIES (one URL, the strawberry delta seam).
- **TIPS RENDERING-CONFORMANCE (`tips_by_stage`) is GATED at A12 (`compound_population_gate.py`, 2026-06-21):** the renderer (`GrowingJourneyCard`) does `tipsByStage[stage.id].text_seasoned`, so a tip ONLY renders if (a) its list is non-empty, (b) it uses the `text_seasoned`/`text_beginner` field (NOT `tip_*`), and (c) its KEY is a real `growth_stages` id. `tips_violations` enforces all three; the generic `empty_compound_violations` (also A12) covers the other consumer compounds (growth_stages/notifications/pests/diseases/failure_diagnostics + weather_triggers outdoor-only). **INDOOR crops are EXEMPT from tips_by_stage** -- their tip surface is `indoor_cycle.tip_*` (gated A6); the IndoorGuide passes `tipsByStage={{}}` by design. This whole class (empty / wrong-field / orphaned-key) shipped on 7 certified crops undetected -- the truthy-but-empty + renderer-shape traps that register_fill (pairs) and register_completeness (unruled keys) are structurally blind to. **BACK-FILL COMPLETE 2026-06-22 (`e4ce254c`):** lemon (12) / orange-navel (12) / strawberry (16) authored + applied -- the last 3; ALL 13 anchors now PASS A12. Same family as the "display-readiness is a separate bar from cert" lesson -- now with a gate. **When `growth_stages` ids change, re-key `tips_by_stage` in the same pass** (the orphaning happened because the stage taxonomy was revised after tips were authored).
- **POST-CERT CORRECTION (strawberry `ca_interior`, the 2nd; 2026-06-22):** a certified cell can still carry a sourcing error. ca_interior leaned on the UC IPM "heaviest production is in May and June" sentence as PLANT-cue + harvest-START + harvest-END all at once. A targeted T1 re-source -- `uc_costs_strawberry_sjv` (UC CE San Joaquin Valley cost study; WebFetch-verified verbatim: **August plant, harvest mid-April through mid-June, heat ends the crop in June**) -- gave the real window. **FIX MECHANICS (within the cert bar, NO flip):** patch the region-constant `plantings[]` rule arms AND the `resolved_by_zone[8/9]` materialized strings TOGETHER (A11 coherence), re-derive `calendar[]` (the Jul `season_over` gap + Aug `plant` emerge), recast the USER-FACING `grown_as_note`/`region_notes` (de-hyphenate per gate C, paraphrase any >=8-word source lift -- claude.ai's recast had both), mint the new source into the **top-level `source_catalog`** (gate E reads THAT registry, NOT `sources_summary`), append a post-cert note to `verification_log_ref`. **The small fall crop is PINCHED, not a harvest band** -- a usually-removed crop must not render as a plannable harvest (Trevor, Exeter grower). The 2nd ca_interior fix (1st = fall-framing 2026-06-21); both rooted in that one over-worked UC IPM sentence.
- **BERRIES_HERBACEOUS ARCHETYPE -- CERT-PROVEN on strawberry (anchor 13 CERTIFIED 2026-06-18; the FIRST and ONLY berries_herbaceous crop; design spec `2026-06-18-strawberry-berries-herbaceous-model-design.md`, D1-D9):** a herbaceous perennial whose LIFECYCLE is region-dependent. **`calendar_basis: perennial_herbaceous`** (the 6th value; frost resolution stays ON -- plant/bloom/harvest windows anchor to frost exactly like an annual; the basis is a SHAPE signal, set by `build_region_shells` at 3.5, next to `perennial_chill_gated`/`perennial_evergreen`). **Per-cell `grown_as` in {perennial, annual}** = the lifecycle discriminator (mirrors tree `suitability`): north = perennial matted row, hot-summer CA/FL = annual (fall-plant plugs, replant yearly) -- the answer to "how does z9 central valley work as an annual." **Calendar vocab:** `dormant` + ONE new token `renovation` (perennial June-bearing cells, month after harvest); annual cells use `season_over`, perennial cells use dormant+renovation + NEVER season_over. **THE DERIVER (`tools/berry_calendar.py`) is proven across 5 EMERGENT shapes:** perennial frost-bracketed (dormant+renovation) + perennial frost-free (hawaii: no frost dates -> growing year-round, the evergreen analog) + annual carried (windows wrap -> no season_over) + annual pulled (summer gap -> season_over emerges) + annual wrapping (coastal). The carried-vs-pulled split is EMERGENT (zero discriminator code); A11 gates coherence. **Propagation:** `start_method.start = "bare_root_dormant"`. **Photoperiod is the deliberate INVERSE of onion:** type (june_bearing/day_neutral/everbearing) is a VARIETY attribute + the elevated `type_selection_*` CP section, NOT `gating_factors:["photoperiod"]`, NO per-cell day-length field, NO A9. Self-fertile, NO cross-pollination machinery; chill INFORMATIONAL (`chill_hours_required` null, never a gate). **GATES (test-first, no-op off basis):** `berry_herbaceous_violations` (whole_crop_gate **A10** structural) + `berry_calendar_violations` (**A11** calendar coherence). **VARIETY/DELTA SEAM (Phase 5, spec Section 8):** types stay ONE strawberry URL; cultivars-within-a-type are clean deltas; the TYPE axis is a structural sub-mode at the variety-page layer, NOT separate crops. **BOUNDARY SCALARS = a CONSUMER VISUAL:** `hardiness_zone`/`reliable_fruit_zone` render on the site/app, so the maxes must cover where the crop ACTUALLY fruits (incl. annual culture), not just the perennial range -- strawberry's were extended 10/9 -> 11/11 to match the documented z3-11 fruiting (Trevor, 2026-06-18). Open findings at cert: hawaii lifecycle inferred + OSU EC1307 403-unfetchable, both `blocks_launch:false`.
- **PER-ZONE `successions_realized` = a DERIVED integer, NOT sourced (succession pass, 2026-06-15):** `resolved_by_zone.<z>.successions_realized` = the realized sowing count a zone's season supports, replacing the lossy crop-level cap (season-length-driven -> zone-dependent; carrot/lettuce carried no count at all). Deriver (`tools/derive_realized_successions.py`, gap-aware): year_round -> `min(floor(52/interval_weeks),12)`; authored `succession_spring`/`succession_fall` lists -> count; else day-precise `[first_plant_date,last_plant_date]` (wrap-aware) split ONLY at internal `heat_pause`/`cold_pause` months, summing `floor(span/(iw*7))+1` per sub-window. **Cap 12 GLOBAL** (all 4 crops' crop-max = 12). Scope = `succession_policy.suitable==True` + region-filled + non-indoor. `whole_crop_gate` **A8** re-derives-and-asserts-equality + presence + reconciliation + out-of-scope absence -- a future succession anchor (green-beans/zucchini/broccoli) CANNOT certify without it. Strictly additive; spec v1.1 in `05-methodology/current/`.
- **STEPS 6-8 BLOCK-COHERENT authoring (scope A) = the cert-ready bot template:** author each TOUCHED prose block as a UNIT -- the `_seasoned`/`_beginner` pairs + close its structured nulls + ANCHOR the block (`sources`+`anchoring_urls`) in ONE pass. Anchoring is REQUIRED `launch_ready_core` cert work (Step-11), not optional polish -- close it during 6-8 while the sources are open. Surface beyond-prose changes (retro-anchoring + structured nulls) as a LABELED sub-section for the release review. **N/A fields get N/A PROSE, never null** (`register_fill_gate` rejects null). **Structured nulls the register-pair gate does NOT list but block-coherence INCLUDES (confirmed vs peach + proven on strawberry):** fertilizer scalars (frequency/type/timing/example_product/notify_days_after/stage_id), watering scalars (watering_method/drought_tolerance), `watering.schedule_by_stage[]` ({stage_id,system,rate,frequency,level,note_*}), `yield_expectations.factors_seasoned` (SINGLE-REGISTER, no _beginner twin by design). **Stage-id wiring is the AUTHOR lane** (define `growth_stages[].id`, reference from fertilizer/schedule/notifications); Claude Code verifies references resolve at release. **Region-tip override (v1.4 rider): a pressure GRADIENT is not a divergent ACTION** -- author an override ONLY where >=2 T1 from different regions prescribe DIFFERENT grower actions.
- **WARM-REGION WINDOW STRUCTURE = region-specific, PAUSE-DISCRIMINATED:** single long frost-bracketed season vs spring+fall double window. The double-window SHAPE is chosen by the PAUSE, not the count: a real midsummer NO-PLANT GAP -> TWO arms (beginner main + `track:"second_planting"`, the cherry model); CONTINUOUS through summer with two sowing windows -> ONE `track:"beginner"` arm with two labeled entries (the lettuce model). **`year_round` ONLY at frost-free z11 hawaii, CLIMATE-DERIVED** (stated honestly in `calendar_basis`, NEVER a fabricated source_quote; logged as a `blocks_launch:false` cert open_finding). **DESERT HEAT-STALL: no T1 stall = NO `heat_pause`** -- express peak heat as a planting-window edge. Two-window DISPLAY strings use a COMMA separator. **`resolved_from` is the FROST-DATE audit ONLY (`{last_frost, first_frost}`)** -- no prose keys. **Run `register_completeness_gate` explicitly in verification** (whole_crop_gate B + release_verify do NOT catch a novel unruled prose key; the CURRENT_STATE regen does, as a backstop).
- **FLOWER REGION WINDOWS anchor to FLOWER FACTSHEETS + ornamental calendars, NOT vegetable charts (cluster-13, proven on zinnia se_gulf):** dated last-frost-anchored windows live in dedicated flower factsheets + regional ornamental calendars. **REGION-ROOT carries `sources` but NO `anchoring_urls`** (anchoring lives per-arm in `plantings[]` + per-cell in `resolved_by_zone`). **SUB-ID MINT (Claude Code RELEASE lane, autonomous):** a specific-page sub-ID under a trusted parent inherits the parent tier (T1) + records `_admission_provenance` -- when the patch cites a BARE key (`ctahr_hawaii_county`, `uc_anr_8256`), re-point to the existing family parent (`uhawaii_ctahr_*`, `ucanr_ext_8256`) rather than fragment. **A5 ANTI-CROSS-REGION:** window structure + grown_as is a SOURCE finding PER region, never analogized. **PROOF-CELL-FIRST for a new sourcing archetype:** one cell end-to-end, STOP, re-scope (NA-3b).
- **`start_method.start` ENUM = {direct, both, indoors, bare_root_dormant, grafted_nursery_tree}** (carrot/lettuce=`direct`, basil/zinnia/onion=`both`, tomatoes=`indoors`, peach/apple/strawberry=`bare_root_dormant`, orange=`grafted_nursery_tree`). `build_region_shells` reads it: `direct` -> `direct_sow` shape; `both`/`indoors` -> transplant shape; the perennial/tree paths read archetype/basis. **STEP-3.5 ADMISSION STATE:** after the shell build, regions legitimately carry skeletons + null `region_notes` (Steps 4-7 fill them); `precommit_release_verify.drop_shell_build_unmasks` EXEMPTS the `region_notes both null` violations of stub->shell graduated regions. The ratified 3.5 skeleton carries `anchoring_urls:{}` per planting (cleaned/relocated at Steps 4-5).
- **FLOWER / ORNAMENTAL ARCHETYPE -- proven on zinnia (anchor 10, the FIRST flower):** frost-tender heat-lover on the basil annual rails; grown for BLOOMS not food -> bloom-centric care (deadhead + cut; NO `bolting` field). Flower slots live IN the shell: `flower_type` scalar, `deadheading_*` pair, `harvest_ready_*` repurposed for the cut cue, vase life -> `storage.notes_*`. Disease = powdery mildew, resistance is VARIETY-led. The template for the 5-crop `Companion & Pollinator` family.
- **COMPANION ITEM SHAPE = the certified carrot rich-object shape (NOT a flat `why`).** `good_seasoned[]` = {name, category, timing, why_seasoned, notify_weeks_before, provenance:{...}, sources, anchoring_urls}; `good_beginner_seasoned[]` = {name, why_beginner}. **VOCAB = `research_backed` / `likely` / `traditional`** -- the cherry-era labels are DEPRECATED. `register_completeness_gate` HALTs on a flat `companions.*.why`. For a pollinator crop the frame INVERTS (the crop is the DRAW).
- **SOURCE-TIER: certified crops carry ZERO non-T1.** `johnny_seeds` / seed-company / almanac sources = T2 -- evidence-log corroboration only, NEVER a dataset claim citation; `whole_crop_gate` E flags any non-T1 in scope.
- **HERB ARCHETYPE -- cert-proven on basil (anchor 9):** heat-LOVING (inverse of lettuce); frost-limited both ends; `year_round` for frost-free hawaii; bolting herb-central; chilling-injury storage; foliar-wetness disease nexus; DMR variety notation. The template for the other tender heat-loving annual herbs.
- **CERT VERBATIM GATE = SOURCE FIDELITY, not just lifts:** verify source CLAIMS against the LIVE source at cert; a self-contradicting source cannot sole-anchor a window; re-source, never quick-edit dates. CITATION HYGIENE: re-pin only after fetch+verify; unreachable URLs filed NOT-COVERED (blocks_launch:false), never hidden.
- **ANNUAL CALENDAR DERIVATION + COHERENCE (`tools/annual_calendar.py`):** the DERIVER computes simple summer-centered calendars from resolved windows; it is NOT used to re-derive complex multi-cycle cells (hand-authored). The always-on enforcement is `annual_coherence_violations` (whole_crop_gate A5): consistency, not re-derivation. Derive the simple, hand-author the complex, gate BOTH for coherence.
- **PROCESS ATTESTATIONS -> STATE_HISTORY, not crop data.** **`*_basis` evidence prose is BACKEND (A3):** bare `*_basis` keys EXCLUDED via `_basis_family`. **6-8 worklist is a COMPUTED SWEEP** (`register_fill_gate` + empty compounds; release re-runs to 0).
- **ANCHORING:** every `plantings[]` arm anchors to the SPECIFIC verified page, never the homepage; nested `{id:{url,verified:DATE}}`. A split sub-ID scopes a multi-topic publisher to the specific bulletin.
- **DISPLAY-READINESS FIELDS ARE A SEPARATE BAR FROM CERT (garden-calculator-surfaced):** the calculator gates PLACEABILITY on `spacing_inches` (+ uses `companions`) and CONTAINER offer on `container_notes.container_ok==true` AND `min_pot_gallons != null`; the GS-arc cert validates BIOLOGY/sources and does NOT require them. Back-fill certified crops missing these; ADD `spacing_inches`+`companions`+container fields to the GS-arc definition-of-done. Each is a SOURCED figure, never guessed. **Audit display-readiness PER consuming surface.**
- **EVERGREEN + HEAT model -- COMPLETE + cert-proven (lemon cold-only, orange cold+heat).** heat `marginal` -> suitability `marginal`. Ready to replicate to grapefruit. TREE per-variety schema = lemon's 11-key set incl. `delta`. claude.ai self-checks are advisory -- the gates are the defense.
- **CERT mechanics / THE FLIP:** source-verbatim (vs cited URLs) is the flip gate; sibling-crop echo is a separate voice call. The FLIP = `verification_status` block (status verified_gs_arc + phase + launch_ready x2 + last_audited + source_set + verification_log_ref + open_findings all blocks_launch:false) + top-level last_reviewed/_session.
- **INDOOR / `non_seasonal_indoor` ARCHETYPE -- proven on microgreens-mix (anchor 11, the FIRST indoor crop):** grown INDOORS year-round in a sow -> blackout -> light -> harvest CYCLE; NO frost / season / hardiness zone. **`regions{}` AND `zones{}` COLLAPSED to `{}`** (empty-dict passes A2 by vacuity). Self-describes via `zone_independent:true` + `indoor_cycle{}` + `calendar_basis:non_seasonal_indoor`. **NO Steps 3.5 / 4-5** -- arc is 1-3 -> 6-8 -> 9 -> cert. **RENDER = a relative day/week timeline**, a SEPARATE plant-astro indoor page template. `build_region_shells` NO-OPs (`_is_indoor`). The template for the ~8 microgreen siblings; mushrooms = SEPARATE indoor archetype. **VARIETY `recommended[].note` = USER-FACING-CATEGORICAL** (bare/no-suffix, read identically by both registers; DISTINCT from top-level `varieties.note` CP).
- **TOOLING (basil + microgreens-surfaced, all test-first):** `apply_patch` `add` tolerates empty-equivalent shells + `_is_empty` RECURSES (whole-block add over a 2.9 null scaffold absorbed); `sources_summary` subtree EXCLUDED. `whole_crop_gate` §3 container = `min_pot_gallons` OR `depth_inches_min` (indoor TRAY); A6 = the Step 5.5 indoor-cycle PRESENCE gate. **CP-FIELD PLACEMENT = suffixed SIBLINGS, GATED at A7:** a CP field renders as `parent.X_seasoned`/`parent.X_beginner` DIRECT sibling keys, never `parent.X.{X_seasoned}`; A7 flags any key K whose dict value carries a child `K_seasoned`/`K_beginner`.
- **PHOTOPERIOD / DAY-LENGTH ARCHETYPE -- cert-proven on onion (anchor 12, the FIRST photoperiod crop; design spec `2026-06-16-onion-photoperiod-model-design.md`):** day length gates which CULTIVAR bulbs by latitude (long/intermediate/short_day), NOT the calendar -- onion STAYS `calendar_basis:frost_anchored`. Crop-level `gating_factors:["photoperiod"]` + a dual-register `photoperiod` block; variety-level `day_length_type`; per-cell `recommended_day_length_type` + `day_length_note_*`. **A9 gate (`photoperiod_violations`):** every variety + resolved cell has a valid type + the COVERAGE invariant (every resolved type has >=1 matching variety); the recommended set spans all 3 classes. **BOLTING is VERNALIZATION (cold-then-warm), NOT photoperiod.** The allium family inherits this. **hawaii_tropical = short_day, NOT year_round** even though frost-free z11 (the photoperiod gate OVERRIDES the zinnia frost-free->year_round inference). **PHOTOPERIOD-CELL ANCHORING = frost where it exists, month-resolution where it does not** (frost-free cells: `resolved_from: null` + a `resolution_method`); a minted `bulb_threshold_*` was REJECTED (non-resolvable with no per-zone day-length table).
- **FROST-DATE RECONCILE IS A MANUAL RELEASE CHECK -- NO gate catches it:** per-zone render strings + `calendar[]` are `last_frost + the stable plantings[] offset`; if an author resolves them from REMEMBERED frost dates instead of the live `zone_frost_data`, the cell is internally coherent but WRONG. ALWAYS hand claude.ai `zone_frost_data.json` for a region-fill step + RE-RECONCILE each cell's `resolved_from` at release (strawberry's perennial dormancy brackets + warm_arid all reconciled clean).
- **CERT RE-VERIFICATION = an INDEPENDENT SOURCE FETCH FOR FIDELITY, not just the lift scan (onion cert; re-confirmed clean on strawberry):** claude.ai's verbatim scan checks COPYING (>=8-word lifts) and can read 0 while a QUANTITATIVE claim CONTRADICTS its cited source. At cert, Claude Code MUST independently fetch a SAMPLE of the cited URLs (via WebFetch -- the Bash sandbox has NO network) and check the NUMBERS match. onion's fertilizer passed the lift scan yet contradicted its cited rate (caught only by the fetch); strawberry's fetch (umd/uc_ipm/nmsu/uf) came back CLEAN, no discrepancies. The fetch sample is the defense the self-scan structurally cannot be. **SLICE INTEGRITY = a CROP SHA, not the LATEST full-file SHA:** a kickoff slice is preflighted by `sha256(json.dumps(crop, sort_keys=True, separators=(',',':')))`, carried as a `SLICE_INTEGRITY.md` line (LATEST's full-file SHA cannot verify a single-crop extract).


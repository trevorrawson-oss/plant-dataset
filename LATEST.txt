SHA: decb944d51e591ef9c7b0f657a258a0a7690f2ad1aa8804dad4b83a235db90c0
Date: 2026-08-24
Session: PLA-8 -- **BATCH 3, THE THREE CUCUMBERS, AND THE BATCHING TOOL THAT WAS MEASURING THE WRONG THING** (`c13ddea5` -> `decb944d`; ONE promote; 3 crops, 27 problems, **137 rungs**; no control_method, no source, no crop outside the three; roster laddered **16 -> 19**). **THE TOOL SAID TWIN GROUP AND IT WAS WRONG.** `ladder_batch.py families` reported the three cucumbers as a TWIN GROUP and instructed the batch-2 shape: author ONE crop, propagate the ladders mechanically, assert the copies byte-identical. Its signature was `tuple(sorted(problem_name(p)))` -- **problem NAMES ONLY**, never a character of prose -- while printing *"identical prose means the read is one problem set plus a mechanical equality check on its siblings"*. The 31% byte-identical figure two lines above came from a DIFFERENT function (`prose_key`) and was an unrelated aggregate. **MEASURED AGAINST `c13ddea5`: 0 OF 10 REPORTED TWIN GROUPS WERE REAL** -- collards/kale 28.7%, the 4 microgreens 0.0%, beefsteak/cherry-tomato 34.4%, the 3 cucumbers 72.2%, the 3 beans 75.7%. **THE CORNS ARE WHY NOBODY CAUGHT IT.** Batch 2 genuinely measures 96.2% with all twelve differences on ONE problem (Raccoons: *"sweet corn"* vs *"corn"*, same one-rung `exclusion_fencing` ladder on all four), so that propagation WAS sound. The group had been selected for a reason unrelated to prose and came out right anyway, which made the method read as proven. **A mechanical proxy standing in for reading is reproducible and wrong; reproducibility is not validity.** **WHAT THE PROPAGATION WOULD HAVE COST HERE**, in both directions: `pickling-cucumber`'s prose alone names **wilt-tolerant varieties such as County Fair** (on BOTH cucumber-beetles and bacterial-wilt) and **CMV-resistant** pickling varieties (on aphids), where `cucumber`'s and `slicing-cucumber`'s name **non-bitter** varieties that attract fewer beetles -- a claim about the VECTOR, not a resistance claim. Copying cucumber onto pickling erases County Fair and two earned rungs; copying pickling outward invents CMV resistance on two crops that never claim it. **So this batch was AUTHORED THREE TIMES, and its promote INVERTS batch 2's central guard**: it refuses if the three staged files are identical, refuses if the three promoted crops carry identical ladders, and pins **9 crop-distinct claims + 2 prose claims** to the one crop entitled to each. **THE READ: ZERO METHOD-MEANING MISMATCHES** (batch 1 was 22 of 165; batch 2 was 0 of 88). Five loose fits were flagged by the authoring agents and **ALL FIVE RESOLVED TO ACCEPT ON SHIPPED PRECEDENT**. Consequence clauses clean (no generic clause reused across problems -- batch 1's worst defect); hedges 4/4 preserved; **0 absolutes in 274 new strings**. **ONE READ-FIX**: `cucumber` was missing the `resistant_varieties` rung its own prose earns on Cucumber beetles, where it carries prose BYTE-IDENTICAL to slicing's. Its agent refused, reading the catalog's `best_use` (*"for diseases that recur in your beds"*) as disease-only; shipped precedent settles it, since `resistant_varieties` is `applies_to:["any"]` and already carries varietal NON-PREFERENCE on insects -- `jalapeno/pepper-maggot` (*"goes for thick-walled bell types, and slender hot peppers are seldom bothered"*) is nearly the identical construction, plus corn husk-cover x4, `fig` tight-eye and `apple` rootstock. **The bacterial-wilt divergence is CORRECT and stays**: that is the known structural limit, a vector-borne disease cannot carry a rung aimed at its vector, first hit on Stewart's wilt. **NEW MEASURED FINDING: `best_use` IS NARROWER THAN THE METHOD'S OWN SHIPPED USE ON 11 OF 49 METHODS.** `prepare` hands `best_use` to the authoring agents as *what the method MEANS*, and it was written around each method's motivating crop and never widened: `garden_sanitation` says *black rot* but ships on 45 problems / 16 crops / 7 types; `resistant_varieties` says clubroot + black rot, ships 29/14/5; `crop_rotation` 24/12/4; `floating_row_cover`, `handpick` and `bt` all say *cabbageworms*; `airflow_spacing` says *celery*; `balance_nitrogen` *leafy and cole*; `even_watering` *celery blackheart*; `off_season_tillage` *hornworm*; `bottom_watering` *fungus gnats, trays*. Batch 2 logged `off_season_tillage`'s narrow gloss as a one-off; **it is one of eleven**, and it caused four of this batch's five false flags plus the missing rung. Deferred arc, now scoped and measured. **HARNESSES. Batch-3 promote: 30 injections, 30 CAUGHT, 0 survivors**, preflight 31/31, positive control GREEN, sentinel RED; six families -- **distinct 10** (the staged-distinct check disabled, cucumber propagated onto slicing, pickling propagated outward, a resistance rung added to bacterial wilt, County Fair copied onto cucumber, the owner stripped of County Fair and of CMV, both pinned loops disabled, check_distinctness never called), readfix 5, reach 6, blast 4, mechanics 4, ids 1. **IT TOOK THREE RUNS: 4 survivors, then 1, then 0**, and every one was a MASKING defect invisible to 66 passing tests -- `expect_after` mathematically subsumes the stale-index pre-check; the read-fix COUNT check is unreachable because `apply_read_fixes` already asserts the same thing; the per-crop rung count is masked by the TOTAL rung count; and the write-back guard read its own `before` state INSIDE the test body, so an earlier test calling `staged()` had already triggered the write-back and it compared a mutated file to itself. **Twice more, the isolation tests written to fix those hit the same bug** -- the per-crop test tripped `tiers decrease` first, and the count test reused a method the target ladder already carried so the *already present* refusal fired first. Writing the isolating case is itself subject to the bug. `ladder_batch` guards: 9 tests, **8 injections, 8 caught**, which found two more gaps (the load-bearing-field test built its fixture from the CLASSIC schema only, so dropping the microgreens half of `PROSE_FIELDS` survived -- the identical blind spot `prose_key`'s docstring already records). **`release_verify` COLLATERAL CHECK CORRECTED.** It was `changed == [a.slug]`, exactly one crop, so **every multi-crop promote reported `1 CONCERN(S) -- block + review before promoting`** however clean it was. Reproduced on batch 2's own shipped promote (`0754031d` -> `0e12689b`, the four corns), which four STATE_HISTORY entries nonetheless record as *"release_verify clean"*. Nobody was careless: the concern is benign every time, which is the problem -- a gate that always fires stops being read, and then real collateral rides through under the familiar message. New `--expect-changed` lets the promoter DECLARE the blast radius, verified EXACTLY: an undeclared crop changing is a concern, and **so is declaring one that did not change**, so it cannot be padded to quiet a run. 7 tests, RED before GREEN. Batch 2 re-verifies clean retroactively under a declaration, so the old record was substantively true, just not literally what the tool printed. **GAUNTLET: gate_all 121/121; whole_crop_gate PASS; control_ladder_gate 0; register_completeness PASS; release_verify CLEAN (blast radius declared); COMPACT preserved; 187 tests green across the ladder_batch, release_verify and three promote suites.** **CORRECTION TO MY OWN BRIEFING, recorded because it nearly cost content**: I told all three authoring agents that cucumber and slicing make no angular-leaf-spot resistance claim. **Wrong** -- all three carry *"Buy clean seed and resistant varieties"* in `prevention_*`, byte-identical, which is exactly why it never appeared in the diff I reasoned from. Two agents pushed back and were right. A diff shows what DIFFERS, never what a record asserts. No output was corrupted, because the agents author from the source file rather than from my note.

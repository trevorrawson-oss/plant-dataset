SHA: dd24b180939de37117719b96aec974873dfc8263ab8104af7f6026acdbf856f7
Date: 2026-07-29
Session: POST-ASPARAGUS HARDENING, all four items closed (b0d01f13 -> dc545be6 -> b961d502 -> 7bc4b954 -> dd24b180, four guarded promotes, 2 crops, count 128 / 121 certified unchanged). Item 3 RULED: verification_log_ref is APPEND-ONLY + cert-dated with a required [CORRECTION <date>: ...] append; the kickoff's audit-trail reason is recorded as the WEAK one (open_findings already carry it) and the real reason is that a living summary is unenforceable at 116 crops -- 13 of 115 already stale, 7 purely from the roster growing 10 -> 16 regions. Two-class rule: context growth needs nothing, retired reasoning needs the line. Count gate BUILT, MEASURED AND LEFT UNWIRED (14 rows, exactly 2 real). Applied to asparagus (18/8/13 -> 25/4/10 + the retired chill mechanism) and artichoke (25 marginal -> 22 annual_only). Item 4 SHIPPED after verifying BOTH consumers already hide unsuitable cells: A32 and the A46 floor now exempt unsuitable (TDD RED 10+10), the note requirement STAYS, and 11 fabricated all-growing calendars are emptied (10 asparagus + artichoke ca_desert z11). Roster-wide suitability RULED NOT WANTED -- cell absence already answers it for the other 107. Item 2: 2a RE-SOURCED to TAMU EHT-066 TEXT (window unchanged at Feb 1 - Feb 28, method -> nmsu_tamu_arid_month_resolution, verified by urllib+pypdf); 2b ACCEPTED (Midwest Veg Guide states ONE window April 15 - May 15 across z3b-z7a with zero zone refs; frost re-derivation tested and rejected) with the Fusarium-in-cold-wet-soil rationale RETRACTED AS FABRICATED -- it is not in UMaine #2071; 2c was ALREADY FIXED and its stale open finding is what commissioned a 16-document pass against a value that no longer existed. UNPLANNED AND BIGGEST: CURRENT_STATE.md had LOST its title + binding SESSION PROTOCOL header (dropped by 93d5a59's own regen, missing 40+ commits), which armed a fail-open in gen_current_state.static_header() that returned THE WHOLE FILE as the header -- a regen would have duplicated 351KB. Header recovered from 93d5a59^; the fail-open is now a hard abort. Test suite 78/78 from 7 failing, every fix a diagnosis: 3 hardcoded counts now DERIVED, a REAL tool bug in verify_demux_footprint.py (hardcoded 124 -> false problem on every run), 2 stale premises from real progress (region_harness now has an explicit None remove-sentinel restoring A31 coverage), 2 discharged one-shots now skipping loudly, and A51's own test which had been failing AT IMPORT since annual_only shipped -- so that extension shipped with broken coverage while its log claimed all suites passed. GAUNTLET: gate_all 121/121, release_verify clean but the expected 2-crop footprint concern, all standalone gates 0, COMPACT preserved. OWED: the citation cleanup arc (+9 verified instances; tier A alone is insufficient, portal roots all return 200) and a DECISION on CURRENT_STATE.md, which has re-accumulated 79 entries (353KB vs a 28KB regen), 70 with prose in neither history file -- so this trio was done SURGICALLY by prepend, a flagged departure from protocol #2.

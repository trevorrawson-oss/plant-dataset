SHA: 76f92a20faae0b8e5336ef8e7e1d9c852b9c734c93ae84fc6cccd65f49bcf3ce
Date: 2026-08-10
Session: PLA-202 -- **PLA-202 -- THE VERBATIM REWRITE PASS: 25 HARD hits repaired across 22 fields, 15 crops** (`c16071bc` -> `76f92a20`; ONE promote, REPLACEMENT-ONLY -- 22 enumerated prose fields move, guards prove every other byte identical including the catalog; no sources, findings, cert logs or calendars touched). Precondition landed the same day: the PLA-202 adjudication read ALL 333 verbatim HARD hits (308 benign with per-hit recorded rulings; 25 rewrite -- R1 attributed near-quote x18, R2 unattributed lift x7). Replacement prose authored in the claude.ai lane and applied verbatim from tools/staging/pla202_rewrites.json; attribution retained, content restated; the two paraphrase catches (soil->ground, burrows->bores) rebuilt with new sentence structure. Two flagged authored deviations applied: raspberry cultivars->varieties (common-tongue convention) and strawberry's UC-boilerplate-shaped window sentence folded away -- removed on suspicion, and the source-truth sample then FOUND UC IPM publishing "Middle to late August generally is the best time to plant strawberries in all locations," corroborating the suspicion; recorded as a sub-threshold-class removal, NOT a 26th hit. VERBATIM RECHECK on all 15 crops: the 25 cleared runs GONE, remaining hits EXACTLY the adjudicated-benign sets, ZERO new hits introduced by the new prose. POST-PROMOTE ROSTER RECOMPUTED, not derived: 55 criterion-satisfied / 49 all-benign-coverage-incomplete / 0 rewrites owed / 17 zero-hit coverage-blocked (sum 121); cherry-sour cleared both its hits and thereby MOVED TO BLOCKED because its uncovered documents now control the exit code -- the recompute caught what copying forward would have missed. The per-hit ledger docs/pla202_verbatim_adjudication_c16071bc.json is HISTORICAL as of this promote (its 25 rewrite rows describe prose that no longer exists); current measurement = docs/pla202_verbatim_roster_76f92a20.json. Stated plainly: this close does NOT mean the dataset is verbatim clean -- 96 cited documents were never compared and 2,362 borderline hits were never adjudicated; successors named (coverage endgame for the 96, sources_summary scan-scope ruling, close-paraphrase reading-only sampling). Guards: TDD RED->GREEN, replay-pinned via promote_fixture (base c16071bc -> post 76f92a20), 15 checks green under pytest AND the direct runner, 6 sabotages all caught; CLEARED_RUNS pinned from the adjudication LEDGER, not from the staged table the script reads. Gauntlet: whole_crop_gate PASS x15 (every touched crop), gate_all 121/121, release_verify's only new concern is its known single-crop collateral shape reporting EXACTLY the 15-crop footprint (the 6 tree/berry key-diff concern sets proven PRE-EXISTING by running the tool base-vs-base), source-truth sample 6/6 attributed claims found in cached sources, COMPACT preserved. Evidence: docs/2026-08-10-pla202-verbatim-adjudication.md (adjudication + post-promote addendum), docs/2026-08-10-pla202-extension-reuse-terms.md, docs/2026-08-10-pla202-rewrite-workorder.md, both roster JSONs + the ledger.

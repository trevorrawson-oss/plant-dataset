SHA: ce98b0a6f83cc04b380a6c3be3009709a7c6c3626b2611c88fafec1164997144
Date: 2026-09-04
Session: ROSEMARY CERT-LOG CORRECTION -- batch 25's last owed data item (`132980d5` -> `ce98b0a6`; ONE promote, ONE appended string, ONE crop). The 2026-07-06 cert log recorded rosemary's root and crown rot as "correctly Phytophthora-only (UC IPM, Pythium already dropped)". **The PNW Plant Disease Handbook's rosemary root-rot page, which 403s to every direct fetch path this project has, was finally retrieved during batch 25** and names *Pythium*, *Berkeleyomyces* sp. (formerly *Thielaviopsis basicola*) and *Rhizoctonia* and **no Phytophthora**, so Pythium was dropped on a reading the document does not carry. **APPENDED, NEVER EDITED**: `verification_log_ref` is an append-only cert-dated historical record, so the original 900 characters survive byte-for-byte as an exact PREFIX (the promote refuses otherwise) and the correction is appended in the convention's `[CORRECTION <date>: ...]` form, 900 -> 1787 chars. **THE CAVEAT IS PART OF THE CORRECTION**: that page was reached through a text proxy plus a corroborating search snippet, which is two consistent retrievals and NOT a first-party read. Enough to retract an over-claim, a move toward saying LESS; NOT enough to assert those three genera, and nothing in the dataset does. Rosemary's rot stays pinned to the problem-class umbrella `crown-and-root-rot` and its consumer prose names no genus, which this correction confirms was the right call rather than merely the cautious one. Suite 16/16; **mutation harness 9 injected / 9 caught / 0 survived / 0 broken** after one survivor exposed an unreachable serializer (the write format was inlined in `main()`, which no test called, so a switch to `indent=2` survived the whole suite; `serialize()` extracted and driven). Gauntlet: `whole_crop_gate rosemary` PASS, `gate_all` **121/121 PASS**, `doc_roster_claim_gate` 0.

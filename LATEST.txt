SHA: 80519a28548586aedd9754a664f1618b722fb55976d8eb6c3314891ddc5c328f
Date: 2026-09-03
Session: CHIVES APHIDS RETIRED (`b89763b7` -> `80519a28`; ONE promote; one problem removed, roster 913 -> 912; catalogs steady at 64/219). **THE ENTRY FAILS BOTH TESTS THE REST OF THE CHIVES PEST SET RESTS ON.** Chives' pest entries are, by the record's own admission, the ALLIUM FAMILY SET modeled onto chives -- open finding `chives_pilot_pests_diseases_allium_modeled` says so and flagged the set "for the source-truth sample". That sample has now run. Every other entry survives it by inheriting from a class-level authority; aphids inherits from nothing. **CROP LEVEL**: no US extension publication treats aphids as a chives pest, and three that ENUMERATE chives' problems omit it (USU's Chives in the Garden, NC State's toolbox "No serious problems", PlantVillage); the page this entry cites asserts the opposite -- Wisconsin Horticulture, "Chives have no significant insect or disease problems" -- and both cited documents contain "aphid" ZERO times. **CLASS LEVEL**: UC IPM's onion and garlic guidelines have **no aphid page at all**. Rust, white rot and downy mildew all pass that test; UC IPM's rust page even names chives. **NOT a claim that aphids never occur on chives** -- onion aphid (Neotoxoptera formosana) is a real Allium specialist -- and the reason is written into the record so a later pass does not re-add it from a forum thread. **NOTHING USEFUL IS LOST AND A GUARD PROVES IT**: the entry's one valuable claim, that chives repel aphids from neighbours, survives in SIX places under `companions`, hedged better there ("passed along by MSU citing Penn State, but not a measured trial"); `check_companion_claim_survives` refuses below five. **THE FINDING IS AMENDED, NOT INVALIDATED**: it enumerates the set including aphids, so a dated CORRECTION is appended and the original wording must survive byte-for-byte as an exact prefix. The promote also refuses outright if the target carries an id, a shipped ladder, or if the crop carries any `resistance`/`ladder_delta` -- a problem id is a JOIN KEY and removing a referenced entry orphans whatever points at it. Suite 42; **harness 27 injected / ZERO survivors** across 8 families. **FOUR BLAST SURVIVORS ON RUN 1**: one was a real undriven branch (only a post-apply second-finding edit reaches the changed-count check); the other THREE were verified redundant -- "the entry was not removed" is caught by four independent checks, so no injection can isolate any one -- and are WITHDRAWN with the reason written into the harness, the checks kept in the promote. Gauntlet: `gate_all` **121/121**, chives PASS, `control_ladder_gate` 0, `register_completeness` PASS, `release_verify` byte-identical. **THE ARC STOPS HERE AND HANDS OFF**: see `docs/2026-09-03-pla8-batch24-handoff.md`. The record pass is 3 of 10 decisions done, and **71% of batch 24 (20 of 27 problems, 58 of 82 rungs) needs re-authoring** because its inputs changed.

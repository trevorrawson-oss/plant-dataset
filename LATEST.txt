SHA: c6f50a1417a82786356fef764e524641143d41f973dc8f7097eb18454cb3fe5a
Date: 2026-07-31
Session: CLEANUP BATCH: the two schema-shape defects the audit's framing had WRONG (172e4e7a -> e353fadb -> c6f50a14). Trevor-ruled as 'the unblocked and cheap ones'. Two guarded promotes, both footprint-exact. >> PROMOTE 1 (e353fadb) ARTICHOKE'S open_findings SAT WHERE NOTHING READS THEM: 12 findings relocated under verification_status, BYTE-FOR-BYTE. Artichoke was the ONLY crop of 128 storing them at the top level (120 use the nested key; 7 uncertified shells have neither), and every gate and scan reads the nested key -- whole_crop_gate line 1062 is `vs.get("open_findings") or []` -- so artichoke was silently skipped and EVERY ROSTER FINDING COUNT WAS SHORT BY 12. The external blind audit's 768 is really 780, and its '62 crops with unfinished findings' excluded artichoke outright. ORIGIN FOUND IN SOURCE NOT GUESSED: tools/promote_artichoke.py line 340 writes crop["open_findings"] = ... -- the cert promote used the wrong path; a bug, not an archetype choice, since asparagus (the other herbaceous perennial) uses the nested key. EXPLICITLY NOT AN EMERGENCY AND THE GUARD PRINTS IT: all 12 are status=accepted / blocks_launch=false, so nothing that would block certification was hidden -- the cost was MEASUREMENT INTEGRITY, not a shipped defect. SCOPE HELD TO RELOCATION: the entries also use title + note_internal where the roster uses summary, but summary is read by NO gate and NO general scan (only one-off promote scripts pinned to a named finding, one of which already falls back to another key), so normalizing it is a separate ruling. Guards 15/15 both runners incl. a sabotage proving any text edit during the move aborts. >> PROMOTE 2 (c6f50a14) ASPARAGUS'S FIVE MISSING TOP-LEVEL FIELDS BACKFILLED AS NULL, AND THE RECORD WAS WRONG. Kickoff 48 section 4 calls asparagus's 7 missing fields 'a perennial-schema question, not an omission to backfill'. MEASURED: 56 top-level keys appear on >=90% of the 128 crops, asparagus lacks exactly 7 and is the SOLE crop missing each, and ARTICHOKE -- THE ONLY OTHER herbaceous_perennial -- LACKS NONE OF THE 56. The archetype is not the explanation. Each null rests on a convention re-measured against the file rather than quoted: 36 of the 37 crops with days_to_maturity == [] carry days_to_maturity_mid null and ZERO carry a value; propagule == 'crown' means no indoor start exists for weeks_indoors and the only other crown crop carries null; first_planting_notify_days (52 null), last_reviewed and last_reviewed_session (22 null each) are all null on artichoke. NULL IS THE HONEST VALUE, NOT A FILLED SHAPE: writing a date into last_reviewed would assert a top-level review that never happened -- the lettuce-leaf `verified` defect one field over. Guards 12/12 both runners incl. premise checks that ABORT if DTM is ever populated or the propagule stops being crown (the null is only correct while those hold), plus a sabotage proving a non-null backfill is refused. >> TWO OF THE SEVEN DELIBERATELY LEFT OPEN, each its own ruling: yield_expectations is present AND NON-NULL on all 127 other crops (zero nulls roster-wide), so a null would assert 'no yield data exists' when the truth is 'nobody authored it' -- it needs real dual-register authoring with T1 sources; and zones is the one genuine schema question, where plant-astro's today.ts / [crop]/[zone].astro / TreeGuide.astro read zones{} and FAIL OPEN, so the consumer behaviour must be checked before choosing placeholder-vs-absent. >> WHY NOTHING CAUGHT EITHER: whole_crop_gate PASSES on both asparagus and artichoke and references none of the seven fields. optional-field-gates-go-vacuous, third occurrence. >> MUTATION-TESTING AGAIN CAUGHT A VACUOUS GUARD OF MY OWN: test_aborts_if_the_nested_key_already_exists passed with the refusal DELETED, because injecting the nested key also pushes the nested-crop count 120 -> 121 and the count preflight aborts first; rewritten to drop another crop's nested key so the count stays 120 and the refusal is the only check standing. Same lesson as the de-zone pass hours earlier. GAUNTLET: guards 27/27 across the two suites on BOTH runners; gate_all 121/121 after each; whole_crop_gate PASS on artichoke and asparagus; all 5 standalone gates exit 0 AND byte-identical to the pre-state capture after each; release_verify differs by EXACTLY ONE LINE each time (the expected crops-changed CONCERN naming only artichoke, then only asparagus); COMPACT preserved; suite 311 -> 338 passed. NOT COMMITTED, NOT PUSHED. >> ALSO MEASURED, NOT FIXED: pet_safe is recorded COMPLETE in memory but tools/pet_safe_coverage.py reports logged=114 of 121 certified with 7 VIOLATIONs (artichoke, asparagus, dry-bean, field-corn, flint-corn, popcorn, sweet-corn). The rollout log docs/superpowers/plans/2026-07-06-pet-safe-rollout-log.json predates all 7 certifications, so it is a STALE COMPLETENESS RECORD, not a regression; closing it needs 7 real ASPCA lookups. >> STILL BLOCKED: the elderberry chill reclassification (Trevor ruled 'fix the classification, not the number') waits on plant-astro, whose BerryChillCard hardcodes 'Will your winter chill these blueberries?' and renders for EVERY berries_woody crop -- so raspberry, blackberry and elderberry guide pages already ask readers about blueberries. Trevor confirms the APP has the same defect. Ship the frontend first.

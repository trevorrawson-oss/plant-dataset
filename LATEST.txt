SHA: 2d496da51b37c68a60402b82cc30a5252d07e45474b6b98edaf299afbc5c69c4
Date: 2026-08-21
Session: PLA-290 -- **THE PROSE-STRING VARIETY ENTRIES BECOME STRUCTURED RECORDS** (`be8a6d1e` -> `2d496da5`; ONE promote; 59 leaves DROPPED, 177 ADDED, 1 CHANGED of 387,240; 11 crops, `varieties.recommended` only -- no calendar, no citation, no catalog, no top-level key). THE SYMPTOM was a Garden On Deck card whose TITLE read "Golden Self-Blanching: a compact, pale-stalked self-blanching type that needs little or no manual blanching", with CELERY as its subtitle. NOT AN APP PARSER BUG: ten crops carried `varieties.recommended` as "Name: blurb" prose, and plant-app's legacy string branch only splits a TRAILING parenthetical ("Sungold (orange, exceptionally sweet)"), so a colon-format entry has no split rule at all and the whole sentence becomes the name -- in variety chips, on-deck rows, seedling detail and community cards alike. 59 entries across beet (7), carrot (5), celery (5), chamomile (6), lemongrass (3), parsnip (7), potato (5), radish (5), sweet-potato (8) and turnip (8) are now `{id, name, note}`. **THE ID RULE IS A COMPATIBILITY CONSTRAINT, NOT A PREFERENCE:** plant-app shipped PLA-291 Part A first, and its `varietyFor()` bridges an already-planted record by DASHED-PREFIX match on the stored sentence-slug, so `golden-self-blanching` must prefix `golden-self-blanching-a-compact-pale-stalked-...` or a real garden record silently unplants. Checked entry by entry: **59 of 59 hold, and 0 cross-claim another entry's stored slug.** THAT CONSTRAINT IS ALSO WHY THE TYPE-ENTRIES ARE NOT SPLIT -- "Nantes types (Scarlet Nantes, Nelson)" as two records would leave neither one a prefix of the stored id. One record per entry. AND `id` == `slugify(name)` EXACTLY, because plant-app resolves a variety slug TWO ways that must agree or art/index lookups miss: `varieties.ts` prefers `v.id`, while `scripts/build-guides-data.mjs` slugifies `v.name` and never reads `v.id`, despite a comment claiming it mirrors the other. All 53 pre-existing id-bearing records already satisfy the invariant, so the two rules agree today **by convention rather than by construction**; preserved here and pinned by a guard, and the divergence itself raised as an app-side finding on PLA-290, NOT fixed from this repo. NO `days_to_maturity` INVENTED: ten notes carry a DTM as prose ("about 90 to 100 days") and a range cannot narrow to the field's integer without inventing precision. FIVE ENTRIES NEEDED A JUDGMENT rather than a mechanical split: where a parenthetical lists example cultivars OF a type it moves to the note (beet Monogerm, carrot Nantes and Round-and-finger, radish Daikon), and where it carries a binomial or an alternate name it stays in the title (the established "Spearmint (Mentha spicata)" shape); lemongrass's East Indian entry keeps its binomial and sheds "also called Cochin or Malabar grass" into the note. A token-level guard proves **every word of all 59 originals survives into name+note**, the single enumerated exception being beet's "such as". nasturtium is the ELEVENTH crop and is deliberately NOT converted: it only sheds the copy-paste "Tagetes... " artifact inherited from marigold, and STAYS A STRING so its six variety slugs do not move. MUTATION HARNESS (PLA-215): `tools/mutate_pla290_variety_suite.py`, liveness-defended -- **20 injections, 18 mutations ALL CAUGHT by their target guard, 0 survivors**, sentinel red across 12 guards, positive control green. Every mutation is injected INSIDE `apply_to()` on purpose: the promote re-checks the shim constraint and the id rule before it writes, so a corrupted TABLE is refused at the door and never reaches the guards -- real protection, but not evidence the guards work. GAUNTLET: TDD RED (12 failed / 9 passed) -> GREEN 21/21; `whole_crop_gate` PASS on all 11 touched crops; `gate_all` 121/121; `release_verify` 1 concern PROVEN PRE-EXISTING base-vs-base (its single-crop-pilot "expected only cherry-tomato" fires identically with ZERO changes); COMPACT preserved. OWED, app side: plant-app regenerates via `build:guides`, adds the PLA-291 Part B tripwire, and OTAs.

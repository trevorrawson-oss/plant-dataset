SHA: e6c986e38e15a0219d64c805cfc11a8786e974320f915e1dd35e6031422f0419
Date: 2026-09-02
Session: PLA-8 -- **BATCH 23, THE ROOTS: parsnip, potato, sweet-potato** (`b118f19d` -> `e6c986e3`; ONE promote; 22 problems / **87 rungs**; roster laddered **94 -> 97 of 121**; catalog steady at 64, `source_catalog` byte-identical, no other crop touched). Ids and types were PINNED BEFORE FAN-OUT and came back with **ZERO drift**, 22/22. The stemmed id scan found **two KINGDOM-LEVEL taxon collisions** read from the crops' own prose: live `black-rot` is the BACTERIUM *Xanthomonas campestris* pv. *campestris* on 10 brassicas while sweet potato's is the FUNGUS *Ceratocystis fimbriata*; live `blackleg` is the FUNGUS *Phoma lingam* on cabbage/kohlrabi while potato's is BACTERIA in *Pectobacterium*. Both minted scoped. Three scope variants minted where a shorter roster id would have named a NARROWER problem (`aphids-virus-vectors`, `whiteflies-virus-vectors`, `wireworms-root-feeding-larvae`). One taxon check PASSED explicitly and is pinned: beet's own prose says *Streptomyces scabies* is "the same organism that causes potato scab", so `common-scab` is REUSED. **THREE DEFECTS WERE FOUND IN MY OWN WORK, EACH BY SOMETHING OTHER THAN ME.** (1) My pre-authoring stemmer turned `beetles` into `beetl` while `beetle` stayed `beetle`, so it reported ZERO stem-equal variants when there were two; the `flea-beetle`/`flea-beetles` pair was known only from the handoff. Caught by a suite driver. (2) `pinned_ids.json` asserted *Itersonilia pastinacae* where the prose says *perplexans* -- a species name written from memory. Caught by an authoring agent. (3) **THE GUARD THIS BATCH WAS BUILT AROUND WAS MEASURING THE WRONG THING.** `check_no_precedent_copy` used `difflib` defaults and averaged the two registers; `autojunk` engages at 200 characters and junks any character in over 1% of the sequence, which describes every seasoned register, and the mean then diluted one copied register against one independent one. It scored the batch's ONLY real copy -- `potato`/`common-scab`/`even_watering` against **beet's rung for the same problem and method, authored earlier in the SAME session** -- at **0.431** and passed it. Under `autojunk=False` with a per-register max it is **0.757**, sharing a 56-character verbatim run. **That guard was reachable (243 comparisons), non-vacuous, and mutation-tested 3/3: every property the PLA-215 bar checks was satisfied and it still did not see the thing it was built to see. A harness proves a guard FIRES; it cannot prove the guard MEASURES THE RIGHT THING.** Recalibrated: independent ceiling **0.684** over 62 singleton pairs, threshold held at 0.70, batch now tops out at 0.660, regression pinned with the real pair. **THE INDEPENDENT SOURCE-TRUTH PASS RAN WHEN EVERY GATE, THE 82-TEST SUITE AND A 64/64 MUTATION HARNESS WERE GREEN, AND FOUND ELEVEN DEFECTS ANYWAY.** Besides the guard: a rung telling readers to store sweet potatoes "warm and **dry**" when the crop's own record cures them at 85-90% humidity; the copper hazard band softened from "highly to very highly toxic to fish and aquatic life" to "toxic to fish" on two BEGINNER rungs; `insecticidal_soap` shipping UC IPM's acute rating on potato but not on sweet-potato; four dropped hedges ("up to about 30 percent" -> "roughly 30 percent"); two catalog cautions narrowed. 12 content edits followed and everything was RE-RUN. **One of my own filed findings was RETRACTED AS FALSE** -- it claimed sweet-potato carries no curing figures anywhere, when it carries them at six sites, and it asserted the absence of the very data that proved the "warm and dry" rung wrong. **One reported defect was REFUTED and NOT applied**: the `beneficial_predators` hedge, where the review compared a beginner rung against the SEASONED source; the sources are register-split and both rungs match their own register. Batch 22's `check_template_sibling_divergence` was DROPPED as vacuous (0 template twins vs batch 22's 9) and the zero is ASSERTED by a guard rather than assumed. Batch 22's blanket temperature BAN was converted into a WARRANT check, because roots carries 5 sourced figures where batch 22 carried none. Two guards were LIFTED OUT of `main()` so the suite could reach them, and two forward assertions were verified unreachable by construction, documented at their site and WITHDRAWN from the harness rather than padded into the total. Re-run: **83/83 suite, harness 64 injected / ZERO survivors** across 14 families, preflight 65 anchors, positive control GREEN, sentinel RED. Gauntlet: `gate_all` **121/121**, `control_ladder_gate` 0, both variety gates 0, `url_health` 0, `register_completeness` PASS, `whole_crop_gate` PASS on all three, `release_verify` clean but for its known single-crop-shaped CONCERN naming exactly the 3 declared crops, catalog +none -none, reference lettuce-leaf byte-identical. NEXT: **catalog r10**, where `certified_clean_stock` widening to reach insect types would unblock **14 rungs** -- roots' 3 plus **11 already-laddered problems** whose own prose names certified stock (all four citrus on Asian citrus psyllid, strawberry and both brambles on aphids and crown borers). **97 / 121 laddered; 24 crops / 116 problems / 6 batches remain.**

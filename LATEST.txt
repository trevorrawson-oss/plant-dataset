SHA: d3a6912fc235408db0f9294f4b47be47b25f14a8f5f8a60cee85ddabf5c70f4c
Date: 2026-07-07
Session: REGISTER #7 CLIMATE THRESHOLDS -- PHASE 1 (contract + TDD gate + 6-crop pilot) (CONTENT release; b15a5a0f -> d3a6912f). The head-start for the notifications/WeatherKit build (~week of 2026-07-14): promote per-crop heat cutoff / frost tolerance from PROSE into STRUCTURED numeric fields so a cheap DETERMINISTIC weather trigger fires without an LLM (memory notifications-ai-architecture). Column GS arc, Phase 1 only -- ROLLOUT (~108 crops) HELD for Trevor's sign-off on the semantic decisions. FIELDS (crop-level, siblings of existing germination_temp_f): heat_threshold_f (int F, or null=reviewed-N/A heat-lover; key-absent=TODO) + heat_effect (enum: bolting/poor_fruit_set/crown_failure/quality_loss/heat_tolerant) + frost_tolerance_f (int F) + frost_effect (enum: killed/foliage_damaged). Semantics: single VALUE each (a threshold crossing point, NOT a band like germ); heat_threshold_f is a daytime-HIGH trigger (night-temp effects OUT of scope v1); coherence invariant frost<heat when both numeric; amend-not-recert. CONTRACT: docs/climate_thresholds_contract.md. GATE: tools/climate_threshold_gate.py (TDD RED->GREEN: 7 defect classes -- frost>=heat, bad enum, orphan effect, non-int, null-but-not-tolerant, missing effect, out-of-range -- all bounce on a scratch copy; clean pilot passes; --coverage prints SET/N-A/TODO). C11 RULING (stop-and-ask, surfaced for confirmation): heat_effect + frost_effect ruled into register_completeness EXCLUDED_KEYS as backend enums (siblings of start/propagule/dtm_anchor); the numeric thresholds need no ruling (ints, out of the C11 string check). register_completeness still PASS. PILOT (6 diverse, each number sourced from the crop's OWN prose): broccoli heat 86 crown_failure / frost 28 foliage_damaged; cherry-tomato heat 92 poor_fruit_set / frost 32 killed; kale heat 82 quality_loss / frost 20 foliage_damaged; okra heat NULL heat_tolerant / frost 32 killed; basil heat NULL heat_tolerant / frost 32 killed; lemon heat NULL heat_tolerant / frost 28 foliage_damaged. Exercises heat SET x3 (3 distinct effects) + heat N/A x3 (the honesty case) + frost killed(tender) vs foliage_damaged(hardy/perennial) + a []-germ perennial. DECISIONS FLAGGED FOR TREVOR (contract sec 3): (1) keep the 2 *_effect enums vs pure-numeric -- RECOMMEND keep (they make the deterministic alert message accurate, the register's broccoli-crown vs tomato-blossom-drop example); (2) add chilling_sensitivity_f as #7b for non-freezing chilling injury (basil 50 / okra 45 / tomato 50, all ABOVE freezing so frost_tolerance_f misses them) -- RECOMMEND yes as a fast follow-on, numbers already in-prose; (3) confirm the C11 enum ruling. Also noted: dataset already carries partial notification scaffolding (hard_freeze/FROST_PROTECT/"Hard freeze ahead" tip tokens on broccoli) -- these fields give those triggers their firing numbers. SHA-guarded splice: EXACTLY the 6 pilot crops changed, all else byte-identical, count 124, canonical COMPACT no trailing newline, +627 bytes; heat_threshold_f:null serialized as JSON null. Gates: climate_threshold_gate PASS (coverage heat SET 3/N-A 3/TODO 118, frost SET 6/TODO 118), whole_crop_gate PASS x6, register_completeness PASS, release_verify no new violations + no novel keys. NEXT: Trevor confirms the 3 decisions -> then the archetype-batched rollout across the remaining ~108 certified crops (heat-lovers take heat_tolerant/null; cool-season + pollen-limited fruiters take a threshold), then fold into the per-crop checklist. (Register #6 seedling light: Trevor will run in a fresh session -- research showed it is standard-with-exceptions; accurate model = germination light/dark per-crop + seedling-light default + exceptions.) Trevor confirms every push: this is the 13th unpushed commit (a03266a watering W1 .. d3a6912f #7 pilot).

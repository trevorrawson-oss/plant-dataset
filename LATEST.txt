SHA: 3ec673a76717c0a9fbfe9861d6d63ee36e574d59a88b3e3b3b97cccb29253027
Date: 2026-08-25
Session: PLA-8 -- **`best_use` WIDENED WHERE IT WAS NARROWER THAN ITS OWN SHIPPED USE** (`decb944d` -> `3ec673a7`; ONE promote; **10 strings**, all `control_methods.<key>.best_use`; no crop, no other field, no source, no ladder, no roster change). **THIS FIELD IS READ ON TWO SURFACES AT ONCE.** `ladder_batch.py prepare` hands it to every authoring agent as *what the method MEANS*, and `MethodSheet.tsx` renders it to users under *When to reach for it*. It was written around each method's motivating crop and never widened as the method spread, so it understated the method on both surfaces together. **MEASURED DURING BATCH 3: 11 of 49 shipped methods.** In that one batch it produced four false gaps and one genuinely missing rung, where cucumber's agent read `resistant_varieties`' *"for diseases that recur in your beds"* as disease-only and declined a varietal non-preference trait that eight shipped rungs across six crops already carry. With ~34 batches left it was a recurring tax on every authoring pass. **READ, NOT COUNTED: 11 FLAGGED, 10 REAL.** `bottom_watering` was spared deliberately. Its `best_use` already names BOTH its shipped problems and confines the method to indoor trays and seedlings, and that confinement is the field's whole job: `bottom_watering` MEANS water from below in trays, and **twelve authored rungs in batch 1 used it to mean water at the base outdoors**. Widening it would have re-opened the worst defect the rollout has produced. A guard family asserts it survives byte-for-byte, that it still says *trays* and *seedlings*, and that `water_at_the_base` still disambiguates itself from it. Same result as the catalog safety sweep, where 13 of 15 hits were correct as written. **ONE OF THE TEN IS A FACTUAL CORRECTION, NOT A WIDENING.** `off_season_tillage` was minted in batch 2's catalog round and glossed around the hornworms that motivated it, so it named a life stage European corn borer does not have: it said *soil-pupating*, and the borer overwinters INSIDE THE STALK. Sweet-corn's own sourced prose says so (*"the borer overwinters in old stalks; shred and turn under cornstalks after harvest"*). The ACTION was right the whole time; only the stated mechanism was too narrow. Batch 2 logged this as a one-off prose defect; it turned out to be one of eleven of the same shape. **THE WIDENINGS, each pinned by a guard to the gap it closes:** `garden_sanitation` (103 rungs / 19 crops / 7 types, 31 of them INSECT, framed entirely around disease); `resistant_varieties` (65 rungs, 15 INSECT -- now admits varietal non-preference, a tight corn husk against earworm); `crop_rotation` (44 rungs, 16 INSECT, and many fungal ones are FOLIAR diseases carried in residue rather than soilborne); `floating_row_cover` (31 rungs of which SEVEN are bacterial -- excluding a VECTOR to prevent a disease with no cure, a use the field never mentioned); `handpick` (now admits slugs and snails, which it already ships on); `airflow_spacing`; `bt` (seven caterpillar problems, not two, and its new last sentence carries the non-target caveat onto the surface a reader consults when DECIDING to use it: *"It acts on caterpillars as a group, so keep it off plants you are growing for butterflies"*); `balance_nitrogen` (the PROBLEM scope was right, the *leafy and cole crops* restriction was not -- it ships on tomato, strawberry, pepper and the cucumbers); `even_watering` (HALF its rungs are spider mites; the field was entirely calcium-framed). **THIS IS CONSUMER COPY**, so all ten follow the copy rules and were length-checked against the existing distribution rather than allowed to balloon: the previous longest was 358 chars (`prune_out_infection`) and the new maximum is 380. **HARNESS: 26 injections, 26 CAUGHT, 0 survivors**, preflight 27/27, positive control GREEN, sentinel RED; six families -- content 7, scope 5, hygiene 4, leftalone 4, refusal 4, mechanics 2. **TWO RUNS: 2 survivors, then 0.** Both were the masking shape yet again: the EXCLUDED/WIDENINGS overlap guard was shadowed by the drifted-text check further down (the injected entry's bogus `old` refused anyway, for a different reason), and the widening COUNT check was unreachable because `apply_widenings` already asserts the same thing. Fixed by pinning each to its own abort MESSAGE and by making the count guard reachable at all. **THE READ FAN-OUT'S MECHANICAL HALF IS BUILT**: `ladder_batch.cross_sibling_conflicts` reports siblings whose SOURCE PROSE agrees but whose authored LADDERS do not. Batch 3's only real defect was exactly that shape and was caught by a hand-built side-by-side; no gate can see it, because each ladder is independently valid and the defect exists only ACROSS crops. Run against batch 3's raw staged output it returns **6 rows: the real defect plus the 5 pickling-cucumber divergences that are CORRECT** and needed adjudicating anyway. Six rows to read instead of 274 strings. It REPORTS, never refuses -- a divergence is right when the crops' prose makes different claims. Wired into `verify`, and its reachability was PROVED end-to-end against a reconstructed pre-batch-3 state rather than asserted. **A DEFECT IN MY OWN COMMITTED TEST FILE, FOUND AND FIXED**: `test_ladder_batch.py` (shipped in `c1cd161`) contained the class `ReportedTwinsAreRealOnCanonical` TWICE. Python shadows the first with the second, and the two bodies were NOT identical -- `test_problem_ORDER_is_part_of_the_identity` lived only in the shadowed copy and **had never run once**. My first diagnosis said the duplicate was verbatim and nothing was lost; the method-set comparison contradicted that and the data won. The order mutation had still been caught, but by a different test than the one written for it. The file is rebuilt deterministically: 9 unique tests, 0 duplicates, all collected, and the harness still returns 8/8 with the recovered test genuinely running. **`release_verify` CORRECTED A SECOND TIME, for a shape the first fix missed.** `--expect-changed` handled MORE crops than the pilot slug but could not express ZERO, because `expected` always contained `--slug`. So every CATALOG-ONLY promote reported a concern -- the bt safety fix and all four catalog mint rounds were catalog-only. `--expect-changed none` now declares it, checked exactly: declaring `none` while a crop did change is still a concern. 10 tests, RED before GREEN. **GAUNTLET: gate_all 121/121; control_ladder_gate 0; register_completeness PASS; release_verify CLEAN (catalog-only declared, `top-level(non-crops) changed: ['control_methods']`, catalog +none -none); COMPACT preserved.** Roster unchanged at 19/121 laddered.

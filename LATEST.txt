SHA: fe26f7833cb9c932fa621c20fb6ebc08af2eb5e66866089e21d847fa4970f57c
Date: 2026-08-21
Session: PLA-290 follow-on -- **THE LAST PROSE VARIETY ENTRY, THE PAREN VARIANT** (`2d496da5` -> `fe26f783`; ONE promote, ONE LEAF -- 1 DROPPED, 3 ADDED, 0 CHANGED of 387,358; echinacea only, no other crop and no top-level key). PLA-290 converted the ten COLON-format crops. This is the one remaining entry whose parenthetical sits MID-STRING, so plant-app's legacy branch -- which splits only a TRAILING parenthetical -- does not match it either, and all 228 characters became the variety's display name. Found by plant-app's new dataset-shape tripwire (PLA-291 Part B) once PLA-290's regenerated data let it run, and **MEASURED HERE BEFORE ACTING** rather than taken from the report: of **756 variety entries roster-wide it was the ONLY one whose app display name exceeded 70 characters** (next longest 59), and 0 display names contained a colon. AFTER: **0 over 70, 0 colons, longest 59** -- the cap holds with real headroom, and the guard asserting it is roster-wide, not echinacea-scoped, so a future regression anywhere reddens it. THE SPLIT IS AUTHORED, NOT MECHANICAL, because there is no colon to split on: the sentence runs THROUGH its parenthetical and out the other side ("...'Tiki Torch') in orange, red, yellow, and doubles; showy but..."), so both the cultivar list and the color list move to the note. A token guard proves every word of the original survives into name+note, and a second names all five cultivars and all four colors explicitly, because a tidier note that quietly dropped two of them would still read fine. THE ID FOLLOWS PLA-290's RULE UNCHANGED: `interspecific-hybrid-color-series` is a dashed prefix of the 228-char stored slug, so any existing planting still resolves, and `id == slugify(name)` keeps `varieties.ts` and `build-guides-data.mjs` agreeing. ECHINACEA'S OTHER SIX ENTRIES STAY STRINGS by decision -- each is a clean trailing parenthetical the app parses correctly, and PLA-290 ruled that family out of scope -- leaving a MIXED array that all three consumers handle per-entry; converting them would be shim-safe, since their slugs already come from the paren-head, but it is a different change. **TWO METHOD FINDINGS, recorded because they generalise.** (1) **A REPLAY-PINNED PROMOTE SUITE HAS NO RED PHASE BY CONSTRUCTION**: its `post` IS the promote's own output, so it goes green the moment the promote is written and can never be observed failing first. PLA-290's suite only had a RED phase because it initially, wrongly, read live canonical. The substitutes here are a REACHABILITY guard asserting the PRE state contained exactly the one offender (it did: `echinacea[6]`, 228 chars), and the mutation harness -- which is therefore not corroboration but the ONLY non-vacuity evidence. (2) **THE HARNESS EARNED ITS KEEP ON RUN ONE**, exiting `HARNESS DEAD` because the promote imported `slugify_variety` from PLA-290's module, absent in the scratch tree. Fixed by DECOUPLING rather than by copying the dependency: a promote script is a pinned historical artifact, and one importing another means editing the older silently changes what the newer did. A guard now asserts the two slug rules agree over all 756 live names plus every legacy prose string. MUTATION HARNESS (PLA-215, liveness-defended): **16 injections, 14 mutations ALL CAUGHT by their target guard, 0 survivors**, sentinel red across 10 guards, positive control green. GAUNTLET: suite 18/18; whole_crop_gate echinacea PASS; gate_all 121/121; release_verify 1 concern PROVEN PRE-EXISTING base-vs-base (its single-crop-pilot "expected only cherry-tomato"); COMPACT preserved. OWED, app side: plant-app re-runs `build:guides` (one command) and both repos close out together.

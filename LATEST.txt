SHA: e5b1aa882d300df54e1eca2e5629a4f5bed7c8f0018228d6845c62fcf90f5d80
Date: 2026-07-06
Session: POST-114 §C spelled-degrees -> °F cleanup + §B offline non-null-URL gate (CONTENT release; 035b950d -> e5b1aa88). The OFFLINE half of the §B/§C backlog pass; the 1,030-URL online liveness sweep is a deferred follow-on. Brainstormed -> Trevor-ratified (offline now, defer liveness; leave the 57 legacy zones{} nulls alone -- backfilling would spread the DEAD uga_b577 PDF; onion = clarify latitude, NOT convert) -> spec + plan (docs/superpowers/) -> TDD. TOOLING (committed Tasks 1-2): tools/temp_scan.py = the ONE shared spelled-temperature scanner+fixer (spelled_temp_hits + convert_temps), replacing the two narrow inline regexes (whole_crop_gate _DEGF_RE + release_verify) that only caught 'degrees F' and MISSED '90 degrees'/bare '50 F'/'degrees Fahrenheit' (all shipped); TDD caught a real bug (degrees?[NS] matched the trailing 's' of 'degrees'). tools/url_health_gate.py = offline non-null-URL gate scoped to the LIVE layers (regions{}+claim), legacy zones{} EXCLUDED, green on canonical (live layer clean; the 57 nulls are all legacy). DATA (this promote): §C spelled-degrees -> °F on 11 crops (beefsteak-tomato, bok-choy, cherry/grape/roma-tomato, lettuce-leaf, orange-navel, pear-european, raspberry, strawberry, tomatillo -- 120 forms), applied via convert_temps then main-loop spot-checked; onion's 3 latitude 'degrees' (day-length/photoperiod prose) clarified to °N (NOT °F) -- the only latitude case, so no bare 'NN degrees' remains. green-beans-bush (named in the kickoff) confirmed CLEAN. SHA-guarded splice from 035b950d: EXACTLY the 12 slugs changed (11 temp + onion), all other crops + every top-level key (incl. source_catalog) byte-identical, 114 certified unchanged. Gates: temp_scan 0 + url_health_gate 0 + register_completeness 0 + whole_crop_gate 12/12, all exit 0; release_verify per slug = 0 NEW concerns vs base (each crop's 9-10 plantings_provenance concerns are pre-existing benign collateral, identical on base). Canonical COMPACT, no trailing newline. 035b950d -> e5b1aa88. NEXT (Task 4, same session): wire spelled_temp_hits into whole_crop_gate C/D + release_verify D (the hardening; data cleaned first so the roster stays green), with an adversarial injection proving it catches a regression. Then Trevor confirms push. DEFERRED follow-ons: the §B online URL-liveness sweep (WebFetch ~1,030 distinct URLs; repoint uga_b577 dead PDF / citrus TAMU redirects / lime bare ucanr.edu / cucumber B577 logo-PDF; build url_health_gate --online; then optionally backfill the legacy zones{} nulls); §D rhs tier (answered in principle by the §A ASPCA precedent); the 108-crop pet_safe rollout (§A follow-on). Docs: spec docs/superpowers/specs/2026-07-06-degrees-url-offline-pass-design.md, plan docs/superpowers/plans/2026-07-06-degrees-url-offline-pass.md.

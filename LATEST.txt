SHA: b118f19d36d021db95d755225e566843676fe3fa393299f250a8d34bb9605710
Date: 2026-09-01
Session: PLA-8 -- **THIN-LADDER BACKFILL: 8 RUNGS ONTO 6 SHIPPED PROBLEMS, AND THE FIRST PROMOTE IN THE ARC THAT EDITS ALREADY-LADDERED CROPS** (`4f33522c` -> `b118f19d`; ONE promote; beet, fig, garlic, strawberry; catalog steady at 64, `source_catalog` byte-identical, no other crop touched). **EVERY ONE WAS FOUND BY THE THIN-LADDER SCAN, AND THE SCAN'S REAL VALUE WAS ITS FALSE POSITIVES.** Population: 775 laddered problems / 3,149 rungs, of which 133 carry two rungs or fewer. A thin ladder is NOT itself a defect, so the check was narrower: the prose names a control that EXISTS in the catalog, is LEGAL for that problem's type, and is missing from the ladder. Instruction phrases generated leads and **every hit was READ**: 10 leads over 9 problems, **6 real and 4 false**. **All four false positives failed for one reason: the prose names the control in order to DISCOUNT it** ("Research into tolerant varieties is ongoing but there is no home cure" on both huanglongbing records; "Few home varieties carry useful resistance" on cantaloupe bacterial wilt; and chamomile's "well-drained" sitting inside a descriptive aside about where gray mold does not occur). **A keyword scan cannot tell "do X" from "X will not help here" and 40% of its hits were the latter, so this must NOT be automated into a gate.** The reassuring half: 10 leads across 133 thin ladders means the thin population is overwhelmingly thin for good reason. **THE SIX FIXED**: `strawberry`/`red-stele` gains `improve_drainage` and `certified_clean_stock` (its prose leads with "Plant in well-drained soil or raised beds, buy certified plants"); both fig problems gain `prompt_harvest`, whose own `best_use` literally names "fig souring" as its case; `beet`/`common-scab` gains `even_watering` and `lower_soil_ph`, the first of which only became LEGAL in r9 and the second only EXISTED from r8; `garlic`/`botrytis-neck-rot` and `garlic`/`fusarium-basal-rot` each gain `cure_and_store`, minted in r8. **Two of the six were unfixable a day ago**, which is the argument for having run the catalog rounds first. **THE GUARD SHAPE IS INVERTED, BECAUSE "ALREADY LADDERED" HAS BEEN A REFUSAL IN EVERY BATCH SO FAR AND IS THE PRECONDITION HERE**: `expect_before` pins each ladder's exact current sequence so a drifted ladder REFUSES rather than being silently rebuilt; every pre-existing rung must be BYTE-IDENTICAL after, dict for dict; the record prose must not move at all; and the rungs added must be exactly the declared set. **`expect_before` earned its place immediately** by refusing the first dry run: I had taken fig's ladder order from the scan's SORTED output rather than the real sequence, and the guard caught it before anything was written. **EVERY ADDED RUNG MUST BE WARRANTED BY ITS OWN RECORD.** `check_warrants` requires a declared phrase from the problem's own prose for each new rung and asserts it is actually present, which is the restate-the-record discipline made checkable: a rung whose warrant is not in the record is an invention, however plausible. **It is the guard that would have caught all four false positives**, none of which could have produced a phrase that survived reading. **THE HARNESS RETURNED SIX SURVIVORS AND EVERY ONE WAS A DEFECT IN MY TESTING, NOT IN THE PROMOTE**: three drivers were hedged across branches that share one constant (`expected 7` appears in both `check`'s message and `check_warrants`'), two guards were only reachable by calling `verify_post` and `apply_to` DIRECTLY rather than through `check`, and **the `main` wiring driver passed with `check()` cut out of `main` because it used a sabotage `verify_post` ALSO catches**. That last one is the r8 lesson recurring one round later in a subtler form: it is not enough to reach the guard through the entry point, the sabotage must be one that ONLY that guard can see. Re-run: **49/49 both runners, harness 41 injected / ZERO survivors** across 6 families, preflight 42 anchors, positive control GREEN, sentinel RED. One FORWARD assertion (the post-state rung count, fixed by construction once every ladder is pinned to its `expect_after`) is documented in the promote and WITHDRAWN from the harness rather than left reported as a permanent gap. Gauntlet: `gate_all` **121/121**, `control_ladder_gate` 0, `register_completeness` PASS, both variety gates 0, `url_health` 0, `whole_crop_gate` PASS on all four, `release_verify` **clean** (only the 4 declared crops changed, catalog +none -none, reference lettuce-leaf byte-identical). NEXT: the roots batch. **94 / 121 laddered; 27 crops / 138 problems / 7 batches remain.**

SHA: 2a9d3c85dbb2da3ccbd69cd1798017d3ca8a3bb6280b8e212d5f63b86adef4af
Date: 2026-08-31
Session: PLA-8 -- **BATCH 17, STONE FRUIT: THE *PRUNUS* CATEGORY, SIX CROPS IN ONE GENUS** (`213cb110` -> `2a9d3c85`; ONE promote; apricot/cherry-sour/cherry-sweet/nectarine/peach/plum -- 49 problems, 137 rungs, NO mint, catalog steady at 61; roster laddered 73 -> 79). **THE HEADLINE IS THAT IDS WERE PINNED BEFORE THE FAN-OUT, NOT MINTED SIX TIMES**: six species in one genus is the exact setup that produced batch 13's defect where all five agents minted the SAME WRONG bacterial id, so the whole convention table was adjudicated against each record's own stated taxon before any agent ran, with agents told to FLAG a pinned id rather than silently change it. THREE REFUSALS, each verified not assumed: `bacterial-spot` is REFUSED because the roster id sits on five peppers whose disease is a generic *Xanthomonas* leaf spot (and is actually NAMED "Bacterial leaf spot" there), while stone fruit is *Xanthomonas arboricola* pv. *pruni*, stated in peach's own cause_seasoned -- it ships `bacterial-spot-pruni`; generic `aphids` is REFUSED in both directions (apricot names green peach + mealy plum, plum says "two aphids specific to plum"); and THE TWO CHERRY FRUIT FLIES DO NOT MERGE (cherry-sour names a three-species complex, cherry-sweet names *R. indifferens* alone -- the batch-16 sweet-pea shape: the record's prose governs, not the kinship), with cherry-sweet's compound borer entry shipping `cherry-borers`. THREE REUSES taxon-verified: `plum-curculio` (apple, both *Conotrachelus nenuphar*), `spotted-wing-drosophila` and `birds` (strawberry). **The pinned map came through with ZERO DRIFT.** `brown-rot` spans all six DESPITE a 3-3 species split, RULED a TEMPLATE artifact not a taxon split (the records fall into two prose families and the species list tracks the template). **TWO CROSS-CROP ADJUDICATIONS, BOTH SETTLED BY READING APPLE'S SHIPPED LADDERS**: `plum-curculio` carries NO `handpick` rung (apple folds jarring into garden_sanitation; apricot and peach were collapsed; the guard covers APPLE too plus its own anti-vacuity check), and `bacterial-spot-pruni` carries a HEDGED TERMINAL COPPER rung on all four crops (apricot and peach refused it reading a rung as an endorsement; apple's fire-blight ladder ships copper exactly that way, so the convention is author-it-hedged). **THE SELF-DENIAL GUARD IS NEW AND CAUGHT A LIVE DEFECT THREE TIMES**: adding that copper rung revealed apricot AND peach each had a cultural rung asserting the ABSENCE of the rung being added ("which is why this ladder stops at cultural steps"; peach's survived its first fix as "no good home cure TO FOLLOW IT WITH"). The prose is LOCALLY TRUE and becomes false only when a rung is added, so it passes every structural gate and every one-rung-at-a-time read. **The guard is a proxy and the proxy was measurably incomplete**: an enumerated phrase list fired on 15 hits, a roster check found 97 shipped notes on 39 crops already using "rung"/"ladder" in consumer prose (~2%, FILED as its own sweep), it narrowed to claims-about-which-rungs-exist (5 hits), and then cherry-sour found a SIXTH by applying the RULE instead of the regex -- so it now implements the rule structurally with the phrase list kept as known shapes, plus a positive control asserting honest MATERIAL hedges are never rejected. **TWO PREMISES DIFFER FROM BATCHES 15/16 AND BOTH NEARLY SHIPPED SILENTLY**: stone fruit is the OLDER FULL SCHEMA, not note-shaped, so batch 16's premise check REFUSED THE ENTIRE BATCH on first run; and `type` is an UPGRADE not an addition (all 49 carried coarse `pest`/`disease`; 579 laddered problems roster-wide carry only fine types), so without check_type_transition the promote would have overwritten an existing field as a side effect of apply_to. **THE CATALOG IS THE PROBLEM, NOT THE AUTHORS**: counted by independent bots -- no trunk-care key at all (6/6), `airflow_spacing` MEANS says "at planting" while every tree uses it for canopy pruning (5), `borer_stem_surgery` MEANS is written for squash vines (5), `prune_out_infection` illegal on insect (4), `even_watering` excludes fungal (4), `balance_nitrogen` lacks bacterial (4, and apple's fire-blight ladder ALREADY smuggles that advice into a garden_sanitation note). `garden_sanitation` carries 29 of 137 rungs across SEVEN distinct actions, EIGHT of them off-meaning and every one forced by the type map -- the `bottom_watering` shape, RULED ship-as-authored with the catalog remediation filed, since the content is right and the key is wrong the same way in shipped data. Suite **61/61**; **HARNESS 38/38, ZERO SURVIVORS** (39 anchors exactly once, positive control GREEN, sentinel RED). **THE HARNESS EARNED ITSELF**: four guard branches were GREEN AND VACUOUS until it ran -- an id branch masked by the check below it, the reuse-anchor branch whose test tripped an earlier message, the structural self-denial rule that every test bypassed via the phrase list, and THE POSITIVE CONTROL ITSELF, whose test re-implemented the comparison instead of driving the promote's function; a fifth (`kept the coarse type`) was unreachable decoration found while writing the suite. Gauntlet: gate_all 121/121, control_ladder_gate 0, register_completeness PASS, whole_crop_gate PASS x6; release_verify's two concern classes both VERIFIED as known tool artifacts (all six crops' `regions` subtrees are BYTE-IDENTICAL base vs candidate, so the 17 "novel keys" are the section-E false flag where an ANNUAL reference lacks tree fields). FILED: cherry-sweet's borer entry calls the American plum borer a beetle when it is a pyralid MOTH; plum's San Jose scale is anchored to a MEALY PLUM APHID page as its sole source (mis-pointed key); brown rot's bloom fungicide names a CLASS and never a material on all four carriers, so the highest-severity disease ships sanitation-only. Evidence: `docs/2026-08-31-pla8-batch17-stone-fruit-outcome.md`. **NEXT: batch 18 (acid citrus) is AUTHORED AND BLOCKED** -- `sooty-mold` is a fungal problem whose entire control is INSECT control, so TYPE_TARGETS forbids its real ladder and `ladder_batch.py merge` crashes on the `null` lemon honestly emitted; RULED (Trevor 2026-08-31) **mint `ant_exclusion` first** spanning insect and disease_general, which both citrus bots independently named the largest citrus gap. Also carried: citrus records say oil is unsafe above 95°F while the catalog CAUTION says 90°F. ~8 batches remain after 18; microgreens LAST.

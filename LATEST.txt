SHA: fabdaae1d3c35d54ccc49704253b5eb4e191700897786c1ec761e340166b5cb6
Date: 2026-09-01
Session: PLA-8 -- **BATCH 21, FLOWERS: A NOTE-SCHEMA BATCH, A SAFETY RULING ON `bt`, AND THE HARNESS FINALLY CATCHING SOMETHING** (`5409c0ce` -> `fabdaae1`; ONE promote; nasturtium/sunflower/viola -- 26 problems, 64 rungs, NO mint, catalog steady at 62; roster laddered 88 -> 91). **THE PREMISE WAS DIFFERENT AND MEASURING IT FIRST IS THE ONLY REASON THE BATCH SHIPPED**: these are NOTE-SCHEMA records (`note_beginner`/`note_seasoned` only, no `symptoms_*`/`cause_*`/`prevention_*`, no `sources` or `anchoring_urls`), the companion-flower shape of batches 15/16. **Copying batch 20's promote would have refused the entire batch on its premise check.** The premise is now asserted in BOTH directions: the note pair present AND the full-schema fields ABSENT, so a later conversion refuses rather than validating prose the promote was not written against. Ladders are correspondingly short (64 rungs over 26 problems against batch 20's 140 over 39) because two notes is the whole evidence base. **`bt` WAS REMOVED FROM VIOLA ON A SAFETY READING, AND THAT IS THE BATCH'S HEADLINE.** The catalog's own caution says Bt kurstaki kills butterfly caterpillars as a group and to spray "**never butterfly host plants**"; viola is a fritillary host. The author kept the rung with a careful qualification (leave native fritillary larvae unsprayed) and its reasoning was good, but it fails on a point the author itself supplied: Bt "does not sort a pest larva from a fritillary", so the instruction is not followable. An INCOHERENT RECOMMENDATION on a plant the caution explicitly excludes. Dropped; the ladder is `handpick` alone, which is what the note actually asks for because handpicking CAN sort them. **nasturtium's bt rung STAYS** and the distinction is clean: its target IS the pest butterfly's own larvae (*Pieris rapae*) and all SIX brassicas carrying that same id ship the same rung, which is the caution's own permitted case. `check_bt_is_scoped` pins BOTH halves. Viola's own note still recommends Bt: FILED as a prose defect. **THE GUARD SHAPE WAS CHOSEN BY MEASUREMENT, NOT INHERITED** -- batch 20's lesson applied. Measured BEFORE writing: 9 of 20 reused-id instances EXACTLY MATCH a shipped ladder and 11 diverge, both correctly, because a generic aphid ladder on a companion flower converges with marigold, borage and calendula. So a batch-20-style "pin every divergence" guard would have been 11 pins of noise and a "must match" guard would have been wrong 11 times. **Shape comparison is the wrong check here and is not used.** The real hazard when ladders converge is copied PROSE, so `check_no_shipped_prose_echo` earns the slot instead, scanning every rung note whole and sentence-by-sentence against the entire shipped corpus. **THE COMPANION INVERSION HELD**: `trap_cropping` forbidden batch-wide, zero "trap"/"decoy" tokens in 128 new strings, and nasturtium's aphid rung states it outright ("Aphids finding this plant is normal rather than a sign that something has gone wrong"). Two pieces of trap-crop content are deliberately UNPLACED because they concern the trap USE, which is why you grow the plant. **THE HARNESS CAUGHT TWO REAL GAPS, THE FIRST SURVIVORS SINCE BATCH 17**: both were ANTI-VACUITY branches with no driver, and the cause is that `check_inversion` and `check_no_shipped_prose_echo` each have TWO vacuity checks while I had written a driver for one of each. The suite looked complete at 71 green tests, including tests literally named "guard is not vacuous", while two of the guards' own self-protection branches were dead. Drivers added; **73/73 both runners, harness 51 injected / ZERO survivors** on the re-run, 14 families. Gauntlet: `gate_all` **121/121**, `control_ladder_gate` 0, `register_completeness` PASS, `whole_crop_gate` PASS x3, `release_verify` section A clean (only the 3 declared crops, catalog +none -none, marigold byte-identical); its ONE section-E concern is the documented reference-gap artifact on a `regions` subtree this promote never touched. **THE PINNING METHOD HAD A BUG AND IT PUT THREE WRONG ENTRIES IN THIS BATCH'S TABLE**: the scan mapped each problem name to its ids then displayed `sorted(ids)[0]`, so for the EIGHT roster names carrying more than one id it silently showed only the alphabetically-first. "Bacterial wilt" carries both `bacterial-wilt` (cucurbit *Erwinia*) and `southern-bacterial-wilt` (eggplant *Ralstonia*), so I concluded the correct organism needed a NEW id when the roster already had exactly the right one. Two authoring agents each flagged one collision and an audit found the rest. **FIXED for batch 22: never collapse a name to one id, and check every intended MINT against the roster BY ID, not by problem name.** **A THIRD ROSTER DEFECT CLASS, MEASURED**: three singular/plural id splits are live -- `cutworm`(1)/`cutworms`(8), `flea-beetle`(1)/`flea-beetles`(31), `japanese-beetle`(1)/`japanese-beetles`(6) -- each with exactly ONE crop on the singular. Batch 20 found the Japanese beetle case and treated it as a one-off; it is a class. My pin table said reuse the singular "to avoid creating a split", which was WRONG because the split already existed; corrected to the plural, and `check_singular_variants_not_taken` now refuses the singular AND refuses if the majority ever flips AND refuses if the splits are repaired. Three one-token repoints would retire the class. NEXT: batch 22 (stragglers) is authored and merging; then roots. 30 crops / 8 batches remain.

SHA: 4f33522cbbf945ea1fe878c64f038623d4a98d0c3cf0211bf57cfa7b5c161866
Date: 2026-09-01
Session: PLA-8 -- **CATALOG ROUND 9: `even_watering` WIDENED TO REACH COMMON SCAB, AND THIS ROUND EXISTS BECAUSE I GOT SOMETHING WRONG IN r8** (`6a67a677` -> `4f33522c`; ONE promote; `applies_to` gains `bacterial`, sources 3 -> 5, `control_methods` steady at 64, `source_catalog` BYTE-IDENTICAL, **ZERO crops touched**). **THE PROMPT WAS A CORRECTION.** r8's record claimed `even_watering` "already carries" the soil-moisture half of common scab control. It did not: its `applies_to` was `['physiological','mite']`, which does not intersect `TYPE_TARGETS['bacterial']`, so it was ILLEGAL on both potato and beet scab, and no method carried a "keep the soil evenly moist" instruction for a disease at all. **I asserted that a method was available without checking that it was reachable**, which is one line of arithmetic. Found by the thin-ladder scan the same day and corrected forward in the live surfaces, with STATE_HISTORY and commit `4c8bf1e` left byte-for-byte. **WIDEN, NOT MINT, AND THE REASON IS THE CATALOG'S OWN SHAPE**: `even_watering` is ALREADY a two-mechanism method, carrying calcium-movement disorders and spider mites under one action with a source each. The ACTION is identical in all three cases, hold soil moisture steady, and only the mechanism differs; minting a second key for the same action is how a catalog acquires two entries telling one problem the same thing under different names, which is the confusion `lower_soil_ph` had to be written to escape. **`bacterial` ONLY, and the alternative was measured**: `bacterial` opens 70 problems, `disease_general` would open 382 by also reaching every fungal and viral problem, and nothing was read for those. The evidence is scab-specific, so the widening is the narrowest one that reaches the case. **The prose scopes what the type vocabulary cannot** -- 70 problems become legal and one is the case this was read for, so `best_use` names potatoes, beets and common scab outright. **THE GUARD SHAPE IS INVERTED FROM r7 AND r8, WHICH BOTH ASSERTED "NO EXISTING METHOD IS TOUCHED."** This round touches one on purpose, so the protection is rebuilt the other way: exactly ONE method may change, `applies_to` may only GAIN, `source_catalog` must be byte-identical, and **every claim the method already made must SURVIVE** -- 37 shipped rungs (25 mite, 12 physiological) were authored against that text, and a widening that quietly rewrote what the method meant would change 37 rungs' meaning without touching a single crop record. `MUST_SURVIVE` pins five fragments by name and is itself checked against the CURRENT text first, so a fragment the method never said cannot pass vacuously. `unblocks_its_case` re-derives legality from `TYPE_TARGETS` and refuses if beet/common-scab is still unreachable, because **a widening whose target does not reach its case is a no-op that reads as progress**. **THE HARNESS RETURNED EIGHT SURVIVORS ON THE FIRST RUN AND ALL EIGHT WERE MINE.** Six were hygiene branches with NO DRIVER AT ALL, because I omitted r8's per-shape hygiene test when writing this suite and `test_shipped_new_prose_is_clean` cannot see a disabled branch: clean prose stays clean either way. One was the `MUST_SURVIVE` table being emptied one entry at a time, invisible because the test iterated whatever the table happened to hold; fixed with a COVERAGE assertion pinning its contents. The eighth, the method-COUNT check, is genuinely unreachable: the set comparison above it fires first for any addition or removal, so with equal sets the count is equal by construction. It is a FORWARD assertion against base drift, now documented as such in the promote and WITHDRAWN from the harness rather than left reported as a permanent gap. Re-run: **53/53 both runners, harness 39 injected / ZERO survivors** across 7 families, preflight 40 anchors each matching exactly once, positive control GREEN, sentinel RED. Gauntlet: `gate_all` **121/121**, `control_ladder_gate` 0, `register_completeness` PASS, `url_health` 0, `source_catalog_title_gate` exit 0, `whole_crop_gate` PASS on beet/potato/celery. NEXT: the thin-ladder backfill (8 rungs onto 6 problems on beet, fig, garlic and strawberry), then the roots batch. **94 / 121 laddered; 27 crops / 138 problems / 7 batches remain.**

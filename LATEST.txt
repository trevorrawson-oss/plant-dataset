SHA: 95e66f6d1a8ea8550b2df3825d3bcbb00d39056e106d037290737923f74d0879
Date: 2026-09-04
Session: PLA-8 BATCH 27, THE MICROGREENS, LANDED -- **THE ARC CLOSES** (`ba61762a` -> `95e66f6d1a8ea8550b2df3825d3bcbb00d39056e106d037290737923f74d0879`; ONE promote; arugula-microgreens, broccoli-microgreens, cilantro-microgreens, pea-shoots, radish-microgreens, sunflower-sprouts, wheatgrass). Roster laddered 114 -> 121. **PLA-8 IS COMPLETE ON ITS OWN COMPLETION TEST, which is every PROBLEM ENTRY laddered, not every crop: 913 of 913 problem entries on 121 certified crops now carry a ladder (was 899 of 913). 121 laddered + 7 shells = 128.** The 7 shells (avocado, olive, and the five mushrooms) carry `pests: []` / `diseases: []` and `verification_status.status = None` -- present and empty by intent, so they hold ZERO problem entries and an instrument counting by id would not see them at all. 43 rungs, 85 register strings, 14 problems, catalog UNCHANGED at 64 methods, zero prose edited (purely additive: `id` + `type` + `control_ladder` onto 14 entries, 42 leaves). **ZERO IDS MINTED** -- `fungus-gnats` and `damping-off` are both REUSED from `microgreens-mix` (the laddered precedent on this same schema) and 13 other crops; the PLA-449 collision guard ran at id-pinning time against POST-APPLY data and reported 42 pairs -> 42, zero introduced, and its liveness was proved separately by a probe: a minted `damping-off-microgreens` is CAUGHT (NAME_SHARED + FAMILY_MEMBER) with the PLA-449 schema fallback and reported BLIND with the fallback disabled. **THE BUNDLING RULE WAS APPLIED AND RESOLVED AGAINST DOCUMENTS, NOT WAIVED**: four of the seven disease names bundle "mold" with damping-off, but Penn State ("Damping-off is the only disease or pest issue we have encountered", figure captioned "Damping-off possibly caused by Rhizoctonia or Botrytis species in a microgreens tray") and Virginia Tech SPES-756 (damping-off plus mildews; the word "mold" never appears) both treat tray fuzz as the damping-off complex, and no readable .edu/.gov source attributes it to saprophytes, so this is ONE complex under one id and a split would have asserted a distinction the documents refute. The bundled NAMES are PLA-453 and were carried byte-for-byte. **THE SEVEN CROPS' PRE-BATCH `whole_crop_gate` GREEN WAS VACUOUS, AND THIS IS MEASURED, NOT ASSERTED**: `control_ladder_gate`'s identity/type/applies_to checks skip any problem whose ladder is `None`, so an identical sabotage (`type: 'not-a-real-type'` on wheatgrass) PASSES on the pre-promote state and is 1 VIOLATION on the post-promote state. This batch armed the gate on seven crops it could not previously see. NO MATERIAL RUNGS: all 43 are cultural or physical, enforced by a safety refusal spec, because these are cut at cotyledon stage inside 7 to 28 days and eaten whole and raw (wheatgrass juiced raw) and no pre-harvest interval has been ruled for such a crop. Suite **53/53**; mutation harness **36 injected / 36 caught / 0 survived / 0 broken**, anchor preflight 36/36 exactly-once, positive control green, sentinel reddened -- and the harness earned its keep, catching a VACUOUS `test_similarity_is_symmetric` whose synthetic string pair was symmetric anyway, now repinned on a real 0.288/0.828 asymmetric pair. `whole_crop_gate` PASS on all seven; `gate_all` **121/121**; `control_ladder_gate` **0 violations with 913 of 913 laddered**; `register_completeness` PASS; collision gate on live canonical 42 pairs, 20 registered, 22 open (the pre-existing set, plus nothing); `release_verify` clean with exactly the seven declared crops changed (`--ref microgreens-mix`, `--slug wheatgrass`). 15 authored notes were rewritten after the copy guard caught verbatim lifts (three from the PRECEDENT's own rung notes, one a 0.776 two-donor recombination with no shared run), and 5 more after the house-style guard caught internal ladder vocabulary ("this rung", "the ladder stops") leaking into consumer prose. **plant-app E1: the export is built from `ce98b0a6`, so it was ONE dataset revision behind before this promote and is TWO after -- NOT the ~15 the arc records imply. `npm run build:guides` DID run mid-arc; batch 24's "eleven revisions behind" and `dfd8d85`'s "built from c24d7754" are both stale.**

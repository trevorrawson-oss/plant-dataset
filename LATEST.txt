SHA: 394bb8bdf63c989eeff7241ba41d1c37c829201733ce199f4dffc88490d8f660
Date: 2026-08-18
Session: PLA-161 -- **PLA-161 -- THE COMPLETION CONTRACT: PREDICATES THAT REFUSE AN UNJUSTIFIABLE ZERO AND AN UNJUSTIFIABLE COMPLETION** (`76f92a20` -> `394bb8bd`; ONE promote, FINDINGS-ONLY -- a single adjudicating declaration appended to lemon, guards prove every other byte identical; NO citation repointed, no prose, no calendar, no catalog, no other crop touched). PRE-BUILD RE-VERIFICATION FIRST, and it changed the build three ways: (a) the issue's stated base `72284f02` was six promotes stale and its SHA preflight would exit 1; (b) `assert_absence_reportable` was documented as latent, "protected today only by cache incompleteness, returns True for ZERO of 128 crops" -- MEASURED AT 58 of 128, with 28 of those certifying absence over list-only documents the walk structurally could not see, so the predicted failure had already arrived; (c) the issue's named RED case for the sibling re-key (lemon/`lsu_agcenter`) is a PHANTOM -- that id has zero bare citations dataset-wide and lemon never cites it, at `76f92a20` AND at the issue's own base. PREDICATE 1: `cited_urls()` widened to resolve ids named only in `sources`/`source_set` through `source_catalog`; justified/unjustified split moved 30 justified + 28 UNJUSTIFIED -> 30 justified + 0 unjustified, 3,359 -> 3,575 documents enumerated, ZERO crops newly passing (monotonic by construction). A source id is NOT a document (cherry-tomato anchors `clemson_hgic` at six factsheets; 366 crop/id pairs carry >1 URL), so the unit stays the URL and the catalog fallback applies only to ids a crop never anchors. `umn_ext`'s catalog URL is DEAD (404 under both user-agents, cache correctly recording it) -- split to PLA-140, not fixed here; counterfactual confirms 28 -> 8. PREDICATE 2: `tools/reporting_contract.py` EXTRACTED from `hunt_footprint` (not reimplemented), adopted in campaigns B, C, D and `bare_host_scan`. C's `HONEST OPEN after re-scope: 0 of 25 decisions` and B's `HONEST OPEN, document work: 0 of 33 decisions` are both now COMPLETION REFUSED, naming 39 masked-only DECISIONS / 195 masked node-CITATIONS (C) and 16 / 28 (B); D refuses its `10 of 12 hunts` line and names hunts #25, #26, #27, #28, #29 as producing no rows with no reason given. Residues are re-derived live from `hunt_footprint` on every run, never pinned. SIBLING RE-KEY shipped ADDITIVE, deliberately: `sibling_leads()` is same-path/any-id, `pathed_by_sibling` still drives the SIBLING-PATHED verdict on the narrow key, because folding the re-key in would re-price a CLOSED campaign as a side effect of a refactor (6 -> 19 DECISIONS at the pin, 3 -> 12 live; filed as its own issue instead). Naming its replacement RED case surfaced a real bug: `pathed_by_sibling` never checked the SUBJECT was citrus, so edamame (a legume) was being handed a citrus IPM page and a citrus variety collection; `sibling_set_for()` guards it, and the issue's own "3 -> 38" was measured WITHOUT that guard (correct figure 3 -> 36 NODES / 12 DECISIONS). SELF-PATHED shipped: 315 bare CITATIONS / 155 SOLE / 78 DECISIONS / 37 CROPS, masked rows included deliberately. `blob()` now serialises the whole finding rather than five enumerated fields (`detail` existed on ZERO findings, `basis` on 361 / 88,392 chars); 45 TRIPLES over 20 FINDINGS on 13 CROPS become visible and ZERO decision verdicts move in C or D at either SHA -- the under-read was real, its verdict reach into those campaigns is nil, and both facts are pinned so neither can be quoted as the other. HUNT #28, the only sanctioned canonical change, was REFUTED BY THE DOCUMENT and filed as a declaration instead of the scoped repoint: `hgic.clemson.edu/cold-tolerance-in-citrus/` was read in full (2,223-char body), mentions lemon EXACTLY ONCE in a taxonomy list, publishes one figure (15F satsuma), and contains zero occurrences of Gulf/Southeast/zone/container/wrap/cover. The node's high-20s F threshold and `survives_no_fruit` rest on none of it, and this crop's OWN resolved finding `lemon_cold_threshold_was_miscredited_now_uc8100` had already ruled that page publishes no lemon number. CASE 2: declaration filed mirroring hunt #31's warm_arid precedent key-for-key, citation left BARE by design so the record of examination survives. Guards: TDD RED->GREEN throughout (predicate 1 5 RED, re-key 4 RED, SELF-PATHED 7 RED, blob 2 RED, promote 7 RED), replay-pinned via promote_fixture, blast radius asserted BOTH directions so an ADDITION cannot hide, 3 promote sabotages caught (wrong base SHA, node no longer bare, double-file). TWO CORRECTIONS AGAINST MYSELF, recorded because this issue is about counts that cannot show their warrant: a claimed "94 SELF-PATHED decisions" was my own keying artifact (the issue's 78 is right, 72 region-scoped + 6 crop-level), and a docstring claim that the cohort guard was "not load-bearing" was FALSE -- 0 differences over campaign D's real nodes but 468/117 over a synthetic cross product, all non-citrus subjects. Gauntlet: whole_crop_gate lemon PASS, gate_all 121/121, release_verify 16 concerns ALL proven PRE-EXISTING base-vs-base (zero new; the one differing line is its collateral check correctly confirming exactly lemon changed), source-truth sample 9/9 assertions verified against the cached document, COMPACT preserved.

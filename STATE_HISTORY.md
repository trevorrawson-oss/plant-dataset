# plant -- STATE HISTORY (append-only)

> **This is the append-only recovery log.** It is the full chronological record of every session's state. **NEVER rewrite or delta-edit this file -- only APPEND** new entries at the top of the chronological stack (below this header). The lean live-state surface is `CURRENT_STATE.md`; this file is what you read to RECONSTRUCT the true position when the live surface is ambiguous or a step was skipped (it is what caught the premature-flip near-miss). Split out of `CURRENT_STATE.md` on 2026-06-03, seeded with the entire prior body verbatim.
>
> **Append protocol:** at session close, after fully regenerating `CURRENT_STATE.md`, append a dated entry here capturing what happened (the detail that does not belong on the lean surface). Most-recent-first, beneath this header.

---

## 2026-06-06 — session `m16_cherry_step4_fl_peninsula` (claude.ai authored; Claude Code releases)

**Cell authored:** `cherry-tomato.regions.fl_peninsula` (zones 10–11, South Florida). 8th of 10 region cells. Step 4 fill 7/10 → 8/10.

**Predecessor (start) SHA:** `842ee139c711e533110e6dc3868d77676e85cefda4b98915570d644dbb2327bf` (warm_arid). Preflight clean against the uploaded artifact. NOTE: the kickoff prompt named an expected start-SHA of `9d8784f7…` (the pre-`warm_arid` checkpoint); that was a stale carry, reconciled against CURRENT_STATE which confirms `842ee139` canonical and `fl_peninsula` as the correct next cell. No drift in the dataset.

**claude.ai output SHA (working copy, for Claude Code to re-verify on merge):** `7d4bf50cc56801017ed30d027343c55378a4ffe7631a736dd93284b2ade48140`. Single crop changed; collateral audit PASS (lettuce + 121 others byte-identical).

### Two decisions taken with Trevor before authoring
1. **Q1 — summer encoding = B (cherry-narrowed peak-summer `heat_pause`), NOT cool-season-only, NOT `year_round`.** Peak fruit-set-failure months for South FL cherry = **July + August `[7,8]`**. Basis: UF/IFAS Broward South Florida planting guide lists *cherry tomatoes* in the **June** summer column (with okra, southern peas, tropical vegetables) while restricting "tomatoes (larger fruit varieties)" to Sep–Mar; legacy zone-11 note: new fruit set fails above ~78°F nighttime. June/September are shoulder-productive, so the exclusion is the two peak months only. Same *direction* as se_gulf (cherry narrows the pause); larger-fruited beefsteak will re-derive wider.
2. **Q2 — zone split = source-decides → ONE unified resolution for both z10 and z11.** VH021/EP452 divide Florida into North/Central/**South**; the peninsula (zones 10–11) is South Florida, served by one calendar (EP452) + the statewide VH021. The legacy `zones{}` 10-vs-11 difference was a **USCRN climate-data artifact** and is **NOT carried forward as verified**. `resolved_by_zone[10]` and `[11]` are byte-identical.

### Window structure (source finding, not default)
**Single long cross-year cool-season window** (`single_window_cross_year_cool_season`), NOT the desert two-window shape and NOT two discrete crops. Frost is rare on the peninsula, so the planting window runs continuously: fall transplant Sep–Oct → through the mild winter/spring → last planting Feb (EP452: "last month to plant … tomatoes for a late spring harvest"); seed-start indoors Aug. **No `second_planting` band** — `plantings[]` carries `succession_id` 1 only.

**Resolved windows (both zones):** plant_out `Sep 15 – Feb 12`; start_indoors `Aug 1 – Aug 21`; harvest `Nov – May` (`harvest_start` Nov 15, `harvest_end` Jun 30); `planting_note: cross_year_range`. `calendar[12]` Jan–Dec: `harvest×5, growing(Jun), heat_pause(Jul), heat_pause(Aug), plant(Sep), plant(Oct), growing(Nov), harvest(Dec)`. calendar heat_pause months match the `heat_pause` block exactly.

### Sourcing
Cited the **catalog-canonical** IDs `uf_ifas_vh021` (SP103/VH021 statewide guide, https://edis.ifas.ufl.edu/publication/VH021) and `uf_ifas_south_cal` (ENH1191/EP452 South Florida calendar, https://edis.ifas.ufl.edu/publication/EP452), verified live 2026-06-06. Both already in the catalog (trust_tier high) and already cited in this crop's legacy zones 9–11 → **nothing for Claude Code to admit**. The `fl_peninsula` SHELL carried `ufifas_ext` (institution-level scout ID) in `sources` + `sources_pending_admission`; **`ufifas_ext` is NOT used in the authored cell**, and `sources_pending_admission` is cleared to `[]` (analogous to the stale-marker cleanup in the warm_arid release). Read-not-cited: UF/IFAS Broward county planting chart (cherry-vs-larger-tomato split) and Gardening Solutions seasonal guide (cherry among summer-tolerant crops), both folded under the UF/IFAS institutional voice.

### Conventions / lessons carried from warm_arid
- `°F` in all user-facing **and** backend prose (region_notes + basis_seasoned + synthesis_note_seasoned); §C/D 0. Em dashes 0 in user-facing copy (commas only). §C/D and em-dash checks: **PASS**.
- `plantings_provenance` uses **`verified_against`** (not `sources`); leaf provenance and `anchoring_urls` use the `{url, verified}` shape.
- §E: only `uf_ifas_vh021` + `uf_ifas_south_cal` referenced; 0 uncatalogued, 0 non-T1. Catalog stays at 82 entries (no admission this cell).

### Gate delta (claude.ai estimate; Claude Code confirms on the gate run)
`region_notes`-null violations expected **4 → 3** (fl_peninsula pair now authored). No new violation of any class introduced (single window → no `second_planting` shape surface; calendar/heat_pause consistent; user-facing temp/dash clean). Remaining region_notes-null: `northern_tier`, `hawaii_tropical` (and `hawaii_tropical` is the last cell). Lettuce expected byte-identical / `GATE: PASS`.

### Flagged, not actioned (Claude Code / Trevor)
- **Stale `launch_ready` flags on cherry.** `verification_status.launch_ready_core` and `launch_ready_seasoned` are `true` (leftover from M10 cleanup) while cherry is mid-Step-4. Left untouched (flipping is Claude Code's gated lane at Step 11); flagging so the Step 11 flip doesn't treat them as already-correct.

### Owed to claude.ai downstream (unchanged from prior, plus this cell)
- Step 5/5.5: 4-round side-by-side on the fl_peninsula windows (incl. the Jun shoulder-vs-pause boundary and the Nov harvest-start maturation check); the July–August `[7,8]` cherry-narrowing ruling re-confirmed against beefsteak when beefsteak's wider peninsular exclusion is derived.
- Step 6/7: beginner siblings already authored inline as `region_notes_beginner`; dual-voice walker count to re-check.

### Next
**`hawaii_tropical`** (CTAHR B-91, zone 11) — year-round encoding likely applies (lettuce Hawaii precedent: `year_round:true` + `calendar_basis` + 12-month all-active calendar); `uhawaii_ctahr` catalogued. Then `northern_tier` calendar (cold; `cold_pause`, no midsummer heat_pause). Preflight the next cell against the **released** SHA (Claude Code re-pins LATEST after merge).

**Claude Code release note (2026-06-06, session `m16_cherry_step4_fl_peninsula`):** `842ee139` -> `7d4bf50cc56801017ed30d027343c55378a4ffe7631a736dd93284b2ade48140`. **CLEAN release -- no merge, no normalization needed** (second run of the new author/release model; claude.ai applied every warm_arid lesson): authored on the CORRECT base (`842ee139`, reconciling the stale kickoff SHA against CURRENT_STATE itself); cited the already-catalogued `uf_ifas_vh021` + `uf_ifas_south_cal` (NOT the institution-level `ufifas_ext` scout ID) so NO admit was needed; cleared its own stale `sources_pending_admission` markers; used `°F` in user-facing fields. Encoding decisions (Trevor + Claude Code ruled): **Q1 = B** (cherry-narrowed soft `heat_pause` `[7,8]`, the two peak >78°F-night months -- honors the source's cherry-summer-tolerant signal AND the new-fruit-set-stop, vs A's cool-season-only or C's year_round); **Q2 = two distinct zone resolutions** (z10 + z11, source-decided, not legacy-carried). Verification: only `cherry-tomato` changed, catalog 82 unchanged, lettuce byte-identical; gate residual `25 -> 24` (fl_peninsula region_notes cleared; the 24 all downstream); §A2 shape 0; §E 0 uncatalogued; §F only the pre-existing cornell gap; `GATE: PASS`. Owed to claude.ai (Step 5/5.5): confirm the `[7,8]` cherry-narrow + the z10/z11 split at the 4-round side-by-side.

---

## 2026-06-06 — M16 cherry-tomato Step 4: `warm_arid` authored (claude.ai)

**Lane:** claude.ai authored cell content (biology + source discovery + consumer copy). Hand-off to Claude Code for release (catalog admit, gate, SHA/LATEST/CURRENT regen, git). Per new working model: claude.ai delivers updated JSON + this entry; Claude Code computes verified SHA and promotes.

**Input SHA:** `9d8784f7a0e80ffd53fb6ecdc4500182c2519889ae15c2ff77202f19a7890e3c` (verified at preflight against LATEST.txt).

**What was done:** Authored `cherry-tomato.regions.warm_arid` (zone 8, Las Cruces / Mesilla Valley NM) — the cell held PENDING last session because window structure was unresolved. Cell filled: `plantings[]` (2 entries), `resolved_by_zone["8"]` (full envelope + `heat_pause` + `second_planting` + 12-mo calendar), both `region_notes_*`, `plantings_provenance`. PENDING sentinel cleared. Collateral audit clean (only `warm_arid` changed; 122 other crops + 6 filled cells byte-identical; 707 `°F` intact, 0 escaped; no em-dashes in user-facing notes).

**Window-structure ruling (the held question):** TWO-WINDOW. Resolved by reading sources directly this session, not by analogy.
- NMSU CR457 (catalogued `nmsu_ext`, statewide): warm-season crops = spring planting only; summer-for-fall slot is cool-season; fruit-set thresholds 95°F day / 55°F night. CR457-B (current Jan-2025 rev) Table 2 carries NO planting-date columns (spacing/days-to-harvest/yield only) — the "planting dates" in old search descriptions are from a prior edition; could not anchor window count from it.
- Doña Ana County MG Las Cruces Vegetable Planting Chart (NMSU extension, T1-eligible, © Darrol Shillingburg): tomato row shows TWO distinct transplant windows — spring (start ~late Jan–Feb, transplant ~March) and fall (start ~late Jun–Jul, transplant ~late Jul–Aug), separated by an Apr–Jun gap. Legend convention (faded "start" → solid "transplant") confirmed against the Peppers row directly above. Read from a screenshot Trevor supplied (chart PDF text layer is gutted; sandbox can't reach the NMSU subdomain — same proxy constraint as the San Diego MG admit).

**Source conflict + hybrid resolution (Trevor ruled):** The two NMSU T1 sources disagree on window count AND spring date. Resolved by letting each govern what it is authoritative about:
- Window STRUCTURE → Doña Ana MG chart (locality-specific extension instrument governs its own locality over the statewide average; NMSU's own >95°F set-failure threshold + Mesilla Valley summer climate independently predict the spring-crop / summer-gap / fall-crop shape). → two-window, `second_planting`, no longer single-window.
- Spring transplant DATE → NMSU CR457-B frost-free guidance (~early-mid May), NOT the chart's March. Rationale: March sits on the zone-8a last-frost edge (Feb 28–Mar 30) — fine for experienced growers with protection (who a Shillingburg handout implicitly addresses) but a frost risk for plant's first-season audience. Beginner-safety divergence.
- Net: spring transplant ~May (from `last_frost`), fall transplant ~late Jul–Aug (from `heat_pause_end`), recorded in `plantings_provenance`.

**Cherry-narrow heat_pause ruling (claude.ai's call, per-region judgment):** `heat_pause.months = [7]` (July only), NOT [7,8]. Rationale: Mesilla Valley zone 8 is hot (clears 95°F Jun–Aug) but ARID — strong diurnal swing, nights into the 60s–low 70s give real recovery, unlike the zone-9 low deserts (`ca_desert`, `low_desert_az`) where the pause is climatic-absolute and was NOT narrowed for cherry. Cherry's faster set + cool arid nights justify a single-month pause, mirroring the `se_gulf` narrowing logic. **Beefsteak re-derives independently and will likely hold a wider [7,8]** — the correct cherry-vs-beefsteak divergence.

**Citations:** `nmsu_ext` (CR457) = spring timing + fruit-set thresholds; `nmsu_donaana_mg` = two-window structure + fall window. CR457 url `https://pubs.nmsu.edu/_circulars/CR457.pdf`; chart url `https://donaanamastergardeners.nmsu.edu/documents/foodgardenplantingchart-1.pdf` (both verified live 2026-06-06).

**FLAGGED FOR CLAUDE CODE (release blockers / structural):**
1. **Catalog admit `nmsu_donaana_mg`** (NEW T1 reference — Claude Code lane). NMSU county-extension parallel to `ucanr_san_diego_mg`. Catalog `url` → program home `https://donaanamastergardeners.nmsu.edu/` (mirror San Diego MG: url = program home, document cited at point of use). Tier T1, class extension_master_gardener_program. Admission provenance caveat: single-author graphical handout (© Darrol Shillingburg) on the county MG program domain; admitted as the locality-specific zone-8 NM instrument; spring DATE not adopted from it (see hybrid). Cell currently carries `sources_pending_admission: ["nmsu_donaana_mg"]` on the region, the z8 resolved cell, and `plantings_provenance` — remove these flags once admitted. Gate §E will pass once the ID is in the registry.
2. **`second_planting` structural shape** (structural — Claude Code lane). Authored to mirror `se_gulf` z8 exactly; confirm the shape conforms to the region-shell/`second_planting` spec.

**FLAGGED FOR STEP 5/5.5 (claude.ai, later):**
- Calendar April derivation: encoded `wait` (empty bed; seedlings indoors mid-Mar→May, captured by `start_indoors` month + rule window). `se_gulf` shows `growing` in April only because it plants late March (already in-ground). Confirm `wait` is right for the May-planting case at the calendar-validation pass.
- Fall-window maturation: late-Jul/Aug transplant → ~Oct harvest is tight against ~mid-late-Oct zone-8 first frost. `harvest_end` set to Oct 21 (conservative). Re-check at 4-round side-by-side; may pull fall transplant toward late July if too tight.
- Cornell zone-6 url:null gate item still outstanding (unrelated to this cell).

**Next:** `fl_peninsula` (UF/IFAS VH021 — verify window count per source; CV137 tomato table found this session: S-FL Aug–Feb, W-central Aug–Sept/Jan–Feb, N-FL Jul–Aug/Feb–Apr), then `hawaii_tropical` (CTAHR, year-round encoding may apply), then `northern_tier` calendar/region_notes (cold_pause, NO heat_pause up north), then Steps 5–8, then Step 11 + flip.

**Claude Code release note (2026-06-06, session `m16_cherry_step4_warm_arid`):** Released onto canonical `bf96d1d1` -> `842ee139c711e533110e6dc3868d77676e85cefda4b98915570d644dbb2327bf` (NOT the entry's input SHA `9d8784f7`). The handoff flagged the `nmsu_donaana_mg` admit as a Claude-Code release blocker, but it was ALREADY admitted in `bf96d1d1` (a parallel promote) -- so this was a MERGE, not a re-admit: kept canonical's 82-entry catalog (incl. donaana) and swapped in the authored `warm_arid` cell. The new model's release-verification caught three things a wholesale promote would have shipped wrong:
1. claude.ai's JSON was built on the PRE-admit base (catalog 81, no donaana) yet CITED `nmsu_donaana_mg` -> a wholesale promote would have dropped the admission + shipped an uncatalogued citation. Fixed by the merge (canonical catalog retained; §E back to 0 uncatalogued).
2. Two stale `sources_pending_admission:["nmsu_donaana_mg"]` markers (region + plantings_provenance) -- cleared (donaana is admitted), per the entry's own instruction.
3. Two structural deviations from the cell's own `low_desert_az` exemplar, mechanically normalized (CONTENT untouched): `plantings_provenance.sources` -> `verified_against` (clears a §F anchoring false-gap -- provenance metadata, not a citation leaf), and `heat_pause.basis_seasoned` spelled "degrees F" -> `°F` (the `_seasoned` suffix makes it user-facing copy; low_desert_az + M16-CA-INT-003 both use `°F`).

Final gate: cherry residual `26 -> 25` (warm_arid region_notes cleared; the 25 are all downstream: 3 region_notes-null + 21 dual-voice + 1 cornell_ext); §A2 shape 0; §E 0 uncatalogued / 0 non-T1; lettuce byte-identical (`GATE: PASS`). The cherry-narrow `heat_pause` [7] ruling + the fall-maturation check remain claude.ai's (owed at Step 5/5.5), untouched by this release.

**SEQUENCING LESSON (new model):** a cell needing a NEW catalog source must be authored AFTER the admit, on the post-admit SHA -- not in parallel with the admit flagged for later. Here the only conflict was the catalog entry (trivially merged), but the general rule is: claude.ai requests the admit -> Claude Code admits + reports the new SHA -> claude.ai authors on THAT SHA. (First real exercise of the new author/release split: it worked -- the verification caught the base-cross before it corrupted canonical.)

---

## 2026-06-06 -- M16 catalog admission: `nmsu_donaana_mg` (Claude Code lane)

**SHA:** `9d8784f7...` -> `bf96d1d156417dd19d061edb05baf717e9bcf6e9ed69c016eb81debd9016d868`. Session `m16_catalog_admit_donaana_mg`.

**The admission.** claude.ai handed the `warm_arid` discovery batch with a two-step structure (admit, then author). One entry admitted: **`nmsu_donaana_mg`** -- Doña Ana County Master Gardeners, NMSU Cooperative Extension (`https://donaanamastergardeners.nmsu.edu/`, verified live by claude.ai), `tier:T1`, `source_class: extension_master_gardener_program`, parallel to the admitted UC ANR county MGs. **Borderline recorded in `_admission_provenance`:** the anchoring document is a single-author graphical handout (copyright Darrol Shillingburg) hosted on the county MG program domain -- admitted as the locality-specific extension instrument for zone-8 NM, with anchoring SCOPE limited to the two-window STRUCTURE + the FALL transplant timing. The spring-transplant DATE is NOT taken from the chart (its March transplant sits on the zone-8a last-frost edge); beginner-safe spring plant-out comes from NMSU CR457-B frost data (Trevor's hybrid ruling -- each source governs what it is authoritative about: chart = does Las Cruces support a fall crop; CR457-B = when is it beginner-safe to set out).

**Discipline + collateral.** SHA-gated against `9d8784f7` (clean committed base, re-preflighted first this time). Catalog `81 -> 82`, exactly `+nmsu_donaana_mg`; raw UTF-8 preserved (`ñ` not escaped; 0 `\u`). Collateral: ONLY `source_catalog` changed, NO crop changed, every existing entry byte-identical. Gate regression: uncited entry -> cherry residual stays **26**, §E 42 IDs / 0 uncatalogued / 0 non-T1, lettuce `GATE: PASS`.

**Step 2 (`warm_arid` cell authoring) NOT done -- raised as a lane + data question.** claude.ai's handoff asked Claude Code to author the cell from a biology brief. Returned to claude.ai's lane (the same lane that authored the 6 prior cells): the exact window dates come from the sources claude.ai READ (the chart + CR457-B; Claude Code has not read them and cannot set verified dates), and the `region_notes` + `basis_seasoned`/`synthesis` prose are consumer copy under the voice methodology. The handoff itself kept the cherry-narrow `heat_pause` ruling and the fall-maturation check as owed-back-to-claude.ai, confirming the biology is not fully determined. Symmetric to claude.ai correctly refusing to admit catalog sources: do not author the other lane's work while guessing. **NEXT:** claude.ai preflights `warm_arid` against `bf96d1d1` and authors the cell (mirror low_desert_az shape; two-window + `second_planting`; spring plant-out ~May per CR457-B; cite `nmsu_ext` + `nmsu_donaana_mg`).

---

## 2026-06-06 -- M16 cherry Step 4: 4 coastal+desert cells (claude.ai lane) [history entry added by Claude Code -- was missing from the recovery log]

**SHA:** `813bade9...` -> `9d8784f7a0e80ffd53fb6ecdc4500182c2519889ae15c2ff77202f19a7890e3c`. Session `m16_cherry_step4_coastal_desert_4cells`. (This entry reconstructs the session from the CURRENT_STATE "what happened" detail -- claude.ai's 4-cell promote updated CURRENT_STATE + LATEST + the dataset but did NOT append here; preserved by Claude Code before the CURRENT_STATE regen could discard it. Promote-before-collapse rule.)

**Four Step-4 biology cells authored, four genuinely different source-decided shapes (no template imposed). Gate 30 -> 26. Only `cherry-tomato` changed; lettuce + the 2 prior cells byte-identical.**
- **`ca_north_coast` (zones 9-10) -- COOL-LIMITED exception.** Single window, May only (UC ANR Table 13.2 North & North Coast = "May", no fall window). NO heat_pause -- limiting factor is LACK of summer heat (marine layer, sub-55°F nights, UC Marin MG), not a summer exclusion; harvest_end production-tapered ~October (cooling + light), NOT frost-cut; fall months read `wait` not `cold_pause`. Sources `ucanr_ext` + `ucanr_marin_mg`.
- **`ca_south_coast` (zones 9-10) -- mild marine, LONGEST window.** Single but long: Table 13.2 South Coast = "April - July 15" (105-day plant-out). Confirmed single continuous window, NOT a pause-split (San Diego MG: harvest early summer through end of year coastal). NO heat_pause. z10 (frost-free) harvest to December; z9 tapers to November. Sources `ucanr_ext` + the newly-admitted `ucanr_san_diego_mg` (FIRST use; §E recognized it). `fruit_set_temp_f` anchor logged (San Diego MG ~65°F night - 90°F set).
- **`ca_desert` (zones 9-10) -- heat-inverted, TWO-WINDOW.** Table 13.2 Desert Valleys = "Dec-March" (winter is the growing season). heat_pause Jun-Aug, climatic-absolute (Imperial/Coachella >95-110°F). CHERRY PAUSE NOT NARROWED (contrast se_gulf -- peak desert summer stops set even for cherry). `second_planting`: Sep fall crop into January. Sources `ucanr_ext` + `uc_ipm`.
- **`low_desert_az` (zone 9 ONLY) -- heat-inverted, TWO-WINDOW.** U of Arizona az2078 (peer-reviewed): spring transplant ~February (racing the late-May Phoenix heat wall); summer fruit-set failure Jul-Aug; September fall replant into December. heat_pause Jun-Aug, cherry-not-narrowed. zone 9 only (zone-10 desert served by `ca_desert`). Source `uariz_ext` (az2078 + AZ1005).
- **`warm_arid` deliberately NOT authored** -- window structure unresolved at the time (resolved by THIS session's handoff). Held for a clean source read.
- **Catalog:** `ucanr_san_diego_mg` first use; `ucd_plantsci` DROPPED (UC Davis VRIC is processing-tomato research, wrong register). Write discipline: SHA-gated scratch applies, minified, atomic swap, collateral audited.

---

## 2026-06-06 -- M16 catalog admission: `ucanr_san_diego_mg` (Claude Code lane)

**SHA:** `349fb7af...` -> `813bade981f488e54479f83db2ba72d06f3a12f0cb791149e638d1a14da6a040`. Session `m16_cherry_catalog_admit_sandiego_mg`.

**Lane clarification (the trigger).** claude.ai (Step 4, heading into `ca_south_coast`) was correctly blocked: the South Coast corroborator it discovered (San Diego County Master Gardeners) was not in `source_catalog`, and the sourcing guard forbids introducing an uncatalogued ID inline. Trevor questioned the lane ("I thought catalog admission was claude.ai's"). The record settles it: **catalog admission = Claude Code** (the live Lane split; claude.ai operated under it this very session -- the ca_interior `ucd_plantsci` catch). Trevor's recollection matched the OLD practice -- the existing `ucanr_marin_mg`/`ucanr_santa_clara_mg` carry `"Admitted in S9"`, an earlier claude.ai session; the lane clarified since. **The split:** source DISCOVERY/verification = claude.ai (find it, confirm live URL + T1 + content); ADMISSION (canonical ID + registry write + the source-tier gate) = Claude Code. **Why:** one shared registry across 121 crops -- centralizing admission prevents ID drift, unverifiable tier claims, and a catalog the gates cannot trust.

**The admission.** claude.ai handed the discovery batch as `{source, confirmed-live URL, why-T1, corroborates}`. One entry admitted: **`ucanr_san_diego_mg`** -- UC ANR Master Gardener San Diego County program (`https://ucanr.edu/site/uc-master-gardener-program-san-diego-county`, verified live by claude.ai via the rendering fetcher; sandbox curl proxy-blocked), `tier:T1`, `source_class: extension_master_gardener_program`, identical class to the two admitted CA county MGs. `citable_for` + `_admission_provenance` record the discovery basis and the **Association-domain caveat** (catalog url is the ucanr.edu program home; `mastergardenersd.org` prose is read-not-cited unless independently T1-anchored at cite-time). **`ucd_plantsci` HELD** -- claude.ai has no confirmed stable citable URL yet (it will confirm a specific UC Davis VRIC / Central Valley tomato doc next, then report whether to admit). Do not admit against a bare department root.

**Write discipline + collateral.** SHA-gated against `349fb7af`. (The gate first CAUGHT a stale-SHA attempt against the remembered `f916e8fe` -- canonical had moved to `349fb7af` via claude.ai's ca_interior promote; re-preflighted before acting. The discipline worked exactly as designed.) Catalog `80 -> 81`, exactly `+ucanr_san_diego_mg`. Collateral: ONLY `source_catalog` changed, NO crop changed, every existing catalog entry byte-identical. Gate regression: an UNCITED entry changes no cited-source result -- cherry residual stays **30** (§E 40 IDs / 0 uncatalogued / 0 non-T1), lettuce `GATE: PASS`.

**Git hygiene (recurring gap).** Committed claude.ai's uncommitted `ca_interior` (`349fb7af`) promote FIRST to keep git current, then this admission. The pattern: claude.ai's promote scripts land on disk (Trevor runs them) but the git commit+push step is not firing -- Claude Code has been committing each one (`f916e8fe` Steps 9+10, now `349fb7af` ca_interior). Worth folding the commit+push into the promote scripts. Two stray root apply scripts (`apply_ca_interior.py`, `fix_ca_interior_temp_form.py`) left untracked.

**NEXT:** claude.ai preflights `ca_south_coast` against **`813bade9`** (NOT `349fb7af`) and authors it citing `ucanr_san_diego_mg`; the cert gate verifies catalogued + T1. Other coastal/warm anchors are all already catalogued, so this one admission effectively unblocks the rest of the sweep.

---

## 2026-06-05 — session `m16_cherry_step4_ca_interior` (Step 4 second cell: ca_interior sourced; gate confirmed)

**SHA:** `339933f2bdbcd8e13d11bf507c7c40efdd2cd1d2cba3342d9f069b090075220f` → `928210dd012b2f8640772f1c84373f0890d4a48b2bb8b38d3ca1d52216a89450` (cell apply) → `349fb7af6c3ea909970671d627cb0b7176e8b7251376eaea614d0c263329b55b` (§C/D fix). **Two applies this session; pin the FINAL `349fb7af…`, not the cell-apply value.**
**Schema:** 2.8 → 2.8 (unchanged). **Crop count:** 123 → 123 (unchanged).
**Arc position:** M16 cherry, Step 4 (warm-region window sourcing) — second cell. se_gulf done prior; `ca_interior` is the first of the 8 remaining warm/CA regions, per `region_source_map` order.

### What happened
- **`cherry-tomato.regions.ca_interior` (zones 8–9) sourced + authored to the lettuce/se_gulf reference bar.** The only cell changed this session. PENDING shell → filled.
- **Gate finally run (resolves the se_gulf gate-not-run caveat).** Baseline confirmed at **31 VIOLATIONS** — matching the kickoff's hand-derived estimate exactly. Decomposition: 9 region_notes-null (incl. ca_interior pre-apply) + 21 dual-voice (gate-counted) + 1 `cornell_ext` zone-6 anchoring. §A2 shape counts (stub/null-track/stale-nested) all 0. The "~31 DERIVED" figure from the se_gulf close is now gate-confirmed.
- **Windows re-derived from T1, not lifted** (climate-contamination guard):
  - **z8 + z9 = UC ANR Master Gardener Handbook Table 13.2** via the online *Time of Planting* page (`ucanr.edu/program/uc-master-gardener-program/time-planting`, updated 2026-05-08; the page self-identifies as Table 13.2). Interior Valleys tomatoes = **April–May** (spring transplant; `*` = transplants used). Corroborated by **Sacramento County MG monthly tips** (single late-April, soil ~65°F / nights >50°F planting).
  - Both fetched + verified live this session; verified-field = session date-string.
- **SINGLE-WINDOW verified PER SOURCE — the `fl_peninsula`-style trap landing at ca_interior.** Two independent T1 sources give NO fall tomato window for the interior valleys (contrast se_gulf, where UGA C943 gives an explicit ~Jul 20 fall deadline → two-window). UC Davis Plant Sciences (read for confidence, NOT cited) independently frames Central Valley tomato as one spring planting with a single long harvest tail (70–120 d). → **`ca_interior` carries one main planting; NO `track:"second_planting"`, NO `heat_pause`.** The valley grows one continuous season; cherry is heat resistant and sets through peak summer heat. calendar[12] reads `growing`/`harvest` straight through summer.
- **Second-planting decision SETTLED for ca_interior (decision owed this arc):** z8 + z9 both single-season, no second planting. Resolved by confirming absence with a second T1 source (Trevor's direction), not by inventing a fall window or dropping it silently.
- **region_notes pair authored** (seasoned + beginner) — deliberately carrying the "why no fall planting here" explanation per Trevor's calendar-UI call (the explanation lives in region_notes, NOT a dedicated absence-field; same field se_gulf uses to explain its two-season split, opposite content). Beginner keeps the plain hook ("plant once in spring, pick through fall"), drops mechanism, not less true than seasoned. **No region-tip overrides** — sources do not fork the grower action; per-crop attestation recorded.
- **Frost variant: mediterranean (interior-valley NOAA variant), NOT humid_subtropical.** ca_interior resolution anchors to the mediterranean frost variant (z8a Sacramento/Fresno/Bakersfield/Modesto last_spring Jan 24; z9a Stockton/Merced/Livermore last_spring Feb 17), distinct from the humid_subtropical default se_gulf correctly used. The gap between mediterranean last-frost (Jan/Feb) and actual plant-out (Apr–May) is itself the single-season signal: soil-temp/heat-gated, not frost-release.
- **Shape matched to live se_gulf/lettuce exactly:** `plantings_provenance` block (+ `window_structure: single_window`); `resolution_method: ca_interior_month_resolution`; `planting_note: single_season`; cell-level envelope; NO `sources_pending_admission`; NO `lifted_from_zone` (re-derived).

### Sourcing correction (mid-session)
- Initial draft cited UC Davis Plant Sciences as a new ID `ucd_plantsci`. **Caught + corrected:** that ID is not in the 80-entry `source_catalog`, and catalog admission is the Claude Code lane. The single-window finding is fully supported by **already-admitted** IDs: **`uc_mg`** (UC Master Gardener Program — publisher of Table 13.2 / Time-of-Planting + Sacramento County tips; the catalog's `ucanr_ext` note explicitly scopes itself OUTSIDE uc_mg, so Table 13.2 homes to uc_mg) + **`ucanr_ext`** (UC ANR umbrella, handbook parent). §E stayed at 0 on apply BECAUSE of this correction — citing `ucd_plantsci` would have introduced an uncatalogued ID. UC Davis logged in provenance as `sources_read_not_cited`.

### Findings
- **M16-CA-INT-001 — ca_interior single-window** (source finding; se_gulf two-window contrast). Window structure decided by reading the source, not mirroring se_gulf. CONFIRMED.
- **M16-CA-INT-002 — no `heat_pause` for ca_interior** (continuous warm season; cherry heat resistant; no in-source summer exclusion to encode). Per-region heat judgment, not a universal tomato pause.
- **M16-CA-INT-003 — region_notes temperatures use degree-sign `°F`, NOT spelled "degrees F".** Surfaced by gate §C/D: first authoring used "65 degrees F" → flagged. Dataset confirms: ALL region_notes temps use `°F` (14 occ., zero spelled); spelled "degrees F" is correct + dominant in BACKEND prose (2302 occ., incl. `synthesis_note_seasoned`) but §C/D scopes to user-facing strings only. Gate corroborates — it flagged `region_notes_seasoned` but NOT the same cell's `plant_out.synthesis_note_seasoned` (also "65 degrees F"). One-field fix (`65 degrees F` → `65°F`) cleared it. **Carry-forward: author region_notes with `°F` from first draft on every remaining warm region; same user-facing-vs-backend split as the em-dash rule. Add to `tip_region_authoring_standard` if not present.**
- **`gs_exemplar_finding_001`** — ca_interior slice CLOSED. (7 warm cells + `cornell_ext` URL remain for full close.)

### Verification done (gate-confirmed this session)
- **Gate run twice.** Baseline 31 (pre-apply, confirming the derived estimate). After cell apply, gate held at 31 — the §A2 region_notes clear (9→8) was masked by a NEW §C/D temp-form violation (the "65 degrees F" error). After the one-field §C/D fix, **gate = 30** (8 region_notes + 21 dual-voice + 1 anchoring); §C/D temp forms back to 0; §E 0 uncatalogued / 0 non-T1; §F still the 1 known cornell_ext zone-6 item. **31 → 30 is the real drop, now correctly accounted.**
- Both applies SHA-gated (`sys.exit(1)` on mismatch), scratch-copy, minified `separators=(',',':')` / `ensure_ascii=False`, atomic swap, pre-swap backups written.
- Collateral audit (both applies): ONLY `cherry-tomato.regions.ca_interior` changed; all 122 other crops + all top-level non-crop keys byte-identical; **lettuce-leaf AND se_gulf byte-identical** (explicit guards). Cell apply touched only ca_interior; fix touched only `ca_interior.region_notes_seasoned`.
- Independent post-write re-verification reading OUTPUT only (both applies): single-window confirmed, no heat_pause / no second_planting, calendar[12] plant-in-April, region_notes both filled, all anchoring leaves carry url + date-string verified, all cited IDs catalog-admitted.

### Residual after this session (cherry)
**Gate = 30.** 7 warm regions still PENDING (`ca_north_coast`, `ca_south_coast`, `ca_desert`, `warm_arid`, `low_desert_az`, `fl_peninsula`, `hawaii_tropical`); 8 region_notes pairs still null (se_gulf + ca_interior done, `northern_tier` + 7 warm remain); 27 dual-voice siblings (gate shows 21 — blind spot unchanged); `cornell_ext` zone-6 URL; `gs_exemplar_finding_003` extreme-zone record. Then Steps 5–8, then Claude Code Step 11 + flip. **The arc is NOT done — two cells of Step 4 are.**

### Next
`ca_north_coast` (zones 9–10; zone 8 dropped per `region_source_map` marker resolution) — the **cool-LIMITED exception**: marine layer, warm season constrained by LACK of summer heat, NOT a summer pause. Heat-primary anchoring does NOT apply. Same `ucanr_ext`/Table 13.2 institution (North/North Coast column) + county MG corroboration. Author region_notes with `°F` per M16-CA-INT-003.

### Deliverables
`phase_3_cherry_m16_step4_ca_interior_findings.md` (1_for_project_knowledge); this entry (append to STATE_HISTORY, top of stack); regenerated `CURRENT_STATE.md`; `ca_north_coast` kickoff (2_for_next_chat); both apply scripts (3_for_your_records). Dataset-update model = apply-already-run (canonical already at `349fb7af…` on the machine via the two scripts). PROMOTE is Trevor's manual run: re-pin `LATEST.txt` → `349fb7af…` / 2026-06-05 / `m16_cherry_step4_ca_interior`, regenerate this file, append here, sync `00-current/`, commit + push.

---

## 2026-06-05 -- M16 cherry-tomato Steps 9 + 10: T2 de-citation + source-name dash -- claude.ai lane

**SHA:** `1b4fea68e63ed63c02cc50aeb8bd55a761c6d6534fe037fba371eab7130df68a` -> `f916e8fec820bb21a3f52e667e5efe6760764a12dfc2824a3ee20234872b8fd1`. Session `m16_cherry_steps9_10_mechanical`. Same calendar day as the shell build + `lifted_from_zone` follow-up; first claude.ai-lane session on cherry's downstream work. Preflight clean (start SHA matched `LATEST.txt`).

**Frame.** First downstream (Steps 4-11) session on cherry after Claude Code closed Step 3.5. Two zero-biology items were pulled forward out of checklist order (Steps 9 + 10 ahead of 4-8) because both rulings were already settled and both are provably independent of the 4-8 authoring surface. **The out-of-order pull was surfaced to Trevor and accepted** (the cleaner path is checklist order; these two were safe to pull because neither touches what 4-8 build -- the dash is in a source NAME that 4-8 never regenerate, the T2 is on a pre-arc germination tip that warm-region sourcing never touches). Lesson reaffirmed: kickoffs SUMMARIZE; arc position derives from the checklist -- so an out-of-order action gets named as such, not silently folded in.

**Scope discipline (data-grounded before acting).** Trevor's "bump them all to T1" was checked against the data rather than assumed: a full non-T1-source-as-evidence scan of the ENTIRE crop (every `sources` array + `anchoring_urls` dict, all layers, against the `source_catalog` tier map) returned exactly ONE T2-as-evidence source -- `harvest_to_table` -- on one claim-bearing leaf, present in both its `sources` and `anchoring_urls` (gate counts the leaf once = "1 harvest_to_table T2"; raw field count = 2). ZERO T2-as-evidence anywhere else: not legacy `zones{}`, not region rollups. So "bump them all" == this one citation; the scope question dissolved against the data instead of via a second round-trip.

**Step 10 -- `harvest_to_table` T2-as-evidence ruling (ARC DECISION, precedent for every pre-arc-built crop).** Decision (Trevor): T1-only as evidence, **no grandfathering** -- grandfathering would create a permanent exception class every future Step-10 audit must reason about; lettuce set a clean T1-only bar and cherry holds it. Execution was a **DROP, not a swap**: the germination bottom-heat tip the T2 backed is already anchored by four T1 extensions that directly support the claim (Clemson "starting-seeds-indoors", MSU "time to start vegetable garden seeds", UMN + Cornell tomato guides), so the redundant T2 was removed with no replacement sourcing needed. **Root-fix tail (the catch):** the SHA-gated apply's collateral guard fired because two REGISTRY references still held `harvest_to_table` after the evidence layer was cleared -- `sources_summary.primary` (the `{tier:T2, citation_count:2}` entry; count == the 2 evidence fields) and `verification_status.source_set`. Leaving them would have cherry declaring a source it no longer cites. So the de-citation is a **4-field** operation: (1) `tips_by_stage.germination[0].sources` 5->4; (2) delete `tips_by_stage.germination[0].anchoring_urls.harvest_to_table`; (3) remove the `sources_summary.primary` entry; (4) remove from `verification_status.source_set` (23->22). Result: `harvest_to_table` count in cherry = 0. This is a SEPARATE arc decision, NOT a `gs_exemplar_finding` component (the five are `_001`/`_002`/`_003`/`_004`/`_shell`).

**Step 9 -- residual user-facing em-dash (CLOSES `gs_exemplar_finding_002`).** The four `region_label` em-dashes were resolved by Claude Code at Step 3.5; the one residual user-facing `--` was in a source NAME in `sources_summary`: `"UC Davis Postharvest Technology Center -- Produce Facts Sheets"`. Resolved per-sense to a **comma** (publisher-publication apposition, citation style), distinct from the region_label `place: subregion` colon sense -- so it does NOT inherit the colon (the per-sense, per-crop rule). Result: `"... Center, Produce Facts Sheets"`. **Flagged to Trevor as a one-character copy call** (his copy authority): flip `, `->`: ` if cross-consistency with the region_label colon is preferred. With this resolved, cherry carries zero user-facing `--`/em-dashes (to be confirmed at the Step 11 re-scan).

**Write discipline.** Scratch-copy apply, SHA-gated against `1b4fea68` with `sys.exit(1)` on mismatch; minified `json.dump(..., separators=(',',':'), ensure_ascii=False)`, no trailing newline (verified: 717 raw UTF-8 degree signs preserved, 0 escaped, file does not end in `\n`). Collateral audit: ONLY `cherry-tomato` changed; `lettuce-leaf` byte-identical (explicit guard). Independent post-write re-verification reading the OUTPUT only: 9/9 checks pass (harvest_to_table count 0; tip retains 4 T1 + `evidence_tier:extension_backed`; UC Davis name dash-free; no `harvest_to_table` in summary or source_set; serialization hygiene).

**Gate movement.** Residual 34 -> **32** (cleared the 1 source-name dash + the 1 `harvest_to_table` T2). Remaining 32 are ALL Steps 4-8: 10 region_notes-null, 21 dual-voice siblings (gate-counted), 1 `cornell_ext` zone-6 URL.

**Carried forward (flagged now so they don't surprise the flip):**
1. **`status`-vocab unification is THREE states, not two.** Cherry's `verification_status.status` is currently `"verified_retro_complete"` (from `phase_3_m10_cleanup`), NOT `"unverified"`. Lettuce holds `"unverified"`. So the Appendix-B vocab successor (leaning `verified_arc`, decided now / set at the flip) reconciles three states at Step 11, not two. Do NOT set it before the flip.
2. **Gate blind spot for the structural lane (dual-voice walker).** The dual-voice completeness walker does NOT descend into the `*_beginner_seasoned` companion arrays, so 6 null `why_beginner` siblings (3 `good_beginner_seasoned` + 3 `bad_beginner_seasoned`) are real dual-voice gaps the gate does not count. They are IN SCOPE for this arc's Steps 7/8 -- so the authoring count is **27**, while the gate will display **21** until the walker is fixed. Owed to Claude Code: teach the walker to descend into `*_beginner_seasoned` arrays (same blind-spot class as the lettuce readiness gate; will recur per-crop on the pipeline if unfixed).
3. **`container_notes.overwintering.approach_beginner` null/null -- correctly absent, left as-is.** The object carries `applicable: false`; cherry is `lifecycle: annual`, `perennial: false`, frost-killed. Documented absence (reason field present), not a hole -- do NOT author it.

**Appendix-C status after this session:** `_shell` CLOSED (Claude Code, 3.5); **`_002` CLOSED (this session)**; `_001` (warm sourcing + orphan UMN text + `cornell_ext` URL), `_003` (extreme-zone record), `_004` (`cause_beginner`) remain for Steps 4-8.

**Deliverables.** `phase_3_cherry_m16_steps9_10_corrections.json` + `phase_3_cherry_m16_steps9_10_findings.md` (1_for_project_knowledge); this entry (append to STATE_HISTORY); regenerated `CURRENT_STATE.md`; Step-4 kickoff (2_for_next_chat). Dataset-update bundle carries the new `crops_data_final.json` + `LATEST.txt` + `CURRENT_STATE.md`. PROMOTE is Trevor's manual run.

---

## 2026-06-05 -- M16 cherry Step 3.5 follow-up: `lifted_from_zone` north normalization (all-crops shape fix)

**SHA:** `a65c71758bba944a39059426c0ae5bc6cfe65ea683cb87cfb323b7b1d41b2aae` -> `1b4fea68e63ed63c02cc50aeb8bd55a761c6d6534fe037fba371eab7130df68a`. Session `m16_cherry_step3_5_lifted_from_zone_norm`. Same calendar day as the shell build, immediately after.

**Trigger.** Trevor asked for a direct lettuce-vs-cherry comparison of `northern_tier` / `resolved_by_zone`. The comparison found cherry's north resolved cells matched lettuce on every structural essential (identical region keys, `zone_promoted_verified`, no nested `plantings`, correct single `beginner` track) EXCEPT one stray key: cherry kept `lifted_from_zone` (`"3"`,`"4"`,...`"7"`) while lettuce's `northern_tier` sheds it. In the north `lifted_from_zone` is **tautological** -- the region zone IS the legacy zone, so it always equals the cell's own zone key (that is why lettuce's NA-3h reconciliation dropped it there, while keeping it in multi-zone `se_gulf` where it is informative).

**Trevor's systemic catch:** ALL crops' `northern_tier` is the same 109-crop Phase-A lift, so every one carries this redundant key -- the fix belongs in the TOOL, not just cherry.

**Fix (Claude Code lane).** `tools/build_region_shells.py` `_build_north_from_zones` now `cell.pop("lifted_from_zone", None)` in the north promote (TDD: test asserts no `lifted_from_zone` in north cells). Re-applied to cherry via `apply_region_shells.py` (EXPECTED_SHA bumped to `a65c7175`; the re-run guard kept it idempotent -- warm regions untouched, provenance re-written identically). **Surgical delta confirmed:** only `lifted_from_zone` removed from the 5 cold cells; gate unchanged (cherry §A2 shape = 0, total 34; lettuce `GATE: PASS`); collateral audit = only `cherry.regions` changed. Cherry's north now has ZERO cherry-only cell keys vs lettuce (remaining lettuce-only keys = `heat_pause` + `succession_spring`/`succession_fall` = CORRECT biology differences -- tomato is frost-limited not heat-limited, and is not a succession crop).

**Honest non-shape gaps still owed to claude.ai (by design, NOT this lane):** cherry's north `calendar[12]` is the lifted old calendar with `wait` in the winter-gap months where lettuce has derived `cold_pause` (Step 5.5 pause classification + derive-from-windows); `region_notes` null (Steps 6/7). So cherry's north is at-SHAPE-parity with lettuce but not yet biology-filled.

**Owed checklist amendment (claude.ai):** fold the `lifted_from_zone`-strip into the v1.5 Step 3.5 north sub-procedure text (the tool already enforces it; this is the doc-sync).

---

## 2026-06-05 -- M16 cherry-tomato Step 3.5: region shells BUILT + `second_planting` structure DEFINED -- Claude Code lane

**SHA chain:** `29b3aaa904a62487960c5dc53b4282538454076f696ffec039ac4ab87937801a` (M15 lettuce flip) -> `a65c71758bba944a39059426c0ae5bc6cfe65ea683cb87cfb323b7b1d41b2aae` (M16 cherry Step 3.5). Session `m16_cherry_step3_5_region_shells`. Preflight clean (start SHA matched `LATEST.txt`). Run via the full superpowers flow: brainstorming -> spec -> writing-plans -> subagent-driven execution (4 tasks, each implementer + spec reviewer + code-quality reviewer).

**What was built (Step 3.5, Claude Code structural lane).** All 10 cherry `regions{}` cells brought to the ratified reference shape:
- `northern_tier` (cold zones 3-7) PROMOTED from the verified cold `zones{}`: `track:"beginner"` added to the region-constant `plantings[0]`; the forbidden nested `plantings` key stripped from every `resolved_by_zone` cell (§3b-i); `resolution_method: static_precompute -> zone_promoted_verified`; the "LIFTED VERBATIM" `plantings_provenance` string rewritten to a promotion record. No succession hoist (`succession_policy.suitable=false`).
- 9 warm/CA regions: `plantings:["PENDING ..."]` stubs -> shape-complete RULE skeletons (one `track:"beginner"` rule object with archetype window-rule keys present-but-empty: `start_indoors`/`plant_out`/`harvest_start`/`harvest_end`). `resolved_by_zone` cells left as PENDING fill-targets (derived output; claude.ai sources at Step 4). NO warm zone data carried as if verified (climate-contamination guard).
- 4 `region_label` em-dashes resolved (`California -- X` -> `California: X`).

**Gate result.** `whole_crop_gate.py cherry-tomato`: §A2 SHAPE classes all 0 (`stub:0 | null-track:0 | stale nested:0`) -- closes `gs_exemplar_finding_shell`. Total 42 -> 34; the residual 34 is DOWNSTREAM claude.ai work, NOT a Step-3.5 failure: 10 region_notes-null (copy, Steps 6/7), 21 dual-voice siblings incl. `cause_beginner` (`gs_exemplar_finding_004`, Steps 7/8), 1 source-name dash (Step 9), 1 `harvest_to_table` T2 (Step 10), 1 `cornell_ext` zone-6 URL (`gs_exemplar_finding_001`, Step 5). §F anchoring 3->1 (2 nested `mu_ext` entries stripped); §C/D dash 5->1. **Lettuce regression-guarded: `GATE: PASS`, byte-identical, NOT touched.** Collateral audit: only `cherry.regions` changed (all 10 keys); every other crop + top-level key identical.

**The `second_planting` structure -- DEFINED + design-decided (the bigger outcome).** Cherry is the FIRST crop to need a "second planting" (a discrete second crop for a longer harvest: cold zones 6-7, warm 9+). Trevor put the REPRESENTATION in the Claude Code lane (the renderer is ours to build). Full spec: `docs/superpowers/specs/2026-06-05-second-planting-region-shell-model-design.md`. Decisions:
- **Two honest resolved-layer patterns, not one forced shape.** DISCRETE plantings (`beginner` + `second_planting`) share one window shape; SUCCESSION is a separate cadence (`succession_spring`/`succession_fall`). A second planting reads one way, a succession another -- different kinds.
- **Each crop carries only the structures for what it is; each concept has ONE fixed shape.** Main = flat fields (universal). `second_planting` = a discrete-window object (`{plant_out, start_indoors, harvest_start, harvest_end, sources, anchoring_urls}`) on applicable zones + a region-constant `plantings[]` entry `track:"second_planting"`. Succession = lettuce's existing `succession_spring`/`succession_fall`, RATIFIED as the standard (carrot will match lettuce).
- **Lettuce is NOT touched** -- ratify its shapes rather than reshape it; stays byte-identical + certified, reference shape does not move.
- **Shape-complete shells (not thin)** -- accuracy+scale: Step 4 fills VALUES into a fixed skeleton, not 121 passes each building shape.
- **Visibility = seasoned-only** (like succession; `beginner` universal). Grounded in the EXISTING app: `PlantingCalendarCard` (main, both modes) + `SuccessionCard` (seasoned-only, two render modes mapping 1:1 onto discrete-window vs cadence). The structured `second_planting` REPLACES the cards' current fragile synthesis (comma-string multi-window parsing + `calendar[12]` heuristics + maturity-midpoint guessing). Same-calendar vs separate-card is the renderer's call (current impl = separate seasoned card); the dataset is rendering-agnostic. The renderer rewrite is gated to the Phase C read-layer flip; `zones{}` stays coherent so the current UI keeps working.
- **Gate taught the structure:** `whole_crop_gate.py` §A2 now validates a resolved-cell `second_planting` is a discrete-window dict with the window keys (lenient on null at admission, strict at cert). Forward-looking -- cherry carries NO `second_planting` data yet.

**Lane handoff to claude.ai (Steps 4-8 + biology):** source warm-region windows into the shells; author which zones get a second planting + the dates; region_notes copy + dual-voice siblings (`cause_beginner`); the `harvest_to_table` T2 ruling; the extreme-zone computation record (`gs_exemplar_finding_003`); `cornell_ext` zone-6 URL discovery.

**Process note.** Brainstorming surfaced the `second_planting` design partly via a Trevor-prompted reconciliation against the EXISTING cherry calendar UI (`PlantingCalendarCard` + `SuccessionCard`) -- the design feeds those built components clean data rather than inventing a render model. One Task-1 code-review fix added a re-run guard to the transform so it is safe across the pipeline (never clobbers already-filled data). Spec + plan committed to `docs/superpowers/`.

---

## 2026-06-05 -- checklist v1.5 PROMOTED (Step 3.5 Region shell build)

claude.ai authored the v1.5 integration from the Claude-Code-authored amendment (`checklist_v1_5_shell_build_amendment.md`); Claude Code reviewed + promoted. Review verdict: faithful, high-quality integration -- Step 3.5 matches the authored spec, reference-GS-crop pointer in §2, two-callsite admission/certification model in §1/Step 3.5/Step 11, Step 4 scope correctly narrowed to sourcing-into-shells, arc-at-a-glance + Appendix C (`gs_exemplar_finding_shell`) updated. **One mechanical bug fixed before promote:** line 138 had the `---` rule fused to the Step 4 heading (`---### Step 4`); split to restore heading rendering. **One minor nit left for claude.ai (not blocking):** the H1 still reads "v1.4 (Keystone)" per the on-disk provenance convention; the status block confirms v1.5.

Promote actions (plant-project is not git-tracked -- file ops only): `gold_standard_arc_checklist_v1_5.md` -> `05-methodology/current/` + `00-current/`; `v1_4_1` moved to `05-methodology/archive/` (first on-disk checklist archive -- the checklist only went on-disk at v1.4.1 on 2026-06-04); the `00-current` stale v1.4.1 removed. `northern_tier_reconciliation_checklist_v1_0.md` got a SUPERSEDED-by-v1.5-Step-3.5 banner and is retained in `current/` as the worked example Step 3.5 references (not deleted -- Step 3.5 points to it for the north build procedure). Governing-checklist pointer in CURRENT_STATE bumped v1.4.1 -> v1.5. **Owed: Trevor refreshes claude.ai project knowledge with the v1.5 file** (the disk<->PK sync step).

---

## 2026-06-05 -- post-flip working session: gate tooling shipped + M16 region-fill scoping + the REGION SHELL-BUILD decision

Same calendar day as the lettuce flip, after it landed. No dataset-content mutation (the gate runs are read-only; this entry + the CURRENT_STATE guardrail are the only writes). Three things happened: gate tooling was made permanent, the true M16 starting state was discovered, and a forward rule was decided.

**1. Gate tooling shipped crop-agnostic (Claude Code lane).** The Step 11 certification logic lived only in `/tmp` scratch + this transcript (WORRY 3). Ported to `tools/whole_crop_gate.py` (dual-voice structural walk + dash + °F + source-tier + anchoring 1A with the sibling-pair predicate + `bolting.*` inherit-class + two-field predicate) and `tools/verbatim_scan.py` (systematic copyright/verbatim n-gram scan, two-step fetch/scan, honest coverage reporting). Verified: reproduces lettuce = 0. Commits `3409af5` + two follow-ups.

**2. The M16 starting state -- discovered, and it is worse than first framed (Trevor caught two of my errors).** I initially called M16 a "regression." It is NOT. Live-data survey:
- The schema-2.7.5 "additive region scaffolding" created empty `regions{}` for ALL 123 crops. **Only lettuce-leaf is region-primary FILLED (1 of 123.)** 108 crops = 9 stub + 1 filled-looking northern_tier; 14 (mushrooms/microgreens/sprouts, no `archetype`) = all stub.
- A stub region literally reads `plantings:["PENDING CORRECTION PHASE ..."]`, `region_notes_*` null.
- **The 109-crop `northern_tier` is the OLD shallow Phase-A lift, NOT at the lettuce bar:** `track:None` (no succession), `region_notes_*` null, `resolution_method:static_precompute`, resolved_by_zone cells still carrying nested `plantings` (the pre-Pass-1b shape spec §3b-i forbids). So cherry/beefsteak are effectively **0 of 10 regions at-bar**, not 1 of 10.
- **My gate had the SAME blind spot twice -- the bolting class, in my own new tool.** First it counted PENDING stubs as non-violations (a stub string trips no prose check); fixed with an A2 region-fill check. Then it counted the stale northern_tier as "filled" (its `plantings[0]` is a dict); fixed by adding null-track + nested-cell-`plantings` (§3b-i) detection. Only a hand-check surfaced both. Lesson recorded: a gate only catches the shapes you teach it; treat any new gate's first clean run as suspect until a manual spot-check agrees. Honest counts now: lettuce 0 / cherry 42 / beefsteak 44 / carrot 292 (carrot near-greenfield).
- History clarified: when the lettuce shell was built, the empty containers (123) + a shallow northern_tier (109) propagated dataset-wide in tandem; the other 9 regions + all depth (succession hoist, Hawaii, warm/CA re-derivation, region_notes, Steps 6/7/8) were lettuce-ONLY. None of the M15 quality propagated.

**3. THE DECISION -- region shell-build rule (Trevor 2026-06-05).** Going forward, every crop's arc builds ALL 10 region cells to the lettuce bar. The northern_tier is NOT exempt for "having data" -- the shallow lift gets the full build like any other region. **Source model: north (`northern_tier`, zones 3-7) builds FROM the legacy cold `zones{}`** (promote + verify + hoist succession into region-constant `plantings[]`, strip nested cell plantings, re-stamp `static_precompute`->`zone_promoted_verified`); **warm/CA regions re-derive from T1** (zone data may be climate-contaminated -- the lettuce NA lesson). The gate now enforces the structural side. **Filing decision (this entry answers Trevor's "where does this go?"): the DECISION + reasoning lives here (history); the ACTIVE RULE is surfaced in CURRENT_STATE "Live locked decisions"; the EXECUTABLE STEP is finalized in the gold-standard arc checklist (claude.ai authors Step 0 -> Claude Code promotes -> Trevor refreshes PK).** Proposal vehicle staged: Downloads `proposal_step0_shell_admission_gate.md`.

**Also proposed (claude.ai judgment, not yet decided): archetype-driven biology checklist.** The gates catch unknown EXISTING fields (register-completeness HALT) but structurally CANNOT catch a needed-but-ABSENT field or wrong-but-present biology -- the bolting class generalized. Proposal: per `archetype` (warm_season_fruiting 31, cool_season_annual 27, deciduous_fruit_tree 14, ...), a list of biology phenomena that archetype MUST address (bolting/heat-pause; det-indet/blossom-drop/BER; chill-hours/bloom/cross-pollination; etc.), asserted present-or-ruled-N/A at Step 0. Needs Trevor's per-archetype lists (biology judgment).

**Riders still open (unchanged, claude.ai lane):** s11_finding_004.2 Option-A thermoinhibition-source package; Appendix-A inventory merge. Lettuce flip itself stands (`29b3aaa9`, commit `d307a24`).

---

## 2026-06-05 -- M15 lettuce Step 11 COMPLETE: write-back + clean gate + THE FLIP -- Claude Code lane -- LETTUCE IS THE FIRST FLIPPED ANCHOR

**SHA chain:** `37bfc12d…e7297` -> `6880ed37f4d8f1f521a5317e2f26f76dd51724f9713c5da8b90e4ddf133de208` (write-back of the staged closure work) -> `29b3aaa904a62487960c5dc53b4282538454076f696ffec039ac4ab87937801a` (THE FLIP). Session `m15_lettuce_step11_writeback_flip`. Preflight clean. Staged inputs promoted from Downloads first (CURRENT_STATE + STATE_HISTORY from the claude.ai closure session, verified superset + same SHA pin before overwrite).

**Trevor's three batched decisions (taken in-session, recorded here):**
1. **s11_finding_004.2 = OPTION A** -- keep the 77 to 95°F thermoinhibition figure; re-source the 2 uariz_ext slots to AZ1615 for the seasonal logic AND add a T1 source that actually states the range. The 4.2 write (AZ1615 entries + new source + prose attribution touch-up) rides the next claude.ai discovery session as one coherent unit. 004 stays open, non-blocking.
2. **S9-1: `bolting.triggers` = SP, no `_beginner` sibling** (beginner bolting story already carried by `bolting.note_beginner`). Register at the Appendix-A inventory merge.
3. **A-2: bolting prose = INHERIT-class** -- evidence lives at `tips_by_stage.bolting`; no independent `bolting.sources`/`anchoring_urls` required; the anchoring gate excludes `bolting.*` per-field requirements by definition.

**WRITE-BACK (`37bfc12d` -> `6880ed37`), all from the staged closure log, applied verbatim:** (1a) `harvest_ready_anchoring_urls` = 5 entries; (1b) `description_anchoring_urls` CREATED with 2 entries (cherry-precedent placement: directly after `description_sources`, which is lettuce's last crop key); (2) `soil.preferred_description_seasoned` replaced with the staged own-voice rewrite (kept the read-flow "and"); (4.1) `uga_ext` re-sourced to the C963 canonical permalink on both dead se_gulf slots; findings 001/002/003 -> resolved (`resolved_in: m15_lettuce_step11_writeback_flip`, resolutions recorded); 004 annotated with the 4.1-done + Option-A note, status held open. uariz_ext slots asserted UNTOUCHED (held per Option A). **Collateral audit PASS:** only `crops[33]` among 123; within lettuce exactly the staged paths (revert-and-compare proof); flip-guard still held at this point.

**GATE RE-RUN against `6880ed37` -- 0 VIOLATIONS:** §3 8/8 (incl. the organic-matter prose<->categorical check against the REWRITTEN soil field); dual-voice 130/0/0; dash 0; °F clean; source-tier 40 IDs all T1; **anchoring 1A WITH the sibling-pair predicate fix: 0 gaps** (the 7 new entries close s11_finding_001; C963 closes the 4.1 slots); two-field predicate 0 unresolved blockers. **Verbatim re-scan (86 distinct URLs from live data incl. the fresh-fetched C963 page, 69 text-compared): the NCSU/soil hit is GONE.** Independently measured residual longest run vs NCSU = **3 words ("or sandy loam")**. The only 2 remaining >=8-word hits are both adjudicated: the `.provenance.` backend false-positive and the s11_finding_003 benign ruling. 41 borderline hits unchanged, all benign-class.

**THE FLIP (`6880ed37` -> `29b3aaa9`):** separate minimal SHA-gated write, preconditions asserted in-script (0 blockers via the two-field predicate; flags held False/False/unverified at start). `launch_ready_core` -> **True**, `launch_ready_seasoned` -> **True**, `status` stays `"unverified"` (NOT gold_standard; status vocab is M16's call). Collateral audit: exactly the two booleans changed, nothing else, dataset-wide. **Lettuce is the FIRST of the 9 anchors flipped (1 of 9). Flip gate #1 only -- gates #2/#3/#4 (region read-layer, authoring-model, perennial schema) unaffected.**

**Step 11 is COMPLETE and the M15 lettuce gold-standard arc is CLOSED**, with two non-gating riders: (a) s11_finding_004 open pending the 4.2 Option-A package (claude.ai discovery); (b) Appendix-A registration doc produced, `[INVENTORY-MERGE]` items awaiting the canonical inventory merge (claude.ai; decisions 2+3 above unblock it).

**NEXT: M16 = cherry + beefsteak regression** (both carry launch_ready=True from 2026-05-22/25 but predate the arc rigor and owe the regression pass; must independently rediscover the 4 Appendix-C defects incl. gs_exemplar_finding_004 `cause_beginner`). Then the status-vocab decision + the pipeline/operating-model doc.

---

## 2026-06-05 -- M15 lettuce Step 11: findings-closure work STAGED (s11_finding_001/002/003 + 004-partial) + Appendix-A registration -- claude.ai lane -- no dataset write

**SHA: UNCHANGED `37bfc12d9f3607b17ff8c18c78a66bbb1f6da02088a1aefebd4eb505806e7297`.** Findings-closure + Appendix-A session in the claude.ai voice/IP + anchoring-URL-discovery lane. Preflight clean (uploaded JSON == `LATEST.txt` == `37bfc12d`; lettuce confirmed `crops[33]`, 55 keys, 123 crops; `harvest_ready_anchoring_urls:{}`, `description_anchoring_urls` absent, all 4 s11 findings `open` with the documented `blocks_launch` flags; flip-guard held). **No promote, no re-pin** -- everything below is STAGED for one Claude Code SHA-gated write-back; the dataset still carries all four findings as `open` until that apply. Deliverables: `m15_lettuce_step11_findings_closure_log.md` + `m15_lettuce_step11_appendix_a_registration.md`.

**JOB 1 -- s11_finding_001 (BLOCKING) -- RESOLVED IN STAGING; zero rejections, no replacement discovery.** All 7 sibling-named anchoring entries verified by FETCH + claim-type confirmation against live source pages (not catalog inference):
- `harvest_ready_anchoring_urls` (5): `iastate_ext` (growing-leafy-greens-iowa -- "Harvesting and Storage": outer-leaves-first, harvest-as-needed), `umn_ext` (growing-lettuce-endive-and-radicchio -- "Harvest" section), `ndsu_ext` (H1754 garden-table-leafy-greens -- looseleaf cut-1-in-above-crown regrow), `umd_ext` (growing-lettuce-home-garden -- "Harvesting lettuce" section), `uiuc_ext` (extension.illinois.edu/gardening/lettuce -- re-fetched + reused; harvest-readiness claim-type already in-tree at `tips_by_stage.harvest[1]` verified 2026-05-28, this was the lone "discovery" slot per the kickoff and resolved to reuse).
- `description_anchoring_urls` (NEW KEY, 2): `umd_ext` + `umn_ext` (same growing guides; both confirmed carrying general-description content -- "easy-to-grow, cool-season crop", type catalog, overview).
- All 5 harvest_ready candidates + both description candidates already appear elsewhere in the lettuce tree, so all are known-resolving lettuce pages; the work owed was the claim-type confirmation, now done. Apply-ready JSON staged (key-order mirrors each `*_sources` array). **No candidate failed -> no replacement discovery triggered -> no new rejections** (prior rejections -- umaine planting-chart, msu generic veg tip-sheet -- stay rejected).

**JOB 2 -- s11_finding_002 (BLOCKING) -- RESOLVED IN STAGING.** Own-voice rewrite of `soil.preferred_description_seasoned` (its opening sentence shared a measured 10-consecutive-word run + parallel skeleton with its own first-cited anchor `ncsu_ext`). Rewrote ONLY the opening sentence (+ one "and" inserted to fix a comma splice in sentence 2; droppable for a byte-minimal diff). Verification: longest shared run vs NCSU **10 -> 3 words** ("organic matter and", universal soil-science phrasing, benign-class); cross-checked vs the other soil sources -- UMN 2 words, NDSU 3 words, no fresh lift. Biology preserved per constraint (wide texture tolerance; fertile loam/sandy loam; high organic matter; water-holding capacity). Seasoned register held; `preferred_description_beginner` LEFT UNTOUCHED (scanned clean). §3 cross-field check 3 dependency intact (compost/organic-matter emphasis retained, consistent with `organic_matter_preference: high`). No temperature in the field (°F N/A); no `--`, no em dash. Exact staged string in the closure log.

**JOB 3 -- s11_finding_003 (non-blocking, low) -- RULED BENIGN, no edit.** `start_method.notes_beginner`'s 8-word run "when they are 3 to 4 weeks old" vs usu_ext is a generic transplant-age numeric convention (transplant-readiness window), appears in the seasoned sibling AND independently in UMD, sits in otherwise-independent prose -> benign-class, consistent with the 41 other 6-7-word benign hits. Claude Code stamps resolved with the ruling at write-back.

**JOB 4 -- s11_finding_004 (non-blocking, medium) -- HALF-RESOLVED.**
- **4.1 `uga_ext` (se_gulf `direct_sow[1]` + `harvest_start[0]`) -- STAGED.** The dead white-county PDF was a county-office copy of "Vegetable Gardening in Georgia" = UGA Circular C963 (already named in the slots' synthesis prose). Re-sourced to the live canonical permalink `https://fieldreport.caes.uga.edu/publications/C963/vegetable-gardening-in-georgia/` (verified live 2026-06-05; full review 2026-03-13; Westerfield & Linvill; its Vegetable Planting Chart carries the leaf-lettuce spring/fall dates + DTM the slots anchor). Cleanest same-institution resolution. NOTE: the dataset's OTHER live uga_ext slots use C943 + B577; no change proposed to those.
- **4.2 `uariz_ext` az1099 (low_desert_az `harvest_end[0]` + `resolved_by_zone.9.heat_pause`) -- NOT STAGED; FLAGGED FOR TREVOR.** AZ1099 ("Guidelines for Head Lettuce Production in Arizona," IPM Series No. 12, **1999**, Kerns et al.) is a commercial head-lettuce/IPM Yuma production handbook; its only indexed home is the legacy ACIS site (`cales.arizona.edu`), the `/crops/` path 404s on fetch and the `/crop/` path is Google-indexed but ALSO 404s on fetch -- **no fetch-verifiable AZ1099 URL exists**, and there is no migrated `extension.arizona.edu/publication/...` version. Substantive issue: AZ1099 anchors both the seasonal heat-exclusion logic AND the specific "77 to 95°F" thermoinhibition range. **AZ1615** (`extension.arizona.edu/sites/default/files/2024-08/az1615-2020.pdf` -- live, fetch-verified 2026-06-05, same institution, ALREADY cited + anchored in the low_desert_az tree; "Planting and Harvesting Calendar for Gardeners in Yuma County," reviewed 2020) confirms the seasonal logic -- lettuce row Planting Sep-Jan / Harvest Dec-Mar; cool season Sep-Feb, warm season Mar-Aug is for fruiting crops not lettuce -- supporting BOTH anchored claims (spring harvest closes ~Mar = `harvest_end` bolt_threshold_start-5; May-Aug outside lettuce season = `heat_pause` gap). **BUT AZ1615 does NOT state the 77-95°F thermoinhibition range.** Three options staged (long-term-quality order): **A (recommended)** anchor to AZ1615 for the seasonal logic + add a T1 source that states the thermoinhibition range, keep the figure; **B** re-source to AZ1615, soften the prose to drop the specific range; **C** chase a resolving AZ1099 mirror (lowest value). Re-sourcing a cited biological FIGURE is Trevor's call, not a silent swap -- so 4.2 is held and **s11_finding_004 stays open until 4.2 is decided + applied** (4.1 alone does not close it). Because 004 is non-blocking, it does NOT gate flip #1; but the figure should be settled before lettuce is called done.

**OPTIONAL SWEEP (the 18 verbatim-scan-uncovered URLs) -- partial, not flip-gating.** Pages reachable + read this session (the 5 growing guides above, the C963 permalink, the AZ1615 PDF) spot-checked against the relevant dataset prose: no new verbatim hits beyond the s11_finding_002 NCSU case. The remaining uncovered URLs (other planting-chart PDFs, the bot-blocked ucd_postharvest romaine page, the JS-rendered canr.msu.edu pages) were not all reachable here and still ride 004 for a later environment. Low lift-risk per the kickoff.

**JOB 5 -- Appendix-A registration -- PRODUCED (parallelizable, not flip-gating).** `m15_lettuce_step11_appendix_a_registration.md`, in the Steps-7/8 registration-delta format, grounded in live `crops[33]` structure + the on-disk findings docs:
- **A-1 `bolting.triggers` (FINDING S9-1):** live value `"Heat above 75°F, long days (>14 hours), stress"`. Recommended ruling **SP (seasoned-only)** -- the beginner-facing bolting explanation is already carried by `bolting.note_beginner`; a `_beginner` sibling would near-duplicate it. **DECISION OWED** (SP vs CP-with-new-sibling). Until ruled, S9-1 stays open (not a `blocks_launch` item).
- **A-2 bolting source-set ownership:** live `bolting` block has no own `sources`/`anchoring_urls`; evidence lives on `tips_by_stage.bolting[]`. Recommended ruling **INHERIT** -> `bolting.*` register-prose is inherit-class, **excluded from the per-field anchoring requirement** (register this exclusion alongside the s11_finding_001 sibling-pair predicate fix so the gate doesn't later re-flag `bolting.note_*`). **DECISION OWED** (confirm INHERIT vs require independent set).
- **A-3 resolution-method tokens:** `se_gulf_month_resolution` (the item the kickoff named) + `az_low_desert_month_resolution`, `table_13_2_month_resolution`, `zone_promoted_verified` are backend classification values (parallel to `classification`) -> register as backend-exempt; add to the Step-9 exempt key registry. `[INVENTORY-MERGE]`.
- **B register-conversion rulings:** enumerated **30 CP fields + the SP set** from live presence/absence of `_beginner` siblings (kickoff said "~26"); `thinning.tip` §7 flag marked confirmed-CP; `region_notes` corrected to CP (both siblings present on se_gulf). `[INVENTORY-MERGE]` for the count reconciliation + dedup against existing canonical rows.
- **C succession strings:** `northern_tier.resolved_by_zone.{3-7}.succession_{spring,fall}` (10 strings) registered as user-facing materialized date-lists, kept-by-evidence (§3b-i / NA-3h), not register-split (governed by `track` semantics); contain no temperature/`--`.
- **D 6b container key-pairs:** the 7 `container_notes` register pairs ruled CP; the structural container keys listed as register-exempt.
- **E Steps 7/8 `_beginner` keys (97):** cross-ref only (registered in the Steps-7/8 delta; 130 total, 5 correctly-null).
- **F region `tip_overrides` leaf-keys:** `tip_overrides.bolting[].text_{seasoned,beginner}` ×4 cells ruled CP; confirmed shape carries own `sources`+`anchoring_urls` (all-mode-evidence-class -- DO require per-source URLs, unlike A-2 bolting prose); Hawaii action-divergence override noted as legitimate.
- Items needing the canonical Appendix A / `register_bearing_field_inventory_v1_0.md` (project knowledge, NOT on local disk) are marked `[INVENTORY-MERGE]` -- reconcile wording + dedup at merge; do not author against the missing inventory (flag the doc-lag).

**FLIP-GUARD HELD; FLIP STILL BLOCKED until apply.** `(launch_ready_core=False, launch_ready_seasoned=False, status=unverified)` unchanged. s11_finding_001 + 002 are resolved-in-staging but still `open` in `verification_status.open_findings` (the staged log does not mutate the dataset); the two-field predicate (`blocks_launch AND status != "resolved"`) therefore still returns 2 unresolved blockers and correctly blocks flip #1 until Claude Code writes back + flips them. **"Resolved in staging" is NOT "the dataset says resolved."**

**NEXT.** (1) **Trevor:** 2 batched decisions -- s11_finding_004.2 (A/B/C) + Appendix-A A-1/A-2 (`bolting.triggers` class + bolting source ownership); neither gates the flip, both settle before the bot pipeline replicates these shapes. (2) **Claude Code (short session):** SHA-gated write-back against `37bfc12d` of staged 1a/1b + the soil rewrite + the uga C963 re-source (HOLD uariz 4.2 pending decision 1); flip 001/002/003 -> resolved (004 -> resolved only after 4.2 lands); whole-crop gate re-run (incl. the sibling-pair anchoring predicate + re-scan the rewritten soil field); **THE FLIP on 0 violations** (`launch_ready_core` + `launch_ready_seasoned` -> True; `status` stays unverified-or-successor, NOT gold_standard until M16 settles status vocab); then close ritual (promote, re-pin `LATEST.txt`, regenerate CURRENT_STATE, append here, sync `00-current/`, commit). Then **M16 = cherry + beefsteak regression** (0 of 9 anchors flipped today).

---

## 2026-06-05 -- M15 lettuce Step 11: the 45-slot atomic apply + FIRST whole-crop gate re-run -- Claude Code lane -- FLIP BLOCKED by 2 real findings

**SHA chain:** `df4d24c723b19d8883db8c46f05b981659ef74e359e906a491b7cb9f28bafa3c` -> `cdcbf175c3c7b466234433e543f453206af8732eac0d811e79a9405197476142` (the 45-slot apply) -> `37bfc12d9f3607b17ff8c18c78a66bbb1f6da02088a1aefebd4eb505806e7297` (gate-findings registration). Session `m15_lettuce_step11_apply_and_gate`. Preflight clean (disk JSON == `LATEST.txt` == `df4d24c7`).

**THE APPLY -- DONE.** One atomic SHA-gated write per the CURRENT_STATE discipline block (scratch copy, minified `separators=(',',':')` + `ensure_ascii=False` which round-trips the on-disk byte form exactly, start-SHA gate, end-SHA re-pin). All URL values from the two staged evidence records (`m15_step11_backfill_session_findings.md` crop-field 21 + se_gulf 13; `m15_northern_tier_url_discovery_log.md` northern_tier); `verified: 2026-06-05` on every new slot (apply-session date per the discipline block, overriding the findings doc's 2026-06-04 stamp). **Unit-count note for the record:** the kickoff's "45 slots" is a mixed unit -- 21 crop-field source-entries + 13 se_gulf source-entries + 11 northern_tier LEAVES. In uniform units the apply set is **22 leaves carrying 68 `{url, verified}` entries** (21 + 13 + 34; the northern_tier zone cells carry 5 sources each). The apply script's own leaf-counter caught this (asserted 45, counted 22) before any write; STATE_HISTORY RULING 1's "22 leaves" was the correct uniform count all along. **Audits all PASS:** only `crops[33]` changed among 123; all top-level non-crop keys byte-identical; within lettuce ONLY the 22 target `anchoring_urls` dicts changed; both segment-parent `plantings[0].anchoring_urls:{}` slots (northern_tier + se_gulf) asserted untouched (apply is 45-as-staged, not 46); no existing entry overwritten (insert-only assert). `uga_b577`/`uga_calendar`/`uf_ifas_*` URLs copied exactly from the catalog pins (incl. the lowercase `b577plantingchart.pdf` casing); `uwi_hort` got the rotation PDF on the `rotation` leaf and the salad-greens page on northern_tier z5 (claim-type split per the evidence log).

**WHOLE-CROP GATE RE-RUN (first run against live post-apply data) -- every component PASS except one new finding class:**
- §3 cross-field: **8/8 PASS** (re-derived programmatically from live data, same checks as Step 10).
- Dual-voice coverage (v2 STRUCTURAL-walk logic rebuilt from the Steps-7/8 coverage report -- the script itself lives only in the claude.ai transcript; WORRY 3 "ship the gate into tools/" stands): **denominator 130 populated, missing_keys 0, null_values 0** -- identical to the Steps-7/8 run. Bucket-boundary deltas vs that report (SP 161 vs 158, ruled-empty 11 vs 10, out-of-scope 3 vs 7) are companions-§5 classification edges, all deferred-by-design either way.
- Dash gate: lettuce user-facing `--` = **0**. °F sole form: **0** non-canonical. (First-pass classifier false-positives -- `zone_8_presence`/`zone_10_desert_fold` resolution records + `synthesis_note_seasoned` -- are BACKEND per the Step 9 STEP-0 layer derivation; verified against that record, not hand-waved.)
- Source-tier: 40 distinct source IDs cited; all catalogued, all T1. Roster-completeness gate (`tools/register_completeness_gate.py`): **PASS dataset-wide.**
- Anchoring completeness (1A layer-scoped): all 22 applied leaves CLOSE. **NEW FINDING s11_finding_001 (blocks_launch=true):** 2 claim-bearing leaves the RULING-1 predicate scan structurally missed because their sources arrays use SIBLING NAMING, not a `sources` key: `harvest_ready_sources` (5 IDs) with `harvest_ready_anchoring_urls:{}` EMPTY, and `description_sources` (2 IDs) with NO `description_anchoring_urls` key at all. Both provers (cherry + beefsteak) anchor both slots 5/5 and 4/4 -- the in-tree convention requires them. 6 of the 7 entries can REUSE today's fetch-verified URLs (claim-type check owed); `uiuc_ext` needs genuine discovery (catalog pins a domain root; no URL verified anywhere this arc). **Pipeline lesson: the anchoring-gate predicate must match `*_sources`/`*_anchoring_urls` sibling pairs, not just literal `sources` keys -- encode in the schema-gate addendum + checklist amendment 8.**

**COPYRIGHT/VERBATIM SCAN -- FIRST SYSTEMATIC RUN (the flip-blocking criterion the kickoff owed).** Method: all 86 distinct anchoring URLs fetched (81 HTTP-200); text extracted from 68; 294 user-facing prose strings (same layer classification as the dash gate) compared by shared-word-run (>=8 words = hard flag; 6-7 = borderline).
- **NEW FINDING s11_finding_002 (blocks_launch=true, the real catch):** `soil.preferred_description_seasoned` shares a **10-word verbatim run + parallel sentence skeleton** with its own FIRST-CITED anchor `ncsu_ext` (content.ces.ncsu.edu/lettuce): "range of well-drained soils but performs best on fertile ... organic matter ... good water-holding capacity". **Step 10's §4 spot-check examined this exact field against UMN and passed it ("fully independent wording") -- the lift is from the source the spot-check did not fetch.** The systematic scan caught what spot-checking could not; this is the gate WORKING, exactly as the kickoff framed it. Own-voice rewrite = claude.ai voice lane.
- s11_finding_003 (non-blocking, recommended benign, logged for voice-lane judgment): `start_method.notes_beginner` trips the 8-word threshold on "when they are 3 to 4 weeks old" vs usu_ext -- a generic transplant-age convention inside otherwise independent prose.
- 41 borderline 6-7-word hits all classify as citations/attributions ("UC Master Gardeners of Sacramento County", "Master Gardener Handbook Table 13.2"), universal facts ("lettuce is a cool season crop"), or numeric conventions ("between 6.0 and 6.8"). One scan hit dismissed as false-positive WITH grounds: `companions.*.provenance.reason` is a `.provenance.` path = BACKEND per the Step 9 derivation (the "match" was the source page's title cited in the provenance note).
- **Coverage stated honestly: 18 of 86 URLs not text-compared** (11 PDFs -- no local extractor; 3 bot-blocked 403 incl. today's claude.ai-verified `ucd_postharvest` page; 2 JS-rendered canr.msu.edu pages; 2 dead). The PDF set is mostly planting-chart tables (low prose-lift risk) but the uncovered list rides s11_finding_004 for a claude.ai-environment sweep.
- **NEW FINDING s11_finding_004 (non-blocking, medium):** the fetch pass found 2 anchored URLs HTTP-404 (retry-confirmed): `uga_ext` white-county vegetable-gardening-in-georgia.PDF (anchored at se_gulf `direct_sow[1]` + `harvest_start[0]`) and `uariz_ext` az1099 (anchored at low_desert_az `harvest_end[0]` + `resolved_by_zone.9.heat_pause`). Verified stamps stale; re-source = claude.ai lane.

**STEP 5.5 RE-WALK + NA-3h RECONCILIATION -- PASS, no mutation.** Verified live: ZERO rule-bearing succession structures inside any `resolved_by_zone` (programmatic walk over all 10 regions); the succession RULE lives once in region-constant `northern_tier.plantings[]` tracks `[beginner, succession, succession]` (spec-B / §3b-i). northern_tier-vs-warm shape difference adjudicated as PRINCIPLED: the warm cells carry no succession (single `beginner` arm + `heat_pause`); northern_tier carries the 2 rule arms + per-zone MATERIALIZED `succession_spring`/`succession_fall` date-strings -- kept per Pass 1b's regeneration-equality evidence (z4-7 irreproducible from the published rule; collapse is lossy). Cold-band coherence spot-proof: heat_pause scales with warmth (z3/z4 none -- pauseless summers; z5 Jul; z6 Jul-Aug; z7 Jun-Aug); winters declared `cold_pause`; all 10 succession strings present. **NA-3h stays CLOSED (Pass 1b), now re-verified at Step 11.**

**THE FLIP DID NOT HAPPEN -- and this time the gate, not the narration, is what blocked it.** Flags held `(launch_ready_core=False, launch_ready_seasoned=False, status=unverified)`. 2 blocking findings (s11_finding_001 + s11_finding_002) registered in `verification_status.open_findings` with `blocks_launch:true` so the two-field predicate blocks any future premature flip mechanically. The 45-slot apply itself is COMPLETE and SOUND; what blocks is net-new gate yield, which is what a first systematic run is for.

**bf_003 (SHA-lineage doc-lag) -- RESOLVED by this regeneration:** CURRENT_STATE now pins the truthful chain (`37bfc12d` <- `cdcbf175` <- `df4d24c7` <- `da4b8bc5`); the stale `da4b8bc5`-as-head lineage is gone.

**NEXT:** (1) claude.ai lane -- close s11_finding_001 (6 reuse claim-type confirmations + `uiuc_ext` discovery) + s11_finding_002 (own-voice soil rewrite) (+ rule on 003, re-source 004, optionally sweep the 18 uncovered URLs); Appendix-A registration still parallelizable and owed. (2) Short Claude Code session -- write-back, whole-crop gate re-run, **flip on 0 violations**. Then M16 = cherry + beefsteak regression.

---

## 2026-06-05 -- M15 lettuce Step 11: northern_tier 11-leaf URL DISCOVERY complete; 45-slot apply STAGED (no dataset write)

**SHA: UNCHANGED `df4d24c723b19d8883db8c46f05b981659ef74e359e906a491b7cb9f28bafa3c`.** This session was URL discovery + fetch-verification only (claude.ai lane). No promote, no re-pin. Preflight confirmed uploaded JSON == `LATEST.txt` == `df4d24c7`.

**What ran:** the anchoring-URL discovery + fetch-verify for the full apply set. Outcome: **all 45 slots verified + staged for one atomic SHA-gated write** (the write is a separate Claude Code session). Evidence record presented to Trevor: `m15_northern_tier_url_discovery_log.md`.

**The 11 northern_tier leaves (THE job this session) -- DISCOVERY, not promotion.** Closing the S10-3 correction: the cold legacy `zones[3–7]` carry empty `anchoring_urls:{}`, so there was nothing to promote up; the per-source evidence URLs had to be discovered + fetch-confirmed fresh. Each landed a lettuce-bearing, claim-type-matched T1 URL:
- `msu_ext` -> MSU Lower Peninsula Garden Calendar (`canr.msu.edu/resources/garden_planning_calendar`)
- `ndsu_ext` -> **H1754 "From Garden to Table: Leafy Greens!"** (`ndsu.edu/agriculture/extension/publications/garden-table-leafy-greens`) -- a home-garden Extension pub (McGinnis, NDSU Horticulture), **swapped in per Trevor's request from the originally-verified children's pub FN1372.** Same zone fit (3–4), stronger venue.
- `msu_bozeman` -> Montana Grown fact sheet; `umaine_ext` -> "Keep Your Garden Growing"; `psu_ext` -> "Seeds or Transplants?"; `vce_426_331` -> Pub 426-331 (per-zone lettuce tables); `cornell_ext` -> Warren Co. fall-planting frost table (matched to its harvest_end claim type); `ncsu_ext` -> AG-756-01 central-NC calendar; `uwi_hort` -> "Grow Your Own Salad Greens" (the planting-DATE page, NOT the in-tree rotation PDF).
- Reuse URLs re-confirmed live + lettuce-bearing: `umn_ext`, `umd_ext`, `iastate_ext`, `clemson_hgic`, `uga_c1258_fall`.

**Two candidates REJECTED + logged (so the bot pipeline doesn't re-try them):** (1) the umaine_ext planting-chart -- a genuine dated T1 chart, but **lettuce is not in its crop table** (asparagus→turnip, no lettuce row); used "Keep Your Garden Growing" instead. (2) the msu_ext generic veg tip-sheet -- "plant cool season vegetables," no lettuce-specific date; used the Garden Planning Calendar instead. **Lesson reinforced: a page that looks like the right anchor (right institution, right topic, dated) still fails if it does not carry the SPECIFIC claim -- this is the bf_001 "looks anchored but doesn't carry the claim" failure caught at the source level, before it could propagate to ~114 crops.**

**Three scope/canonicalization calls Trevor surfaced; resolved:**
- **Segment-parent slot -- NO action.** `plantings[0].anchoring_urls:{}` (the parent-level slot alongside the 6 segment-objects) is OUT of the apply set. In-tree precedent settled it: 5 of 8 already-verified regions (incl. se_gulf, northern_tier's sibling) leave that parent slot empty `{}` while fully populated at the segment level -> the whole-crop gate does NOT require it, and filling northern_tier's would make it inconsistent with se_gulf. Apply set stays 45. (Filed a side-finding for the pre-buildout audit queue: the 3-populate/5-empty split across verified regions is itself an inconsistency to normalize before the pipeline scales -- NOT a lettuce-arc item.)
- **`ndsu_ext` venue -- UPGRADED** to H1754 (above), per Trevor's "prefer it not be a children's publication."
- **`umn_ext` URL -- canonical written.** Store `extension.umn.edu/vegetables/growing-lettuce-endive-and-radicchio` (the kickoff's `/yard-and-garden/vegetables/...` form 301-redirects to it). Rule: store the URL that actually resolves + serves the lettuce content, not the redirecting form (avoids an extra hop + future-breakage risk). Trevor deferred the call; Claude decided.

**Flip-guard held:** `(launch_ready_core=False, launch_ready_seasoned=False, status=unverified)`. The flip is NOT done and is NOT yet eligible.

**Lane discipline reaffirmed (corrected a sequencing assumption mid-session):** the whole-crop gate re-run is a **Claude Code** job (it runs `dual_voice_gate_v2.py` against live data -- structural), NOT a "come back to claude.ai" job. Claude Code runs, in sequence: the 45-slot apply -> whole-crop gate re-run (incl. the copyright/verbatim criterion) -> succession reconciliation -> flip on 0 violations. claude.ai retains only Appendix-A registration + the bolting doc-note, which are parallelizable and NOT apply-blocked.

**⚠️ Copyright/verbatim scan status -- explicitly NOT done.** Two senses were distinguished so they don't get conflated: (a) verbatim-hygiene on THIS session's extracted source content -- clean by construction (paraphrase + short attributed fragments; dates/temps are facts, not copyrightable expression); (b) the whole-crop copyright/verbatim scan as a **flip-blocking gate criterion** over lettuce's OWN user-facing prose -- this is the one the kickoff lists as owed, it has NOT run, and it lives in the Claude Code whole-crop gate re-run. "Sources are clean" must NOT be read as "the gate criterion passed."

**Doc-lag note:** the SHA-lineage flag (bf_003) remains open -- CURRENT_STATE predecessor chains historically ran through `da4b8bc5` while `LATEST.txt`/kickoffs name `df4d24c7`; dataset is clean (preflight confirmed); reconcile which SHA the canonical CURRENT_STATE pins when convenient.

---

## 2026-06-04 -- M15 lettuce Step 11 (IN PROGRESS): S10-5 classifier-jargon strip -- claude.ai lane
- **SHA:** `da4b8bc51dc935b0efce115a6405cb7fb82e3e927e520767757fff2e8062dec2` -> `df4d24c723b19d8883db8c46f05b981659ef74e359e906a491b7cb9f28bafa3c`. Session `m15_lettuce_step11_s10_5_basis_jargon_strip`.
- **Lane:** claude.ai (voice/IP judgment). Apply built + run in-session (SHA-gate, value-preservation, collateral audit, flip guard, minify, post-write re-read). Bundle `Phase 3 Lettuce M15-Step11-S10-5 Deliverables.zip` shipped; **Claude Code reviews + promotes + runs the close ritual.** This is the FIRST act of Step 11; the 22-leaf anchoring backfill + whole-crop re-run are still owed before the flip.
- **THE FLIP DID NOT HAPPEN.** Flags held `(launch_ready_core=False, launch_ready_seasoned=False, status=unverified)`. Step 11's last act (per-crop flip, gate #1 only) is gated on 0 violations across every gate, which still requires the backfill + whole-crop re-run below. Recorded here because the human-narration layer is where the premature-flip class slips (the mechanical gate is the real defense) -- "Step 11 is the last step" is NOT "lettuce is done."
- **RULING 1 -- anchoring gate is LAYER-SCOPED (1A, Trevor 2026-06-04).** A schema-accurate scan of the whole crop against the literal `launch_ready_core` predicate ("every field with non-empty `sources` has matching `anchoring_urls`, one entry per source ID") returned **90** violating leaves -- an order of magnitude beyond the ~5 the S10 registrations named (S10-1..S10-4 + ex-6c-3). The 83-leaf gap was NOT missed work; it was an unresolved conflict between two authorities: the schema-spec text (keys the gate to every node with non-empty `sources` -> 90) vs CURRENT_STATE + the Step-10 "scope by layer" standing lesson (legacy `zones{}` + `regions{}` rollups are expected shape, not violations -> far fewer). **Evidence resolving it:** the warm `regions{}` cells (authored most recently, at-bar -- NA-3b on) have ZERO child-leaf gaps (ca_interior 9/9, warm_arid 7/7, low_desert_az 6/6, fl_peninsula 7/7, hawaii_tropical 5/5); only their ROOT `sources` rollups are empty -- proving the author convention is *anchor at the claim leaf, not the summary node*. And `zones{}` is a superseded layer the app dynamic path never reads (region-primary spec; Phase-C deletion path). **Trevor chose 1A:** the gate counts claim-bearing leaves only; the legacy `zones{}` layer (58 leaves) + the 10 `regions{}` root rollup `sources` arrays are EXCLUDED BY DEFINITION (not deleted). Real backfill = **22 leaves** (7 crop-field + 15 region-child; the region-child gaps isolate EXACTLY to northern_tier (11) + se_gulf (4) = S10-3 + S10-4). To be encoded as checklist amendment (8) + a schema-gate addendum so the bot pipeline runs the same gate. **The 22-leaf backfill is the NEXT session.**
- **RULING 2 -- S10-5 basis_seasoned jargon (APPLIED this session).** Resolves the precedent-setting fork the Step-10 entry flagged ("Option A backend vs Option B rewrite -- Trevor's call"). Reading the strings showed each was doing TWO jobs welded together: (1) real seasoned content (bolting threshold ~75 degrees F, regional summer temps, germination inhibition) and (2) a classifier audit trail ("classified via the heat-pause legibility path"/"heat-pause path"; "cold-zone fall heat floor"). Option A would have hidden the real seasoned content; the original Option B undersold the fix. **Trevor's decision: strip the classifier vocabulary, keep the biology + numbers + citations** (the classifier's reasoning already lives structurally in the sibling `classification` key; the path-name vocabulary belongs on the methodology page).
- **THE STRIP -- 16 strings, lettuce only.**
  - 15 x `regions[*].resolved_by_zone[*].heat_pause.basis_seasoned`: **12** warm/CA/desert/FL cells clean-stripped the "Classified via the heat-pause legibility path (warm zones are not governed by the cold-zone fall heat floor)." sentence; **3** northern_tier cells (z5/6/7) had the jargon sentence REPLACED with the jargon-free dual-gap fact -- "Not a frost gap but a summer heat exclusion; the warm edge of this cold region carries both a midsummer heat gap and a winter cold pause." (option (ii), chosen to preserve the one genuine seasoned insight tangled in the jargon: the warm edge of a cold region has both a summer gap AND a winter gap).
  - 1 x `succession_policy.count_note_seasoned`: "the cold-zone fall heat floor" -> "the summer-heat limit on when fall sowing can begin." Found by the strict no-jargon assertion (16th instance of the same coined phrase, different field); folded into the same ruling with Trevor's go-ahead. ("bolt-driven spring close" left intact -- "bolt" is real horticultural vocabulary, not internal shorthand.)
- **REGISTER RULING (S10-5):** `basis_seasoned` is **seasoned-only (SP); NO `basis_beginner` sibling owed** (the beginner calendar shows the summer gap as "no planting"; it does not explain the thermoinhibition mechanism). Sets the basis-prose voice standard for cherry/beefsteak/carrot: user-facing biology in the prose; the classifier's reasoning in the structural `classification` key beside it.
- **APPLY AUDITS (all PASS).** SHA gate `da4b8bc5`; **no residual jargon strings anywhere in lettuce** (asserted on "legibility path"/"heat-pause path"/"cold-zone fall heat floor"); **no `basis_beginner` introduced** (SP confirmed); sources/anchoring_urls/months/classification **unchanged on every touched heat_pause block**; succession_policy sibling keys intact; **collateral** -- only `crops[33]` changed, all 122 other crops byte-identical, all top-level non-crop keys byte-identical; **within-lettuce diff = exactly 16 leaves** (15 `*.basis_seasoned` + 1 `count_note_seasoned`), nothing else. Minified (`separators=(',',':')`, `ensure_ascii=False`). Flip-guard held.
- **DOC-LAG CORRECTED.** S10-5 was registered (Step 10 entry + kickoff) as "12 strings"; live count was **15** `basis_seasoned` (+1 `count_note_seasoned` = 16 edited). The "12" predated a structural walk.
- **CARRIED -- ex-6c-3 / FINDING 6c-3 confirmed against live data:** `rotation` cites `umn_ext` + `uwi_hort` but anchors `umn_ext` only; `uwi_hort` URL missing; NO `pnw_handbook` anywhere on `rotation` (that worry is stale). Single leaf, inside the crop-field 7 -> backfills next session.
- **NEXT (within Step 11):** the 22-leaf anchoring-URL backfill (1A scope; fetch + `{url, verified}` per source ID; mostly promote-from-in-tree since the cold legacy `zones[3-7]` hold trustworthy URLs and the succession arms are already anchored) -> whole-crop gate re-run (§3 cross-field; `dual_voice_gate_v2.py`; lettuce user-facing `--`=0; `°F` sole; source-tier; anchoring completeness post-backfill) -> Step 5.5 re-walk + northern_tier-vs-warm succession-shape reconciliation (NA-3h; region-primary §3b-i forbids rule-bearing succession in `resolved_by_zone`, likely collapse northern_tier to the generative rule) -> Appendix-A registration -> **then, ONLY on 0 violations, the per-crop flip** (gate #1 only; `status` stays `unverified`-or-successor, do NOT set `gold_standard` until M16 settles post-arc status vocab). **Anchor count today: 0 of 9 flipped; cherry + beefsteak built but NOT flipped (owe M16 regression).**


## 2026-06-04 -- M15 lettuce Step 10 (cross-field consistency + copyright/attribution audit) -- claude.ai lane -- STATE-ONLY (no dataset change)
- **SHA:** `da4b8bc51dc935b0efce115a6405cb7fb82e3e927e520767757fff2e8062dec2` -> **UNCHANGED.** No dataset mutation; canonical and `LATEST.txt` stay at the Step-9 pin (`LATEST.txt` session line still reads `m15_lettuce_step9_dash_gate_temp_normalization` because Step 10 changed no bytes).
- **Lane:** claude.ai (voice/IP judgment). §3 mechanical + source-tier/anchoring hygiene also run here.
- **§3 cross-field consistency: 8/8 PASS** against live lettuce (`crops[33]`), scope derived from the dataset not the kickoff's named list. ph range-wrap `[6.0,6.8]⊆[6.0,7.0]`; drainage `well_draining`↔core `[loam,sandy_loam]` (no clay in preferred); OM `high`↔prose; `container_ok=true`↔`min_pot_gallons=1`; `overwintering.applicable=null` (scaffold) → approach-null vacuous; `succession_policy.suitable=true`↔cold 3 / warm 1 plantings; `archetype=cool_season_annual`↔`Leafy Greens`/`crop`; flip-implication vacuous (both False).
- **Source-tier: PASS.** Every anchored `sources` array uses T1 institutions; no T2 cited as evidence.
- **Anchoring-URL hygiene: re-scoped BY LAYER.** A naive whole-crop walk produced 80 "missing" hits; correct reading: (a) legacy `zones{}` 58 empty-`{}` = the deferred re-sourcing layer (finding_001 family), NOT a Step 10 gap; (b) `regions{}` anchors per-arm at `plantings[].direct_sow/harvest_*` + `resolved_by_zone.N.heat_pause` -- region-root roll-up + container levels legitimately carry no URLs; (c) populated blocks clean: 0 stale (>5yr), 0 orphan keys, 0 empty URLs. **Genuine completeness gaps registered:** S10-1 `storage` (5 URLs, empty `{}`); S10-2 `failure_diagnostics[0–4]` (3 URLs ×5, empty `{}`); S10-3 `regions.northern_tier` 11 blocks (lift-from-legacy inherited the no-URL shape); S10-4 `se_gulf` 2 harvest arms (umd/umn timing vs uga anchor -- confirm convention or backfill). These are EVIDENCE/verification work for Step 11's gate, NOT Step 10 rewrites.
- **§4 copyright/attribution: CLEAN, zero rewrites.** External near-duplication checks fetched + compared: soil_seasoned vs UMN (proper paraphrase); ph_seasoned vs UMN/Cornell/UW (own-voice; pH band is uncopyrightable fact); bolting "sesquiterpene lactones" vs Fine Gardening/UC ANR/peer-review (distinct phrasing, well-attested fact); storage (own-voice). Internal beginner/seasoned scan (130 pairs): 16 byte-identical are all micro-strings + 2 ruled `cause_beginner` transforms; substantive prose independently phrased (region notes 0.006–0.013). `companions` quotes = own-coined scare-quotes; `heat_pause.basis_seasoned` parentheticals = attribution not quotation.
- **NEW finding to Trevor (precedent-setting fork): S10-5.** All 12 `regions[*].resolved_by_zone[*].heat_pause.basis_seasoned` carry internal jargon ("classified via the heat-pause legibility path (warm zones are not governed by the cold-zone fall heat floor)"). Flagged not fixed: visibility is undetermined (field reads as backend machinery; 1A visibility map predates these cells). **Option A** (recommended): mark backend/SN in the visibility map, no edit. **Option B:** rewrite all 12 to drop the jargon (small gated mutation; sets the basis-prose voice standard for cherry/beefsteak/carrot). Trevor's call.
- **Carried-item movement.** 6c-3 partially resolved (no `pnw_handbook` on `rotation` -- worry appears stale; live gap is the missing `uwi_hort` URL, folded into the S10 backfill at Step 11). S9-1 untouched (Step 11 Appendix-A). Doc note logged: `bolting.note_*`/`prevention_*` carry no own `sources` -- Appendix-A coverage question for Step 11, not an IP defect.
- **Flip-guard:** `(False, False, unverified)` held. **Next: Step 11** (whole-crop validation; closes S10-1..S10-4 backfills + applies the S10-5 ruling + Appendix-A registration; the per-crop flip is its last act on 0 violations).
- **Close ritual (state-only):** regenerate `CURRENT_STATE.md`, append this entry at the top of the stack, sync `00-current/`, commit (+ push). No promote (no bytes changed).

## 2026-06-04 -- M15 lettuce Step 9 (dash gate + dataset-wide temperature normalization)
- **SHA:** `7e9eeceb` -> `da4b8bc51dc935b0efce115a6405cb7fb82e3e927e520767757fff2e8062dec2`. Session `m15_lettuce_step9_dash_gate_temp_normalization`.
- **Lane:** Claude Code (mechanical/structural normalization). NO biology, NO new copy authored.
- **TREVOR DECISION (reverses FINDING 6c-4):** canonical user-facing temperature form flipped from spelled-out "degrees F" to the **degree symbol `°F`** (e.g. `75°F`). 6c-4 had chosen "degrees F" only because it was the dominant existing form (~2,501 vs 389 symbol + ~875 bare-F) and logged it "Trevor-ratified" -- but that was normalize-to-majority, not preference. Trevor prefers the symbol (shorter; already renders on site; screen readers announce `°` as "degrees" so accessibility favors it). So the sweep converts INTO `°F`, not away from it. See memory `temp_form_degree_symbol`.
- **TREVOR DECISION (reshapes F-6d-2 scope):** dash `--` resolution is **full per-sense** (comma/colon/semicolon judged per instance), which is editorial, not a blind sweep. So dash work is **lettuce-only this session** (the crop in flight); the other 122 crops' `--` defer to their own arcs. **F-6d-2 stays OPEN** as "execute per-crop," NOT closed this session. Temperature normalization is genuinely mechanical, so it ran **dataset-wide** and **closes FINDING 6c-4**.
- **STEP 0 denominator (structural walk, NOT the kickoff roster).** The checklist/register-inventory authority docs live only in claude.ai project knowledge (not on local disk), so the user-facing field set was derived structurally by walking the dataset and classifying every token-carrying leaf. BACKEND/exempt = any sig containing `synthesis_note`/`design_note`/`anchoring_urls`/`plantings_provenance`/`uscrn_validation`/`verification_status`/`sources_summary`/`source_catalog`/`region_source_map`/`previous_remediations`/`sources_schema_note`/`zone_8_presence`/`zone_10_desert_fold`/`verification_log_ref`/`.provenance.`, plus the `PENDING`/`RESOLVED` region `plantings.[]` scaffold strings. Everything else carrying a token = user-facing (incl. `zones.*.notes`/`zone_notes`, region cell prose, `heat_pause.basis_seasoned`, top-level `ph_education`). **Reconciliation proof:** 2,501 total "degrees F" = 198 user-facing + 2,303 backend (synthesis/design source-reasoning), confirming the backend exclusion is right. No spelled-out "Fahrenheit" anywhere.
- **FINDING S9-1 (Appendix-A gap, flagged not guessed):** `bolting.triggers` (4 crops) is a bare field with NO Appendix-A ruling. Per the standing safeguard it is a finding, not a silent guess; treated as user-facing this pass (it carries `--`/temp prose a grower could see). Needs a ruling registered in Appendix A / the register inventory.
- **APPLY (all audits PASS).** SHA-gate `7e9eeceb`; in-memory rebuild; **286 user-facing leaves changed = 269 temperature conversions across 56 crops + 17 lettuce dash conversions**. Audits: backend leaves changed **0**; value-preservation **0 failures** (temp = notation-only via temp-canon equality keeping the number; dash = pure single-`--`->punctuation swap, the only allowed marks comma/semicolon/colon -- no restructure, no case change); **dual-voice regression** lettuce register population identical pre/post (130 `_beginner` present, unchanged -- a notation sweep must not perturb coverage); **flip-guard** `launch_ready_core`/`launch_ready_seasoned` stay False, `status` stays `unverified`. Minified (`separators=(',',':')`, `ensure_ascii=False`); post-write fresh re-read residuals lettuce_dash=0, degw=0, bare=0.
- **BARE-F false-positive vetting:** all 103 dataset-wide bare-F conversions hand-reviewed; every one a genuine temperature (negatives `-13F`->`-13°F`, ranges `32-40F`->`32-40°F`, `60F+`->`60°F+`). 0 false positives.
- **GATES at close:** (1) dash gate -- **lettuce user-facing `--` = 0** PASS (dataset-wide = 5,360 remaining, deferred per-crop, intentionally NOT gated to 0). (2) temperature-form gate -- user-facing `degrees F` = 0 AND bare-F = 0, `°F` is the sole user-facing form, PASS dataset-wide. (3) dual-voice regression PASS (130/130). (4) flip-guard PASS.
- **17 lettuce dash resolutions (per-sense, punctuation-only):** 4 `region_label` + 4 alert titles -> colon; `zones.8.notes`, `thinning.method`, 2 tips -> semicolon; `bolting` tip -> colon; `-- but` contrast + 1 appositive -> comma; 2 zone-7 notes -> comma. Temp examples: `75 degrees F`->`75°F`, `106-107 degrees F`->`106-107°F`, `soil-40F`->`soil-40°F`.
- **Findings ledger:** **FINDING 6c-4 RESOLVED** (temperature-form normalization complete dataset-wide; canonical `°F`). **FINDING F-6d-2 STILL OPEN** (re-scoped: dash conversion is per-crop per-sense; lettuce done, 122 crops pending their arcs). **FINDING Steps78-2 `tip_overrides` notation carry CLEARED** for lettuce (the 4 bolting `tip_overrides` had no `degrees F`/bare-F needing change; `low_desert_az` "100 degree F" converted). New: **FINDING S9-1** (`bolting.triggers` Appendix-A gap).
- **NEXT: Step 10** (cross-field consistency + copyright/attribution audit) **then Step 11** (whole-crop validation; the per-crop `launch_ready` flip is Step 11's LAST act, ONLY on 0 violations). Schema 2.8. **plant-astro submodule NOT bumped** (separate gated action requiring Trevor approval).

---

## 2026-06-04 -- M15 lettuce Steps 7/8 (the `_beginner` siblings + dual-voice coverage gate)
- **SHA:** `5224e13a` -> `7e9eeceb1bf15852c8a1e7a6a7a76ebaaa4b3955aa86b1b89104777f7d45886c`. Session `m15_lettuce_steps78_beginner_siblings_dual_voice_gate`.
- **Lane:** claude.ai (beginner-register authoring derived from the depth-lifted `_seasoned`; the dual-voice coverage gate that flipped M13). Apply built + run in-session (SHA-gate, value-preservation, collateral audit, flip guard, minify, post-write re-read); promote/git per close ritual.
- **Denominator re-walk (checklist + register inventory authority, NOT the kickoff).** Walked the LIVE crop at `5224e13a` and applied Appendix A per field. Step 7 = 36 top-level/dict CP fields (6 already PASS at M13: soil/ph/companions/succession_policy.tip), 30 needing authoring. Step 8 = 66 per-entry compound CP siblings (all NULL, audience:core): pests 12, diseases 8, growth_stages 15, notifications 10, weather_triggers 6, failure_diagnostics 15. tips_by_stage 10 already authored (M13) -- confirmed non-null, NOT re-authored. Kickoff surface = 96 (30+66).
- **FINDING Steps78-1 (KEYSTONE -- the gate caught a real gap no roster named).** `thinning.tip_beginner` was a genuine missing sibling. `thinning.tip_seasoned` carries full prose (lifted at 6c) but its `_beginner` was null. Register inventory §7 + the register-conversion completion addendum rule `thinning.tip` CP (flagged anchor-unvalidated); lettuce is the lettuce anchor, the first crop to exercise it. **The kickoff's Step 7 named-list did NOT include it, and the FIRST dual-voice gate version (`v1`) used a hand-built `dict_specs` list that also omitted `thinning` AND `regions.*.tip_overrides` -- the exact stale-roster failure mode that flipped M13.** Rebuilt the gate as `v2`, a STRUCTURAL whole-crop walk (every dict; any `_seasoned` prose key requires a non-null `_beginner` unless ruled SP / out-of-scope / audience:seasoned). v2 surfaced `thinning.tip` + the 4 region `tip_overrides`. Authored `thinning.tip_beginner` (clean register transform). **Total authored this session = 97.** Carry to M16/pipeline: the dual-voice gate MUST be the structural walk; ship `dual_voice_gate_v2.py`'s logic into the rubric, not a transcribed roster.
- **FINDING Steps78-2 (region tip_overrides already dual-register -- coverage, not authoring).** The 4 `tip_overrides` cells (ca_north_coast, ca_desert, low_desert_az, hawaii_tropical; stage `bolting`) were authored at NA-3c/NA-3f and already carry both `text_seasoned` + non-null `text_beginner`. In the gate denominator (v2 counts all 4), PASS. NOT authored this session. Their `text_seasoned` carry legacy `°F`/"degree F" -- Step 9 / dataset-wide-sweep scope, not Steps 7/8. The v1.4-pre-committed region-tip-override leaf-keys are hereby confirmed registered: `tip_overrides.<stage>[]` with text_seasoned/text_beginner/overrides_tip_id/sources/anchoring_urls/added_in/evidence_tier.
- **Confirmed NOT-in-scope (verified, not assumed):** `companions.{good,bad}_*_seasoned[].why_beginner` (×3 populated why_seasoned, null why_beginner) = the companions array-level register split, register inventory §5/§9.2 OUT OF SCOPE, deferred to the §5 reconciliation session. `start_method.hardening_off` = boolean `False` (non-prose, no sibling). `container_notes.overwintering.approach` = null seasoned (ruled-empty). Gate v2 classifies all 7 companions-`why` as out-of-scope and the other 2 as ruled-empty.
- **The three 6d-corrected fields -- beginners authored against CORRECTED biology (carry honored):** `pests[2]` root aphid `organic_treatment_beginner` (cultural-control-not-spray), `prevention_beginner` (resistance-not-"LR"); `diseases[0]` downy mildew `cause_beginner` (oomycete/water-mold-not-fungus, fungicide caveat).
- **Register criterion (dual-register v1.1):** gloss-then-use applied (bolting -> "send up a flower stalk / go to seed (bolts)"; tipburn -> "brown, dried edges on the inner leaves"; honeydew, ethylene, perlite, NPK numbers, Bremia/water-mold, harden-off all glossed); ranges kept plainly where genuine; "degrees F" not `°F` (only 2 temp refs: per_plant, growth_stages[0].user_action). 4-slot failure-diagnostics template honored.
- **Apply audits (all PASS):** SHA gate `5224e13a`; value-preservation (96 diffs first apply all `_beginner` null->value; +1 on the thinning incremental; **0 `_seasoned` changes** anywhere in lettuce); collateral (only `lettuce-leaf` moved; 122 crops + all non-crop top-level keys byte-identical); **flip guard** (`launch_ready_core`/`launch_ready_seasoned` stay False, `status` stays `unverified`); minified, post-write fresh re-read (97 present + non-null, dash/temp scan clean on all 97).
- **DUAL-VOICE COVERAGE GATE (the M13-flip preventer) -- PASS whole-crop.** `dual_voice_gate_v2.py` structural walk against the written file: denominator 130 CP fields w/ seasoned prose; populated 130; SP seasoned-only 158; ruled-empty/non-prose 10; out-of-scope (companions split) 7; **missing_keys 0, null_values 0.** The 130 = 96 (this session) + 1 (thinning) + 10 (M13 tips) + 4 (M13 top-level) + 4 (NA-3c/3f overrides) + 15 (prior already-authored top-level/dict CP). The hole that flipped M13 would now return non-zero -- demonstrated by the gate catching `thinning.tip`.
- **Step 6 progress: 79 of 119 -- UNCHANGED** (the seasoned-lift tally does not move when authoring `_beginner` siblings). Lettuce `_beginner` keys after session: 130 populated, 5 null (all correctly classified). **NEXT: Step 9 (dash gate)** -- whole-crop user-facing `--`/`°F` resolution, a COORDINATED Claude Code sweep bundled with FINDING 6c-4 + F-6d-2 (NOT per-crop). Then Step 11 (whole-crop validation; per-crop `launch_ready` flip is its LAST act, on 0 violations). Schema 2.8. Size 10.6 MB minified (10,632,328 bytes).

---

## 2026-06-04 -- M15 lettuce Step 6d (the seven compounds)
- **SHA:** `0dfd835a` -> `5224e13aa4e94e2318d4acc7bd5195fbc512b886ac86bedcd37276b7eea73a8d`. Session `m15_lettuce_step6d_seven_compound_seasoned_depth_lift`.
- **Lane:** claude.ai (seasoned prose authoring + full T1 verification). Apply built + run in-session (SHA-gate, collateral audit, value-preservation, tips-sibling guard, flip guard, minify, post-write re-read); promote/git per close ritual.
- **Denominator:** live Appendix-A walk of the seven compounds at `0dfd835a` = **exactly 76** `_seasoned` prose fields -- matched the kickoff's "~76" (no delta this time, unlike 6c). Breakdown: pests 12, diseases 8, growth_stages 15, notifications 10, weather_triggers 6, failure_diagnostics 15, tips_by_stage 10.
- **46 `_seasoned` fields LIFTED** to cherry seasoned-depth (common-form dashes): pests cause/treatment/prevention (×3 entries, symptoms[0]/[1] already at-bar), diseases all-four (×2), growth_stages what_to_look_for+user_action (×5), notifications body (×5), weather_triggers body (×3), failure_diagnostics what_happened+next_season_tip (×5). **30 PASS at-bar:** 20 micro-strings (log_prompt ×5, fd.label ×5, notif title ×5, weather title ×3) + 10 M13 `tips_by_stage.text` (already at depth, beginner siblings present).
- **PEST/DISEASE = full cherry-anchor rigor (Trevor-locked).** All 5 entries had EMPTY `sources`/`anchoring_urls` pre-session -> no candidate-promotion possible; every claim freshly verified side-by-side vs catalog T1. Attached: aphids [clemson_hgic, uf_ifas, wsu_ext]; slugs [osu_ext, ucanr_ext]; root aphid [uc_ipm, usu_ext]; downy mildew [uc_ipm, umass_ext, uf_ifas_edis]; tipburn [uc_ipm]. All anchoring_urls verified:true. `pnw_handbook` NOT used (not in catalog).
- **THREE T1 CORRECTIONS (the payoff of full rigor):**
  - **6d-PD-1** root aphid `organic_treatment` claimed "drench soil with insecticidal soap" -- UC IPM: NO insecticide controls an infested crop; control is cultural. Rewrote to lead with cultural control (remove plants; deep-work + dry soil before replant; avoid Lombardy poplars; keep unstressed).
  - **6d-PD-2** root aphid `prevention` claimed resistant varieties "labeled LR" -- no T1 source for an LR seed-label convention (documented resistance = *Lra* gene / 'Grand Rapids'). Softened to "some varieties carry resistance." (Removing an unbacked claim; Trevor confirmed option-1.)
  - **6d-PD-3** downy mildew `cause` called *Bremia lactucae* a "fungus" -- it's an **oomycete (water mold)** per UF/IFAS EDIS + UMass; matters because fungicides for true fungi may not control it. Corrected + added the implication.
  - (Non-finding: tipburn `cause` lifted from "poor circulation" lay-gloss to the actual transpiration-stream / enclosed-head calcium mechanism per UC IPM -- depth, not a defect.)
- **FINDING 6d-1 (cosmetic doc-lag, RESOLVED):** top-level is **14** sibling keys, not 13 (the 14th is `region_source_map`); long-standing "13" wording corrected in the regenerated CURRENT_STATE. Collateral audit hashed all 14; only crops[33] moved.
- **FINDING F-6d-2 (NEW, forward, mechanical sweep):** Trevor ratified moving the whole dataset OFF the `--` literal to **common form** for user-facing copy (supersedes "no retroactive normalization unless asked" -- he asked). Done immediately on the 46 touched fields (0 `--` remaining in them); the dataset-wide conversion is a Claude Code sweep, **bundled with FINDING 6c-4 (temp-form) + Step 9 (dash gate)** -- explicitly NOT crop-by-crop. ~122 other crops + lettuce's untouched fields still carry `--`.
- **Apply audits (all PASS):** SHA gate `0dfd835a`; collateral (only `lettuce-leaf` moved; 122 crops + 13 non-crops top-level keys byte-identical); value-preservation (exactly 46 `_seasoned` changed, all in LIFTS, nothing else); tips beginner-sibling guard (10 siblings byte-identical pre/post -- not clobbered); **flip guard** (`launch_ready_core`/`launch_ready_seasoned` stay False, `status` stays `unverified`); post-write fresh re-read (lifts present, 0 `--` in touched fields, attributions confirmed). No new keys added (edited existing `_seasoned`; the 5 pest/disease `sources`/`anchoring_urls` keys already existed, were empty).
- **Step 6 progress: 79 of 119** (entering 6d 33; +46 lifted). Count tallies lifted fields only (confirmed against 6c convention); the 30 PASS fields do not increment. **NEXT: Steps 7/8** (the `_beginner` siblings + the dual-voice coverage gate that flipped M13). The six non-tips compounds carry NULL `_beginner` today -- author now from the just-lifted `_seasoned`; tips beginner siblings already exist (M13). **The 3 corrected `_seasoned` fields must get their `_beginner` siblings authored against the CORRECTED text.** Schema 2.8. Size 10.6 MB minified (10,605,251 bytes).

---

## 2026-06-04 -- M15 lettuce Step 6c (top-level / identity / yield / succession)
- **SHA:** `1e19948c` -> `0dfd835a29636868fce6e4b180ec8ca5bbf3b654523636c4354c52d160ef23c8`. Session `m15_lettuce_step6c_toplevel_identity_yield_succession`.
- **Lane:** claude.ai (seasoned prose authoring + live T1 verification). Apply built + run in-session (SHA-gate, collateral audit, minify, post-write re-read); promote/git per close ritual.
- **10 `_seasoned` fields lifted to cherry seasoned-depth (9 CP + 1 SP):** `description`, `harvest_ready`, `yield_expectations.{first_year_note, peak_production, per_plant}`, `rotation.avoid_after`, `start_method.notes`, `thinning.tip`, `varieties.note`, and SP `moon_phase_preference.source_note`. All 9 CP `_beginner` siblings stay NULL (deferred to Steps 7/8); the SP has no sibling. No new keys; edited existing `_seasoned` + attached sources/URLs.
- **DENOMINATOR DELTA (keystone, FINDING 6c-1).** The kickoff's named 6c scope did NOT match the live `1e19948c` dataset. Step 0 re-walk (checklist auto-derived denominator is authority, not the kickoff): (a) `succession_policy.tip` already at full depth w/ beginner sibling, and there is NO `reason_seasoned` key (kickoff name stale; live SP is `count_note_seasoned`, at-bar) -> OUT; (b) `safe_sowing` ×5 already at-bar and lives in `zones{}`, not a top-level object -> OUT; (c) six terse top-level CP fields the kickoff omitted (`peak_production`, `per_plant`, `rotation.avoid_after`, `start_method.notes`, `thinning.tip`, `varieties.note`) ruled in-scope by the register inventory -> IN. Net = the 10 above. Audit trail recorded in findings, not a retro-amended kickoff (kickoff is spent once running).
- **T1 verification (status does NOT inherit) -- TWO of three yield numbers moved.** `peak_production` "harvest every 1-2 weeks" was **CONTRADICTED**: it conflated outer-leaf picking (every few days) with the whole-plant cut-and-come-again regrowth cycle, which T1 puts at **2-4 weeks** (umd_ext 2-3wk + unl_ext 3-4wk). Rewrote to separate the two rhythms. `per_plant` "6-8 weeks before bolting" had no T1 numeric basis -> reframed as bolt-bounded (>75 degrees F, umn_ext). `first_year_note` "30 days" kept but pinned honestly to baby-leaf vs 40-80d full maturity (umd_ext). `varieties` Buttercrunch heat-tol/slow-bolt CONFIRMED (ncsu_ext, new URL verified live 2026-06-04). `rotation.avoid_after` aphid host-range CONFIRMED (peer-reviewed) and reframed to match `note_seasoned`'s ordering (soilborne disease primary, root aphid secondary). `start_method` 3-4wk transplant timing CONFIRMED (umd_ext).
- **Sources/URLs:** populated `yield_expectations.anchoring_urls` (was `{}`) with umd/umn/unl/iastate; dropped `ucd_postharvest` from yield sources (it anchors storage, used correctly in 6a, not yield). Attached sources+URLs to `start_method` (umd), `varieties` (ncsu), `thinning` (umd+unl); added `description_sources` sibling (umd+umn); attached `rotation.anchoring_urls` = umn. `pnw_handbook` NOT used (not in catalog).
- **FINDING 6c-2 (Phase C deletion-gate input):** verified `safe_sowing_note` exists in `zones{}` ONLY; `regions{}` carries none (uses `plantings[].direct_sow[]`). The "region carries everything zones held" precondition is NOT met -> real migration item (Claude Code, Phase C), not confirm-and-delete.
- **FINDING 6c-3 (carried, verify):** 6a `rotation.note` may cite uncatalogued `pnw_handbook`; re-confirm at cross-anchor audit.
- **FINDING 6c-4 (forward, batched style decision):** three competing temperature forms in user-facing prose (62 "degrees F" / 10 bare F / 5 °F). 6c authored "degrees F" (Trevor-ratified Option A). Canonical normalization deferred to a Claude Code sweep.
- **Collateral audit:** only crops[33] moved; 122 crops + all 13 sibling top-level keys byte-identical. Em-dash sweep clean on all 10 user-facing fields. **No flip** (`verification_status.launch_ready_*` stay false, `status` untouched). Step 6 progress: **33 of 119** (+10). NEXT: 6d (seven compounds, ~76 fields). Schema 2.8.
- **Size note:** dataset is ~10.6 MB minified now (regions layer); old "4.6 MB" note stale; exceeds project-knowledge upload limit; stays out-of-band.

---

## 2026-06-04 -- M15 lettuce Step 6b (container cluster)
- **SHA:** `61cddea3` -> `1e19948cc7afe2c1e498d3d28e514b134cefc1170a299c18f5650bfbbd6e2a9c`. Session `m15_lettuce_step6b_container_cluster`.
- **Lane:** claude.ai authored the container seasoned prose (Step 6 = claude.ai's lane); Claude Code validated + ran the canonical apply + git (structural/promote = Claude Code's lane; lane-by-content).
- **7 `container_notes` `_seasoned` fields authored:** `shape_requirements` + `drainage.saucer_practice` were bare-null -> `_seasoned` + null `_beginner` (conversions); `watering_adjustment`/`fertilizer_adjustment`/`soil_mix.amendments` filled from null; `soil_mix.type` = token `container_potting_mix` (matches cherry/beefsteak anchors); `notes_seasoned` light-lift that ALSO resolved an existing `--`. Container `sources` = [umd_ext, osu_ext, uiuc_ext, iastate_ext, wvu_ext] + verified anchoring_urls.
- **6b source preflight SATISFIED:** all 5 container sources ADMITTED in the 80-entry source_catalog (the open item from 6a is closed).
- **Validation (Claude Code, before any canonical write):** ran claude.ai's apply as a DRY-RUN against a temp copy -- SHA gate (`61cddea3`), preconditions, collateral hash audit (only crops[33] changed; 122 crops + 13 sibling keys byte-identical), dash sweep clean (incl. the resolved `--`), copy rules (no mid-sentence "Plant"), source admission. Then promoted the validated output (identical, `1e19948c`).
- **No flip.** Step 6 progress: **22 of 119** (6a 15 + 6b 7). NEXT: 6c (top-level/identity/yield/succession + safe_sowing ×5), then 6d (compounds). Schema 2.8.
- **Schema-version label fix (Trevor caught it; no data change).** CURRENT_STATE flip-gate #4 said "Schema 2.8 perennial bump" while the dataset is ALREADY 2.8 (the register-suffix conversion took it). Corrected: future bumps re-number off 2.8 (the register inventory §9.1 anticipated this), so the region read-flip + perennial extension are **2.9+**, not 2.8. Added a schema-version-lineage note to CURRENT_STATE. The region-primary spec still labels the region flip "2.8" -- flagged stale, re-number to 2.9 when that flip is built (claude.ai's lane).

---

## 2026-06-04 -- M15 lettuce Step 6a (seasoned depth-lift, core biology dicts)
- **SHA:** `e27eec14` -> `61cddea31a58d956406906dcd0d6e49ec0ba8b59293e7ebd5067673957ca518c`. Session `m15_lettuce_step6a_seasoned_depth_lift_core_biology`.
- **Pre-authoring checklist conflict resolved.** Step 0 surfaced that checklist v1.4 Steps 6-9 + Appendix A F5 still described the pre-Phase-0 register model ("default unsuffixed field IS seasoned; no `_seasoned` suffix"), contradicting the live explicit-suffix dataset. Claude Code issued **v1.4.1** (register-naming sync ONLY; field examples bare -> `_seasoned`; denominator mechanism, Appendix A, all gates, and the F5 register-vs-entry distinction unchanged). Dataset did NOT move during the checklist fix. v1.4.1 is now governing. Lesson reaffirmed: when checklist text and live dataset shape disagree, the dataset is authoritative for what is true; flag the doc lag rather than author against fields that don't exist.
- **15 `_seasoned` fields lifted to cherry bar** (54-79w prose; 2 short UX strings): watering.{amount,frequency,method,signs_overwater,signs_underwater}, rotation.note, storage.{notes,fridge,room_temp,freezer}, fertilizer.{notes,notify_message,npk_hint}, bolting.{note,prevention}. Appendix-A marked table: 15/15 marked, 0 fail.
- **3 Trevor-approved content changes** (flagged in session, not silent): (1) rotation.note rationale corrected to lead with Asteraceae soilborne disease (Sclerotinia/bottom rot/downy mildew), root aphid demoted to secondary -- P. bursarius is primarily poplar-host-alternating, only a minority morph overwinters in soil (Dunn 1959; PNW Handbook); old phrasing overstated soil-aphid mechanism. (2) storage.fridge 3-5d -> 7-10d (UC Davis Postharvest). (3) fertilizer.npk_hint "high N" -> "higher N or balanced" (aggressive high-N worsens tipburn via N-surge/Ca-delivery).
- **T1 anchors:** UC IPM/UC ANR Pub 3450 (tipburn = localized Ca deficiency, immobile Ca in transpiration stream, inner-leaf enclosure); UMN Ext (cool-season biology, bolt = long-day primary + heat accelerant, sesquiterpene-lactone bitterness); UMD Ext (day-length + heat -> seedstalk; shallow-rooted); UC Davis Postharvest (ethylene 1ppm -> russet spotting, fast-wilt respiration, freeze injury). Photoperiod-primary/heat-accelerant framing preserved (Waycott USDA 1995) over the common "heat alone" error.
- **Open item carried:** source-catalog check for `umd_ext` + `pnw_handbook` ids (confirm in 80-entry catalog or register) -- run at 6b preflight. UC IPM/UMN/UC Davis Postharvest confirmed members.
- **Apply discipline:** SHA-gate (`sys.exit(1)` armed; caught a real bug first run -- script assumed bare-list top level, actual is {crops:[...], +13 sibling keys}; fixed + extended collateral audit to all sibling keys). Collateral audit: only crops[33] changed, 0 sibling top-level keys moved, exactly 5 lettuce dicts changed, 0 keys added/removed. Independent fresh re-read confirmed. Em-dash sweep clean (all 15 user-facing).
- **Promotion note:** README used a relative bundle path; promote run from ~ archived the canonical (line 2 fired) then failed lines 3-5 (bundle was in ~/Downloads). Recovered with absolute paths; final SHA verified `61cddea3`, archive verified `e27eec14`. 6b kickoff README switched to absolute paths.
- **No flip.** launch_ready_core/launch_ready_seasoned both false; status untouched. Steps 7/8 + Step 11 remain before any flip. Step 6 progress: 15 of 119.

## 2026-06-04 -- checklist v1.4 -> v1.4.1 register-naming sync (NO data change; SHA stays `e27eec14`)

claude.ai's new-chat preflight flagged checklist <-> dataset mismatch: the checklist's naming-model prose predated Phase 0's explicit-suffix conversion (it said "the bare/unsuffixed field IS the seasoned register; there is no `_seasoned` suffix"), but the dataset stores seasoned prose at `X_seasoned` + `X_beginner` with no bare field. Diagnosed as a stale-PROSE issue only -- the dataset is correct (do NOT revert it), and the walk-and-rule denominator logic is sound. Claude Code applied a surgical **register-naming sync** to produce **gold_standard_arc_checklist_v1_4_1.md** (in Downloads -> claude.ai project knowledge): 11 edits, bare field names -> `_seasoned` across Steps 5-8 + the F5 note + gate-status. Self-verified by script: each edit lands exactly once; the two senses of "default" disambiguated (fixed "default field=seasoned"; left "the default [tip]" untouched); the denominator logic, Appendix A, and the F5 register-vs-entry distinction (line 228, "zero seasoned-only entries exist") all preserved; all stale phrases gone. **Dataset byte-identical, SHA stays `e27eec14`** (checklist is a project-knowledge methodology doc, not the dataset repo). This was a 5th checklist amendment (the other 4 -- lane tags, shell-pass-first, Step 11 flip guard, deletion gate -- remain owed, claude.ai's lane). Unblocks the Step 6 kickoff (checklist + dataset + kickoff now agree on the register model).

---

## 2026-06-04 -- freezer micro-addendum + gate refinement + 3 rulings (DATA change; `815efe62` -> `e27eec14`)

claude.ai ruled the 3 items handed back; Claude Code applied. **(1) Closed-set enumeration (WORRY 1)** across the 5 partially-ruled dicts (storage/watering/yield_expectations/rotation/container_notes) BEFORE converting: all came back CLOSED (only-known keys), but the scan caught `watering.frequency` + `watering.method` as the 2nd+3rd un-ruled prose siblings beside `storage.freezer` -- so all three batched into ONE convert instead of three. claude.ai confirmed all CP (anchor check = genuine mechanism prose, not categorical). **(2) Batched micro-addendum:** `storage.freezer` + `watering.frequency` + `watering.method` -> `_seasoned` + null `_beginner`, 123 each (369 renames + 369 scaffolds). Structural-verifier proof; counts exact; independent re-read; no copy. **(3) Null ruling:** SP/CP null-valued occurrences LEFT BARE (empty-by-nature, NOT `_seasoned:null`); registered an inventory rule (walks must key on the bare stem, treat `<stem>` and `<stem>_seasoned` as one field, or null slots are silently missed -- inverse of bolting). **(4) safe_sowing_note:** authored beginner kept (confirmed correct). **(5) Gate refinement:** loaded the positive roster (4 ruled CATEGORICALs recognized; companions §5 split into a DEFERRED bucket). **Clean re-run -> GATE: PASS, 0 unruled prose fields (modulo 4 deferred §5 companions).** The bolting CLASS of bug is now closed by enumeration. **STILL OWED (WORRY 3):** the gate is standalone today; it MUST be wired into the per-crop shell pass (admission HALT) + Step 11 when the shell pass is built (cherry/beefsteak M16) -- a gate that runs only when remembered is the bug waiting to recur. Schema 2.8. git pushed. Back to claude.ai for the Step 6 kickoff with the new SHA.

---

## 2026-06-03 -- register-conversion completion addendum (DATA change; `8a1d8a50` -> `815efe62`)

claude.ai ruled the gate's candidates (`register_conversion_completion_addendum` handoff); Claude Code applied the mechanical conversion -- byte-preserving rename + null scaffold, NO copy authored. **10,801 prose fields** converted: 9,264 SP `_seasoned`-only renames + 1,537 CP (`_seasoned` + null `_beginner`). Bucket A = nested planting-rule evidence prose the original 2.8 walk never descended into (`synthesis_note` 6,367 / `design_note` 1,767 / `source_quote` 823 / `zone_coverage_note` 290, all SP per inventory §3.2). Bucket B = `growth_stages_annual`/`growth_stages_year_one` inner prose (750, CP, the array-name variants the walk didn't recognize). Bucket C = ~18 newly-ruled fields (CP: storage.fridge/room_temp, watering.amount, yield per_plant/peak, rotation.avoid_after, safe_sowing_note, container shape/saucer, tasks.description, tip; SP: count_note, heat_pause.basis, microclimate_note; CATEGORICAL left bare: rotation.good_after, thinning.when, fruit_size/color). **Correctness proof:** a structural verifier confirmed `new` == original with ONLY the roster renames + scaffolds; all counts matched claude.ai's roster exactly (zero mismatches); independent fresh re-read PASS. Path-aware (matched by parent.leaf per the edge guards; top-level `description`/`thinning.tip`/`indoor_cycle.tip` correctly untouched). **Two nuances handed BACK to claude.ai** (in the for_chat handoff): (1) `safe_sowing_note` already had AUTHORED beginner copy (lettuce z3-7) -> renamed base to `_seasoned` and KEPT the authored beginner (per the inventory's "_beginner kept if present" rule), not null-scaffolded; (2) thousands of NULL-valued occurrences left bare per the string-only counts (`source_quote` 2,165 null, `zone_coverage_note` 460, `saucer_practice`/`shape_requirements` 121 each, `synthesis_note` 6) -- claude.ai to rule leave-bare (current; reads as "field absent" per visibility model §0) vs `_seasoned:null` for key-uniformity. Inventory updated (new field rulings registered). Schema stays 2.8. git pushed. **Back to claude.ai for lettuce Step 6** with the new SHA in-chat.

---

## 2026-06-03 -- register-completeness gate built + dataset-wide scan (no data change; SHA stays `8a1d8a50`)

Built `plant-dataset/tools/register_completeness_gate.py` to claude.ai's roster-completeness spec (inversion: any prose-shaped field with no ruling HALTS; stop-and-ask, no auto-fix). Ran it dataset-wide -- it found the bolting class is bigger than bolting: the 2.8 register conversion's tomato-anchored walk also missed (A) the SP planting-rule evidence prose `synthesis_note` (6,373 bare, 0 converted) / `source_quote` (2,988) / `design_note` (1,767) / `zone_coverage_note` across zones + regions -- already ruled SP (§3.2), just never converted; (B) `growth_stages_annual`/`growth_stages_year_one` inner prose (`user_action` 250 bare vs 760 converted) -- ruled CP (§2.3); and (C) ~18 genuinely-new fields not in the inventory (container sub-fields, storage.*, rotation.*, watering.amount, tip, tasks[].description, etc.). Per the no-auto-fix principle, NONE were touched -- candidate list handed to claude.ai to rule (`06-sessions/register_completeness_gate_handoff_2026-06-03.md`). After rulings, Claude Code runs a conversion addendum (byte-preserving rename + null scaffold, no copy; SP = `_seasoned` only, CP = `_seasoned` + null `_beginner`). Gate gap-classes (audit/machinery: plantings_provenance.*, verification_status.*, anchoring_urls.*.note, zone_N primitives) tuned out (already EXCLUDED §4). Gate refinement still owed: positive CP/SP roster to auto-separate ruled-but-unconverted from unruled. Gate committed as a draft tool.

---

## 2026-06-03 -- bolting register-conversion addendum (DATA change; `ed495666` -> `8a1d8a50`)

Phase-0 gap-fix, closes `m15_step6_finding_001`. The register-bearing-field inventory was anchored on tomato shape (tomatoes do not bolt), so the `bolting` dict was missed by the 2.8 register conversion -- surfaced by the lettuce Step 6 register read. On the 9 crops carrying `bolting` (spinach, basil, broccoli, lettuce-leaf, arugula, bok-choy, radish, cauliflower, cilantro-coriander): `bolting.note` -> `note_seasoned` + `note_beginner:null`; `bolting.prevention` -> `prevention_seasoned` + `prevention_beginner:null`; `bolting.risk` (EXCLUDED enum) and `bolting.triggers` (USER-FACING-CATEGORICAL) left as-is. 18 prose renamed (byte-identical), 18 null beginner siblings scaffolded. No content authored; schema stays 2.8 (addendum). Scope independently re-verified (exactly 9 crops, no others). Gates: start-SHA, collateral hash audit (only the 9 changed; other 114 + risk/triggers byte-identical), value-preservation, 0 bare bolting.note/prevention remain, independent fresh re-read. Inventory doc updated (§2.1 + §4 + root-cause note). **Systemic follow-up (next, Claude Code):** a register-completeness gate -- walk all crops' prose fields, assert each has an inventory ruling; unruled = finding (would have caught this). git pushed; LATEST session `bolting_register_conversion_addendum`.

---

## 2026-06-03 -- spec-B succession amendment CODIFIED into the spec doc (no data change; SHA stays `ed495666`)

The succession-encoding clarification was inserted as **§3b-i** of `region_primary_schema_shape_spec_v1_0.md` (arm-OBJECTS prohibited in `resolved_by_zone` / date-STRINGS permitted; regenerable-vs-precompute warm-vs-cold; per-crop pipeline rule). Pure insertion -- §3a/§3b/§6/§4 untouched; verified (read-only) against the live data (north `succession_*` are strings, rule lives in `plantings[]` succession entries, warm cells carry none). The "Region-primary spec amendment" open item is now DISCHARGED and removed from CURRENT_STATE. The one-time amendment wrapper was discarded. Updated spec doc -> claude.ai project knowledge (Trevor's manual swap).

---

## 2026-06-03 -- spec-B succession-encoding clarification (no data change; SHA stays `ed495666`)

A claude.ai review of the handoff caught an apparent contradiction: after Pass 1b, northern_tier carries succession in TWO places -- the rule in `plantings[]` (`track:"succession"`) AND materialized `succession_spring`/`succession_fall` date-strings in `resolved_by_zone[3..7]` -- while the lean CURRENT_STATE said `resolved_by_zone` holds "only date-strings + calendar[]" and "do NOT enumerate succession arms." Resolved as INTENTIONAL + a wording fix (not a data change):

- The prohibition is on succession ARM OBJECTS (rule-bearing) in `resolved_by_zone` -- the NA-3h anti-pattern Pass 1b removed. Materialized succession DATE-STRINGS are permitted there, exactly like the main `plant_out` string; they are static-consumer precompute, not a rule. The guardrail (app recomputes from `plantings[]` + live frost, ignores `resolved_by_zone`) holds -- there is no rule in `resolved_by_zone` to read.
- The warm-vs-cold asymmetry is BY DESIGN: rule-ONLY when cadence regenerates from stored inputs (warm = regular 2-week `succession_policy`); rule + materialized precompute when it does not (north: `soil_temp_40f` spring anchor is unstored -- the whole northern resolved layer is precompute for that reason -- plus per-zone Clause-C counts + fall Jul-1 clamp). The clamp forward-finding alone does NOT buy symmetry (spring anchor still unstored; full symmetry would also need a `zone_soil_temp_40f` input table).
- Actions: sharpened the spec-B guardrail wording in CURRENT_STATE; added an owed region-primary spec-doc amendment (claude.ai lane). Warm cells unchanged. (Slight hedge: warm cadences assumed regular; verify per warm crop at M16 if one turns out irregular.)

---

# plant -- Current State (historical body, pre-split -- preserved verbatim)

**Last updated:** 2026-06-03 (**Pass 2 -- warm-cell consistency cleanup. Done in Claude Code (structural). Canonical `20f9fc2b...` -> `ed495666...`, committed + pushed.** Three consistency fixes across 6 region cells of `lettuce-leaf`: (A) added `track:"beginner"` to the `main` planting of `se_gulf` + the 4 CA cells (`ca_interior`/`ca_north_coast`/`ca_south_coast`/`ca_desert`) -- the 5 that were missing it (`track` is a single scalar naming the calendar: `beginner` = the shared MAIN calendar BOTH audiences see, `succession` = seasoned-only; NOT a beginner/seasoned pair -- na3d_finding_003a; verified no companion seasoned tag exists; deferred dataset-wide rename `beginner`->`main` still open); (B) relabeled `se_gulf` z8/z9 `resolution_method` from the generic `static_precompute` to `se_gulf_month_resolution` (the lone generic label; every sibling cell has a specific derivation label; region-descriptor mint chosen over a source-enumerating one since sources already live in `sources[]`); (C) `warm_arid` z8 winter `wait`->`cold_pause` at Jan + Dec (states-only, matching the universal convention -- 0 cold_pause OBJECTS exist dataset-wide; ca_north_coast is the exact warm-cell precedent; warm_arid stays a legit two-pause cell with its heat_pause OBJECT untouched; the mild-winter nuance was already in its `region_notes_seasoned`, no copy edit). Gates all PASS (collateral hash audit -- 123 crops, only `lettuce-leaf`; within lettuce only `regions{}`, exactly 6 cells changed, `zones[3-7]` + `succession_policy` + northern_tier/low_desert_az/fl_peninsula/hawaii byte-identical; value-preservation = exactly the intended deltas; §6/declare-one-outcome = warm_arid heat_pause object byte-identical; enum = no NEW calendar state introduced, 0 `static_precompute` remain; independent fresh re-read). **This COMPLETES the Claude-Code structural passes (1a + 1b + 2).** **This does NOT advance the gold-standard arc toward the flag-flip** -- the arc now owes only the consumer-copy + validation slice: Step 6 (seasoned depth-lift) -> Steps 7/8 (beginner siblings) -> Step 11 (whole-crop validation), all claude.ai's lane; the flip is the last act of Step 11 on 0 violations.)

**Previously (northern_tier Pass 1b -- succession-rule HOIST, 2026-06-03):** (**northern_tier Pass 1b -- succession-rule HOIST (the §6 shape fix). Done in Claude Code (structural). Canonical `327a2d5c...` -> `20f9fc2b...`, committed + pushed.** The northern_tier succession arms (spring + fall) were hoisted UP from `resolved_by_zone[z]` into the region-constant `regions.northern_tier.plantings[]` rule layer (1 entry -> 3: `main/beginner`, `spring/succession`, `fall/succession`), so the succession RULE now lives where the app dynamic path reads it (region-primary §3a/§6) instead of being invisible inside `resolved_by_zone`. Each resolved cell was reduced to the §3b shape: the nested `plantings[]` arm objects were removed and the per-zone sowings materialized in place as flat date-strings `succession_spring` / `succession_fall` (mirroring the existing flat `plant_out` convention). **Sub-decision 1 (generate-vs-store):** a regeneration-equality test (region rule + each zone's `zone_frost_data` frost dates -> stored sowings?) reproduced ONLY z3 exactly; z4-7 failed both arms (the stored sowings carry Clause-C per-arm counts + soil-temp-40F crossing dates + source-specific frost dates not recoverable from the published rule), so sowings are KEPT MATERIALIZED -- lossless, byte-matched against the canonical nested arms, counts == `derived_zone_totals_cold {3:9,4:9,5:8,6:10,7:7}`. **Sub-decision 2 (heat-floor as a region-constant rule):** z3's fall arm was the lone off-pattern cell (stored the clamped effective `offset -76` + a z3-specific note); the region rule is normalized to the nominal `offset -90` (matching the `main` rule's own fall component, also -90) with the clamp folded into ONE region-constant synthesis_note ("In the coldest zones... the fall start is clamped to roughly July 1 (a heat floor)..."). z3's -76 disappears as a rule; the clamp survives in z3's materialized `succession_fall` starting Jul 1. Heat-floor expressed in PROSE, not a new machine-readable field (no such field exists dataset-wide; inventing one in a structural pass would trip the Appendix-A unruled-field safeguard -- noted as a forward finding if the app resolver later needs to reproduce z3's Jul-1 clamp exactly; the two new resolved-cell keys also want Appendix-A registration at Step 11). Gates all PASS (collateral hash audit -- 123 crops, only `lettuce-leaf` changed; within lettuce only `regions.northern_tier`; `zones[3-7]` + crop-level `succession_policy` byte-identical; calendar[] + heat_pause untouched so §6 / declare-one-outcome intact; value-preservation; enum tracks `[beginner,succession,succession]`; no em-dash; independent fresh re-read). **The §6 succession-shape finding (NA-3h open item) is now CLOSED.** **This does NOT advance the gold-standard arc toward the flag-flip** -- region-cell work remains only Steps 4/5/5.5; the arc still owes Pass 2 (warm-cell consistency cleanup) + Step 6 (seasoned depth-lift) -> Steps 7/8 (beginner siblings) -> Step 11 (whole-crop validation) before any flip.)

**Previously (northern_tier Pass 1a -- calendar exact-derivation, 2026-06-03):** (**northern_tier Pass 1a -- calendar exact-derivation correction. Done in Claude Code (structural; per the 2026-06-02 division of labor: dataset structural work in Claude Code, biology/copy in claude.ai). Canonical `582dbbad...` -> `327a2d5c...`, committed + pushed (git `1b85ddf`).** The post-reconciliation northern calendars (z3-z7) were re-derived EXACTLY from the hard windows (zero tolerance): fixed `plant` shown across the no-sow summer gap, and `harvest` running past the harvest window. The warm edge now carries a SUMMER `heat_pause` scaling with warmth -- z3/z4 none, z5 [Jul], z6 [Jul,Aug], z7 [Jun,Jul,Aug] -- making z7 a TWO-pause cell (hot summer + cold winter) like warm_arid (the recurring warm-edge finding). Residual harvest over-extensions fixed (z3 Oct / z4 Oct / z5 Nov -> cold_pause); non-spec per-resolved-cell verification stamps trimmed. T1-grounded this session: UMN (even Minnesota Jun-Aug is too hot, lettuce bolts above ~75F) + VCE 426-331 (zone-6/7 spring ends May/Jun, fall resumes Aug); the "too warm to plant but still harvesting the earlier cool-sown crop, then heat takes both" biology was Trevor-validated. Gates all PASS (collateral hash audit -- only northern_tier resolved cells changed; §6 sync; exact-derivation; declare-one-outcome; enum). **Succession arms still live in resolved_by_zone (the §6 shape issue) -- NOT touched here; that is Pass 1b.** **This does NOT advance the gold-standard arc toward the flag-flip** -- region-cell work remains only Steps 4/5/5.5; the arc still owes Pass 1b (succession-shape reconciliation, the NA-3h finding below) + Pass 2 (warm-cell consistency cleanup) + Step 6 (seasoned depth-lift) -> Steps 7/8 (beginner siblings) -> Step 11 (whole-crop validation) before any flip. See the corrected banner just below.)

**Previously (M15 region-cell arc + northern_tier reconciliation, 2026-06-01):** (**M15 lettuce — REGION-CELL ARC COMPLETE (NOT the whole gold-standard arc). `northern_tier` reconciled + verified this session; all 10 region cells now carry verified region-sourcing (Step 4) + side-by-side (Step 5) + per-cell Step 5.5 calendar/succession gates. Canonical SHA `55ba1ff0...` -> `582dbbad...` (M15 northern_tier).** ⚠️ **LETTUCE IS NOT READY FOR THE Step 11 FLAG-FLIP.** The region-cell work (NA-1..NA-3g warm cells + this northern_tier session) is ONE slice of the gold-standard arc — Steps 4 + 5 + the per-cell Step 5.5 gates. The arc still owes, per the cherry template and the gold-standard-arc-checklist v1.4: **Step 6 (seasoned depth-lift** across null/thin prose fields — failure_diagnostics, weather_triggers, growth_stages prose, container_notes, etc.); **Steps 7-8 (beginner siblings**, the NC1/NC2 equivalent, ~100+ fields); and the **Step 11 whole-crop validation pass** — which RE-RUNS every prior gate independently against the full crop, INCLUDING a whole-crop Step 5.5 re-walk (succession coherence across all 10 cells + no-drift between `regions{}`, legacy `zones{}`, and `succession_policy` counts — per-cell gates passing on the cells a session touched is NOT whole-crop verification). These are exactly the surfaces the reverted 2026-05-29 M13 flip skipped (siblings missing/null, seasoned register thin, user-facing `--` unresolved, pest `symptoms` byte-identical to cherry). **The flip happens ONLY after Steps 6/7/8 + the Step 11 validation pass return 0 violations.** The next session is the **Step 6 seasoned depth-lift**, NOT the flag-flip. (A prior next-chat kickoff incorrectly framed the flip as the next step by conflating "all region cells done" with "arc done"; corrected — see the drift note below.)

**northern_tier reconciliation detail (this session, all SOUND):** promoted the verified `zones[3-7]` data UP into the region cell (truth flowed zone -> region, the inverse of every warm cell): full succession-arm-object migration (count-audited, z3=9/z4=9/z5=8/z6=10/z7=7, byte-identical to source), nested-plantings duplication collapsed, `track` added (FIRST region-level `track:"succession"` authoring), winter calendars derived from windows with `wait`->`cold_pause` (NO `season_over` — cyclic-annual judgment call: lettuce re-sows after warm-up), v1.5 fall heat floor verified, dual-register region notes authored, provenance re-stamped, zones<->region sync asserted mechanically. **TWO winter-calendar DEFECTS caught by the post-write coherence gates and corrected with continental zone-6b/7a T1 verification (VCE): z7 was internally self-contradictory (Jan/Dec:harvest vs harvest_end Nov 10, zero winter pause) and z6 over-extended (Dec:harvest past Oct 26, pre-sow Feb:plant).** Gate (a) ">=1 cold_pause per frost cell" caught z7; gate (b) "active months subset of declared windows" caught z6; monotonicity would have missed z7 (pause-count z3=5,z4=4,z5=3,z6=1 monotone decreasing, z7=0 slips past). Cold-frame/row-cover winter-harvest capability for zones 6-7 PRESERVED as a season-extension note in `region_notes_seasoned`, not dropped. `m15_na3a_finding_003` (z4/z5 beginner fall late-edge) resolved as a MAIN<->succession trim (z4 Sep 8->Sep 1, z5 Sep 15->Sep 8). Override decision: DEFAULT COVERS (no cold-zone tip-action fork; n=1). z3 USCRN thin-coverage (28 station-years, below 30 threshold but above field standard) deferred to a future per-zone USCRN pass — confidence-tightening, not a defect. SE-source presence in z6/z7 verified ACCEPTABLE (date-driving gate clean across all zones). **zones[3-7] NOT deleted** — for the north they remain the trustworthy copy until Phase C; the deletion-gate "region cell carries everything incl. succession" precondition is now MET. Two NEW forward findings: generative-succession-rule hoist to the rule layer (Phase C / app-resolver; GUARDRAIL: dynamic path must NOT read succession out of resolved_by_zone per region-primary §6) + maritime Pacific-NW zone-7 region split (`pacific_nw` cell, Phase C). The REVERSAL banner below (region-tip overrides) still governs.

**Previously (M15 NA-3g):** NA-1 + NA-2 + NA-3a..NA-3g done; all 9 warm/region cells' region-sourcing complete. NA-3g authored `hawaii_tropical` (zone 11) — the FIRST pauseless/year-round lettuce cell — against University of Hawaii CTAHR (RES-164 + HGV-2 + B-91). Introduced the `year_round: true` flag (Decision 1) + `from:"year_round"` anchor (Decision 3) + Refinements A (all-active cells need the flag; pause-state cells self-declare) and B (declare-one-outcome kept separate from §6 sync). Canonical `fc5c402d...` -> `55ba1ff0...`. The original "M15 lettuce arc in progress" framing for NA-3g is preserved in the historical stack below. NA-3g authored the `hawaii_tropical` warm region cell (zone 11) end-to-end against University of Hawaii CTAHR (RES-164 *Lettuce Production Guidelines for Hawaii* + HGV-2 *Lettuce for the Home Garden* + B-91 *Home Gardening in Hawaii*) — the most divergent cell, and the **FIRST PAUSELESS / YEAR-ROUND lettuce cell in the dataset.** Key result: tropical zone-11 leaf lettuce is **genuinely year-round, NOT a summer-heat-pause cell** — CTAHR RES-164 Table 1 lists green-leaf and red-leaf as `year-round` plantings, and the text + HGV-2 confirm leafy types grow year-round at low elevation and in home gardens (head types are the seasonal exception). The hot-season effect is a QUALITY dip (tip-burn, faster bolting) managed with a heat-tolerant cultivar (UH Manoa, Anuenue) + ~40% afternoon shade + elevation, NOT a no-sow exclusion; a `heat_pause` would have contradicted the explicit T1 year-round statement. Introduced the **NET-NEW `year_round: true` flag** (Decision 1, Trevor-ratified) on `resolved_by_zone[11]` + a `calendar_basis` prose reason (ca_north_coast precedent) + a 12-month ALL-ACTIVE `calendar[]`. **GENUINE action-divergence bolting fork** (Decision 2) — unlike low_desert_az/ca_desert's resolver-mechanics parallel forks, the Hawaii grower action materially differs from the portable default (manage heat with cultivar+shade+elevation, NOT skip a season or harvest-at-first-bolt). Distinct **`from: "year_round"` anchor** (Decision 3 coherence) so the resolver keys skip-frost off a named anchor and the resolved flag is derivable. **Refinement A:** the declare-one-outcome invariant is scoped to the all-active case (a resolved cell with NO pause state in calendar[] MUST carry `year_round: true` + calendar_basis; cells with a pause state self-declare and pass) — passes ca_north_coast + future northern_tier cleanly. **Refinement B:** that invariant is kept SEPARATE from the §6 calendar↔pause-object sync. RE-DERIVED, not lifted (legacy `zones[11]` is 100% SE/south-Florida-contaminated, the finding_001 pattern for zone 11; filed `na3g_finding_001`). Canonical SHA `fc5c402d...` -> `55ba1ff0...` (M15 NA-3g). **9 of 10 region cells now populated AND v1.4-verified (`se_gulf`, 4 CA, `fl_peninsula`, `warm_arid`, `low_desert_az`, `hawaii_tropical`); `northern_tier` is populated-but-UNVERIFIED (lifted verbatim in Phase A); its reconciliation is the arc closer.** See "What happened 2026-06-01 (M15 lettuce NA-3g)" at the top of the stack. The REVERSAL banner below (region-tip overrides) still governs.)

> ## ⚠️ REVERSAL 2026-05-31 (design session — region-tip overrides) — READ BEFORE THE NA-3b ENTRY
>
> A design session on 2026-05-31 (chat-only, **no dataset write; SHA unchanged at `ec4f2b69...`**) reversed the NA-3b regional-content conclusion **for editorial tips**. Several NA-3b/NA-3c entries below still carry the superseded "anchor-phrasing only / NOT a tagging pass / no region tag fields" language; this banner governs on conflict.
>
> **What reversed:** NA-3b concluded editorial timing tips get **anchor-phrasing in prose only, no region axis** ("not a tagging pass"). Trevor was not comfortable with that and revisited it. **New decision (Trevor-ratified, 2026-05-31):** where ≥2 T1 sources from different **regions** (or **zone-bands**, in the north) the crop is actually grown in prescribe a **materially different grower action** for a tip, a **dual-register region-tip override** is authored on the relevant region cell(s).
>
> **What is RETAINED (NA-3b was right about this):** the **anchor-phrased portable tip remains the default** and the permanent **no-ZIP / pre-app fallback**. The override is an enhancement layered on top, authored only where sources fork; the default stands everywhere they don't. **Tips only** — pests stay FLAT and `sunlight` stays one region-neutral string (both NA-3b conclusions unchanged).
>
> **Timing:** overrides are **authored in-arc** (a rider on checklist Step 4, evidence-driven from sources already in hand — NOT a pre-computed bucket grid; bucket counts emergent and provisional, n=1), but **served post-app** (they require the ZIP→region resolver). Authoring in-arc is the cheapest moment, consistent with authoring region calendars ahead of the Phase C flip. Added as a served component of the post-app location-aware layer.
>
> **Schema:** a small **additive** field on the already-live region container (sibling to `region_notes`, dual-register). No new axis, no mass migration, nothing pre-populated; non-forking crops/tips change zero. Exact leaf-keys pinned at the **NA-3c ground-truth check** and registered in checklist Appendix A then (until registered, an unruled override field trips Appendix A's "unruled field is a finding" safeguard).
>
> **Durability:** the criterion is now in **checklist v1.4 Step 4** (principle-level, not a bucket count, so warm-season anchors that fork differently than lettuce still satisfy it), with an **Appendix A ruling** classing overrides CORE-PROSE-NEEDS-SIBLING — which makes Steps 6/8/9 (depth-lift, beginner siblings, dash resolution) cover overrides automatically. Because the checklist becomes the pipeline's `spec.md`/`rubric.md`, all 9 anchors + the ~114-crop pipeline inherit it; no separate rubric edit.
>
> **Standard doc:** `tip_region_authoring_standard_v1_1.md` (supersedes v1.0). **Provisional pending validation against crops 2–3** (cherry/beefsteak warm-season anchors may fork differently than cool-season lettuce). **Northern zone-band keying** is deferred to the first cold-zone tip fork (NA-3c is CA / `region_id`-keyed and does not need it).

**This file is the entry point. Read it first.**

**Promotion status:** canonical SHA is `ed4956664a4bdd486cdcb5fae219c40f0d3d65c75c6c20431b80285fc938241a` (Pass 2 warm-cell consistency cleanup; Claude Code, pushed). Predecessors: `20f9fc2b` (Pass 1b succession-rule hoist), `327a2d5c` (Pass 1a calendar exact-derivation), `582dbbad` (northern_tier reconciliation). **Pass 1a + 1b + 2 are ALL DONE -- the Claude-Code structural slice of the lettuce arc is complete.** **Order before any flag-flip (NOT the flip next):** ~~Pass 1a~~ -> ~~Pass 1b (succession-shape)~~ -> ~~Pass 2 (warm-cell consistency)~~ ALL DONE -> **Step 6 seasoned depth-lift (NEXT, claude.ai)** -> Steps 7/8 beginner siblings -> **Step 11 whole-crop validation** (re-runs every gate independently, incl. a whole-crop Step 5.5 re-walk; at Step 11 register the new `succession_spring`/`succession_fall` resolved-cell keys + the `se_gulf_month_resolution` method label in Appendix A; forward finding -- optional machine-readable fall heat-floor clamp field). The flip happens ONLY after Step 11 returns 0 violations. **Each pass that promotes re-pins this canonical SHA -- confirm `LATEST.txt` at the start of any session.** Pass 1a + 1b + 2 were Claude Code structural work; Step 6/7/8 (seasoned/beginner copy) is claude.ai's lane. **Project knowledge:** add `phase_3_lettuce_m15_northern_tier_findings.md` and swap in this regenerated `CURRENT_STATE.md`. The methodology/spec docs (gold-standard-arc-checklist v1.4, methodology v1.4.1, v1.5 heat-floor, region-primary spec, calendar-model spec, override spec + validator, tip-region standard v1.1) should already be present.

> ## ⚠️ PROMOTION / RECOVERY NOTE 2026-06-01 (NA-3d promote) — workflow lesson, NOT a data problem
>
> The NA-3d promote surfaced two pre-existing workflow drifts (both now half-fixed; no data lost):
> 1. **The prior chat emitted only a CURRENT_STATE *delta*, not a full regenerated file**, and it was missed — so this file was a session behind until regenerated (this regeneration). **Lesson: every session emits the ENTIRE CURRENT_STATE.md, never a patch.**
> 2. **The working `crops_data_final.json` had been deleted from `~/plant-dataset/`, and `LATEST.txt` pointed at na3b (`ec4f2b69`) — a file present nowhere on disk.** na3b/na3c/na3d had only ever been hand-promoted working files; git was still anchored at the M13 commit (`32d5900`). The deleted file was recoverable (git tracked it), and the NA-3d bundle file was whole, so promotion completed cleanly via `cp` from the bundle. NA-3d was then committed to git (`680a54e`), folding the cumulative M13→na3d delta into one commit (per-session na3b/na3c git granularity is gone but lives in the bundle archives + findings docs). **Lesson: keep git current at session close; the hand-promote + uncommitted-git combination is what turned a routine promote into a recovery. → LESSON HELD at NA-3e: committed `3a9c49a` at close, no recovery needed. Still un-pushed to origin (`git push` outstanding for offsite safety).**
> 3. **Archive breadcrumb gap:** the `3bfabbcf` na3c base is not archived locally (archive jumps from `566b45ed`/na2 to na3d). Trevor to grab the na3c file from the na3c chat and drop it into `~/plant-dataset/archive/` as `crops_data_final.2026-05-31.phase_3_lettuce_m15_na3c.3bfabbcf.json`. Nice-to-have for lineage continuity; git already covers practical recovery.

---

## ⚠️ LOCKED DECISION 2026-05-31 — lettuce authors into `regions{}` this arc (OVERRIDES the original M15 kickoff AND the 4c layer choice for lettuce)

**This is the single most important thing for any M15 sub-session to know. It reverses an inherited framing; read it before the historical entries below, several of which now contain superseded "authors on zones[]" language.**

**The decision (Trevor, 2026-05-31, after extended back-and-forth):**
> **Lettuce authors its warm-region data INTO `regions{}` during the M15 arc, per checklist Step 4 as written.** The original M15 kickoff fenced off `regions{}` ("author on the live `zones[]` layer; Phase C carries lettuce's data across later"), echoing the 4c layer-placement decision. **That fence is overridden for lettuce.**

**Why (the reasoning that settled it):**
1. **The checklist is the artifact under test.** Checklist v1.3 is what stress-tests all nine arcs and then becomes the pipeline's `spec.md`/`rubric.md`. Step 4's completion criterion is "every warm region the crop is grown in carries a populated region-constant `plantings[]` rule set ... no PENDING in a grown region." If lettuce runs against a kickoff that fences off Step 4, lettuce isn't testing the checklist — it's testing a modified process nothing downstream uses. The canary doesn't test the thing.
2. **The region-primary schema otherwise gets ZERO exercise across all three test crops.** If lettuce (and cherry/beefsteak) never write into `regions{}`, the region-primary shape — the most structurally novel piece — reaches the Phase C global flip completely untested. That inverts the safety the gating was supposed to provide.
3. **Low risk to do it now: no live consumers read the dataset yet** (established repeatedly in prior sessions). A wrong region shape is a data-rewrite, not a coordinated break across an app. The 4c "don't author into PENDING containers ahead of the gated flip" worry is real but weak for lettuce specifically — lettuce has no legacy stranding (it's authored after the region layer exists), so there's no technical blocker.

**The two guardrails that ride with it:**
- **Keep `zones{}` coherent alongside `regions{}`** through M15 + M16 — a complete fallback while region-primary is on trial. Author region cells AND keep the zone cells they resolve into consistent (checklist Step 5.5 sync invariant covers this).
- **The formal Phase C "region becomes primary" FLIP still waits until cherry + beefsteak prove out at M16.** We *populate* region cells now; we do NOT flip the primary read-layer now. (Distinction matters: "populate now, flip after the three prove out" ≠ "flip now.")

**Provenance note (so trust calibration is accurate):** the original deferral was an inherited framing from the kickoff + 4c, not a fresh on-the-merits decision. It conflicted with checklist Step 4, and that conflict was not flagged when the arc opened — it surfaced only after Trevor pushed three times. The lesson logged: an inherited decision can silently contradict the governing checklist; check the checklist against the kickoff at arc open.

**Consequence for cherry/beefsteak (M16):** their warm data IS legacy-stranded in `zones["9"/"10"]` (authored before the region layer), so for them Step 4 is a genuine retrofit (move data OUT of zones, INTO regions). That part is unchanged. The M16 regression test still rediscovers the Appendix C findings and performs that retrofit.

---

## ⚠️ northern_tier RECLASSIFICATION 2026-06-01 (Trevor-ratified) — READ BEFORE TREATING ANY REGION CELL AS "DONE"

`northern_tier` (zones 3–7) was created in **Phase A** of the region-primary migration (the additive-scaffolding step), by **lifting verbatim from the zone-5 anchor — NOT re-sourced, NOT authored to the v1.4 bar.** Its provenance reads "LIFTED VERBATIM from zone 5 … no re-sourcing in Phase A." It is therefore **populated but UNVERIFIED** — "wrapped, not worked." **Do NOT count it among the gold-standard-verified cells.** A verbatim lift carries forward whatever the source had AND misses everything the standard added after the source was authored. This was surfaced when NA-3d's se_gulf §6 fix prompted the question "what else lifted-and-assumed-done is carrying the same drift." Known/suspected gaps for its reconciliation session:
1. **`track` absent** on its entries (the region layer never got `track`; see below — it's the *name of the shared main calendar*, value `beginner`, NOT a beginner-only tag).
2. **§6 calendar drift (cold analogue of the se_gulf bug):** likely `wait` in winter positions that should be `cold_pause` under the 13-state vocabulary (which post-dates the lift). Verify against extension sources.
3. **Lift fidelity:** confirm the region-constant `plantings[]` faithfully represents zones 3–7 and that per-zone detail is genuinely preserved under `resolved_by_zone` (provenance claims it — verify, don't assume).
4. **Succession coherence:** cold zones are succession-heavy (zone layer has spring/fall succession for 3–7); confirm the region layer is coherent with succession carried via `succession_policy` per Step 5.5.
5. **v1.5 cold-zone fall-heat-floor methodology:** did not exist at Phase A; apply its checks (this is why northern_tier is sequenced AFTER the v1.5 mini-session, as the arc closer).

**Remaining lettuce-arc work after NA-3h: NO region cells — the prose/validation slice.** **LOCKED sequence (Trevor, 2026-06-01), now fully executed for the region cells:** ~~`warm_arid` (NA-3e)~~ → ~~`low_desert_az` (NA-3f)~~ → ~~`hawaii_tropical` (NA-3g)~~ → ~~**`northern_tier` reconciliation (NA-3h — arc closer)**~~ ALL DONE. **This reclassification (northern_tier = populated-but-unverified Phase-A lift) is now RESOLVED: NA-3h reconciled it up to the v1.4 bar.** What remains for lettuce gold-standard is NOT region work: **Step 6 (seasoned depth-lift) → Steps 7-8 (beginner siblings) → Step 11 (whole-crop validation pass + flip).** **Lettuce is NOT fully gold-standard until those complete** — the region-cell slice being done is necessary, not sufficient. (NA-3h opened one non-blocking item: northern_tier's succession-encoding shape disagrees with the warm cells'; likely reconciled at Step 11 / Phase C.)

> **Naming clarification logged 2026-06-01 (na3d_finding_003a, cosmetic, non-blocking):** the `track` field is the *name of the calendar an entry belongs to* — a single scalar (`beginner` for the shared main calendar both audiences see; `succession` for the seasoned-only succession calendar). It is NOT a beginner/seasoned tag pair (confirmed structurally — no companion `seasoned` tag). Trevor's design: the **main calendar is shown to both beginner and seasoned**, with an optional beginner NOTE where tighter dates help; **succession is seasoned-only**, a separate calendar built graphically later. The word "beginner" is thus a slightly misleading *name* for "main/shared." Whether to rename the value to `main` dataset-wide (981 entries + Step 5.5 enum + schema spec + validator) is a deferred standalone vocabulary session — low_desert_az (NA-3f), like fl_peninsula before it, matched the established `beginner` value rather than introduce a lone `main`.

---

## What happened 2026-06-01 (M15 lettuce NA-3h — northern_tier reconciliation, the REGION-CELL ARC CLOSER) (MOST RECENT — read first after the locked decisions)

**Session name:** `phase_3_lettuce_m15_northern_tier` (sequence name **NA-3h** — follows NA-3g; NA-3f/3g were already spoken for by low_desert_az/hawaii_tropical).

**NA-3h — `northern_tier` reconciliation (zones 3–7). Dataset write. SHA `55ba1ff0...` -> `582dbbad...` (git `fa95e23`, pushed).** The LAST region cell, and the closer of the region-cell slice of the arc. NOT a from-scratch author: a PROMOTE-UP. The correct, v1.5-compliant cold-zone data already lived in legacy `zones[3-7]` (NA-3a: two-track model, heat floor, re-sourced `succession_policy`); `northern_tier` was the stale Phase-A verbatim lift. **Truth flowed zone -> region (the INVERSE of every warm cell, where the region cell was correct and the legacy zone data was SE-contaminated).** Docs: `phase_3_lettuce_m15_northern_tier_findings.md`.

- **Succession migration (DATA-LOSS-CRITICAL) — clean.** Full spring+fall arm OBJECTS relocated verbatim from `zones[3-7]` into each `resolved_by_zone[z]` (full `direct_sow` payload: cadence rule + `sowings[]` + per-arm provenance). Count-audited, count-in == count-out per zone: z3=9, z4=9, z5=8, z6=10, z7=7. FIRST region-level `track:"succession"` authoring.
- **TWO winter-calendar DEFECTS caught by the post-write coherence gates + corrected** (continental zone-6b/7a T1, VCE): **z7** was internally self-contradictory (`Jan/Dec:harvest` vs `harvest_end Nov 10`, ZERO winter pause); **z6** over-extended (`Dec:harvest` past `Oct 26`, pre-sow `Feb:plant`). Gate (a) ">=1 cold_pause per frost-anchored cold cell" caught z7; gate (b) "active months ⊆ declared windows / no harvest >1mo past harvest_end" caught z6. **Monotonicity would have MISSED z7** (pause-count z3=5,z4=4,z5=3,z6=1 monotone-decreasing → z7=0 slips past); both gates necessary, both retained in the post-write sweep. Corrected dormancy: z3{Jan,Feb,Mar,Nov,Dec}, z4{Jan,Feb,Nov,Dec}, z5{Jan,Feb,Dec}, z6{Jan,Feb,Dec}, z7{Jan,Feb,Dec}. z5/z6/z7 keep `Nov:harvest` (legitimate last-fall-sow baby-leaf maturity tail; z7 Oct 11 sow + 30d matures ~Nov 10 = harvest_end, a viable outdoor edge, NOT the cold-frame case).
- **Method lesson:** the original build REMAPPED the stale calendar (`wait`->`cold_pause`) instead of DERIVING from windows; the remap preserved the z6/z7 defects (their winter months were mis-coded `harvest`/`plant`, not `wait`, so nothing remapped). Step 5's mandate is *derive from windows* — corrected build does so, asserts equality for z3-z5 (stored was right) and corrects z6/z7. Contained to northern_tier (warm cells were derived-from-scratch, no stale calendar to remap).
- **Shape:** nested-plantings duplication collapsed (region-constant MAIN rule once at region level; resolved cells = date-strings + `calendar[]` + the legitimately-per-zone succession arms). `track:"beginner"` on region main entry. Winter `wait`->`cold_pause`; NO `season_over` (cyclic-annual judgment call, Trevor-ratified: lettuce re-sows after warm-up, so winter is a pause not a year-end). v1.5 fall heat floor verified present (z3 fall offset -76). Dual-register `region_notes` authored (both were null; em-dash clean; seasoned note frames northern_tier as the standard non-inverted reference pattern + carries the cold-frame/row-cover season-extension note for z6/z7). Provenance re-stamped (`zone_promoted_verified`; `lifted_from_zone`/`static_precompute` dropped). zones[3-7] <-> region sync asserted mechanically (all 60 zone-months).
- **C8 (NA-3g Refinement A):** every northern_tier cell carries `cold_pause` and SELF-DECLARES; NO `year_round` flag added to any cold cell (verified in post-write sweep).
- **Decisions:** `m15_na3a_finding_003` (z4/z5 beginner fall late-edge) RESOLVED as a MAIN↔succession trim (z4 Sep 8->Sep 1, z5 Sep 15->Sep 8; §2a envelope now passes all five zones). Override: **DEFAULT COVERS** (no cold-zone tip-action fork; n=1 discipline; the "Northern zone-band keying" deferral did NOT trigger). z3 USCRN thin-coverage (28 station-years, below the 30 threshold but above field standard) DEFERRED to a future per-zone USCRN pass — confidence-tightening, not a defect. SE-source presence in z6/z7 resolved-cell `sources` VERIFIED ACCEPTABLE (Code-requested date-driving gate clean across ALL zones — dates driven by northern T1 (msu/ndsu/umn/umd/usu); SE sources only in the cell-level roll-up, climatically defensible for upland VA/NC/SC zones 6-7; not a defect, not routed forward).
- **Integrity:** start-SHA gate (`55ba1ff0`); in-memory dry-run; collateral hash audit (122 other crops + 13 top-level keys + 52 other lettuce fields byte-identical; within lettuce ONLY `regions.northern_tier` + `zones[3-7].calendar` + `zones[4]/zones[5]` `plant_out`/`last_plant_date` changed — all DECLARED intended); full-arm byte-identity; window-derived calendars with independent re-derivation; override validator (V5 dataset-convention fix per na3g_finding_002) 0 violations; declare-one-outcome (Refinements A+B); dataset-wide enum sweep (actual 12-state vocab); minified; fresh re-read outside the apply/verify scripts (12/12 independent checks); promoted + committed + pushed.
- **zones[3-7] NOT deleted** — for the north they remain the MORE-trustworthy copy until the Phase C flip; the deletion-gate "region cell carries everything `zones[3-7]` held incl. succession" precondition is now MET. Remaining deletion-gate preconditions (all consumers read region-first; round-trip; frost-input independence — P4 confirmed: frost data lives in top-level `zone_frost_data`, not inside per-crop `zones{}`) are Phase C.

**⚠️ CRITICAL — what this DOES and does NOT mean.** This closes the **region-cell slice** of the lettuce arc (Steps 4 + 5 + the per-cell Step 5.5 gates, across all 10 cells). It does **NOT** make lettuce gold-standard. Per the cherry template + gold-standard-arc-checklist v1.4, the arc STILL owes: **Step 6 (seasoned depth-lift)**, **Steps 7-8 (beginner siblings, ~100+ fields)**, and the **Step 11 whole-crop validation pass** (re-runs every prior gate independently against the full crop, INCLUDING a whole-crop Step 5.5 re-walk — see the succession-shape finding below). **The next session is the Step 6 seasoned depth-lift, NOT the flag-flip.** The flip is the LAST act of Step 11, only on 0 violations. (An earlier next-chat kickoff conflated "all region cells done" with "arc done" and framed the flip as next — corrected; the drift never reached a flipped flag because the Step 11 gate re-derives from the crop, not from a kickoff.)

**NEW open finding (non-blocking) — succession-encoding shape disagreement.** The CA/warm region cells encode succession as a resolved span + generative rule (succession lives at `succession_policy`), while northern_tier (this session) enumerated `track:"succession"` arms INSIDE `resolved_by_zone`. **These two shapes disagree.** Being adjudicated against region-primary spec §6 (guardrail: the dynamic app path must NOT read succession out of `resolved_by_zone`). **Likely outcome: northern_tier reconciles DOWN to match the others (generative rule hoisted to the region-constant rule layer), NOT the reverse.** Filed open/non-blocking so the next session knows northern_tier's succession shape is NOT settled. (This subsumes the "hoist generative succession rule" forward finding noted in the session findings doc.) **Companion forward finding:** maritime Pacific-NW zone-7 region split (`pacific_nw` cell, Phase C, cold-edge analogue of the warm-zone region forks) — continental z7 correctly gets a winter `cold_pause` here; maritime z7/8 is a future split, deliberately not allowed to muddy continental z7.

**Running gate-status (checklist v1.4, lettuce) at NA-3h close:** Step 0 done; Step 1 done; Step 2 done; Steps 3 -> NB; **Step 4 DONE — all 10 region cells populated AND v1.4-VERIFIED (`se_gulf`, 4 CA, `fl_peninsula`, `warm_arid`, `low_desert_az`, `hawaii_tropical`, `northern_tier`); 0 PENDING; 0 unverified**; Step 5 done across all cells; **Step 5.5 — per-cell gates PASS on all 10 cells, but the WHOLE-CROP Step 5.5 re-walk is owed at Step 11** (succession coherence across all cells + no-drift between `regions{}`, legacy `zones{}`, and `succession_policy` counts; the succession-shape finding above is the live open item there); **Steps 6, 7-8, 11 NOT STARTED — Step 6 (seasoned depth-lift) is NEXT.** **30 open findings + NA-3b (001 zone-sourcing reconcile OPEN — zones 8-11 characterized; 002 RATIFIED+APPLIED) + NA-3d (001 z10 CA-contamination OPEN; 002 se_gulf §6 CLOSED; 003 cosmetic OPEN; 004 seam note) + NA-3e (001 z8 SE OPEN; 002 NB) + NA-3f (001 z9 SE OPEN) + NA-3g (001 z11 SE OPEN; 002 NB — V5 portion RESOLVED at NA-3h) + NA-3h (succession-shape OPEN/NB; pacific_nw split NB) findings; m15_na3a_finding_003 RESOLVED; z6/z7 winter over-extension RESOLVED. 0 unresolved blockers** (two-field predicate; + NA-3a finding 003 RESOLVED at NA-3h).

---

## What happened 2026-06-01 (M15 lettuce NA-3g)

**NA-3g — `hawaii_tropical` warm region cell authored (zone 11). Dataset write. SHA `fc5c402d...` -> `55ba1ff0...`.** The sixth and FINAL batch of warm cells (after `se_gulf`, the 4 CA cells, `fl_peninsula`, `warm_arid`, and `low_desert_az`). All 9 warm/region cells are now complete. Anchored to University of Hawaii CTAHR. The most divergent cell, and the **FIRST PAUSELESS / YEAR-ROUND lettuce cell in the dataset.** Docs: `phase_3_lettuce_m15_na3g_findings.md` + `m15_na3g_verification_log.md`.

- **PAUSELESS YEAR-ROUND, NOT a soft heat_pause (Decision 1, Trevor-ratified — the hinge).** The kickoff flagged Hawaii as possibly pauseless and pre-ratified the encoding *if* verification confirmed it. It did, decisively, and the leaf-vs-head split makes the call clean. UH CTAHR **RES-164** *Lettuce Production Guidelines for Hawaii* (fetched + full text extracted) **Table 1 lists green-leaf and red-leaf lettuce as `year-round` plantings** at low elevation; the text states leafy/semi-head types grow year-round at low elevations (especially under sprinklers) and that lettuce is grown year-round in most home and community gardens. **HGV-2** describes 'UH Manoa' (a Green Mignonette selection) as heat-tolerant, slow to bolt, tip-burn resistant, grown year-round. Head types ARE seasonal (cooler months at low elevation, or high elevation) — but the leaf type, the `lettuce-leaf` crop, is year-round. A `heat_pause` would encode a no-sow exclusion that **directly contradicts** the explicit T1 year-round statement; the hot-season effect is a QUALITY dip (tip-burn, faster bolting) managed with cultivar + ~40% afternoon shade + elevation, NOT a production stop. Per the locked step 1, a quality dip that doesn't halt sowing is NOT a heat_pause. → introduced **`year_round: true`** (the FIRST boolean `year_round` KEY in the dataset; the prior 510 `year_round` occurrences were prose or the legacy `planting_note` render value, which is ALSO set here for render consistency).
- **Encoding (the locked treatment):** `resolved_by_zone[11]` carries (a) a **`calendar_basis` prose reason** stating year-round + BOTH pauses intentionally absent + the CTAHR/elevation/cultivar basis (matching the `ca_north_coast` no-heat_pause precedent — but here BOTH pauses absent, vs ca_north_coast's heat_pause-only absence with cold_pause still present); (b) the **`year_round: true`** machine-readable flag (because Hawaii is the FIRST cell with NO pause signal in `calendar[]` at all — ca_north_coast still carries detectable `cold_pause` STATES); (c) a 12-month **ALL-ACTIVE `calendar[]`** (no pause state). Crop-level `calendar_basis` stays `frost_anchored` (year-round-ness is a cell-level fact; only the Hawaii cell is aseasonal). No `season_model` enum built (n=1 discipline — a single boolean covers the one new shape).
- **Refinement A (Trevor): the declare-one-outcome invariant is SCOPED TO THE ALL-ACTIVE CASE.** Defined as: *a resolved cell whose `calendar[]` contains NO pause state (heat/cold_pause/season_over) MUST carry `year_round: true` + its `calendar_basis` reason; a cell WITH any pause state self-declares and passes without a flag or pause object.* NOT "every frost_anchored cell carries one of {heat_pause object, cold_pause object, year_round}" — that would false-positive `ca_north_coast` (cold_pause STATES + prose, NO object) on day one. The scoped form passes ca_north_coast + future northern_tier cleanly and targets exactly the all-active ambiguity `year_round` exists to resolve. Enforced by the verifier (C8), DELTA-SCOPED with a no-regression check (pre-existing all-active-undeclared backlog measured at **615 cells** — un-reconciled northern_tier + dormant Phase-A region scaffolds, the ARC CLOSER's job; this session introduced 0 new offenders).
- **Refinement B (Trevor): that invariant is kept SEPARATE from the §6 `calendar[]`↔pause-object sync invariant.** Complementary, not a replacement: §6 catches "calendar disagrees with the pause object it has" (needs an object); the new one catches "pause entirely omitted, cell silently reads all-active." Both run; neither absorbs the other.
- **GENUINE action-divergence bolting fork (Decision 2, Trevor-ratified).** UNLIKE low_desert_az/ca_desert (parallel forks of the SAME skip-summer action, authored on resolver-mechanics grounds) and UNLIKE the portable default ("harvest the whole thing right away at first bolt"): the CTAHR grower action is **materially different** — choose a heat-tolerant variety (Manoa/Anuenue), grow YEAR-ROUND, use afternoon shade or higher elevation through the hottest/longest-day months, protect from heavy rain; bolting is managed and slowed, NOT the trigger for a season-ending harvest and NOT avoided by a seasonal skip. The kickoff flagged Hawaii as the most likely cell to surface a genuine fork; it did. Override forks `tip_vntsyt8f` (bolting), CTAHR-sourced, FORK + OWN (`added_in: m15_na3g`, `evidence_tier: T1`) + INHERIT inherited. **First GENUINE action-divergence fork in the dataset** (ca_desert/low_desert_az were resolver-mechanics forks of the same action; se_gulf/fl_peninsula/warm_arid were no-fork).
- **Distinct `from: "year_round"` anchor (Decision 3 coherence ask, Trevor).** No frost in zone 11 (`frost_free_days: 365`), so the desert cells' `bolt_threshold_*` anchors don't transfer to a continuous window. Region-constant `direct_sow` uses a NAMED `from: "year_round"` anchor (NOT a frost/bolt anchor overloaded with `window_days: 365`), so the resolver keys "skip frost resolution, use windows as-is" off the named anchor, and `resolved_by_zone[11].year_round: true` is **derivable** from it. Verifier asserts the biconditional (C7): region anchor == `year_round` ⟺ resolved-cell flag set. `harvest_start` = `direct_sow_start + 55` (CTAHR Manoa ~50–60 day direct-seed maturity, used over the crop-level [30,45] mainland figure — mirrors low_desert_az using the AZ-local maturity).
- **RE-DERIVED from CTAHR, NOT lifted (source guard fired).** Legacy lettuce `zones[11]` is **100% SE/south-Florida-contaminated**: `[uf_ifas_vh021, uf_ifas_south_cal, uf_ifas_central_cal, uc_mg, tamu_agrilife]` with south-Florida synthesis prose (Sep–Jan + summer-heat-pause shape, "summer heat ends production"). Same class as finding_001 / na3d (z10) / na3e (z8) / na3f (z9), this instance for zone 11 / Hawaii. Filed **`na3g_finding_001`**. Note the structural divergence the region model exists to resolve: `fl_peninsula` (UF/IFAS, south Florida) legitimately covers zone 11 with a summer heat_pause for the SE; the Hawaii cell is the SAME zone number, DIFFERENT region, YEAR-ROUND. Recorded deliberate via a minted `resolution_method: "ctahr_year_round_resolution"`; no `lifted_from_zone`.
- **`track: "beginner"`** on the single `main` entry. Stale `sources_pending_admission: ["uhawaii_ctahr"]` marker dropped — **`uhawaii_ctahr` was ALREADY T1-admitted** (Phase B 2026-05-30; full `_admission_provenance`; `citable_for` names RES-164 + B-91 as the zone-11 tropical anchor; catalog has 80 entries). The kickoff's "check whether already admitted" resolved to YES — no admission needed.
- **na3g_finding_002 (NB — harness/convention gap):** `validate_region_tip_overrides.py` V5 expects boolean `verified: true` (per the spec EXAMPLE), but the dataset convention is a date-string `verified` (903/903 anchoring_urls; 0 booleans; all 3 shipped lettuce overrides use date strings). Running the validator as written would FALSE-FAIL all three shipped cells. This session's verifier applied V5 against the dataset convention (non-empty `verified`: str OR True). Recommend relaxing the standalone validator's V5 OR a one-pass `verified` type-normalization. (Also: the validator's `main()` harness iterates `data["crops"]` as a DICT, but live `crops` is a LIST — the substantive `validate_crop` logic is correct; the wrapper needs a list branch.) NB — does not block.
- **No honesty-flag caveat (contrast NA-3e).** CTAHR RES-164 PDF returned full machine-readable text via `web_fetch`; HGV-2 + B-91 + the CTAHR 40%-shade trial returned cited content via search. A `web_fetch` SERVER_ERROR on the HGV-2 PDF is a sandbox/egress artifact (same content came back via search), NOT a dead link; `verified: 2026-06-01` flags honest (NA-3f URL-honesty lesson applied).
- **Integrity:** start-SHA gate (`fc5c402d`); in-memory build; collateral hash audit (122 other crops + every top-level key byte-identical; within lettuce ONLY `regions.hawaii_tropical` changed — northern_tier + 4 CA + se_gulf + fl_peninsula + warm_arid + low_desert_az + `zones{}` byte-identical); value-preservation (no live PENDING; stale marker dropped); minified (`separators=(',',':')`, byte-for-byte == compact dump, 10,444,344 bytes); independent post-write re-verify (C0–C9) INCLUDING the override checks (0 violations, dataset-convention V5) + independent calendar re-derive + the NEW declare-one-outcome invariant (Refinements A+B) + a dataset-wide enum/track/calendar_basis sweep; plus a fully-independent fresh re-read outside the apply/verify scripts. Clean, all gates. End SHA `55ba1ff0...`.

**Running gate-status at NA-3g close (historical — superseded by the NA-3h gate-status above):** Step 4 — 9 of 10 region cells populated AND v1.4-VERIFIED (`se_gulf`, 4 CA, `fl_peninsula`, `warm_arid`, `low_desert_az`, `hawaii_tropical`); `northern_tier` populated-but-UNVERIFIED (its reconciliation, NA-3h, was the arc closer and is now DONE — see above); 0 warm regions PENDING. Step 5.5 PASS on hawaii_tropical (all-active, no drift). Steps 6-11 not started. (For current status see the NA-3h gate-status above; Step 4 is now CLOSED, all 10 cells verified.)

**`northern_tier` reconciliation (next — THE ARC CLOSER) scope:** the cold-zone region cell (zones 3–7), populated-but-UNVERIFIED (lifted verbatim from zone 5 in Phase A). This is NOT another warm cell. Per the staged `northern_tier_reconciliation_checklist_v1_0.md` (5-check list): (1) add `track` to its entries (the region layer never got `track`; value `beginner`, the shared main-calendar name — NOT a beginner-only tag); (2) §6 calendar drift — `wait` in winter positions that should be `cold_pause` under the 13-state vocab, verify against extension sources; (3) the all-active C8 invariant applies to its resolved cells once reconciled (its zones are NOT year-round — they have a real winter, so they should carry `cold_pause` states, NOT a `year_round` flag); (4) the succession-track migration is DATA-LOSS-CRITICAL (promotes zones[3–7] UP into northern_tier); (5) verify against the cold-zone fall-heat-floor methodology v1.5 (the fall arm + heat-floor clamp). **Runs AFTER the v1.5 cold-zone fall-heat-floor mini-session.** Lettuce is NOT fully gold-standard across all regions until this pass completes; once it does, all 10 region cells are gold-standard and lettuce flags flip (Step 11), the first crop through the full v1.4 arc. **Reconciliation start-SHA = `55ba1ff0...`.**

---

## What happened 2026-06-01 (M15 lettuce NA-3f)

**NA-3f — `low_desert_az` warm region cell authored (zone 9). Dataset write. SHA `8c2d0ff0...` -> `fc5c402d...`.** The fifth batch of warm cells (after `se_gulf`, the 4 CA cells, `fl_peninsula`, and `warm_arid`). Anchored to University of Arizona Cooperative Extension. Docs: `phase_3_lettuce_m15_na3f_findings.md` + `m15_na3f_verification_log.md`.

- **low_desert_az is a SINGLE continuous Sep–Feb window, mirroring `fl_peninsula` (Decision 1, Trevor-ratified) — REFINES the kickoff hypothesis.** The kickoff predicted "single long fall–winter window à la `ca_desert` (Sept–Dec/Jan)." Single-window CONFIRMED, but the U of A window is LONGER than ca_desert's: AZ1615 (Yuma) gives a "September through February" cool season for lettuce; AZ1099 names sowing slots running to Dec 10 plus a February replant per Maricopa MG; AZ1005 (Maricopa) runs leaf lettuce across the cool months with a summer break. Structurally the `fl_peninsula` single-long-window shape (single `bolt_threshold_end` window, `window_days: 165`, Sep–Feb), NOT the two-window arid (`warm_arid`/`se_gulf`) shape and NOT `ca_desert`'s shorter Sep–Dec window. Three independent U of A T1 sources converge. (Window-structure-per-source discipline held: decided on the source, not by analogy to ca_desert.)
- **Heat pause = May–Aug `[5,6,7,8]`, resolved on CITABLE climate data — NOT a fl_peninsula analogy borrow (Decision 2, Trevor-ratified).** Trevor asked whether the month boundary was citable rather than interpolated. It is: Phoenix climate normals (NWS, 1991–2020) put the first 100°F day at ~May 2; April avg high ~85°F; Jun–Aug avg highs 104–106°F (lows 78–84°F). AZ1099 gives lettuce germination thermoinhibition at 77–95°F. So May–Aug = hard exclusion (soil/air well above the thermoinhibition band); **April EXCLUDED** from the pause (85°F = spring bolting-close for an established crop / the `harvest_end` edge, not a no-sow month); **September EXCLUDED** (soil-gated fall sow-resume, the `bolt_threshold_end` anchor; U of A "wait if soil ≥85°F, plant after Sep 15"). Lands identical to `fl_peninsula`'s `[5,6,7,8]` **but for an independently-sourced desert reason**, and narrower than `ca_desert`'s `[4,5,6,7,8]` — defensible because the Phoenix April-85°F boundary is citable where ca_desert's Imperial Valley "90–120°F" basis did not pin April. Basis recorded in `heat_pause.basis` prose for auditability.
- **RE-DERIVED from U of A, NOT lifted (source guard fired).** Legacy lettuce `zones[9]` is **100% SE/humid-East-contaminated** for the low-desert AZ region: zone-level `[uf_ifas_vh021, uf_ifas_north_cal, uf_ifas_nwdistrict, tamu_agrilife, clemson_hgic]`; rule-object sources `uga_c1258_fall, uga_calendar, uga_b577, clemson_hgic, umd_ext, umn_ext` — Georgia/Clemson/Maryland/Minnesota/Florida humid-East, with explicit "warm zones (8-9)" SE synthesis prose and a two-window Oct–Nov + Jan–Feb humid-East shape. Same class as finding_001 / na3d_finding_001 (z10) / na3e_finding_001 (z8), this instance for zone 9 / low-desert AZ. Filed **`na3f_finding_001`**. Re-derived from U of A; recorded deliberate via a minted `resolution_method: "az_low_desert_month_resolution"` (the AZ analogue of the CA cells' `table_13_2_month_resolution`, fl_peninsula's `vh021_table_1_month_resolution`, and warm_arid's `nmsu_tamu_arid_month_resolution`); **no `lifted_from_zone`** (matching the CA + fl_peninsula + warm_arid re-derived shape).
- **PARALLEL bolting tip-override fork (Decision 3, Trevor approved Claude's lean).** `ca_desert` already forks `tip_vntsyt8f` (bolting) with low-desert content; the override-spec's own Shape A example is written for the low desert. low_desert_az is the SAME heat-inverted biology, so this is **NOT an action-divergence fork** (the grower action — winter crop, skip summer, harvest whole plant at first flower stalk — is identical to the portable default and to ca_desert). It was authored because **overrides are region-scoped and served per-resolved-region**: a low_desert_az user never reads ca_desert's cell, so without its own override that user falls back to the portable default and loses the low-desert specificity ca_desert users get. Rationale is resolver-mechanics, NOT a manufactured second action-fork, so consistent with n=1 discipline. Override carries FORK (`text_*`/`sources`/`anchoring_urls`) + OWN (`added_in: m15_na3f`, `evidence_tier: T1`); inherits INHERIT-class fields from the base tip (override-spec §3). `validate_region_tip_overrides.py` V1–V6: 0 violations. **This is the first PARALLEL override (ca_desert + low_desert_az now both fork tip_vntsyt8f) — distinct from se_gulf/fl_peninsula/warm_arid no-fork results; the bucket model gains a second low-desert instance, still emergent.**
- **`track: "beginner"`** on low_desert_az's single `main` entry (matches the dataset; see the naming clarification above). Stale `sources_pending_admission: ["uariz_ext"]` marker dropped (`uariz_ext` already T1-admitted Phase B 2026-05-30; `citable_for` names AZ1005 + AZ1615 as the zone-9 low-desert anchor; catalog has 80 entries) — exactly as warm_arid/se_gulf dropped their own stale flags.
- **Calendars derived PROGRAMMATICALLY** from window + declared pause (NA-3c/3d/3e lesson) — and the verifier *independently re-derives* the 12-month array from the encoded window + `heat_pause` and asserts equality with the stored array, so they cannot disagree by construction. Render convention mirrors fl_peninsula (`plant` run Sep–Nov, Dec–Feb display `harvest` as the bed is dominantly producing; `plantings[].direct_sow` authoritative for sowing).
- **NO honesty-flag caveat this session (contrast NA-3e).** Unlike the NA-3e Las Cruces MG Gantt chart (`na3e_finding_002`, not machine-readable → triangulated), the **AZ1005 Maricopa Gantt chart WAS machine-readable** via `web_fetch` text extraction this session, and AZ1615/AZ1099 returned full text via search/fetch. No triangulation-softness caveat for low_desert_az — windows read directly from U of A sources. **URL-honesty note logged:** a bash `curl` 403 on the arizona.edu domains is a sandbox-egress allowlist artifact (those domains aren't in the bash allowlist + they block non-browser UAs), NOT a dead link — confirmed all three URLs return their cited content via web_fetch/web_search; `verified: 2026-06-01` flags are honest.
- **Integrity:** start-SHA gate (`8c2d0ff0`); in-memory build; collateral hash audit (122 other crops + every top-level key byte-identical; within lettuce ONLY `regions.low_desert_az` changed — northern_tier + 4 CA + se_gulf + fl_peninsula + warm_arid + the 1 still-PENDING warm cell (hawaii_tropical) + `zones{}` byte-identical); value-preservation (no live PENDING; stale marker dropped); minified (`separators=(',',':')`); independent post-write re-verify (V0–V7) INCLUDING `validate_region_tip_overrides.py` (0 violations) + independent calendar derivation + a dataset-wide enum/track/calendar_basis sweep (0 violations); plus a fully-independent fresh re-read outside the apply script AND a re-run of the full post-verify against the unzipped bundle canonical (bundle proven sound). Clean, all gates. End SHA `fc5c402d...`.

**Running gate-status at NA-3f close (historical):** Step 4 — 8 of 10 region cells populated AND v1.4-VERIFIED (`se_gulf`, 4 CA, `fl_peninsula`, `warm_arid`, `low_desert_az`); `northern_tier` populated-but-UNVERIFIED; 1 warm region still PENDING (`hawaii_tropical`, authored in NA-3g above). (Superseded by the NA-3g gate-status above.)

---

## What happened 2026-06-01 (M15 lettuce NA-3e)

**NA-3e — `warm_arid` warm region cell authored (zone 8). Dataset write. SHA `3ab42b5d...` -> `8c2d0ff0...` (git `3a9c49a`).** The fourth batch of warm cells (after `se_gulf`, the 4 CA cells, and `fl_peninsula`). Docs: `phase_3_lettuce_m15_na3e_findings.md` + `m15_na3e_verification_log.md`.

- **warm_arid is a TWO-window structure (spring + fall bracketing a summer heat pause), sources-derived — Trevor pressed on whether this came from the research or the legacy JSON; confirmed it is the research.** Three independent arid T1 anchors converge: TAMU EHT-056 Table 1 (leaf lettuce, Texas Region III = USDA zone 8 per Table 3, fall ≈ Sep 1) + the TAMU El Paso County calendar (lettuce in the VERY-EARLY spring slot AND a separate fall garden; frost-free Apr 1–Nov 10) + NMSU bolting biology. The discarded legacy zone-8 data was *also* two-window but Georgia-sourced and in different months — independent derivation, not a lift. Mirrors the se_gulf / ca_interior two-window shape, NOT the fl_peninsula single-continuous one. (Window-structure-per-source discipline held: each cell decided on its own source, not by analogy.)
- **RE-DERIVED from NMSU/TAMU, NOT lifted (source guard fired).** Legacy lettuce `zones[8]` is **SE/humid-East-contaminated** for the warm-arid region: sourced from `uga_b577 / uga_calendar / clemson_hgic / tamu_agrilife / uga_c1258_fall` (+ `umd_ext`/`umn_ext` on harvest arms) = Georgia/Clemson/Maryland/Minnesota, with explicit "warm zones (8-9)" SE synthesis prose. Same class as fl_peninsula's `na3d_finding_001` (and finding_001), this instance for zone 8 / arid. Filed **`na3e_finding_001`**. Re-derived from NMSU/TAMU; recorded deliberate via a minted `resolution_method: "nmsu_tamu_arid_month_resolution"` (the arid analogue of the CA cells' `table_13_2_month_resolution` and fl_peninsula's `vh021_table_1_month_resolution`); **no `lifted_from_zone`** (matching the CA + fl_peninsula re-derived shape).
- **Heat pause = May–Aug `[5,6,7,8]` (matches se_gulf z8) + a beginner-register SOFTENER note (Trevor's call).** The structural pause stays four months in the calendar; Trevor's "note about the shorter window" is encoded NOT as a competing shorter pause but as a beginner-facing softener in `region_notes_beginner`: Jun–Aug is the hard no-go core, May is the ramp-in, and the note gives the actionable edges (last spring sow ~early May; resume fall ~late Aug) — his "tighter dates help a beginner" pattern, applied to the pause shoulders. Basis: frost-free Apr 1–Nov 10 (TAMU El Paso) + NMSU bolt biology (bolts >70°F long days; germination inhibited >~79°F).
- **NO tip-override fork (Trevor-ratified, attested at write).** Arid grower action is identical to the heat-inverted bolting default already carried by `succession_policy` and the other warm cells: skip the summer heat, grow the cool shoulders, harvest the whole plant at the first flower stalk. The two-window structure is a *window-shape* difference, not a *grower-action* difference. `tip_overrides` ABSENT (not present-and-empty). n=1 discipline. Validator V1–V6: 0 violations.
- **`track: "beginner"`** on warm_arid's single `main` entry (matches the dataset; see the naming clarification above). Stale `sources_pending_admission: ["nmsu_ext"]` marker dropped (`nmsu_ext` already T1-admitted; catalog has 80 entries) — exactly as se_gulf dropped its own stale flag.
- **Calendars derived PROGRAMMATICALLY** from window + declared pause (NA-3c/3d lesson) — and the verifier *independently re-derives* the 12-month array from the encoded `plant_out`/`harvest`/`heat_pause` and asserts equality with the stored array, so they cannot disagree by construction.
- **HONESTY FLAG — `na3e_finding_002` (NB): the Doña Ana County Master Gardener Las Cruces planting chart (the most granular NMSU date source, named in the `nmsu_ext` catalog `citable_for`) is a Gantt-style PDF whose colored month bars do not survive text extraction, and the donaanamastergardeners.nmsu.edu domain was not reachable from the sandbox to rasterize.** Exact month boundaries were instead **triangulated** from TAMU tabular dates + NMSU text + the El Paso County calendar (all T1, convergent). Trevor cleared proceeding on the triangulated dates. Window boundaries carry slight residual softness on this account; recorded in `plantings_provenance.verification_note`, not hidden. Tighten the spring-open / fall-open edges if/when the chart becomes machine-readable. **This is the one spot where "all gates green" does NOT cover biology — the structural verifier passed, but the precise window edges rest on triangulation, not on the single most-granular source.**
- **Integrity:** start-SHA gate (`3ab42b5d`); dry-run on in-memory copy; collateral hash audit (122 other crops + every top-level key byte-identical; within lettuce ONLY `regions.warm_arid` changed — northern_tier + 4 CA + se_gulf + fl_peninsula + the 2 still-PENDING warm cells + `zones{}` byte-identical); value-preservation; minified; independent post-write re-verify (V0–V7) INCLUDING `validate_region_tip_overrides.py` (0 violations) + independent calendar derivation + a dataset-wide enum sweep (0 violations). Clean, all gates. End SHA `8c2d0ff0...`; committed to git `3a9c49a` at close.

**Running gate-status (checklist v1.4, lettuce):** Step 0 done; Step 1 done; Step 2 done; Steps 3 -> NB; **Step 4 IN PROGRESS — 8 of 10 region cells populated AND v1.4-VERIFIED (`se_gulf`, 4 CA, `fl_peninsula`, `warm_arid`); `northern_tier` populated-but-UNVERIFIED (reconciliation queued as arc closer); 2 warm regions still PENDING** (`low_desert_az`, `hawaii_tropical`); Step 5 cold-zone succession + se_gulf + 4 CA + fl_peninsula + warm_arid windows done, remainder -> NA-3f/NB; Step 5.5 PASS (calendar coherence clean on warm_arid); Steps 6-11 not started. **30 open findings + NA-3b findings (001 zone-sourcing reconcile STILL OPEN — zone 8 now concretely characterized; 002 heat-anchoring RATIFIED + APPLIED) + NA-3d findings (001 z10 CA-contamination OPEN; 002 se_gulf §6 CLOSED; 003 cosmetic naming OPEN; 004 SR-line seam note) + NA-3e findings (001 z8 SE-contamination OPEN; 002 MG-chart readability gap NB), 0 unresolved blockers** (two-field predicate; + NA-3a finding 003 deferred non-blocking).

**NA-3f (next) scope:** the `low_desert_az` warm region cell (zone 9), anchored to University of Arizona Cooperative Extension (AZ1005 / Maricopa County vegetable planting calendar). **Decide single-vs-two-window PER SOURCE** — low desert AZ (Phoenix/Yuma) is the hottest warm cell; U of A likely gives lettuce a single long fall–winter window (à la `ca_desert`, Sept–Dec/Jan) as the spring window collapses into winter, BUT verify — do not assume from warm_arid. **Decide lift-vs-re-derive at the source guard** — `zones[9]` was UF/IFAS/TAMU/Clemson per NA-3c (SE-contaminated), so expect re-derive. **Override:** `ca_desert` already forked a low-desert bolting override (winter crop Sept–Dec, skip summer); low_desert_az is the SAME biological situation, so EITHER a parallel override is warranted OR the existing default + ca_desert override already covers it — decide on evidence, do not miss a real fork ca_desert already established, do not manufacture one. The low-desert summer heat pause may run wider than warm_arid's May–Aug (verify, e.g. Apr/May–Sep). `track: "beginner"`. Derive calendars programmatically; verifier re-derives. PENDING-guard targets live slots. **NA-3f start-SHA = `8c2d0ff0...`.** Then: `hawaii_tropical` (UH CTAHR, z11 — most divergent, author last of the warm cells); then **`northern_tier` reconciliation (arc closer).**

---

## What happened 2026-06-01 (M15 lettuce NA-3d)

**NA-3d — `fl_peninsula` warm region cell authored (zones 10, 11) + `se_gulf` §6 calendar drift corrected. Dataset write. SHA `3bfabbcf...` -> `3ab42b5d...`.** The third batch of warm cells (after `se_gulf` and the 4 CA cells). Docs: `phase_3_lettuce_m15_na3d_findings.md` + `m15_na3d_verification_log.md`.

- **fl_peninsula is a SINGLE continuous Sept–Feb window, NOT the two-window se_gulf mirror (Option A, Trevor-ratified).** VH021 Table 1 gives peninsular (central + south FL) lettuce as one continuous Sept–Feb span — unlike the two cool-season windows UGA gives for the more-northern SE/Gulf zones 8–9. The summer exclusion is the long heat pause OUTSIDE the window, not an interior between-window gap. Sanctioned by `region_primary_schema_shape_spec` §7b (which named this exact shape for fl_peninsula). **Lesson: "mirror se_gulf" was the wrong instruction once the source was verified — the kickoff flagged "verify; may be more compressed," and verification came back "structurally single-window."**
- **Both zones RE-DERIVED from VH021, NOT lifted (Trevor-ratified granularity decision).** zone[11] was clean UF/IFAS, but **zone[10] is CA-contaminated** — its synthesis note reads "coastal California, south Florida, south Texas" and cites `uc_anr_7216`, **which is not even in the source_catalog**. Lifting would import that blend. Chose single-provenance re-derivation from VH021 Table 1 for both zones (`resolution_method: "vh021_table_1_month_resolution"`, the VH021 analogue of the CA cells' `table_13_2_month_resolution`); **no `lifted_from_zone`** (matching the CA re-derived shape); **no `resolved_from`** (no cell carries it — confirmed by family audit, NOT a heat/frost distinction). Recorded as deliberate, not drift.
- **Heat pause = May–Aug `[5,6,7,8]` for BOTH zones** (broader than se_gulf's z8 [5,6,7,8] / z9 [6,7,8], as the kickoff predicted for the peninsula). Derived from EP450 (central) + EP452 (south) monthly calendars: lettuce drops out of both plant lists in March (warm-crop flip), reopens September; last Feb sowings finish harvest ~April, so the no-activity gap is May–Aug. (An earlier working hypothesis of Mar–Aug was corrected — Mar/Apr carry the harvest tail.)
- **z10/z11 differ in FROST EXPOSURE, not pause width** (corrects the session's early hypothesis that z11's pause would be longer). Both share the Sept–Feb window + May–Aug pause. Central FL (z10) has likely Jan–Feb freezes that can damage an exposed crop; South FL (z11) rarely freezes. Encoded in `zone_notes` + `region_notes_seasoned`, NOT in heat_pause months.
- **NO tip-override fork (Trevor leaned on Claude's call).** Peninsular FL does not diverge from the heat-inverted bolting framing already carried by `succession_policy`/se_gulf — both are winter-active, summer-excluded. The single-window structure is a *window-shape* difference, not a *grower-action* difference; the bolting action is identical. No override manufactured (n=1 discipline). `tip_overrides` ABSENT (not present-and-empty). Validator V1–V6: 0 violations.
- **se_gulf §6 drift CORRECTED under the stale-overwrite policy** (`na3d_finding_002`, CLOSED). se_gulf's `calendar[]` showed `wait` in summer months though its `heat_pause` object declared the pause — the §6 sync-invariant drift, carried from pre-v1.4 (NA-3b) authoring. Fixed: z8 months 5–8, z9 months 6–8 `wait`→`heat_pause`, bringing each `calendar[]` into agreement with its (already-clean) `heat_pause` object. Narrowly scoped — nothing else in se_gulf touched. **Policy operative: pre-v1.4 / pre-this-arc artifacts are STALE and overwritten as correct findings surface.**
- **`track: "beginner"`** on fl_peninsula's single `main` entry (matches the dataset; see the naming clarification above). Beginner region-note points at the reliable cool core of the window (Nov–Jan) — Trevor's "tighter dates help a beginner" pattern, applied.
- **Calendars derived PROGRAMMATICALLY** from window + declared pause (NA-3c lesson) — array and heat_pause object cannot disagree by construction.
- **A bug the gate caught (recorded):** the first apply used a blanket "PENDING substring" guard that tripped on the legitimate `plantings_provenance.supersedes: "PENDING scaffold..."` line (which mirrors se_gulf). The cell was correct; the GUARD was over-strict. Narrowed it to check live-data slots only, re-ran clean. **Lesson: PENDING/marker checks target live data slots, not provenance prose.**
- **Integrity:** start-SHA gate (`3bfabbcf`); dry-run on in-memory copy; collateral hash audit (122 other crops + 12 top-level keys byte-identical; within lettuce only `regions.fl_peninsula` replaced + `se_gulf` z8/z9 calendar arrays changed; northern_tier + 4 CA + the 3 still-PENDING warm cells + zones{} byte-identical); value/coherence checks; minified; independent post-write re-verify INCLUDING `validate_region_tip_overrides.py` (0 violations) + calendar-coherence re-check. Clean, all gates. End SHA `3ab42b5d...`.

**Running gate-status (checklist v1.4, lettuce):** Step 0 done; Step 1 done; Step 2 done; Steps 3 -> NB; **Step 4 IN PROGRESS — 7 of 10 region cells populated, but only 6 v1.4-VERIFIED (`se_gulf`, 4 CA, `fl_peninsula`); `northern_tier` populated-but-UNVERIFIED (reconciliation queued as arc closer); 3 warm regions still PENDING** (`warm_arid`, `low_desert_az`, `hawaii_tropical`); Step 5 cold-zone succession + se_gulf + 4 CA + fl_peninsula windows done, remainder -> NA-3e/NB; Step 5.5 PASS (calendar coherence clean on fl_peninsula's 2 cells + se_gulf's 2 corrected cells); Steps 6-11 not started. **30 open findings + 2 NA-3b findings (001 zone-sourcing reconcile STILL OPEN; 002 heat-anchoring RATIFIED + APPLIED) + 4 NA-3d findings (001 z10 CA-contamination OPEN; 002 se_gulf §6 CLOSED; 003 cosmetic naming OPEN; 004 SR-line↔USDA-zone seam note), 0 unresolved blockers** (two-field predicate; + NA-3a finding 003 deferred non-blocking).

**NA-3e (next) scope:** the `warm_arid` warm region cell (zone 8), anchored to NMSU (Circular 457 class) + TAMU AgriLife. **Decide single-vs-two-window PER SOURCE** (do NOT assume — se_gulf is two-window, fl_peninsula is single-window; the desert pattern is its own question). **Decide lift-vs-re-derive at the source guard** (check whether `zones[8]` is NMSU/TAMU-sourced and arid-appropriate, or carries the SE/CA contamination per finding_001 / na3d_finding_001). Classify summer `heat_pause` if the arid summer is a hard exclusion (may differ from peninsular — verify). Capture a bolting override ONLY if the arid sources fork the grower action (no-fork is legitimate). `track: "beginner"` on the main entry. Derive calendars programmatically. PENDING-guard targets live slots not provenance. **NA-3e start-SHA = `3ab42b5d...`.** Then: `low_desert_az` (U of A AZ1005, z9); `hawaii_tropical` (UH CTAHR, z11 — most divergent, author last of the warm cells); then **`northern_tier` reconciliation (arc closer).**

---

## What happened 2026-05-31 (M15 lettuce NA-3c)

**NA-3c — all four California `regions{}` cells authored: `ca_desert`, `ca_interior`, `ca_south_coast`, `ca_north_coast`. Dataset write. SHA `ec4f2b69...` -> `3bfabbcf...`.** The second batch of warm cells (after `se_gulf`), and the first real exercise of the region-tip-override mechanism. Single session, no split — verification was clean and authoring mirrored established precedents. Docs: `phase_3_lettuce_m15_na3c_findings.md` + `m15_na3c_verification_url_log.md`.

> **Naming:** this is NA-3c (the cell-authoring the original NA-3c kickoff scoped). A prior chat did NA-3c's source-verification front half (located Table 13.2 via a resolving URL, ratified finding_002 with a scope qualifier, built + promoted the region-tip override spec/validator; no dataset write, SHA unchanged). The next session is NA-3d (fl_peninsula). (Trevor confirmed this scheme 2026-05-31.)

- **All 4 CA cells anchored to UC ANR Master Gardener Handbook Table 13.2** (lettuce row), reached via the resolving UC MG "Time of Planting" URL (verified live 2026-05-31). **WXXE1505 DOI + ANR catalog 7267 stay DROPPED, never cited** (bad planning refs caught at the source guard last session). Table 13.2 windows matched the kickoff table EXACTLY. Per-cell T1 county corroboration verified side-by-side: Imperial County CE (desert), Sacramento County MG "Growing Lettuce in Warm Weather" (interior — heat dormancy + 106-107F bolting trials), SLO County MG (south coast), Marin County MG (north coast).
- **finding_002 (heat-primary/frost-secondary) APPLIED with its ratified scope.** The 3 heat-inverted cells took the heat-primary anchor; `ca_north_coast` is the cool-limited exception (Feb-Aug, summer-INCLUSIVE) and took the COOL-END anchor on the `northern_tier` precedent. Shapes: `ca_desert` (Sept-Dec, single window, summer Apr-Aug hard heat_pause); `ca_interior` (Aug; Nov-March, two windows, summer May-Jul heat_pause); `ca_south_coast` (Aug-April, single long mild window, soft summer gap May-Jul); `ca_north_coast` (Feb-Aug, NO heat_pause — summer is the active season — `calendar_basis` reason carried per resolved zone instead).
- **ca_north_coast shape decision (Trevor-ratified in-session):** take `northern_tier`'s COOL-END ANCHOR LOGIC (`soil_temp_40f` spring arm, summer-inclusive, `first_frost`-bounded close) but `se_gulf`'s COMPLETENESS SHAPE (authored dual-register notes, date-string-only resolved cells, structured provenance). `northern_tier` itself carries two non-gold-standard traits (null region_notes; nested per-zone `plantings[]` duplication) that were NOT inherited. **Sets the uniform precedent: all warm/CA resolved cells use the date-string-only shape** (region-constant rule layer is the single source of truth, materialized to date strings per zone).
- **Resolved-date granularity decision (Trevor-ratified):** CA resolved cells carry **Table-13.2 MONTH resolution** (`resolution_method: "table_13_2_month_resolution"`), a **deliberate T1 re-derivation** from the UC ANR windows — NOT lifted from the SE-sourced `zones{}` calendars. A legitimate per-region granularity difference from `se_gulf`'s day-resolution (se_gulf correctly lifted its day-dates because SE sources ARE the right home for those SE zones). Recorded as intentional, not drift; the value-preservation gate's "unless deliberately re-derived with T1 basis" clause covers it.
- **Region-tip override (the 2026-05-31 reversal's first application) — fork CAPTURED on the two clear poles only.** Evidence gate met: the day-length bolting cue is source-stated on the Table 13.2 page (lettuce flowers as days lengthen in late spring). Shape-A `tip_overrides` authored on `ca_desert` (winter crop, summer AVOIDED) and `ca_north_coast` (summer-ACTIVE) — both forking the bolting tip `tip_vntsyt8f`, both registers, T1 + verified URL, no INHERIT-class fields. **`ca_interior` + `ca_south_coast` kept the anchor-phrased default (no override)** — the base bolting tip reads correctly for both; per the evidence gate's "no manufactured overrides to fill a grid." Bucket model still provisional, n=1. **`validate_region_tip_overrides.py` post-write: 0 violations (V1-V6).** Override leaf-keys are now PINNED by ground-truth authoring: `regions[r].tip_overrides[stage][]` with `overrides_tip_id` + `text_beginner`/`_seasoned` + `sources` + `anchoring_urls` + `added_in` + `evidence_tier`.
- **Carried action CLOSED:** `succession_policy.tip_beginner` + `tip_seasoned` rewritten region-portable + anchor-phrased (read correctly in BOTH cold and warm zones; the prior cold-zone-only "your last frost" framing is superseded). `sunlight` left region-neutral and `pests` left flat — UNCHANGED per the supplement (the reversal is tips-only via the override mechanism).
- **A bug the gate caught (recorded):** the first apply pass wrote hand-placed `calendar[]` arrays whose `heat_pause`/`cold_pause` months disagreed with the declared `heat_pause.months`. The calendar-plantings coherence check flagged it; root-cause fix was to restore to start-SHA and **derive** the calendars programmatically from windows + declared pauses (precedence pause > plant > harvest > growing > wait), eliminating the placement-error class. Re-applied clean. (Lesson for NA-3d: derive calendars, don't hand-place month arrays.)
- **Integrity:** start-SHA gate (`ec4f2b69`); dry-run on in-memory copy; collateral hash audit (122 other crops + 13 top-level keys byte-identical; within lettuce only the 4 CA cells + `succession_policy.tip_*` changed; `se_gulf` + `northern_tier` + the 5 other warm cells byte-identical; region keyset unchanged); value-preservation (Table-13.2 month resolution, no PENDING leaked); minified; independent post-write re-verify INCLUDING the override validator + the calendar coherence re-check + a final integrity sweep (T1 discipline, URL verification, tip portability, override field classes, round-trip, no em-dash). Clean, all gates.

**Running gate-status (checklist v1.4, lettuce):** Step 0 done; Step 1 done; Step 2 done; Steps 3 -> NB; **Step 4 IN PROGRESS — 6 of 10 region cells done (`northern_tier`, `se_gulf`, + the 4 CA cells); 4 warm regions remain** (`fl_peninsula`, `warm_arid`, `low_desert_az`, `hawaii_tropical`); the Step-4 region-tip-override sub-criterion exercised for the first time (clean); Step 5 cold-zone succession done + se_gulf + 4 CA windows done, remainder -> NA-3d/NB; Step 5.5 PASS (calendar coherence re-check clean on all 8 new resolved cells); Steps 6-11 not started. **30 open findings + 2 NA-3b findings (001 zone-sourcing reconcile STILL OPEN; 002 heat-anchoring now RATIFIED + APPLIED), 0 unresolved blockers** (two-field predicate; + finding 003 deferred non-blocking).

**NA-3d (next) scope:** the `fl_peninsula` warm region cell (zones 10, 11), anchored to UF/IFAS — the closest sibling to `se_gulf` (same sourcing, same heat-inverted shape). Mirror the se_gulf shape; decide resolved-cell granularity (lift-vs-rederive) at the source guard per finding_001; capture a bolting override ONLY if the peninsular sources fork the grower action (a no-fork result is legitimate — peninsular FL may not diverge from the se_gulf framing). Derive calendars programmatically. **NA-3d start-SHA = `3bfabbcf...`.** Then: warm_arid (NMSU/TAMU, z8) / low_desert_az (U of A AZ1005, z9); hawaii_tropical (UH CTAHR, z11 — most divergent, author last).

---

## What happened 2026-05-31 (M15 lettuce NA-3b)

**NA-3b — first warm `regions{}` cell authored: `se_gulf` (zones 8, 9). Dataset write. SHA `a2f2a3df...` → `ec4f2b69...`.** First real exercise of the region-primary shape (the spec was previously validated only against a hand-written placeholder, cherry §7). Doc: `phase_3_lettuce_m15_na3b_findings.md` + `m15_na3b_se_gulf_verification_log.md`.

- **Scope decision (in-session, Trevor-ratified): one cell fully, then re-scope — NOT all 9.** NA-3b proved the shape on one fully-verified cell rather than pushing 9 at uncertain confidence (the cluster-13 false-confidence lesson). `se_gulf` chosen as the proof: the only warm cell already partly grounded (zone 8 = `sourced`, not `re-verify`), it is where the existing SE-sourced `zones{}` data actually belongs (so authoring it satisfies the orphaned-prose half of Step 4), and it exercises every mechanic (two-zone span, inverted calendar, heat pause, resolved_by_zone).
- **`se_gulf` authored end-to-end:** region-constant `plantings[]` (single `main`, **heat-anchored** — fall `from: bolt_threshold_end`, spring `from: soil_temp_40f`), `resolved_by_zone[8]`/`[9]` materialized from the existing T1-verified zone calendars (value-preservation gate: resolved dates == zone calendars, no re-derivation), per-zone `heat_pause` block (z8 [5,6,7,8] / z9 [6,7,8]), dual-register `region_notes_beginner`/`_seasoned`, `plantings_provenance`. Mirrors the `northern_tier` region shape exactly. Stale `sources_pending_admission` flag cleared (uga_ext + ufifas_ext already admitted T1).
- **Succession shape = mirror `northern_tier`, NOT a new region-succession arm.** `northern_tier` carries succession via `succession_policy` + resolved-cell notes, with NO separate region-level `sowings[]` arms. se_gulf mirrors this. Consequence: the NA-3a `sowings[]` date-array shape was NOT exercised at region level this session — the three NA-3a ratification flags remain ratified-as-working, untested against warm succession arms.
- **NEW PRECEDENT (`m15_na3b_finding_002`, ratification-pending): warm-region anchoring is HEAT-primary, frost-secondary** — the inverse of the cold-zone frost-primary/heat-floor-edge rule. Origin: Trevor's in-session observation "isn't it both?" — yes, asymmetrically (heat governs both season boundaries; frost a secondary cool-end modifier). Grounded in 2× UGA (B577 + C963, agreeing exactly: leaf lettuce spring Jan 15–Mar 1, fall Sep 1–Oct 1, DTM 60–85) + UF/IFAS corroboration (season Sep→Apr/May, peak Jan–Feb). **Becomes the warm-region rule for the remaining 8 regions + the pipeline. RATIFY before authoring the next warm cell.**
- **Regional-content design thread (extended in-chat; STRUCTURAL ANALYSIS, not all T1-verified — captured in the findings doc):** worked through whether tips/sunlight/pests need a region axis. Outcome: **(1)** pests/diseases stay FLAT (crop-specific; only `severity` is region-correlated and flat-is-not-wrong; deferred to runtime location layer); **(2)** ~~editorial timing tips adopt **anchor-phrasing in prose** ... instead of "your growing window" — NO schema change~~ **— ⚠️ REVERSED 2026-05-31 (see top REVERSAL banner): anchor-phrasing is RETAINED as the default/fallback, but region-tip OVERRIDES are now authored in-arc where T1 sources fork the grower action. Tips ride an additive region-container field; checklist v1.4 Step 4 owns the criterion**; **(3)** sunlight = one region-neutral string naming the warm/cool flip, NO axis (unchanged); **(4)** the **community-tip location axis is a CONFIRMED post-app forward item** — justified by community contribution (contributor location is captured metadata, not phrasing; stamp zone always + region when present; the always-on zone stamp dissolves the `northern_tier` coarseness problem without splitting it). Bundled with ZIP→region resolution + severity-by-location + region-tip-override serving as ONE post-app location-aware layer. None is dataset work now.
- **Carried action into NA-3c:** rewrite `succession_policy.tip_beginner`/`tip_seasoned` as anchor-phrased portable defaults (draft in findings doc); optionally tighten the flat `sunlight` string. **PLUS (2026-05-31 reversal): capture region-tip overrides in-arc** — for each editorial tip, check the CA T1 sources read in Step 4 for divergent grower actions across CA regions and author a dual-register override on each cell where they diverge; keep the anchor-phrased default where they agree. **First run the NA-3c ground-truth check** (where `tips_by_stage` / `succession_policy.tip_*` actually sit in the live structure) to pin the override leaf-keys, then register them in checklist Appendix A at NA-3c close. This is NO LONGER "not a tagging pass" — overrides ARE region-keyed fields, per checklist v1.4 Step 4. (Anchor-phrased defaults remain; overrides are additive on top, served post-app.)
- **Integrity:** start-SHA gate (`a2f2a3df`); dry-run on throwaway; collateral hash audit (122 other crops + all top-level keys byte-identical; within lettuce only `regions.se_gulf` changed); value-preservation (resolved dates == zone calendars); minified; independent post-write re-verify. Clean, all gates.

**Running gate-status (checklist v1.3, lettuce):** Step 0 ✅; Step 1 ✅; Step 2 ✅; Steps 3 → NB; **Step 4 IN PROGRESS — 1 of 9 warm regions done (`se_gulf`); 8 remain** (CA batch recommended next as NA-3c); Step 5 cold-zone succession ✅ + se_gulf windows ✅, remainder → NA-3c/NB; Step 5.5 PASS on binding gates; Steps 6–11 not started. **30 open findings + 2 NA-3b findings (001 zone-sourcing reconcile, 002 heat-anchoring ratification), 0 unresolved blockers** (two-field predicate; + finding 003 deferred non-blocking).

**NA-3c (next) scope:** the 4 California `regions{}` cells (`ca_interior`, `ca_north_coast`, `ca_south_coast`, `ca_desert`), single-source (UC ANR). Mirror the se_gulf shape. **First: ratify `m15_na3b_finding_002`** (heat-anchoring), and NA-3c is the explicit test of whether UC ANR frames CA's seasons the same way UGA did. Biological wrinkle to verify: coastal CA grows lettuce near year-round (little/no `heat_pause`) while interior/desert invert like se_gulf — do NOT copy se_gulf's pause months blindly. Plus the carried tip-rewrite action above **and the new region-tip-override rider (2026-05-31 reversal): run the ground-truth check on tip field placement first, capture overrides where CA sources fork the grower action, register the override leaf-keys in checklist Appendix A at close.** **NA-3c start-SHA = `ec4f2b69...`.** Then: fl_peninsula (UF/IFAS, cheap after CA), desert/arid (AZ1005 / NMSU 457), hawaii_tropical (UH CTAHR, most divergent — last).

---

## What happened 2026-05-31 (M15 lettuce NA-3a)

**NA-3a — cold/temperate succession authoring (zones 3–7) + `succession_policy` re-grounding. Dataset write. SHA `566b45ed...` → `a2f2a3df...`.** First `track:"succession"` windows in the dataset; first real exercise of checklist Step 5.5's coherence gates. Doc: `phase_3_lettuce_m15_na3a_findings.md`.

- **`succession_policy` re-grounded on verified cadence (Step 2 gap closed).** The unsourced `successions: 6` / `max_successions_per_season: 6` cap was BOTH unsourced AND an undercount — T1 sources (USU, UMN, UMD, UF/IFAS) frame lettuce succession as *interval (2 wk) across a plantable window, paused in summer heat*, never a fixed count. Removed the cap; added `count_basis: "derived_per_zone"` + `derived_zone_totals_cold` ({3:9,4:9,5:8,6:10,7:7}) + `count_note`; added `sources` (usu/umn/umd) + verified `anchoring_urls`; authored `tip_beginner` (was null). First arc to exercise methodology v1.5 **Clause C** (per-arm count ≠ season cap) — confirmed: season total is a derived per-zone calendar fact.
- **Cold-zone `track:"succession"` windows authored** (zones 3–7, 43 sowings total). Shape: one entry per ARM (spring, fall), dates enumerated in a `sowings[]` array (date-level enumerate-each — readable + light for the UI). Spring arm `soil_temp_40f` → bolt-driven close capped at fall-arm start (no overlap); fall arm heat-floored `first_frost` offset (z3 = −76/~Jul 1 per Clause A; z4–7 = −90). Each window T1-verified side-by-side (status does NOT inherit). Beginner (main) entries left byte-identical.
- **Two-calendar model confirmed + safe-sowing NOTE pattern established.** `track:"beginner"` = MAIN planting calendar (T1 source windows, beginner-facing, simple); `track:"succession"` = every-2-weeks detail (seasoned depth — too much for a first-season grower as the default). "Safest planting dates" are a NOTE on the main calendar (`safe_sowing_note` + `_beginner`, dual-register) that NAMES forgiving real sowings — NOT a separate window. This dissolves coherence drift at the root (a note can't fall outside the succession envelope).
- **Fall-arm maturity corrected (finding 002, resolved).** Mid-session the Step 5.5 coherence gate fired; chasing it corrected the fall-arm rule to **baby-leaf 30d, light-frost-tolerant** (latest sow = first_frost − 30d, no extra margin) — verified against Cornell MG / UMD / UMN. The over-conservative −5d margin (treated lettuce as frost-intolerant) and a 60d head-maturity reading were both wrong. The gate did its job: caught a biology error prose-screening would miss.
- **Deferred finding (003, non-blocking, → NB):** z4/z5 beginner fall window late-edge runs ~7d past the last viable baby-leaf succession sow (pre-existing M13 anchor over-extension). Main windows left byte-identical this session; NB bounds them.
- **⚠️ THREE ratification flags for Trevor (NA-3a findings §3 — do NOT treat as settled):** (1) the `sowings[]` date-array encoding as the canonical succession shape; (2) the beginner↔succession coherence reformulation (note-dates-are-real-sowings + beginner-start ≥ succession-start, with beginner late-edge as a finding not a hard gate); (3) `succession_policy` count semantics (derived-per-zone, no integer cap). These become precedent for all nine arcs + the pipeline spec.
- **Integrity:** start-SHA gate; collateral hash audit (122 other crops + 13 top-keys + 52 lettuce sub-objects byte-identical; beginner entries + warm zones byte-identical; only `succession_policy` + zones[3-7]{plantings + 2 note keys} changed); minified; independent post-write re-verify (PASS, all binding gates).

**Running gate-status (checklist v1.3, lettuce):** Step 0 ✅; Step 1 ✅; **Step 2 ✅✅ (succession_policy gap closed)**; Steps 3 → NB; **Step 4 → NA-3b**; Step 5 cold-zone succession ✅, remainder → NA-3b/NB; **Step 5.5 PASS on binding gates (first real exercise — 43 sowings, no drift, no overlap, z3 heat floor)**; Steps 6–11 not started. **30 open findings, 0 unresolved blockers** (+ finding 003 deferred non-blocking).

**NA-3b (next) scope — unchanged:** warm-region fills + region succession (the region-schema test, per the locked decision). Populate the 9 warm `regions{}` cells per Step 4; first move = verify grown-vs-not per region; source each from its region-appropriate T1 anchor (re-verify/locate cells need side-by-side, don't inherit); author region succession reusing the `sowings[]` shape; classify summer `heat_pause` per region via the heat-pause path (NOT Clause A). Keep warm `zones{}` coherent. **NA-3b start-SHA = `a2f2a3df...`.** *(RESOLVED — NA-3b done; see the NA-3b entry at the top. It authored 1 of the 9 cells (`se_gulf`) as a proof-of-shape and re-scoped the remaining 8; region succession was carried via `succession_policy` mirroring `northern_tier`, not a separate `sowings[]` arm.)*

---

## What happened 2026-05-31 (M15 lettuce NA-1 + NA-2) (read this FIRST after the locked decision — most recent)

**M15 lettuce arc opened. Preflight clean against `518a6a36...`.** The arc is large and split into sub-sessions (NA scaffolding/authoring → NB verification grind + depth-lift → NC siblings + dashes → ND validation/flip). NA itself split: **NA-1** (methodology, no write), **NA-2** (Step 1 + Step 2, write), **NA-3** (succession + region authoring — next).

**NA-1 — methodology v1.5 ratified (markdown only, no dataset write).** The cold-zone fall heat-floor rule, pulled forward from its post-lettuce slot (Trevor option B) so the canonical first succession crop authors against a ratified rule. **The rule:** a fall cool-season sow is clamped to `max(first_frost − 90, ~Jul 1)` — never scheduled into the >80°F summer-heat germination-failure window (thermoinhibition). **T1 basis:** USU Extension (soil >80°F → lettuce/spinach seed dormant; lettuce-specific fact sheet), corroborated by NC State + MSU; mechanism from two peer-reviewed journals (ABA/GA). **Threshold corrected 85°F → 80°F** (the 85°F figure was T2 seed-company/blog; 80°F is T1 and more conservative). **For lettuce the floor binds in exactly ONE zone — zone 3** (already at `first_frost−76` = Jul 1 from M13). Three clauses: (A) heat floor; (B) fast-maturity assumption for compressed windows; (C) **per-arm succession count ≠ season-wide `successions` cap** — a cross-arc PATTERN decision (all nine arcs inherit it: a 3–4-week cold-zone fall arm supports 1–2 sowings, not the season's 6). **Resolves finding `m13_s1b_nc_methodology_candidate_001`.** Generalizes to spinach/radish/cilantro — mechanism inherits; threshold + source re-verified per crop (radish/cilantro are bolt/quality floors, not germination floors). Scaling path logged (not built): a per-zone USCRN soil-80°F-crossing anchor replacing the uniform ~Jul 1 floor. Doc: `cold_zone_fall_heat_floor_methodology_v1_5.md`.

**NA-2 — Step 1 + Step 2 (dataset write). SHA `518a6a36...` → `566b45ed...`.**
- **Step 1: `source_set` populated** — was empty; now 35 catalog IDs, derived by a live walk of every `sources` array + `anchoring_urls` key on lettuce (0 orphans). Matches the cherry/beefsteak precedent that `source_set` is the full draw-on set, not the per-arc-verified subset. Inheritance assertion held (M13 records are candidate input; only Step 5 side-by-side confers verified status).
- **Step 2: v1.4.1 extreme-zone check recorded** — checked the fall direct_sow anchor at the coldest zones against the heat floor. **PASS, no correction needed:** zone 3 already floored to `first_frost−76`; zones 4–7 use `−90` and clear the floor; warm zones use the heat_pause path (NA-3). Recorded as `verification_status.extreme_zone_check`.
- **Structured surfaces confirmed launch-ready-core quality (read live):** `soil` (dual `_core`/`_seasoned` vocab, 4 T1 + anchoring URLs, both registers authored); `ph` ([6.0,6.8] preferred / [6.0,7.0] tolerated — wraps correctly; equal lower bound intentional per `s1c_finding_003`; 4 T1; both registers). The remaining Step-2 per-surface gating (`succession_policy`, `container_notes`, `plantings[0]` T1-floor) folds into NA-3 since succession authoring touches `plantings[]` directly. **`succession_policy` currently has NO `sources`/`anchoring_urls` — a Step 2 gate gap to fix when authoring succession in NA-3.**
- **Integrity:** start-SHA gate; collateral hash audit (every crop except lettuce + every top-level key byte-identical; within lettuce only `verification_status` changed: `source_set` + `extreme_zone_check` + `verification_log_ref`); minified; independent post-write re-verify. Clean.

**Running gate-status (checklist v1.3, against lettuce):** Step 0 ✅; Step 1 ✅ (source_set=35); Step 2 ✅ (soil/ph confirmed, extreme-zone PASS; remainder → NA-3); Steps 3–11 not started. **30 open findings, 0 unresolved blockers** (two-field predicate). NA-1 resolves the methodology candidate. No checklist friction so far (Step 5.5's real test is NA-3).

**NA-3 (next sub-session) scope — two halves:**
- **NA-3a — cold/temperate succession (zones 3–7; `northern_tier` region already populated).** Author `track: "succession"` windows (shape (a) enumerate-each, 2-wk interval, per-arm counts per Clause C, fall arm clamped per the heat-floor rule). Add `succession_policy.sources` + anchoring URLs. Verify each window per T1 (Step 5). Assert beginner↔succession coherence + calendar↔plantings sync.
- **NA-3b — warm-region fills + region succession (the region-schema test, per the locked decision).** Populate the 9 warm `regions{}` cells per Step 4 (each grown region: verified region-constant `plantings[]` from its region-appropriate T1 anchor — `se_gulf`→uga/ufifas, `ca_*`→ucanr, `warm_arid`→nmsu/tamu, `low_desert_az`→uariz, `fl_peninsula`→ufifas, `hawaii_tropical`→uhawaii_ctahr). Author region-level succession; classify summer `heat_pause` per region (biological claim, verify per T1); keep warm `zones{}` coherent. **First move: verify grown-vs-not per warm region** — "not grown here" is a legitimate Step 4 outcome, not a gap.
- **Recommended order: 3a first** (prove the succession-encoding shape on already-clean ground, then carry it into the warm-region complexity). Split further if heavy. **NA-3 start-SHA = `566b45ed...`.**

**Warm-zone authoring note (lettuce-specific biology, on record):** zones 9–11 are inverted (fall/winter/spring growing, summer shut). Existing zone-9 beginner anchors already use per-window maturity offsets (60d fall / 45d winter) reflecting slower cool-season growth (UMN T1 principle) — succession windows inherit this, NOT a flat 38-day maturity. UF/IFAS confirms warm-zone lettuce is an Oct–Feb crop. The Jun/Jul/Aug interior `wait` months are the `heat_pause` reclassification target.

---

## What happened 2026-05-31 (4d checklist v1.3) (kept for context)

**Markdown-only session. No dataset write.** Finalized gold-standard arc checklist **v1.3**, converting Step 5.5's three schema-pending criteria into real, enforceable gates now that 4c built the fields they were waiting on. Verified against the live post-4c dataset before writing (981 `plantings[]` entries all carry `track`; 123 crops all carry `calendar_basis` as 109 frost_anchored / 13 non_seasonal_indoor / 1 generic_placeholder; the 3 pause states defined-and-reserved, populated zero times; zero validator violations).

**What changed v1.2 → v1.3 (Step 5.5 only; rest of checklist untouched):**
- **The gate now branches by `calendar_basis` first.** `frost_anchored` (109) → full calendar/succession/track/pause criteria, `plantings[]` must be populated. `non_seasonal_indoor` (13) → `plantings[]` legitimately empty, cyclic `calendar[]` is source of truth, track/succession/sync criteria N/A, frost-resolution suppressed. `generic_placeholder` (1, heirloom-tomato) → calendar/succession criteria N/A until varietal split; gold-standard not claimed in generic form.
- **Beginner↔succession coherence is now a real gate** — the relationship v1.2 could not check because only one track existed. With `track`, it gates: where both tracks exist, the beginner window sits within the succession envelope (never shows a plantable month succession lacks). Single-track crops pass trivially.
- **`track`-valid + exactly-one-beginner-per-zone** gates added (all 981 entries currently `track: "beginner"` — correct pre-arc state).
- **Succession-encoding gate** now checks for `track: "succession"` entries specifically (49 `suitable: true` crops must author them at gold-standard; currently zero have — the legitimate pre-arc state).
- **Pause legibility** sharpened from "explain in a findings note" (v1.2 interim) to "must be a specific `cold_pause`/`heat_pause`/`season_over` state" (the real 4c vocabulary), classified per-month Step-5-style.
- **`calendar[]` 13-state enum** validity gate added.

**Open reconciliation note (carried, not blocking):** the beginner↔succession coherence criterion as written uses the "beginner window sits within the succession envelope" formulation. **NA-3 is the place to confirm this matches `calendar_model_schema_spec_v1_0.md` §2a** (the spec governs on conflict) — it becomes a live gate the moment lettuce authors its succession track. (Spec §2a does state "the beginner window sits within the succession envelope," so they appear aligned; confirm at NA-3a.)

**Project-knowledge action:** remove checklist v1.2, add v1.3 (one checklist version at a time).

---

## What happened 2026-05-31 (4c calendar-model schema) (kept for context)

**Dataset mutation session (design + scaffold). SHA `f6b2d800...` → `518a6a36...`. Schema 2.8 unchanged.** Built the calendar model whole — the structure lettuce (M15) and every later crop needs — scaffolded but NOT populated. Authoritative spec: `calendar_model_schema_spec_v1_0.md`.

**⚠️ Note on the 4c layer-placement decision:** 4c decided `track`/`calendar_basis` land on the `zones[]` layer and `regions{}` stays untouched, with the reasoning that M15 lettuce would author on `zones[]` and Phase C would carry data across. **For the `track`/`calendar_basis` SCAFFOLD fields, that placement stands** (they live on `zones[]` and migrate at the flip). **But the broader implication 4c drew — that lettuce authors its warm DATA on `zones[]` only and defers region population to Phase C — is OVERRIDDEN by the 2026-05-31 locked decision above** (lettuce populates `regions{}` this arc per checklist Step 4). The scaffold-field placement and the data-authoring layer are separable; only the latter changed.

**What the apply did (scaffold, not populate):**
- **`track` added to 981 `plantings[]` entries**, all re-tagged `track: "beginner"`. **Zero `succession`/`second_planting` entries authored.**
- **`calendar_basis` added to all 123 crops:** 109 `frost_anchored`, 13 `non_seasonal_indoor`, 1 `generic_placeholder` (`heirloom-tomato`).
- **Relaxed validator:** `plantings` length ≥ 1 UNLESS `calendar_basis ∈ {non_seasonal_indoor, generic_placeholder}`.
- **`calendar[]` vocabulary extended** 10 → 13 (`+cold_pause`/`+heat_pause`/`+season_over`). The 3 new states appear **ZERO times** — reserved, not populated.
- **Non-seasonal indoor mode defined; sync invariant defined.**
- **Zero calendar months reclassified. Zero prose authored. Zero succession windows authored.**

**Drift FLAGGED for owning arcs (NOT fixed):** cherry-tomato zone-9 `calendar[]` shows continuous harvest Apr–Jul with no pause, though its `notes` prose claims a mid-summer heat pause. **Owning arc: M16** (overlaps Appendix C `gs_exemplar_finding_001`). The 34 interior `wait` months dataset-wide are the prime reclassification candidates.

**Phase C coordination outcome:** composes, does not collide. `track` migrates with each `plantings[]` entry at the flip; `calendar_basis` is crop-level/region-independent; the 3 new `calendar[]` states apply identically to `regions[r].resolved_by_zone[z].calendar[]`.

**Spec docs (into project knowledge):** `calendar_model_schema_spec_v1_0.md` (authoritative), `schema_2_8_calendar_model_addendum.md`.

---

## What happened 2026-05-31 (Calendar/succession planning) (kept for context)

**Markdown-only planning session.** Surfaced + scoped the calendar/succession work. Five decisions carried into checklist v1.2 + finding docs:
1. **Checklist v1.1 → v1.2: added Step 5.5 "Planting + succession calendars are true and make sense."** Previously UNOWNED.
2. **Succession-encoding shape LOCKED: shape (a) enumerate-each-window + a `track` field** (`beginner`/`succession`/`second_planting`). *(Implemented in 4c.)*
3. **Succession deep dive:** `succession_policy` (cadence) + `plantings[]` (windows) is sound; multi-window was DELIBERATELY deferred to lettuce. The 49 suitable-but-one-window crops are the deferral as data.
4. **Empty `plantings[]` = THREE legitimate meanings → `calendar_basis` reason field** + relaxed validator. *(Implemented in 4c.)*
5. **CALENDAR-MODEL SCHEMA SESSION inserted before M15.** *(That session is 4c — DONE.)*
Standing principle adopted: **absence always carries a reason field; the gate checks the reason exists, not that the slot is filled.** Finding docs: `succession_schema_2_7_deep_dive_finding.md`, `calendar_track_structure_finding.md`.

---

## What happened 2026-05-31 (Companions reconciliation) (kept for context)

**Decision + dataset mutation. SHA `68ca19a8...` → `f6b2d800...`. Schema 2.8 unchanged.** The companions `_core`/`_seasoned` split reshaped to the **three-array register-by-membership model** (`*_beginner` / `*_beginner_seasoned` / `*_seasoned`; same triple for `bad`). 511 entries conserved; each `why` → `why_seasoned` (byte-identical), `why_beginner: null` scaffolded on both-modes entries. `launch_ready_*` flags + `cause_beginner` nulls deliberately untouched (M16). Cherry/beefsteak `why_beginner` is the one un-authored field on two otherwise-complete crops.

---

## What happened 2026-05-31 (Phase 0 Part 2) (kept for context)

**Dataset mutation. SHA `74fa36f0...` → `68ca19a8...`. Schema 2.7.5 → 2.8.** Renamed every register-bearing prose field to the symmetric `_seasoned`/`_beginner` shape, byte-preserving every value; CP fields got a `_beginner` sibling (kept or null-scaffolded); SP fields `_seasoned` only; all 3 anchors converted. 14,553 renames; all integrity audits passed.

**Structural notes carried forward:** `tips_by_stage` is a dict-of-stages, not a flat list (compound tooling must special-case it); `yield_expectations.factors` is list-valued prose; prose leaf-names recur outside the conversion surface (orphan-key checks must be path-scoped).

---

## What happened 2026-05-30 (Phase B + Phase A + region model) (kept for context)

- **Phase B — correction phase — COMPLETE.** Built the region→source map at top-level `region_source_map{}` (which T1 anchor serves which region/zone; build-time infra, NOT a runtime read). Catalog 76 → 80 (`ufifas_ext`, `nmsu_ext`, `uariz_ext`, `uhawaii_ctahr`, all T1). **Per-crop warm `sources` arrays NOT yet proven T1** — that is the gold-standard re-runs, claim-level verified via methodology v1.4 Step 4. **(M15 NA-3b is where lettuce makes its per-crop warm sourcing true, per the locked decision.)**
- **Phase A — region scaffolding (additive) — COMPLETE.** Added `regions{}` sibling to all 123 crops; identical 10-region menu. `northern_tier` REAL (cold-zone rule lifted verbatim, payload under `resolved_by_zone[z]`). Warm regions empty PENDING containers.
- **Region model LOCKED:** region-primary (region-beside-zone); 10 slugs ratified; two-layer cut; `lifecycle_override` REGION-scoped.

---

## What happened 2026-05-30 (checklist + register decision) (kept for context)

Gold-standard arc checklist authored → stress-tested twice → v1.1 → (later v1.2/v1.3). Auto-derived denominator (Appendix A) closes the lettuce incomplete-denominator failure mode permanently. EXPLICIT-REGISTER SCHEMA DECISION: symmetric `_seasoned`/`_beginner` suffixing. Key enabler: NO live consumers read the dataset yet, so the register rename + region flip are NOT reader-coordinated migrations. Forward path re-sequenced: Phase 0 → M15 lettuce → M16 cherry+beefsteak AS A REGRESSION TEST.

**The four cherry/beefsteak exemplar findings on record (Appendix C; for M16):** `gs_exemplar_finding_001` (UMN/Minnesota prose in the SE-sourced zone-9 cell, BOTH anchors), `_002` (16 user-facing `--` each), `_003` (extreme-zone offset computation not recorded), `_004` (no `cause_beginner`). *(4c added a fifth flag: cherry z9 calendar/prose heat-pause drift.)*

---

## Dataset

- **Canonical location:** `~/plant-dataset/crops_data_final.json`
- **Current SHA:** `582dbbad8a0eda15d2fc9e2332c29dc1c1355c7dfd638ecf257a870937d1919e` (M15 lettuce NA-3h — northern_tier reconciliation, 2026-06-01). Promoted AND committed to git (`fa95e23`) AND pushed. `LATEST.txt` == `582dbbad...`.
- **SHA lineage (recent):** `566b45ed...` (M15 NA-2) → `a2f2a3df...` (NA-3a) → `ec4f2b69...` (NA-3b) → `3bfabbcf...` (NA-3c) → `3ab42b5d...` (NA-3d) → `8c2d0ff0...` (NA-3e) → `fc5c402d...` (NA-3f, low_desert_az) → `55ba1ff0...` (NA-3g, hawaii_tropical) → `582dbbad...` (NA-3h, northern_tier). *(na3b/na3c local files + the na3c archive breadcrumb were not all on disk historically — see the Promotion/recovery note up top; git now holds the chain through `fa95e23`.)*
- **Schema version:** **2.8** on disk. Spans: register suffixing (Phase 0), companions register-by-membership, region scaffolding (Phase A/B, additive), calendar-model scaffolding (4c). The pending region-primary FLIP (Phase C) and perennial extension also claim "2.8" — version bookkeeping pinned per session.
- **Crop count:** 123
- **Zones:** USDA hardiness 3-11
- **Regions:** 10-region menu scaffolded per crop. Lettuce: ALL 10 region cells are v1.4-VERIFIED — `se_gulf` (zones 8–9, NA-3b) + the 4 CA cells (`ca_desert`, `ca_interior`, `ca_south_coast`, `ca_north_coast`; NA-3c) + `fl_peninsula` (zones 10–11, NA-3d) + `warm_arid` (zone 8, NA-3e) + `low_desert_az` (zone 9, NA-3f) + `hawaii_tropical` (zone 11, NA-3g) + **`northern_tier` (zones 3–7, NA-3h — reconciled from the Phase-A verbatim lift up to the v1.4 bar)**. That is **10 of 10 lettuce region cells populated AND v1.4-verified; 0 PENDING; 0 unverified.** The region-cell SLICE of the arc is closed; lettuce still owes Steps 6/7/8 + the Step 11 validation pass before the flip (see banner). All OTHER crops' warm regions remain PENDING until their own arcs. **Open:** northern_tier's succession-encoding shape (`track:"succession"` arms in `resolved_by_zone`) disagrees with the warm cells' (`succession_policy` generative rule); likely reconciles down at the Step 11 / Phase C work — non-blocking.
- **Calendar model:** multi-track — `track` on every `plantings[]` entry (981 `beginner` + 10 `succession`, all lettuce cold-zone NA-3a; 0 second_planting; region cells carry `track: "beginner"` on their single `main` entry — note the region-layer `track` gap on the older cells, na3d_finding_003a); `calendar_basis` on every crop (109/13/1); `calendar[]` 13-state vocab (`heat_pause` populated in lettuce `se_gulf` + the 3 heat-inverted CA resolved cells + `fl_peninsula`'s 2 cells [May–Aug], NA-3d + **`warm_arid` z8 [May–Aug], NA-3e**; `se_gulf`'s `calendar[]` `heat_pause` STATE was corrected from `wait` in NA-3d; `cold_pause` populated in lettuce `ca_north_coast` resolved cells, NA-3c; `season_over` still reserved). Region resolved cells carry a `resolution_method` stamp: `se_gulf` day-resolution (`static_precompute` + `lifted_from_zone`); CA cells `table_13_2_month_resolution` (re-derived, no `lifted_from_zone`); `fl_peninsula` `vh021_table_1_month_resolution` (re-derived, no `lifted_from_zone`), NA-3d; **`warm_arid` `nmsu_tamu_arid_month_resolution` (re-derived, no `lifted_from_zone`), NA-3e.**
- **Region→source map:** top-level `region_source_map{}` (build-time infra, NOT a runtime read)
- **Source catalog:** 80 entries
- **Format:** minified JSON

## Methodology

- **v1.5** (2026-05-31, cold-zone fall heat-floor rule — `cold_zone_fall_heat_floor_methodology_v1_5.md`); v1.4.1 (patch 2026-05-26; base v1.4 locked 2026-05-19); dual-register v1.1; language/copy v1.1.

---

## Where we are in the work

- **M15 lettuce arc: IN PROGRESS — region-cell slice DONE, prose/validation slice remaining.** NA-1 (methodology v1.5) + NA-2 (source_set + extreme-zone check) + NA-3a (cold-zone succession + `succession_policy` re-grounding) + NA-3b (`se_gulf`) + NA-3c (4 CA cells + region-tip-override first-exercise) + NA-3d (`fl_peninsula`; se_gulf §6 fix) + NA-3e (`warm_arid`) + NA-3f (`low_desert_az`) + NA-3g (`hawaii_tropical` — first year-round cell) + **NA-3h (`northern_tier` reconciliation — the region-cell arc closer; succession migration, two winter-calendar defects caught+fixed)** ALL DONE (SHA `582dbbad`, git `fa95e23`). **All 10 region cells verified; Step 4 closed.** **NEXT: Step 6 seasoned depth-lift** (NOT a region cell, NOT the flip). **Remaining to gold-standard: Step 6 (depth-lift) → Steps 7-8 (beginner siblings, ~100+ fields) → Step 11 (whole-crop validation pass, incl. whole-crop Step 5.5 re-walk; flip both `launch_ready` only on 0 violations).**
- **Gold-standard arc checklist:** **v1.4** (adds the Step 4 region-tip-override criterion + Appendix A ruling; v1.3 made Step 5.5's calendar/succession criteria real gates, branched by `calendar_basis`). M15 lettuce is the first proving ground; its succession + region authoring is the first real exercise of Steps 4 + 5.5, and NA-3c first-exercised the Step-4 override sub-criterion (clean).
- **Calendar model:** SCAFFOLDED WHOLE (4c). NOT populated — succession windows + pause-month classification are crop-arc work (lettuce first, in NA-3).
- **Explicit-register schema:** DONE (Phase 0). **Companions:** DONE (reconciliation).
- **No live consumers read the dataset yet.** This is why the region flip + register rename are not reader-coordinated — and the basis for authoring lettuce's region data now at low risk.
- **Region migration:** Phase A + Phase B COMPLETE. **Phase C (region-primary flip) BUILT-AND-READY but GATED** behind M15 lettuce + M16 cherry/beefsteak carrying real warm data. **Per the locked decision, lettuce now CARRIES that real warm data in `regions{}` (ALL 10 of 10 cells populated AND v1.4-verified, as of NA-3h)** (the formal flip still waits for M16). The NA-3h succession-shape finding (northern_tier's per-zone arms vs the warm cells' generative `succession_policy` rule) is part of what Phase C must reconcile.
- **Cherry tomato (M6) + beefsteak (M7):** COMPLETE under the OLD gate; carry 4 known Appendix C defects + the 4c-flagged cherry z9 heat-pause drift → all re-discovered/cleared at the M16 regression test (which includes their zones→regions retrofit, since their warm data IS legacy-stranded).
- **Anchors: 2 of 9 done under the old gate** (cherry, beefsteak), both pending M16. Lettuce in progress. Strawberry = 9th anchor.

---

## FORWARD PATH (4c + 4d DONE; M15 lettuce IN PROGRESS)

Dependency-ordered.

1. **~~Phase A — region scaffolding.~~ DONE.** SHA `15da4a9c...`.
2. **~~Phase B — correction phase.~~ DONE.** SHA `74fa36f0...`.
3. **~~Gold-standard arc checklist (keystone).~~ DONE; v1.4** (Step 4 region-tip-override criterion + Appendix A ruling; v1.3 made Step 5.5 real gates).
4. **~~Phase 0 — register inventory + apply.~~ DONE.** Schema → 2.8. SHA `68ca19a8...`.
4b. **~~Companions reconciliation.~~ DONE.** SHA `f6b2d800...`.
4c. **~~CALENDAR-MODEL SCHEMA SESSION.~~ DONE.** SHA `f6b2d800...` → `518a6a36...`.
4d. **~~Checklist v1.3 finalization — markdown-only.~~ DONE.** SHA stays `518a6a36...`.

5. **M15 — Lettuce gold-standard arc. ← IN PROGRESS (region-cell slice DONE).** First crop to author `track: "succession"` windows; first real test of Step 5.5's succession + coherence gates AND (per the locked decision) Step 4's region-population gate; **first real exercise of the region-primary shape (NA-3b, `se_gulf`) and of the region-tip-override mechanism (NA-3c, CA bolting fork).** Authored succession on the `zones[]` layer AND populated all 10 warm/cold `regions{}` cells per checklist Step 4 (`zones{}` kept coherent as fallback). **NA-1 + NA-2 + NA-3a..NA-3h ALL DONE (SHA `582dbbad...`, git `fa95e23`, pushed); all 10 region cells v1.4-verified; Step 4 closed.** **NEXT: Step 6 seasoned depth-lift.** **Remaining to gold-standard: Step 6 (seasoned depth-lift, the cherry 1B-NB equivalent) → Steps 7-8 (beginner siblings — ~85 null siblings: 19 top/dict + 66 compound, tips 10/10 already done) → Step 11 (whole-crop validation pass: re-run every gate independently incl. a whole-crop Step 5.5 re-walk and the ~39 zones-layer content dashes + the 30 outstanding findings; flip both `launch_ready` ONLY when every gate returns 0 AND every review flag is cleared).** **`status` left as `unverified`-or-successor — NOT `gold_standard`** until M16 settles the post-arc status vocabulary (Appendix B F6). The NA-3h succession-shape finding (northern_tier arms vs warm-cell generative rule) is a live open item for the Step 11 / Phase C work. Expect sub-sessions.

6. **M16 — Cherry + beefsteak re-run as a REGRESSION TEST (new-shape).** Pass bar: independently rediscover the 4 Appendix C defects + the 4c-flagged cherry z9 heat-pause drift. Corrects them → performs the warm-zone retrofit (real warm data OUT of legacy `zones{}`, INTO `regions{}`) — the precondition for the Phase C flip. Settles the post-arc `status` vocabulary (F6).

7. **Phase C — region-primary FLIP.** Unblocked once lettuce (5) + cherry & beefsteak (6) carry real warm data in the region layer. Region becomes primary; `zones{}` collapses to frost-refinement; region-scoped `lifecycle_override` lands; **PENDING guard.** Inherits 4c's `track` + pause vocab; `calendar_basis` rides at crop level. Re-pin start-SHA to post-step-6 canonical.

8. **Carrot gold-standard arc.** Completes the cool-season annual anchor pair. BEFORE the perennial extension. (Will reuse the succession-encoding shape M15 proves — different cadence, no warm-zone inversion, but same track/sibling/pause structure.)
9. **Schema-2.8 perennial extension.** Perennial field surfaces + region-scoped `lifecycle_override` if not shipped in Phase C. Before the first perennial anchor.
10. **Remaining anchors + builder/auditor pipeline.** Apple, lemon, blueberry, strawberry, oyster, zinnia/bee balm, basil; then the pipeline promotes ~114 crops.

**Honest framing:** correctly ordered AND a lot of work. Nine anchors (two as regression re-runs) + Phase C + a 114-crop pipeline. Delivery target Fall 2027 – Spring 2028, Kickstarter decoupled.

---

## Locked / done

- Schema 2.8 live (register suffixing + companions + additive region scaffolding + calendar-model scaffolding); **methodology v1.5**; language v1.1; soil_education + ph_education complete; M12 scripted pass complete.
- Region model: region-primary LOCKED; 10 region_ids ratified; `lifecycle_override` region-scoped; two-layer cut.
- **Region-authoring policy (2026-05-31): each crop populates its warm `regions{}` during its own gold-standard arc, per checklist Step 4 — NOT deferred to Phase C.** Lettuce first, now COMPLETE across all 10 cells (M15 NA-3b `se_gulf`, NA-3c's 4 CA cells, NA-3d `fl_peninsula`, NA-3e `warm_arid`, NA-3f `low_desert_az`, NA-3g `hawaii_tropical`, **NA-3h `northern_tier` reconciliation — 10 of 10 populated AND v1.4-verified**). The Phase C flip relocates the primary read-layer; it does not author per-crop region biology. **Shape precedents set across NA-3c..NA-3h:** (a) all warm/CA resolved cells use the date-string-only shape (no nested per-zone `plantings[]`); (b) resolved-date granularity may legitimately differ per region (se_gulf day-resolution vs. month-resolution elsewhere), stamped via `resolution_method`; (c) **window structure is decided PER SOURCE** (se_gulf two-window, fl_peninsula single-window, warm_arid two-window, northern_tier two-arm succession); (d) **re-derive vs. lift is a source-guard call** — re-derive where the legacy zone band is contaminated (the warm cells), promote-up where the legacy zone band is the trustworthy copy (northern_tier — the INVERSE case); (e) **OPEN: succession encoding is not yet uniform** — warm cells use a generative `succession_policy` rule; northern_tier (NA-3h) enumerated `track:"succession"` arms in `resolved_by_zone`; reconciliation (likely northern_tier down to the generative rule) is owed at Step 11 / Phase C, guarded by region-primary §6 (dynamic path must not read succession out of `resolved_by_zone`).
- **Region-tip-override policy (2026-05-31 design session, Trevor-ratified — reverses NA-3b for tips):** editorial tips fork along the climate-signal axis (region in the warm/diverse south & west, zone-band in the north) ONLY where ≥2 T1 sources from different buckets prescribe a materially different grower action; the anchor-phrased portable tip is the retained default + no-ZIP/pre-app fallback. Authored in-arc (Step 4 rider), served post-app (resolver). Encoded in checklist v1.4 Step 4 + Appendix A; standard `tip_region_authoring_standard_v1_1.md`. Tips only (pests flat, sunlight region-neutral). Provisional pending crops 2–3; northern band-keying deferred to first cold-zone tip fork; a/b is a calendar-date axis and never a tip-forking axis. SHA unchanged by this session (`ec4f2b69...`).
- Calendar model (4c): `track` on all plantings entries; `calendar_basis` on all crops; relaxed validator; 13-state `calendar[]` vocab (pause states reserved); non-seasonal indoor mode; sync invariant defined.
- Methodology v1.5 cold-zone fall heat-floor rule: ratified, T1-backed (USU 80°F); resolves `m13_s1b_nc_methodology_candidate_001`; generalizes to spinach/radish/cilantro (per-crop threshold re-verification).
- Lettuce: 9 zones verified (M13); soil 12/12; ph preferred [6.0,6.8] / tolerated [6.0,7.0]; tips 10/10 attributed; `source_set` 35 (NA-2); 30 open_findings in-record (0 unresolved blockers).

## Open (non-blocking) items

- **Per-crop warm `sources` arrays NOT yet fully proven T1 dataset-wide.** Verified one crop at a time in the gold-standard arcs (methodology v1.4 Step 4). **For lettuce, ALL 10 region cells are now proven** — `se_gulf` (NA-3b), 4 CA (NA-3c), `fl_peninsula` (NA-3d, UF/IFAS VH021 + EP450/EP452), `warm_arid` (NA-3e, TAMU EHT-056 + El Paso County calendar + NMSU 457/457-B), `low_desert_az` (NA-3f, U of Arizona), `hawaii_tropical` (NA-3g, UH CTAHR RES-164/HGV-2/B-91), and `northern_tier` (NA-3h, MSU/UMN/USU/UMD/NDSU + VCE for the z6/z7 winter correction). All OTHER crops' warm regions remain PENDING until their own arcs. `region_source_map` `source_status` flags carry the screened-vs-verified boundary.
- **Pause-state population is arc work.** Leading/trailing `wait` months + interior `wait` unclassified across most crops; the interior ones are the prime heat/cold-pause candidates. Each crop's arc classifies its own — lettuce in progress (warm-zone summer `heat_pause` populated in `se_gulf`, the 3 heat-inverted CA cells, and `fl_peninsula` [May–Aug]; NA-3d also corrected se_gulf's `calendar[]` from `wait` to the `heat_pause` STATE per §6; remaining warm regions in NA-3e+; `northern_tier`'s cold `cold_pause` classification is part of its queued reconciliation).
- **~~`succession_policy.sources`/`anchoring_urls` missing on lettuce~~ — CLOSED in NA-3a** (sources usu/umn/umd + verified anchoring URLs added when succession was authored).
- **`m15_na3b_finding_001` (non-blocking): warm `zones{}` 8–11 sourcing is SE-only**, mis-attributed for the non-SE regions. STILL OPEN. The user-facing harm is now handled for ALL populated regions: a CA user reads the CA cells, a south-FL user reads `fl_peninsula`, an arid-SW user reads `warm_arid`, a low-desert user reads `low_desert_az`, a tropical user reads `hawaii_tropical`, a cold-zone user reads `northern_tier` (all correct + verified), not the raw SE-sourced legacy zone layer. But the legacy `zones[8/9/10/11]` entries are still internally mis-sourced (z8 Georgia/Clemson/Maryland/Minnesota per `na3e_finding_001`; z9 UF/IFAS/TAMU/Clemson per `na3f_finding_001`; z10 CA-contaminated per `na3d_finding_001`; z11 SE/south-FL per `na3g_finding_001`). Left unchanged as agreed (full legacy-zone re-sourcing is its own pass — can ride the Phase C flip or be standalone; Trevor's call, no session slotted). se_gulf zone 8/9 sourcing is correct. NOTE: the cold legacy `zones[3-7]` are the INVERSE — they are the *trustworthy* copy (NA-3h promoted FROM them), not deletable until the Phase C flip.
- **`na3d_finding_001` (NEW, non-blocking, OPEN — finding_001 sibling): lettuce `zones[10]` is CA-contaminated.** Its synthesis note reads "coastal California, south Florida, south Texas" and cites `uc_anr_7216` (absent from the source_catalog) — a blended multi-region band, the INVERSE of finding_001's SE-mis-sourcing. The `fl_peninsula` region cell now carries correct peninsular data (re-derived from VH021); legacy `zones[10]` left for the dedicated zones{} re-sourcing pass (rides finding_001's pass).
- **`na3d_finding_002` (NEW, CLOSED): se_gulf §6 calendar↔heat_pause-object drift — corrected in NA-3d** (z8 months 5–8, z9 months 6–8 `wait`→`heat_pause`) under the stale-overwrite policy. The cold analogue of this drift is a known/suspected gap on `northern_tier` (see its reclassification).
- **`na3d_finding_003` (NEW, OPEN, cosmetic/naming, non-blocking):** (a) `track: "beginner"` is the *name of the shared main calendar* both audiences see, not a beginner-only tag (confirmed structurally — single scalar, no `seasoned` sibling tag); succession is the seasoned-only separate calendar. Possible future rename of the value to `main` dataset-wide (981 entries + Step 5.5 enum + schema spec + validator) is a deferred standalone session. (b) `resolution_method` strings are source-opaque (`table_13_2_month_resolution`, `vh021_table_1_month_resolution`); a self-describing convention could be adopted dataset-wide. fl_peninsula matched the established opaque pattern rather than introduce a one-off.
- **`na3d_finding_004` (NEW, note/track):** Florida's climatic zones in VH021 are drawn by State Road line (North > SR 40; Central SR 40–70; South < SR 70), NOT a clean USDA-zone overlay. Peninsular USDA 10 spans parts of both Central and South FL. VH021 gives both the same Sept–Feb lettuce window, so the imperfect overlay does not change the dates — noted in fl_peninsula provenance so no one later implies a crisp SR-line↔USDA-zone correspondence.
- **`na3e_finding_001` (NEW, non-blocking, OPEN — finding_001 sibling): lettuce `zones[8]` is SE/humid-East-contaminated for the warm-arid region.** Sourced from `uga_b577 / uga_calendar / clemson_hgic / tamu_agrilife / uga_c1258_fall` (+ `umd_ext`/`umn_ext` on harvest arms) = Georgia/Clemson/Maryland/Minnesota, with explicit "warm zones (8-9)" SE synthesis prose — wrong provenance for the arid Southwest it nominally feeds. Same class as `na3d_finding_001` (z10) and finding_001, this instance for zone 8 / arid. The `warm_arid` region cell now carries correct NMSU/TAMU arid data (re-derived); legacy `zones[8]` left for the dedicated zones{} re-sourcing pass (rides finding_001's pass).
- **`na3e_finding_002` (NEW, note/track, NB): Doña Ana County Master Gardener Las Cruces planting chart not machine-readable.** The most granular NMSU planting-date source (named in the `nmsu_ext` catalog `citable_for`) is a Gantt-style PDF whose colored month bars do not survive text extraction, and the donaanamastergardeners.nmsu.edu domain was not reachable from the sandbox to rasterize. warm_arid's exact month boundaries were triangulated from TAMU tabular dates + NMSU text + the El Paso County calendar (all T1, convergent) instead; residual softness on the spring-open / fall-open edges recorded in `warm_arid.plantings_provenance.verification_note`. Tighten the edges if/when the chart becomes machine-readable. Non-blocking.
- **`m15_na3b_finding_002`: heat-primary/frost-secondary warm anchoring precedent. RATIFIED-WITH-SCOPE (2026-05-31) + APPLIED (NA-3c, NA-3d).** Heat-primary for heat-inverted / warm-arid regions (limiting season = summer heat); does NOT generalize to cool-limited regions. Applied in NA-3c (3 heat-inverted CA cells; `ca_north_coast` the cool-limited exception) and NA-3d (`fl_peninsula`, heat-inverted, heat-primary). Governs all remaining warm regions + the pipeline; the next warm cell (`warm_arid`, z8) is heat-inverted (heat-primary) — but verify its window structure per source.
- **POST-APP location-aware layer (NA-3b design thread + 2026-05-31 reversal — NOT dataset work now, deliberate design at app phase):** one coherent capability — (1) ZIP→region resolution (read path: no ZIP = generic zone calendar + prompt to enter ZIP; ZIP = region-resolved content); (2) `severity`-by-location for pests/diseases (runtime-derived, not authored per-region); (3) community-tip location stamp (zone + region; the contribution-flow model that lets the community grow — design BEFORE any contributions exist to avoid retrofitting tags onto collected tips); (4) **region-tip-override serving** (the resolver selects a region's override where one exists, else serves the anchor-phrased default; overrides are authored in-arc now but cannot be served until the resolver exists). Bundle as one design. Reminder (in memory): the app does not exist yet.
- **Region-tip-override leaf-keys: PINNED (NA-3c ground-truth authoring).** Shape A in use: `regions[r].tip_overrides[stage][]`, each entry `overrides_tip_id` + `text_beginner`/`_seasoned` + `sources` + `anchoring_urls` + `added_in` + `evidence_tier` (INHERIT-class fields {audience, display_priority_override, last_reviewed*, author} omitted; validator flags them). `validate_region_tip_overrides.py` (V1-V6) ran post-write: 0 violations. Checklist Appendix A's CORE-PROSE-NEEDS-SIBLING ruling covers the nested `text_*` path. The bucket model remains provisional, n=1 (only lettuce has exercised it; cherry/beefsteak warm-season anchors may fork differently).
- **~~Carried tip-rewrite action (NA-3b -> NA-3c)~~ CLOSED in NA-3c.** `succession_policy.tip_beginner` + `tip_seasoned` rewritten region-portable + anchor-phrased (read correctly in both cold and warm zones); the prior cold-zone-only "your last frost" framing is superseded. `sunlight` left region-neutral, `pests` left flat (the reversal is tips-only).
- **cherry z9 heat-pause drift** (4c flag) — M16.
- **Checklist beginner↔succession coherence reconciliation** — confirm the "within the succession envelope" formulation matches `calendar_model_schema_spec_v1_0.md` §2a; do it in NA-3a when the gate goes live (appears aligned).
- `sources_pending_admission` per-crop scaffold markers backed by real catalog entries; sweep cosmetically at re-run.
- m12_audit_finding_001 (beefsteak soil key-order): cosmetic.
- Lettuce open_findings = 30 in-record. Final-audit candidates: s2c_na_004, s1c_003, m13_companions_003, m13_companions_005.
- M9B anchoring_urls manual browser verification (m9b_finding_001).
- Catalog deferred: add `nrcs_soils`; 2 dangling refs (britannica, walter_reeves).
- Methodology doc path correction: `soil_education.<vocab>` (not `.textures.<vocab>`).
- **blocks_launch counting:** 6 resolved S1A/S1B findings retain `blocks_launch:true` with `status:"resolved"`. Readiness gates MUST use the two-field predicate (`blocks_launch AND status != "resolved"`), never a bare count.
- **README paste hygiene:** session-close READMEs keep inline `#` comments and parentheses OUT of copy-paste command blocks (zsh chokes on `(...)`). Commands-only fenced blocks; explanation in prose above.

## Schema versions -- do not conflate

**2.8 (current on disk)** spans: register suffixing (Phase 0); companions register-by-membership; additive region scaffolding (Phase A/B — `regions{}` sibling, `northern_tier` populated, warm PENDING except lettuce-in-progress); calendar-model scaffolding (4c). All additive/within-version reshapes against a dataset with no live readers.

**The region-primary FLIP** (Phase C, step 7) — region becomes the primary calendar key; `zones{}` collapses to frost-refinement; wrap-as-dict; region-scoped `lifecycle_override`. **GATED — do not apply early.** Ships only after lettuce + cherry + beefsteak carry real region-layer data. **Note the distinction the 2026-05-31 decision sharpened: populating a crop's region cells (arc work, happening now) is SEPARATE from the global flip (gated, after M16).** 4c's `track` migrates with each `plantings[]` entry; `calendar_basis` rides at crop level.

**The perennial extension** (step 9) — perennial field surfaces. Gated before the first perennial anchor; carrot precedes it.

**`lifecycle_override` is REGION-scoped.** Crop-level `lifecycle` stays; `regions[r].lifecycle_override` carries `practical_lifecycle` + reason prose (both registers) + sources. Strawberry Central Valley annual is the driver. Provisional list (each per-region): strawberry, basil zones 10-11, tomato/pepper zones 10-11, borderline kale.

---

## State-doc maintenance protocol (collapse cadence) — adopted 2026-06-01

**Purpose:** keep this file lean as the arc list grows across nine crops. The "What happened …" entry stack grows ~15–30 lines per session; without collapse it would reach thousands of lines, defeating the file's job (the thing a cold session reads FIRST to orient). The fix is *progressive collapse at checklist-defined boundaries*, not deletion — recent work stays granular, old work flattens to a summary, and nothing load-bearing is ever lost.

**Vocabulary (checklist v1.4 is the source of truth — do NOT conflate these layers):**
- **The arc = Steps 0–11** (the per-crop gold-standard checklist). A single Step can span several sessions (Step 4 currently spans na3b–na3f + the northern_tier reconciliation).
- **Session names (NA/NB/NC/ND)** are this project's chunking convention for a long arc, NOT checklist structure. NA ≈ the scaffolding/authoring stretch; NB ≈ verification grind; etc. The checklist does not define them.
- **Phase A / Phase B / Phase C** are the one-time REGION-MIGRATION milestones that bracket all arcs (A = scaffolding, done; B = `region_source_map` correction, done; C = the gated region-primary flip, after M16). They are NOT stages within a crop arc. *(Lesson logged 2026-06-01: these three layers were conflated in conversation; the checklist settles it — arc = Steps 0–11; A/B/C = migration milestones.)*

**Collapse cadence:**
1. **While a Step is active:** each session appends its full granular "What happened …" entry (detail is wanted at current working depth).
2. **At Step close (gate returns 0, work moves to the next Step):** collapse that Step's session entries into ONE "Step N summary" entry.
3. **At arc close (Step 11 flip, crop certified):** collapse the crop's Step summaries into ONE crop summary.
4. **Migration phases (A/B/C):** their own one-time milestone lines (A/B already single lines; C collapses when it fires).

**The always-promoted-before-collapse rule (so collapse never loses memory):** before discarding any session entry, three things must already live in a PERMANENT section, in full:
1. **Open findings** → the findings ledger ("Open (non-blocking) items"). A finding never lives ONLY in a session entry.
2. **Standing lessons** → the "Standing lessons" section below (promoted the first time a lesson crystallizes; the per-entry retelling is then dropped).
3. **Active locked decisions** → their own permanent blocks (REVERSAL banner, regions-this-arc decision, northern_tier reclassification, etc.), kept until SUPERSEDED or fully discharged — not collapsed away for being old.
A collapsed summary is then short: a gist + pointers ("findings → ledger; lessons → standing list").

**Status right now:** **Step 4 (warm-region population) is CLOSED** — all 10 region cells authored + verified, `northern_tier` reconciled (NA-3h). **The first collapse is now ELIGIBLE to fire:** na3b–na3h (the region-cell sessions) collapse into one "Step 4: warm-region population" summary. The na3a entry (cold-zone succession, Step 5.5) collapses when Step 5.5 fully closes — which, per the NA-3h succession-shape finding, is NOT yet (the whole-crop Step 5.5 re-walk + the northern_tier-vs-warm shape reconciliation are owed at Step 11 / Phase C). The NA-3h entry itself stays uncollapsed until Step 4's collapse fires (it is both the Step-4 closer and the carrier of the open succession-shape finding). Recommend firing the Step-4 collapse at the start of the Step 6 session (it reads the checklist fresh anyway).

## Standing lessons (promoted from session entries — survive all collapses)

Cross-cutting rules that have crystallized across the arc. These are the institutional memory the collapse cadence preserves; each is stated once here and not re-told per session entry.

- **Emit the ENTIRE CURRENT_STATE.md at every session close, never a delta/patch.** A prior chat emitted only a delta; it was missed and the file fell a session behind (caught + regenerated 2026-06-01).
- **Keep git current at session close.** The hand-promote workflow + uncommitted git (anchored at M13 while na3b/na3c/na3d lived only as working files) is what turned the NA-3d promote into a recovery. Commit the canonical at close.
- **Derive calendars programmatically** from windows + declared pauses (precedence: pause > plant > harvest > growing > wait) — never hand-place month arrays. Hand-placement caused a calendar↔`heat_pause`-object coherence bug the gate caught (NA-3c) and is the placement-error class eliminated by deriving.
- **Verify per source; don't assume the inherited framing.** "Mirror se_gulf" was wrong for fl_peninsula once VH021 was read (single-window, not two-window). The kickoff said "verify"; verification overturned the assumption. Status does NOT inherit — Step 5 side-by-side confers it.
- **Window structure is decided PER SOURCE** (se_gulf two-window vs. fl_peninsula single-window); **re-derive vs. lift is a source-guard call** — lift only from clean region-appropriate zone calendars, re-derive (no `lifted_from_zone`) where the legacy zone band is contaminated.
- **PENDING/marker guards target LIVE DATA SLOTS, not provenance prose.** A blanket "PENDING substring" check tripped on the legitimate `plantings_provenance.supersedes: "PENDING scaffold…"` line (NA-3d). The cell was correct; the guard was over-strict.
- **Lifted-and-assumed-done is suspect until re-verified.** `northern_tier` was lifted verbatim in Phase A and treated as done; it carried real gaps. NA-3h reconciled it (succession migration + two winter-calendar defects that a remap-not-derive shortcut had preserved — caught only because Step 5 mandates deriving calendars from windows, and because gates (a) ">=1 cold_pause" + (b) "active ⊆ windows" both ran; monotonicity alone would have missed the zero-pause z7). A verbatim lift carries forward old gaps AND misses everything the standard added since. **The lesson generalizes: any "wrapped, not worked" cell needs the full bar, and a remap of stale data is NOT a derivation.**
- **Arc POSITION is a derived value, not a carried claim — re-derive it from the checklist, never from the prior kickoff's prose.** (2026-06-01) A next-chat kickoff stated "all 10 region cells done → Step 11 flag-flip next," conflating "the region-cell SLICE is done" with "the ARC is done." The region work is Steps 4 + 5 + per-cell 5.5 — ONE slice; the arc also owes Step 6 (depth-lift), Steps 7-8 (siblings), and the Step 11 whole-crop validation pass. This is the SAME failure class as the reverted 2026-05-29 M13 flip (trust an assertion instead of re-deriving from the source of truth). It reached only PROSE (CURRENT_STATE + a kickoff), never a flipped flag, because the Step 11 gate re-derives readiness from the crop, not from a kickoff. **Rule: every kickoff opens by reading the gold-standard-arc-checklist and deriving the next unowned step from the live crop state + the checklist; if the kickoff's prose and the checklist conflict, the checklist wins. Kickoffs SUMMARIZE; they are not authority on arc position.** (Tooling caught nothing here; Trevor did, after three pushes — the human-readable narration layer is the weak point, the mechanical Step 11 gate is the real defense.)
- **A corrected header on a stale body is WORSE than a uniformly-stale file** — it contradicts itself and a reader can't tell which half to trust. (2026-06-01) Banner-only patching after NA-3h left the body three sessions behind (still "8 of 10, NA-3f next"). **This is exactly why the rule is "emit the ENTIRE file every close": the whole file moves forward together, header AND body AND SHA lines AND next-pointers AND gate-status.** Patching one section reintroduces the drift.
- **Stale-overwrite policy:** pre-v1.4 / pre-this-arc artifacts are STALE and overwritten as correct findings surface (exercised on se_gulf's §6 calendar drift, NA-3d — narrowly, finding-scoped, not a blanket rewrite).
- **Two-clean-results is a sample of 2, not proof the rest are clean.** Accuracy over velocity; surface scope choices to Trevor, don't pivot mid-session.
- **README paste hygiene:** session-close command blocks are commands-only, no inline `#` comments or `(...)` (zsh chokes); explanation goes in prose above. **BUNDLE-PATH GUARD:** derive promotion `mv`/`cp` paths from actual `unzip -l` output, not the assumed working dir.
- **Two-field readiness predicate, never a bare count:** `blocks_launch AND status != "resolved"` (6 resolved findings retain `blocks_launch:true`).
- **Step 10 anchoring-URL hygiene must be scoped BY LAYER, and anchoring backfill is Step-11 evidence work, not a Step-10 rewrite.** (2026-06-04) A naive whole-crop walk for `sources`/`anchoring_urls` pairing flagged 80 "missing" -- but most are the established two-layer shape: legacy `zones{}` carries `sources` with empty `anchoring_urls:{}` (the deferred re-sourcing layer, finding_001 family), and `regions{}` anchors per-arm at `plantings[].direct_sow/harvest_*` + `resolved_by_zone.N.heat_pause` (region-root + container levels legitimately carry none). Only crop-field-level and genuinely-empty per-arm leaves are real gaps. **And closing those gaps is fetch+verify (Step 4/5 evidence + the schema `launch_ready_core` gate), NOT voice rewriting** -- Step 10 (the IP/voice lane) DETECTS + REGISTERS them, Step 11 closes them. Checklist amendment (7) owed to encode this.
- **The schema `launch_ready_core` gate is an INDEPENDENT readiness check from the two-field findings predicate.** (2026-06-04) Anchoring-URL completeness (one entry per source ID on every all-mode field) is required by the schema gate (Surface 4) even when `blocks_launch AND status != "resolved"` returns 0. A crop can show 0 unresolved blockers AND still fail the flip on anchoring gaps. Step 11 must run BOTH.

---

- **A registered gap’s described REMEDIATION PATH is itself an unverified claim until checked against the live data.** (2026-06-04) S10-3 catalogued the `northern_tier` anchoring gap with the note “the cold legacy `zones[3–7]` hold the trustworthy URLs to promote up”; this rode forward verbatim into CURRENT_STATE line 63 and the kickoff. Verified at backfill time: the legacy `zones[3–7]` ALSO carry empty `anchoring_urls:{}` — there was nothing to promote. The NA-3h “trustworthy copy” judgment was correct for PLANTING DATA (promoted + re-verified, `zone_promoted_verified`) but was silently extended to URLs, which the legacy layer never had (it predates per-source anchoring). Impact was citation-only, not data (no date moved). **The lesson generalizes the “lifted-and-assumed-done” + “verify per source” rules one step further: it is not only LIFTED DATA that needs re-verification, but the GAP-REMEDIATION INSTRUCTIONS a prior session writes about how a future gap will be closed. “The URLs exist to promote” is a claim, not a fact, until the source slot is opened. Confirm the remediation path empirically before scoping work around it — and when a precedent says “promote-up where the legacy band is trustworthy,” scope it to CONTENT, never URLs (else the bot pipeline replicates the empty-URL assumption to ~114 crops).** Caught at backfill preflight, surfaced to Trevor; CURRENT_STATE line 63 corrected same session.

*Update this file at each session close.*

---

## 2026-06-05 — session `m16_cherry_step4_se_gulf` (Step 4 first cell: se_gulf sourced)

**SHA:** `f916e8fec820bb21a3f52e667e5efe6760764a12dfc2824a3ee20234872b8fd1` → `339933f2bdbcd8e13d11bf507c7c40efdd2cd1d2cba3342d9f069b090075220f`
**Schema:** 2.8 → 2.8 (unchanged). **Crop count:** 123 → 123 (unchanged).
**Arc position:** M16 cherry, Step 4 (warm-region window sourcing) — first cell. Steps 3.5 + 9 + 10 already done; this is the first biology cell of the Step 4–8 authoring surface.

### What happened
- **`cherry-tomato.regions.se_gulf` (zones 8–9) sourced + authored to the lettuce reference bar**, closing the se_gulf slice of `gs_exemplar_finding_001`. The only cell changed this session.
- **Windows re-derived from T1, not lifted** (climate-contamination guard; warm `zones{}` is SE-mis-sourced):
  - **z8 = UGA C943** *Vegetable Garden Calendar* (full-reviewed 2026-03-10) — middle-Georgia baseline (Columbus–Macon–Augusta belt) + the explicit South-Georgia offset rule ("spring 2–3 wk earlier, fall 2 wk later in extreme south Georgia") that derives z9. Tomato seed-start 6 wk; spring transplant late Mar–early Apr; fall deadline ~Jul 20.
  - **z9 = UGA C943 + UF/IFAS VH021** *Florida Vegetable Gardening Guide* (modified 2026-02-10), North Florida column (north of State Road 40). Table 1 Tomatoes (supported) North = "Late Mar–Apr, Aug"; 70–90 d from transplant.
  - Both publications fetched + verified live this session; verified-field = session date-string.
- **Two-window spring/fall structure verified PER SOURCE** (standing rule). The summer gap between VH021's "Late Mar–Apr" and "Aug" IS the heat pause — read off the table, not assumed.
- **Cherry-specific heat finding (cultivar-type universalization guard):** VH021 Table 2 — "Cherry types are heat resistant"; UF/IFAS Gardening Solutions lists cherry tomato among the few crops productive through Florida summer. → cherry's se_gulf `heat_pause.months = [7]` (single month, NARROW). A beefsteak cell from the same biology gets a WIDER pause (Jul–Aug). Logged so M16 beefsteak does not copy cherry's soft pause.
- **`second_planting` data authored on z8 + z9** against Claude Code's ratified structure (resolved-layer sibling object + region-constant `plantings[]` entry `succession_id:2, track:"second_planting"`). De-multiplexed the legacy `zones["9"]` comma-string (`Feb 7 – Feb 28, Jun 24 – Jul 8`, "two full seasons") into main band (spring, beginner) + second band (late summer), dates re-sourced from the real region anchor. Discrete, not succession (`succession_policy.suitable=false`).
- **z9 `calendar[]`↔prose heat-pause drift resolved at the region layer.** New `resolved_by_zone["9"].calendar` encodes `heat_pause` at the summer core, derived programmatically (precedence pause > plant > harvest > growing > wait). The legacy `zones["9"].calendar[Aug]="plant"` contradiction is the thing re-derived away from; legacy layer not mutated.
- **region_notes pair authored** (seasoned + beginner; beginner keeps the plain causal hook, drops mechanism, not less true than seasoned). **No region-tip overrides** — UGA + UF/IFAS do not fork the grower action for se_gulf; per-crop attestation recorded.
- **Shape matched to live `lettuce-leaf.regions.se_gulf` exactly:** added `plantings_provenance`; `resolution_method: se_gulf_month_resolution`; `planting_note: multi_season`; cell-level `harvest`/`first_plant_date`/`last_plant_date` envelope across both bands; structured `heat_pause`; **dropped** `sources_pending_admission` (lettuce has no such key on a sourced cell). NO `lifted_from_zone` (re-derived, per Claude Code).

### Findings
- **`gs_exemplar_finding_001`** — se_gulf slice CLOSED. (Full close when the remaining 8 warm cells are sourced + the `cornell_ext` zone-6 URL is found; `_001` per the kickoff spans those too.)
- **`phase_3_tomatoes_s3c_finding_001`** (pre-existing, deferred) — z9 slice superseded by the se_gulf re-source (the Minnesota text on `zones["9"].start_indoors` is replaced for z9). Cross-referenced, NOT duplicated. Zones 3–8/10/11 slices remain open for family close.
- **NEW — blossom-drop `fruit_set_temp_f` field proposed (ruling owed).** T1 anchors now in hand: VH021 ("Blossom drop is usually due to too high or too low temperatures") + existing s1b_finding_006 (Clemson HGIC / PSU: >90°F day / <55°F night). Schema-touching → Trevor rules shape; Claude Code adds. Rule together with s1b_finding_006.
- **Three kickoff-vs-live discrepancies surfaced** (logged in the deliverable, none material once corrected): (1) Minnesota text sits on `start_indoors` not the `plant_out` window the kickoff implied, and is already filed as s3c_finding_001; (2) legacy `zones["9"]` already half-encoded the heat pause via a `heat_pause_start` anchor — the drift is specifically `calendar[Aug]`; (3) `uga_c963` is not a discrete catalog ID (folds under `uga_ext`).

### Verification done (in lieu of the gate, which this session could not run)
- Collateral hash audit: ONLY `cherry-tomato.regions.se_gulf` changed; all 122 other crops + all top-level non-crop keys byte-identical; lettuce byte-identical.
- Key-parity check against live `lettuce-leaf.regions.se_gulf`: exact match (cherry adds `second_planting`, intended; omits `lifted_from_zone`, intended).
- Serialization: minified `separators=(',',':')`, `ensure_ascii=False`, no trailing newline (verified on bytes).
- **Gate NOT run** (claude.ai session, no `~/plant-dataset/tools/` access). Residual figure in CURRENT_STATE is DERIVED (32 → ~31 by the se_gulf region_notes pair). **Confirm with `whole_crop_gate.py cherry-tomato` at next preflight.**

### Residual after this session (cherry, claude.ai lane)
8 warm regions still PENDING (`ca_interior`, `ca_north_coast`, `ca_south_coast`, `ca_desert`, `warm_arid`, `low_desert_az`, `fl_peninsula`, `hawaii_tropical`); 9 region_notes pairs still null (se_gulf now done, `northern_tier` + 8 warm remain); 27 dual-voice siblings (gate shows 21); `cornell_ext` zone-6 URL; `gs_exemplar_finding_003` extreme-zone record. Then Steps 5–8, then Claude Code Step 11 + flip. **The arc is NOT done — one cell of one step is.**

---

## 2026-06-06 — session `m16_cherry_step4_coastal_desert_4cells` (claude.ai lane)

**Start-SHA:** `813bade981f488e54479f83db2ba72d06f3a12f0cb791149e638d1a14da6a040` (post catalog-admit baseline)
**End-SHA:** `9d8784f7a0e80ffd53fb6ecdc4500182c2519889ae15c2ff77202f19a7890e3c`
**Gate:** 30 → **26** (gate-confirmed this session — claude.ai had `whole_crop_gate.py` available and ran it on every apply). Lettuce `GATE: PASS`, byte-identical.

### What happened
Four Step-4 region cells authored on `cherry-tomato`, all to the lettuce/se_gulf/ca_interior reference bar. Region fill 2/10 → **6/10**. The four cells took four genuinely different shapes from their sources (no template imposed) — this is the heart of the session.

- **`ca_north_coast` (zones 9-10) — COOL-LIMITED exception.** Single window, May only. UC ANR Table 13.2 North & North Coast tomatoes = "May" (no semicolon → no fall window; contrast snap beans "July; May–June"). NO heat_pause: limiting factor is lack of summer heat (marine layer; UC Marin MG "Tomato time!" gives early-May transplant + sub-55°F nights any month). Two independent T1 (Table + Marin MG) confirm single-window + absence of second window. harvest_end production-tapered ~October (cooling + light), documented as NOT a frost cutoff; fall calendar months read `wait` not `cold_pause`. Sources `ucanr_ext` + `ucanr_marin_mg`. San Mateo/SF MG + Monterey Bay MG read-not-cited. Contra Costa MG (named in region_source_map) not catalogued → not cited.

- **`ca_south_coast` (zones 9-10) — mild marine, longest window.** Single but long: Table 13.2 South Coast = "April – July 15" (105-day plant-out, widest CA region). Confirmed single continuous window not a pause-split (San Diego MG growing guide: harvest "early summer through end of year in coastal areas"; the "two-season" blog framing is the option to plant later within the long window). NO heat_pause. Real z9/z10 differentiation: z10 (frost-free immediate coast) harvest to December; z9 (cooler inland-edge) tapers to November. Sources `ucanr_ext` + `ucanr_san_diego_mg` (FIRST use of the newly-admitted ID; §E recognized it as catalogued T1 — the admit paid off). Per option (a): San Diego corroboration anchored to the ucanr.edu Vegetable Resources page; Association-domain (mastergardenersd.org) dated prose read-not-cited per the catalog entry caveat. **`fruit_set_temp_f` anchor found + logged:** San Diego MG Vegetable Resources gives tomato set ~65°F night–90°F (candidate for the deferred field).

- **`ca_desert` (zones 9-10) — heat-inverted, TWO-WINDOW.** Table 13.2 Desert Valleys = "Dec–March" (inverted: winter is the growing season). heat_pause Jun–Aug, climatic-absolute (Imperial/Coachella >95–110°F; fruit set fails >95°F, viable pollen collapses; mechanism via UC IPM / UC ANR 8159). second_planting: Sep fall crop running into January (mild desert winter). Sources `ucanr_ext` + `uc_ipm`. UC ANR 8159 + UC Riverside MG + U Arizona az2078 read-not-cited.

- **`low_desert_az` (zone 9 ONLY) — heat-inverted, TWO-WINDOW, AZ dates.** U of Arizona az2078 "Growing Tomatoes" (peer-reviewed Feb 2024): two short seasons; spring transplant ~February (racing the late-May Phoenix heat wall — LATER than the CA desert's Dec–March); summer fruit-set failure Jul–Aug (>90°F reduces viable pollen); September fall replant into December (often the better season). heat_pause Jun–Aug. zone 9 only (region_source_map Phase B: zone-10 desert served by `ca_desert`). Source `uariz_ext` (az2078 + AZ1005 Maricopa, two UofA docs agreeing — single-institution sufficient). AZ1615 Yuma read-not-cited (same institution).

### Key per-region judgment recorded (precedent)
**Cherry's heat resistance NARROWS the summer pause only where the heat is marginal, NOT where it is climatic-absolute.** se_gulf: narrowed to one month (month 7). `ca_desert` + `low_desert_az`: NOT narrowed — peak desert summer stops fruit set even for heat-tolerant cherry. Documented in both desert provenances, explicitly contrasted with gs_exemplar_finding_001. This is a per-region call, not a universal rule. beefsteak re-derives (wider everywhere).

### `warm_arid` deliberately NOT authored (the discipline working)
Southern NM (Las Cruces/Mesilla, zone 8) reads MILDER than the zone-9 low deserts — NMSU Circular 457/457-B + extension-specialist guidance (Las Cruces Bulletin, Stephanie Walker / Israel Joukhadar) emphasize spring-after-frost planting and transplants maturing before summer heat, with NO emphatic lethal-summer-pause structure. It leans single-window/no-heat_pause, but the CR457-B tomato-date table and the Doña Ana County MG Las Cruces chart (the documents that actually decide window count) were only seen as index pages this session, not the tomato row. Per "window structure is a source finding, confirm before authoring," `warm_arid` is held for a clean CR457-B + Doña Ana chart read at the top of next session. Did NOT stamp the desert two-window shape on it by analogy.

### Catalog
- `ucanr_san_diego_mg` used first time (`ca_south_coast`). No new admits this session (`uc_ipm`/`uariz_ext`/`ucanr_marin_mg` pre-catalogued).
- `ucd_plantsci` DROPPED from the batch: UC Davis's citable vegetable center (vric.ucdavis.edu) is processing-tomato research (96% of US processed tomatoes), wrong register for home-garden timing; home-garden postharvest already covered by catalogued `ucd_postharvest`. No clean home-garden-timing UC Davis doc to pin.

### Verification done (gate RAN this session)
- Collateral hash audit: ONLY `cherry-tomato` changed (4 region cells); all 122 other crops + all top-level non-crop keys byte-identical; **lettuce byte-identical**; se_gulf + ca_interior (prior cells) byte-identical.
- Key-parity: each new single-window cell exact-matches `ca_interior` shape; two-window cells carry the proven `heat_pause` + `second_planting` blocks (all 4 required window keys present, gate-validated).
- Calendar-vs-declared-pause consistency: `ca_desert`/`low_desert_az` calendar `heat_pause` months [6,7,8] == declared `heat_pause.months`.
- Serialization: minified `separators=(',',':')`, `ensure_ascii=False`, no trailing newline.
- **Gate RAN: `GATE: 26 VIOLATION(S)`** (this session had `whole_crop_gate.py`). 30 → 26, the drop = the 4 region_notes pairs cleared. No new violation of any kind from the 4 cells. §C/D 0 dash / 0 temp forms; §E 0 uncatalogued / 0 non-T1 (42 IDs); §F 1 gap (known cornell_ext zone-6).

### Residual after this session (cherry)
**Gate 26.** 4 region_notes pairs still null (`northern_tier`, `warm_arid`, `fl_peninsula`, `hawaii_tropical`); 21 dual-voice siblings (author 27, gate shows 21); `cornell_ext` zone-6 URL; `gs_exemplar_finding_003` extreme-zone record. 3 warm regions + northern_tier calendar remain on Step 4 (`warm_arid` next, window-structure read required). Then Steps 5–8, then Claude Code Step 11 + flip. **The arc is NOT done — 6 of 10 cells of one step are.**
